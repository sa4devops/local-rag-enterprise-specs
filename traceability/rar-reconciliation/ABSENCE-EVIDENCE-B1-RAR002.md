# ABSENCE-EVIDENCE-B1-RAR002 — B1 Commit 3

> **مخرج traceability/تحليلي غير حاكم** — لا سلطة موازية. حزمة أدلة غياب **قابلة لإعادة التنفيذ** لبنود RAR-002 المصنَّفة «دلتا حقيقية». كل حالة بمرساة HTML صريحة بأحرف صغيرة. **HEAD المنفَّذ عليه البحث:** `c00a3243d0eb331a20fa0a26327bff6adc146b47` · **تاريخ البحث:** 2026-07-26.
> **نطاق مشترك مستثنى من «الحاكم»:** `references/analysis-inputs/**` (مدخل غير حاكم خارج authority ladder) · `traceability/rar-reconciliation/**` (مخرجات B1 نفسها — لا تُثبت ذاتها) · `superseded/**` (تاريخي منسوخ). **الأمر القياسي:** `grep -rInE "<terms>" . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`. النتيجة الصفرية تعني: لا مصدر حاكم نافذ يقنِّن البند عند HEAD أعلاه. غياب العثور **ليس إثباتاً وجودياً**؛ يُقرأ ضمن نطاق البحث المذكور فقط، ويثبت غياب **المصطلح المبحوث** — لذلك يرافق كل حالة قسم «مطابقات قريبة فُحصت واستُبعدت» حين وُجدت مطابقات.
> **أهلية الدليل:** حالة الملف في `INDEX.md` تحكم أهليته. `architecture/PROVIDER_MODEL_SECRET_CONFIG_SPEC.md` حالته **`Proposed (v0.7)`** (`INDEX.md:16`) ⇒ **غير نافذ** لإثبات سياسة، ويُذكر عند وروده كمطابقة قريبة مستبعَدة لا كدليل.
> **ممنوع** وصف مطابقة بأنها «الوحيدة» ما لم يُرجع الأمر الموثَّق ذلك العدد بالضبط.

---

<a id="absence-b1-rar002-001"></a>
### ABSENCE-B1-RAR002-001
- **البند [01]:** مسار «طلب إنشاء Agent» من مستخدم لا يملك صلاحية الإنشاء، بمراجعة أدمن وقرار قبول/تعديل/رفض بسبب، ينتهي بـDraft مملوك (`CANON-RAR002-REQFLOW` — G02).
- **السؤال المراد إثبات غيابه:** هل يقنِّن المستودع الحاكم مسار طلبٍ واعتمادٍ لإنشاء وكيل؟
- **المصطلحات والمرادفات:** `طلب إنشاء Agent` · `طلب إنشاء وكيل` · `agent request` · `agent creation request` · `Agent Request` · `طلب وكيل`.
- **المسارات المشمولة:** كل `.md` حاكم في الشجرة. **المستثناة:** النطاق المشترك أعلاه.
- **الأمر:** `grep -rInE 'طلب إنشاء (Agent|وكيل)|agent (creation )?request|Agent Request|طلب وكيل' . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
- **النتيجة:** **0**.
- **لماذا لا يثبت الموجود البند:** الشاشة الوحيدة المسماة `admin.agents` هي «نماذج الوكلاء (per-agent)» بمالك **سوبر أدمن** وبياناتها `agent_bindings+params` وفعلها `agent.binding_change` — ربط نموذج، لا إنشاء وكيل ولا طلب اعتماد. المطلب نفسه ثابت وواضح (طلب → مراجعة → قرار → Draft) والناقص تقنينه ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_ADMIN_CONSOLE_MODEL.md:81` (OD-BLD-1) — **لماذا بدت قريبة:** تحسم أوضاع «الباني» وتقرّ خطاً محكوماً `draft→migration→checks→approval→apply`. **لماذا استُبعدت:** نطاقها باني **الشاشات/الأنواع** (`admin.record_types`) لا الوكلاء، وتقرّر أن البناء للأدمن. **لماذا لا تثبت البند:** لا تنشئ مسار طلبٍ من مستخدم غير مخوَّل ولا تمنعه.
  - `ui/UI_SCREEN_INVENTORY.md:54` · `:110` — **لماذا بدتا قريبتين:** تعرِّفان `admin.agents`. **لماذا استُبعدتا:** «الوكيل» فيهما = ربط نموذج ومعاملات وfallback. **لماذا لا تثبتان البند:** لا كيان Agent قابلاً للإنشاء ولا تدفق طلب.

<a id="absence-b1-rar002-002"></a>
### ABSENCE-B1-RAR002-002
- **البند [02]:** تمثيل الحالة الجانبية `Dependency Missing` في دورة حياة الوكيل.
- **السؤال:** هل يقنِّن المستودع حالةً للوكيل تعني «تبعية مفقودة»؟
- **المصطلحات:** `Dependency Missing` · `dependency_missing` · `تبعية مفقودة`. **المسارات:** كل `.md` حاكم عدا المستثنى.
- **الأمر:** `grep -rInE 'Dependency Missing|dependency_missing|تبعية مفقودة' . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
- **النتيجة:** **0**. **لماذا لا يثبت الموجود البند:** لا دورة حياة للوكيل في المستودع أصلاً ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-4-advanced-permissions-feature-management.md:33` (FR-4.8) — **قريبة:** مخطط تبعيات من manifests و«تعطيل قدرة يُخفيها ويُعطّل مساراتها بلطف». **مستبعَدة:** موضوعها **القدرات/الأعلام** لا الوكلاء. **لا تثبت البند:** لا تُنتج حالةً للوكيل ولا تصف أثر نقص تبعية عليه.

<a id="absence-b1-rar002-003"></a>
### ABSENCE-B1-RAR002-003
- **البند [03]:** تمثيل الحالة الجانبية `Knowledge Updating`.
- **السؤال:** هل يقنِّن المستودع حالةً للوكيل أثناء تحديث معرفته؟
- **المصطلحات:** `Knowledge Updating` · `knowledge_updating` · `تحديث المعرفة`. **المسارات:** كل `.md` حاكم عدا المستثنى.
- **الأمر:** `grep -rInE 'Knowledge Updating|knowledge_updating|تحديث المعرفة' . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
- **النتيجة:** **0**. **لماذا لا يثبت الموجود البند:** ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-5-files-rag-ocr-media.md:28` (FR-5.2) — **قريبة:** «Pipeline ingestion غير متزامن بحالات قابلة للتتبع وإعادة محاولة». **مستبعَدة:** حالات **المستند/الخط** لا الوكيل. **لا تثبت البند:** لا تُسند إلى وكيل ولا تحجب استعماله أثناء التحديث.
  - `phases/designs/phase-5-files-rag-ocr-media.md:37` (FR-5.11) — **قريبة:** إبطال الكاش وإعادة الفهرسة عند نسخة جديدة. **مستبعَدة:** أثرها على الفهرس لا على حالة وكيل. **لا تثبت البند:** لا حالة معلنة يقرأها المستخدم أو الأدمن.

<a id="absence-b1-rar002-004"></a>
### ABSENCE-B1-RAR002-004
- **البند [04]:** تمثيل الحالة الجانبية `Policy Conflict`.
- **السؤال:** هل يقنِّن المستودع حالةً للوكيل تعني تعارض سياسة؟
- **المصطلحات:** `Policy Conflict` · `policy_conflict` · `تعارض سياسة` · `تعارض السياسات`. **المسارات:** كل `.md` حاكم عدا المستثنى.
- **الأمر:** `grep -rInE 'Policy Conflict|policy_conflict|تعارض سياسة|تعارض السياسات' . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
- **النتيجة:** **0**. **لماذا لا يثبت الموجود البند:** ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-4-advanced-permissions-feature-management.md:30` (FR-4.5) — **قريبة:** «مصفوفة تعارض أدوار تمنع الإسناد المتعارض وتمنع الفعل المزدوج». **مستبعَدة:** التعارض بين **أدوار مستخدمين** (SoD) لا بين سياسات تمسّ وكيلاً. **لا تثبت البند:** تمنع الإسناد ولا تُنتج حالة تشغيلية للوكيل.

<a id="absence-b1-rar002-005"></a>
### ABSENCE-B1-RAR002-005
- **البند [05]:** تمثيل الحالة الجانبية `Degraded` للوكيل.
- **السؤال:** هل يقنِّن المستودع حالة «متدهور» **للوكيل** (لا للمزوّد/النموذج)؟
- **المصطلحات:** `agent … degraded` · `degraded … وكيل` · `متدهور` مقترناً بالوكيل. **المسارات:** كل `.md` حاكم عدا المستثنى.
- **الأمر:** `grep -rInE 'agent.*(degraded|متدهور)|(degraded|متدهور).*(وكيل|agent)' . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
- **النتيجة:** **0**. **لماذا لا يثبت الموجود البند:** التدهور المقنَّن يخص المزوّد/النموذج وأثره على التشغيل، لا حالةً للوكيل نفسه ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_RUN_EXECUTION_MODEL.md:58` — **قريبة:** «Safe Mode ⇒ تشغيلات غير A10 تتجمد بحالة معلّلة؛ model_down ⇒ خطوات الوكلاء تتوقف». **مستبعَدة:** حالة **التشغيل/الخطوة** استجابةً لتعطّل النموذج. **لا تثبت البند:** لا تسم الوكيل بحالة `Degraded` ولا تعرّف تعافيه.
  - `ui/UI_AI_WORKSPACE_MODEL.md:18` · `:55` — **قريبة:** `banner.degraded` وأوضاع التدهور. **مستبعَدة:** لافتات سطح المحادثة. **لا تثبت البند:** عرضٌ لا حالة كيان.
  - `architecture/PROVIDER_MODEL_SECRET_CONFIG_SPEC.md:36` — **قريبة:** `health_state … degraded`. **مستبعَدة مرتين:** موضوعها **المزوّد**، وحالة الملف **`Proposed (v0.7)`** (`INDEX.md:16`) ⇒ غير نافذ لإثبات سياسة. **لا تثبت البند.**

<a id="absence-b1-rar002-006"></a>
### ABSENCE-B1-RAR002-006
- **البند [06]:** تمثيل الحالة الجانبية `Revoked` للوكيل.
- **السؤال:** هل يقنِّن المستودع سحب وكيل بعد نشره؟
- **المصطلحات:** `agent … revoke` · `revoked … وكيل` · `سحب الوكيل`. **المسارات:** كل `.md` حاكم عدا المستثنى.
- **الأمر:** `grep -rInE 'agent.*revoke|revoked.*(وكيل|agent)|سحب الوكيل' . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
- **النتيجة:** **0**. **لماذا لا يثبت الموجود البند:** ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-7-integrations-app-store.md:29` (FR-7.3) — **قريبة:** «انتهاء/تجديد، إلغاء ذاتي وإداري» لربط الحساب. **مستبعَدة:** موضوعها **ربط حساب المستخدم بموصل**. **لا تثبت البند:** لا تسحب وكيلاً ولا تعرّف أثر السحب على تشغيلاته الجارية.
  - `phases/designs/phase-4-advanced-permissions-feature-management.md:27` (FR-4.2) — **قريبة:** منح ReBAC «تُلغى بانتهاء العلاقة». **مستبعَدة:** إلغاء **منح صلاحية** لا سحب كيان. **لا تثبت البند.**

<a id="absence-b1-rar002-007"></a>
### ABSENCE-B1-RAR002-007
- **البند [07]:** تمثيل الحالة الجانبية `Pending Revalidation`.
- **السؤال:** هل يقنِّن المستودع حالةً للوكيل بانتظار إعادة تحقق؟
- **المصطلحات:** `Pending Revalidation` · `pending_revalidation` · `بانتظار إعادة التحقق`. **المسارات:** كل `.md` حاكم عدا المستثنى.
- **الأمر:** `grep -rInE 'Pending Revalidation|pending_revalidation|بانتظار إعادة التحقق' . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
- **النتيجة:** **0**. **لماذا لا يثبت الموجود البند:** ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_ACTION_BUTTON_MODEL.md:40` — **قريبة:** «إعادة تحقق حية للسياق (نسخة السجل/السياسة)». **مستبعَدة:** إعادة تحقق **لحظة تنفيذ الفعل**، بلا حالة مخزَّنة. **لا تثبت البند:** لا تُجمِّد وكيلاً بانتظار إعادة اعتماد.
  - `constitution.md:24` (م16) — **قريبة:** إعادة تحقق live عند تغيّر النسخة/النطاق. **مستبعَدة:** سياسة **كاش**. **لا تثبت البند.**

<a id="absence-b1-rar002-008"></a>
### ABSENCE-B1-RAR002-008
- **البند [18]:** فحص الملفات (محتوى/برمجيات خبيثة) — الشقّ غير المقنَّن من «File scanning/classification»؛ شقّ **التصنيف** مقنَّن (`phases/designs/phase-5-files-rag-ocr-media.md:27`).
- **السؤال:** هل يقنِّن المستودع فحصاً أمنياً لمحتوى الملفات عند الرفع؟
- **المصطلحات:** `antivirus` · `malware` · `فحص الملفات` · `برمجيات خبيثة` · `file scanning` · `مسح الفيروسات`. **المسارات:** كل `.md` حاكم عدا المستثنى.
- **الأمر:** `grep -rInE 'antivirus|malware|فحص الملفات|برمجيات خبيثة|file scanning|مسح الفيروسات' . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
- **النتيجة:** **0**.
- **لماذا لا يثبت الموجود البند:** FR-5.1 يوجب الميتاداتا والتصنيف والصلاحيات الموروثة، وFR-5.4 يوسم كل chunk بالتصنيف — كلاهما **تصنيف** لا **فحص**؛ ولا خطوة فحص في خط الـingestion (FR-5.2). المطلب واضح والناقص تقنينه ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-4-advanced-permissions-feature-management.md:77` — **قريبة:** تحتوي كلمة «فحص» ضمن «مصفوفة سيناريوهات سياسات مركّبة … كاختبار». **مستبعَدة:** اختبار سياسات صلاحية. **لا تثبت البند:** لا صلة بالملفات.

<a id="absence-b1-rar002-009"></a>
### ABSENCE-B1-RAR002-009
- **البند [23]:** حدود بيئة اختبار الوكيل (Test sandbox boundaries).
- **السؤال:** هل يقنِّن المستودع بيئة اختبار معزولة لتجريب وكيل قبل نشره وحدودها؟
- **المصطلحات:** `sandbox` · `صندوق اختبار` · `بيئة اختبار معزولة`. **المسارات:** كل `.md` حاكم عدا المستثنى.
- **الأمر:** `grep -rInE 'sandbox|صندوق اختبار|بيئة اختبار معزولة' . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
- **النتيجة:** **1** مطابقة (لا صفر — وليست «الوحيدة» بمعنى الكفاية).
- **لماذا لا يثبت الموجود البند:** المطابقة الوحيدة تخص الموصلات لا الوكلاء، وبقيد «حيث أمكن». ويؤكد `SEMANTIC-MERGE-MAP.md:23` (مدخل مجمَّد) أن `RAR002-S14-017` **component مختلف** عن عزل تنفيذ حزم RAR-004 ⇒ لا يُستعار دليلها. ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-7-integrations-app-store.md:13` — **قريبة:** «dry-run/sandbox للموصلات حيث أمكن». **مستبعَدة:** نطاقها **الموصلات**، وقيدها «حيث أمكن» يجعلها قدرة اختيارية. **لا تثبت البند:** لا تعرّف حدود بيئة اختبار وكيل (أي بيانات يراها · أي أفعال يُسمح بها · كيف تُمنع الآثار الحقيقية).

<a id="absence-b1-rar002-010"></a>
### ABSENCE-B1-RAR002-010
- **البند [25]:** منع تسرّب البيانات بين الوكلاء (Cross-agent data leakage).
- **السؤال:** هل يقنِّن المستودع عزلاً يمنع وصول وكيل إلى سياق أو بيانات وكيل آخر؟
- **المصطلحات:** `cross-agent` · `agent isolation` · `عزل الوكلاء` · `بين الوكلاء` · `عبور البيانات بين` · `تسرب بين`. **المسارات:** كل `.md` حاكم عدا المستثنى.
- **الأمر:** `grep -rInE 'cross-agent|عبور البيانات بين|بين الوكلاء|agent isolation|عزل الوكلاء|تسرب بين' . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
- **النتيجة:** **0**.
- **لماذا لا يثبت الموجود البند:** الضوابط النافذة تعزل **المستخدم** (م2 · RLS/FLS · ADR-0037) لا **الوكيل عن الوكيل**؛ و`agent_bindings` تربط نموذجاً ومعاملات لكل وكيل ولا تعرّف حدود سياق بينها. المطلب واضح والناقص تقنينه ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-6-gateway-agents-sql-chat-reports.md:52` — **قريبة:** تعدّد `agent_bindings (per-agent model+params)` و`chat_sessions/messages`. **مستبعَدة:** كيانات ربط وجلسات، بلا حدّ سياق بين الوكلاء. **لا تثبت البند.**
  - `phases/designs/phase-7-integrations-app-store.md:83` — **قريبة:** تذكر «تسرب رموز» ضمن سجل المخاطر. **مستبعَدة:** تسرب **أسرار الربط** لا بيانات بين وكلاء. **لا تثبت البند.**

---
**ملاحظات إنفاذ:** 10 مراسٍ صريحة فريدة (`absence-b1-rar002-001..010`) · كل مرساة تخدم بنداً واحداً · لا إعادة استخدام مرساة لعدة صفوف · كل استشهاد في `RECONCILIATION-RAR-002.md` يحلّ إلى مرساة موجودة هنا · لا مرجع مكسور · لا مرساة يتيمة. **عدّ المراسي يشمل `<a id=...>` الصريحة وحدها.**

**قواعد سارية:** (1) العمود الرابع لا يستشهد بـ`references/analysis-inputs/**` إطلاقاً — دليل حاكم أو مرساة غياب أو سلطتان متعارضتان فقط. (2) نصٌّ حالته `Proposed` غير نافذ لإثبات سياسة `Accepted`. (3) بند نوعه المجمَّد `REQ` لا يُحوَّل إلى «تعارض يحتاج قراراً» لمجرد غياب التقنين أو احتمال صياغته خياراً؛ يلزم تسمية القرار المالك وبديلين يغيّر اختيارهما التزاماً نافذاً وإثبات أحد محفّزات §14-هـ الأربعة.

**درس تدقيقي مُطبَّق في هذه الجولة:** إعادة تنفيذ الأوامر قبل الكتابة نقلت `[15] Tool output sanitization` من «دلتا» إلى **«مقنَّن»**: المصطلح في المستودع عربي («تعقيم مخرجات الأدوات قبل دخولها سياق النموذج») في `phases/designs/phase-7-integrations-app-store.md:62`، ولم يكن ليظهر ببحث إنجليزي وحده. النتيجة الصفرية تثبت غياب المصطلح لا غياب الحكم.
