# ABSENCE-EVIDENCE-B1-RAR001 — B1 Commit 2

> **مخرج traceability/تحليلي غير حاكم** — لا سلطة موازية. حزمة أدلة غياب **قابلة لإعادة التنفيذ** لبنود RAR-001 المصنَّفة «دلتا حقيقية» (`001..011`) أو «تعارض يحتاج قراراً» (`012..020` — نوعها المجمَّد `DQ`). كل حالة بمرساة HTML صريحة بأحرف صغيرة. **HEAD المنفَّذ عليه البحث:** `188ad379d04b07ca9e1b4eeee38dc68ecca29914`.
> **نطاق مشترك مستثنى من «الحاكم»:** `references/analysis-inputs/**` (مدخل غير حاكم) · `traceability/rar-reconciliation/**` (مخرجات B1) · `superseded/**` (تاريخي). **الأمر القياسي:** `grep -rInE "<terms>" . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`. النتيجة الصفرية تعني: لا مصدر حاكم نافذ يقنِّن البند عند HEAD أعلاه. غياب العثور **ليس إثباتاً وجودياً**؛ يُقرأ ضمن نطاق البحث المذكور فقط.

<a id="absence-b1-rar001-001"></a>
### ABSENCE-B1-RAR001-001
- **البند:** عقود Schedule وAutomation (ضمن «Contracts للأنواع الأربعة»، الصف #9). Task/Workflow لهما تغطية جزئية (`ui/UI_ACTION_BUTTON_MODEL.md:67` · `contracts/screens/admin.workflows.md:19`).
- **المصطلحات/المرادفات:** `schedule.` · `contracts/screens/schedule` · `Schedule Contract` · `automation.` · `Automation Contract`.
- **النطاق:** كل `.md` حاكم (contracts/ · ui/ · catalogs/ · decisions/ · phases/) عدا المستثنى.
- **الأمر:** `grep -rInE 'schedule\.(create|list)|contracts/screens/schedule|Schedule Contract' … ` و`… 'automation\.(create|list)|contracts/screens/automation|Automation Contract' …`
- **النتيجة:** **0** لكلٍّ. **لماذا لا يكفي الموجود:** task.create/admin.workflows يغطيان نوعين فقط؛ Schedule/Automation بلا عقد. ⇒ دلتا حقيقية → R3.

<a id="absence-b1-rar001-002"></a>
### ABSENCE-B1-RAR001-002
- **البند:** Lifecycles رسمية لـTask/Schedule/Automation (الصف #10). workflow lifecycle موجود (`contracts/screens/admin.workflows.md:22`).
- **المصطلحات:** `task lifecycle` · `schedule lifecycle` · `automation lifecycle` · `دورة حياة (مهمة/جدولة/أتمتة)`.
- **الأمر:** `grep -rInE 'lifecycle|دورة حياة' contracts/ ui/ | grep -iE 'task|schedule|automation'` (خارج المستثنى).
- **النتيجة:** **0** لدورات حياة رسمية لهذه الأنواع الثلاثة (الموجود يخص workflow/records). ⇒ دلتا حقيقية → R3.

<a id="absence-b1-rar001-003"></a>
### ABSENCE-B1-RAR001-003
- **البند:** API Operation Contracts للأنواع الأربعة (الصف #11). النمط العام معرَّف (`decisions/adr/ADR-0025-api-contract-rest-openapi-sse.md:7`) لكن لا عقود عمليات مُصنَّفة لكل نوع.
- **المصطلحات:** `API Operation Contract` · `api_operation` لكل نوع · `OpenAPI operation` لـtask/schedule/automation.
- **الأمر:** `grep -rInE 'API Operation Contract|api_operation' … | grep -iE 'task|schedule|automation'`.
- **النتيجة:** **0** عقود عمليات مخصّصة (الموجود: نمط ADR-0025 + أفعال UI متفرقة). ⇒ دلتا حقيقية → R3.

<a id="absence-b1-rar001-004"></a>
### ABSENCE-B1-RAR001-004
- **البند:** نموذج موارد موحّد أو فصل واضح بين الأنواع الأربعة (الصف #17).
- **المصطلحات:** `نموذج موارد موحد` · `unified resource model` · `Resource Model`.
- **الأمر:** `grep -rInE 'نموذج موارد موحد|unified resource model|Resource Model' …`.
- **النتيجة:** **0**. ⇒ دلتا حقيقية → R3.

<a id="absence-b1-rar001-005"></a>
### ABSENCE-B1-RAR001-005
- **البند:** Intent Interpretation Contract (الصف #19).
- **المصطلحات:** `Intent Interpretation` · `Intent Contract` · `intent_interpretation`.
- **الأمر:** `grep -rInE 'Intent Interpretation|Intent Contract' …`.
- **النتيجة:** **0**. ⇒ دلتا حقيقية → R3.

<a id="absence-b1-rar001-006"></a>
### ABSENCE-B1-RAR001-006
- **البند:** Resolution Contract للأشخاص/السجلات/الوحدات (الصف #20).
- **المصطلحات:** `Resolution Contract` · `resolution_contract`.
- **الأمر:** `grep -rInE 'Resolution Contract|resolution_contract' …`.
- **النتيجة:** **0**. ⇒ دلتا حقيقية → R3.

<a id="absence-b1-rar001-007"></a>
### ABSENCE-B1-RAR001-007
- **البند:** Recurrence (تكرار الجدولة) (الصف #24).
- **المصطلحات:** `Recurrence` · `RRULE` · `cron` · `تكرار`.
- **الأمر:** `grep -rInE 'Recurrence|RRULE|cron' …`.
- **النتيجة:** **0** حاكم. ⇒ دلتا حقيقية → R5.

<a id="absence-b1-rar001-008"></a>
### ABSENCE-B1-RAR001-008
- **البند:** Timezone في الجدولة (الصف #25).
- **المصطلحات:** `Timezone` · `time zone` · `منطقة زمنية` (في سياق جدولة/تشغيل).
- **الأمر:** `grep -rInE 'Timezone|time zone|منطقة زمنية' …`.
- **النتيجة:** **0** حاكم لسياسة توقيت الجدولة. ⇒ دلتا حقيقية → R5.

<a id="absence-b1-rar001-009"></a>
### ABSENCE-B1-RAR001-009
- **البند:** Business Calendar (الصف #26).
- **المصطلحات:** `Business Calendar` · `business_calendar` · `تقويم العمل` · `holidays`.
- **الأمر:** `grep -rInE 'Business Calendar|business_calendar|تقويم العمل' …`.
- **النتيجة:** **0**. ⇒ دلتا حقيقية → R5.

<a id="absence-b1-rar001-010"></a>
### ABSENCE-B1-RAR001-010
- **البند:** Event Trigger abstraction (الصف #27).
- **المصطلحات:** `Event Trigger` · `event_trigger` · `Trigger abstraction`.
- **الأمر:** `grep -rInE 'Event Trigger|event_trigger|Trigger abstraction' …`.
- **النتيجة:** **0** حاكم (جاهزية Event Contracts مسارها R10). ⇒ دلتا حقيقية → R10.

<a id="absence-b1-rar001-011"></a>
### ABSENCE-B1-RAR001-011
- **البند:** Audit Event Catalogue **موحّد** (الصف #35). حقل `audit_event` معرَّف لكل عقد (`ui/UI_FIELD_NAMING.md:23`)، لكن لا كتالوج أحداث تدقيق مُجمَّع.
- **المصطلحات:** `Audit Event Catalog` · `كتالوج أحداث التدقيق` · `audit event catalogue`.
- **الأمر:** `grep -rInE 'Audit Event Catalog|كتالوج أحداث' …`.
- **النتيجة:** **0** لكتالوج موحّد (الموجود: أحداث لكل شاشة/عقد). ⇒ دلتا حقيقية → R3.

## أدلة غياب أسئلة §14 المفتوحة (تصحيح Commit 2 — الصفوف [39][40][41][42][44][45][46][49][53])
> نُفِّذت البحوث عند HEAD `9450a3b39457c116069f4ce26c29ac1cb0c71f82`. أثبتت أن **المستودع الحاكم لا يحسم** كل سؤال (Path A)؛ لم يظهر تعارض حاكم فعلي (Path B) لأيٍّ منها. **لا يُستشهد بـ`references/analysis-inputs/**` في العمود الرابع** — بديله مرساة الغياب أدناه.
>
> **[تصحيح دلالة النتيجة — لا إعادة تنفيذ]** أوامر البحث ونطاقه ومصطلحاته وSHA ونتائجه الصفرية ومراسيه **لم تتغير**. المتغيّر هو **قراءة النتيجة**: البنود التسعة نوعها المجمَّد في `NORMALIZATION-AND-ATOMIC-MAP.md:14` هو **`DQ` (سؤال قرار)** — «تبقى أسئلة قرار (لا تُحوَّل إلى إلزام)». لذلك يثبت الغياب **بقاء سؤال القرار** ولا يرقّيه مطلباً تنفيذياً: الحكم لكلٍّ منها **«تعارض يحتاج قراراً»** لا «دلتا حقيقية». `absence evidence` يثبت غياب الحسم الحاكم، ولا يثبت أن الإجابة المطلوبة «نعم».
>
> **أقسام «مطابقات قريبة فُحصت واستُبعدت»** المضافة أدناه توثيقية للتدقيق فقط: لا توسّع نطاقاً ولا تعدّل أمراً ولا تغيّر حكماً — تسجّل ما بدا قريباً من السؤال ولماذا لم يكفِ لحسمه. **لم يُستوفَ استثناء «الدليل الحاكم الموجب المباشر» في أيٍّ من الحالات التسع.**

<a id="absence-b1-rar001-012"></a>
### ABSENCE-B1-RAR001-012
- **البند [39]:** هل Task مورد مستقل أم تجسيد مبسط لـWorkflow؟ (تعريف علاقة Task↔Workflow).
- **النطاق/المصطلحات:** `Task vs Workflow` · `مورد مستقل` · `projection/تجسيد` · `علاقة Task`. **الأمر:** `grep -rInE 'Task.*(مورد مستقل|projection|تجسيد)|Task vs Workflow|علاقة Task' . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`.
- **النتيجة:** **0** حاكم. task.create وadmin.workflows يتعايشان دون نصٍّ يحسم استقلالية Task عن Workflow. **⇒ لا حسم حاكم؛ نوع البند المجمَّد `DQ` ⇒ يبقى Owner Decision / تعارض يحتاج قراراً → SA (R3 pending decision).**
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `knowledge/BUSINESS_GLOSSARY.md:13` — **لماذا بدت قريبة:** تعريف Task «الملزم» مع الموطن التقني. **لماذا استُبعدت:** تجعل الموطن **«queue.tasks · workflow steps» معاً**، وتميّز Task عن *Run* و«Task Card» لا عن *Workflow*. **لا تحسم السؤال:** تحتمل القراءتين (خطوة الـworkflow قد تكون Task / Task كيان مستقل) بدل أن تختار بينهما.
  - `ui/UI_SCREEN_INVENTORY.md:71` — **لماذا بدت قريبة:** `queue.tasks` له `primary_data` باسم `tasks`، مقابل `workflow_definitions` لـ`admin.workflows` (`:100`). **لماذا استُبعدت:** جرد شاشات يصف بيانات الشاشة، لا نموذج موارد يقرّر العلاقة. **لا تحسم السؤال:** وجود `tasks` كبيانات شاشة لا ينفي كونها تجسيداً لخطوات workflow — استنتاج ضمني لا دليل مباشر.

<a id="absence-b1-rar001-013"></a>
### ABSENCE-B1-RAR001-013
- **البند [40]:** الحد بين Schedule وAutomation Trigger. **المصطلحات:** `Schedule Automation` · `Automation Trigger boundary` · `حد Trigger`. **الأمر:** `grep -rInE 'Schedule.*Automation|حد.*Trigger|Automation Trigger boundary' … `.
- **النتيجة:** **0** حاكم (Schedule/Automation غير مقنَّنين أصلاً — قارن ABSENCE-001). **⇒ لا حسم حاكم؛ نوع البند المجمَّد `DQ` ⇒ يبقى Owner Decision / تعارض يحتاج قراراً → SA (R3 pending decision).**
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `knowledge/BUSINESS_GLOSSARY.md:14` (Schedule) و`:16` (Automation) — **لماذا بدتا قريبتين:** تعريفان **ملزمان** للمصطلحين معاً، وأحدهما يصف الجدولة بأنها **«مشغِّل»**. **لماذا استُبعدتا:** يعرّفان الطرفين ولا **يرسمان الحد** بينهما؛ ونصّ المعجم نفسه يترك عقد Schedule معلَّقاً بعبارة «(يقنَّن عقده بمرحلته)». **لا تحسمان السؤال:** قراءة «Schedule نوعُ مُطلِق زمني داخل Automation» وقراءة «Schedule مورد مستقل» كلتاهما متوافقة مع النصّين — وهذا عين سؤال القرار المطروح.

<a id="absence-b1-rar001-014"></a>
### ABSENCE-B1-RAR001-014
- **البند [41]:** أين تخزَّن صياغة المستخدم الأصلية؟ **المصطلحات:** `صياغة المستخدم الأصلية` · `original utterance` · `user phrasing` · `raw user input`. **الأمر:** `grep -rInE 'صياغة المستخدم الأصلية|original utterance|user phrasing|raw user input' …`.
- **النتيجة:** **0** حاكم. **⇒ لا حسم حاكم؛ نوع البند المجمَّد `DQ` ⇒ يبقى Owner Decision / تعارض يحتاج قراراً → SA (R3 pending decision).**
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-6-gateway-agents-sql-chat-reports.md:52` (§11 Data Model Notes) — **لماذا بدت قريبة:** تثبت الكيان `chat_sessions/messages(+citations)` في `platform_db`، وهو أقرب موضع تخزين محتمل. **لماذا استُبعدت:** تعدّد كيانات المرحلة ولا تتناول **صياغة المستخدم الأصلية** بوصفها موضوعاً؛ لا تنصّ على حفظ النص **حرفياً** ولا تمنع تخزين صيغة مُعالَجة بدله. **لا تحسم السؤال:** الاستدلال بأن `messages` = «الصياغة الأصلية» **استنتاج ضمني** لا يجيز استثناء الدليل الموجب.
  - `ui/UI_SCREEN_INVENTORY.md:70` — **لماذا بدت قريبة:** `primary_data` لـ`ws.main` = `sessions/messages/citations/action-proposals`. **لماذا استُبعدت:** بيانات شاشة لا عقد احتفاظ/تخزين. **لا تحسم السؤال:** لا تذكر أصلية النص ولا مدّة حفظه ولا مالكه.

<a id="absence-b1-rar001-015"></a>
### ABSENCE-B1-RAR001-015
- **البند [42]:** تخزين خطة LLM الوسيطة (الجزء الذرّي غير «منع اعتبارها حقيقة» المقنَّن بم1). **المصطلحات:** `خطة LLM` · `LLM plan` · `intermediate plan` · `الخطة الوسيطة`. **الأمر:** `grep -rInE 'خطة LLM|LLM plan|intermediate plan|الخطة الوسيطة' …`.
- **النتيجة:** **0** حاكم لآلية التخزين. **⇒ لا حسم حاكم؛ نوع البند المجمَّد `DQ` ⇒ يبقى Owner Decision / تعارض يحتاج قراراً → SA (R3 pending decision).**
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_SCREEN_INVENTORY.md:70` — **لماذا بدت قريبة:** تدرج `action-proposals` ضمن `primary_data` لـ`ws.main`، وهي المقابل التطبيقي لـ«الخطة الوسيطة». **لماذا استُبعدت:** بيانات شاشة لا كيان مخزَّن معلن. **لا تحسم السؤال:** لا تقول **هل تُحفظ** ولا **أين** ولا **إلى متى**.
  - `phases/designs/phase-6-gateway-agents-sql-chat-reports.md:52` — **لماذا بدت قريبة:** هي عقد نموذج بيانات المرحلة التي تُنشئ الدردشة. **لماذا استُبعدت:** تعدّد الكيانات المخزَّنة ولا تذكر كياناً لاقتراحات الأفعال — وهو **صمت** لا قرار بالمنع. **لا تحسم السؤال:** الصمت لا يجيب «هل تحفظ؟».
  - `NORMALIZATION-AND-ATOMIC-MAP.md:49` (مدخل مجمَّد — للسياق لا للاستشهاد) — يوثّق أن انقسام `N-RAR001-S14-004` أنتج ذرّتين: الثانية «منع اعتبارها Source-of-Truth» **مقنَّنة دستورياً (م1)** والأولى **«سؤال تخزين»**. أي أن نوع هذه الذرّة سؤالُ قرارٍ بالتطبيع نفسه.

<a id="absence-b1-rar001-016"></a>
### ABSENCE-B1-RAR001-016
- **البند [44]:** نموذج التحقق Deterministic لمخرجات LLM. **المصطلحات:** `Deterministic verification` · `نموذج التحقق` · `deterministic check`. **الأمر:** `grep -rInE 'Deterministic verification|نموذج التحقق|deterministic check' …`.
- **النتيجة:** **0** حاكم (م1 يمنع اعتبار LLM مصدر حقيقة لكنه لا يعرّف نموذج تحقق حتمياً). **⇒ لا حسم حاكم؛ نوع البند المجمَّد `DQ` ⇒ يبقى Owner Decision / تعارض يحتاج قراراً → SA (R3 pending decision).**
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_UX_ASSUMPTIONS.md:15` (A-UX-2) — **لماذا بدت قريبة:** تنصّ على أن مخرج النموذج «اقتراح action مهيكل **يمر تحققاً حتمياً** وصلاحية ومعاينة وتأكيداً قبل أي أثر» — أي المصطلح العربي المقابل. **لماذا استُبعدت:** تقرّر **وقوع** التحقق وموضعه في السلسلة، ولا تعرّف **النموذج** (أي فحوص · ما ضمانات الحتمية · كيف يُقاس مخرج النموذج على الحقيقة). وترويسة الوثيقة نفسها (`:4`–`:5`) تضعها **«تحت الكتالوج وتصاميم المراحل»** وتقرّر أن «أي بند غير محسوم = Open UX Decision». **لا تحسم السؤال:** «ما النموذج؟» يبقى بلا جواب.
  - `ui/UI_AI_WORKSPACE_MODEL.md:29` (S5 VALIDATED) — **لماذا بدت قريبة:** «تحقق خادمي — الخادم (نفس validators الـRenderer)». **لماذا استُبعدت:** تحدّد **المنفِّذ** لا **النموذج**؛ وهي حالة في دورة بطاقة الفعل. **لا تحسم السؤال:** تكافؤ الـvalidators مع الـRenderer لا يعرّف نموذج تحقق حتمي لمخرجات LLM.

<a id="absence-b1-rar001-017"></a>
### ABSENCE-B1-RAR001-017
- **البند [45]:** ربط Preview بنسخة العقود المتحقَّق عليها. **المصطلحات:** `Preview نسخة العقود` · `preview contract version` · `contract version pin`. **الأمر:** `grep -rInE 'Preview.*نسخة العقود|preview.*contract version|contract version pin' …`.
- **النتيجة:** **0** حاكم (توجد بطاقة معاينة لكن بلا ربط بنسخة عقد). **⇒ لا حسم حاكم؛ نوع البند المجمَّد `DQ` ⇒ يبقى Owner Decision / تعارض يحتاج قراراً → SA (R3 pending decision).**
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_AI_WORKSPACE_MODEL.md:35` («إبطال المعاينة») — **لماذا بدت قريبة:** «البطاقة تحمل **نسخة السياق**؛ تغيّر السجل/السياسة قبل التنفيذ ⇒ إعادة تحقق حية إلزامية». **لماذا استُبعدت:** المربوط هو **نسخة السياق** (سجل/سياسة) لا **نسخة العقود المتحقَّق عليها** — كائنان مختلفان؛ والآلية إبطال وإعادة تحقق لا ربط بنسخة عقد. **لا تحسم السؤال:** مطابقة تقريبية على كائن آخر.
  - `constitution.md:24` (م16) — **لماذا بدت قريبة:** مفاتيح الكاش تشمل «نسخة المستند/الفهرس/**السكيم**/نطاق الصلاحية» + إعادة تحقق live. **لماذا استُبعدت:** نصّ في **الكاش** لا في المعاينة، ويخص RAG لا عقود الأفعال. **لا تحسم السؤال:** نص عام لا يجيب سؤال ربط Preview بنسخة العقد.

<a id="absence-b1-rar001-018"></a>
### ABSENCE-B1-RAR001-018
- **البند [46]:** سياسة «كل تعديل ينتج Version جديداً». **المصطلحات:** `كل تعديل Version` · `version per edit` · `version-on-edit`. **الأمر:** `grep -rInE 'كل تعديل.*Version|version per edit|version-on-edit' …`.
- **النتيجة:** **0** حاكم (ADR-0032 يعرّف التخزين الهجين/UUIDv7 لا سياسة versioning-per-edit). **⇒ لا حسم حاكم؛ نوع البند المجمَّد `DQ` ⇒ يبقى Owner Decision / تعارض يحتاج قراراً → SA (R3 pending decision).**
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `contracts/enums/ENUMS_DICTIONARY.md:25` — **لماذا بدت قريبة:** «الإضافة = تعديل عقد **بنسخة جديدة**». **لماذا استُبعدت:** نطاقها **قاموس التعدادات** وحده، ونوع التغيير **الإضافة** فقط. **لا تحسم السؤال:** لا تقول شيئاً عن «كل تعديل» في تعريفات الأفعال/المهام/الجدولة.
  - `ui/UI_FIELD_NAMING.md:5` — **لماذا بدت قريبة:** «التغيير = نسخة جديدة». **لماذا استُبعدت:** مشروطة بـ**المعرّف التقني** و**بعد الإصدار** فقط. **لا تحسم السؤال:** تحصر القاعدة في إعادة التسمية لا في التعديل عموماً — بل تقترح قراءة معاكسة (ليس كل تعديل).
  - `ui/UI_ACTION_BUTTON_MODEL.md:93` — **لماذا بدت قريبة:** تعريف/تعديل العقود من `admin.actions` يجري «مع versioning وأحداث ACTION_DEFINED/CHANGED». **لماذا استُبعدت:** تثبت وجود versioning ولا تحدّد **متى** تُنشأ نسخة. **لا تحسم السؤال:** «هل **كل** تعديل ينتج نسخة؟» يبقى مفتوحاً — والنصوص الثلاثة معاً تحتمل نطاقين متنافسين.

<a id="absence-b1-rar001-019"></a>
### ABSENCE-B1-RAR001-019
- **البند [49]:** Dry Run للمهام/الأتمتة قبل التشغيل الحي. **المصطلحات:** `Dry Run` · `dry-run` · `تشغيل تجريبي`. **الأمر:** `grep -rInE 'Dry Run|dry-run|تشغيل تجريبي' …`.
- **النتيجة:** `phases/phase-0-foundation-full-stack-skeleton.md:93` تخص **offline-install dry-run** (مفهوم بناء/تثبيت مختلف) لا Dry-Run تنفيذ الأتمتة. **0** حاكم لـDry-Run تنفيذ المهام/الأتمتة. **⇒ لا حسم حاكم؛ نوع البند المجمَّد `DQ` ⇒ يبقى Owner Decision / تعارض يحتاج قراراً → SA (R3 pending decision).**
  > **[تصحيح عيب تقني — أُبلغ عنه لـSA]** كان النص يصف مطابقة `phase-0:93` بأنها **«المطابقة الوحيدة»**؛ وهذا **غير صحيح**: الأمر الموثَّق أعلاه يعيد **ثلاث** مطابقات عند SHA نفسه. أُسقطت كلمة «الوحيدة» وأُدرجت المطابقتان الأخريان في قسم المطابقات القريبة أدناه. **لم يتغيّر:** الأمر · النطاق · المصطلحات · SHA · المرساة · المعرِّف · الحكم النهائي (لا حسم حاكم).
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-7-integrations-app-store.md:13` — **لماذا بدت قريبة:** تنصّ على «**dry-run/sandbox للموصلات حيث أمكن**» — أقرب ذكر لتجربة فعل قبل تنفيذه. **لماذا استُبعدت:** مقيَّدة بـ**الموصلات** وبقيد «**حيث أمكن**» أي قدرة اختيارية غير ملزمة. **لا تحسم السؤال:** «هل نحتاج Dry Run؟» لتنفيذ المهام/الأتمتة يبقى بلا جواب ملزم.
  - `phases/designs/phase-7-integrations-app-store.md:83` — **لماذا بدت قريبة:** تعدّ dry-run **ضابط تخفيف** لخطر «فعل خطير رغم الضوابط» مع «HITL إلزامي + مصفوفة P4». **لماذا استُبعدت:** بند في **سجل مخاطر** لا عقد إلزام، ويخص P7/الموصلات. **لا تحسم السؤال:** بل تُظهر قوّتين إلزاميّتين متفاوتتين للمفهوم نفسه داخل الوثيقة ذاتها — وهو ما يجعل السؤال قراراً مالكياً لا مطلباً مستقراً.

<a id="absence-b1-rar001-020"></a>
### ABSENCE-B1-RAR001-020
- **البند [53]:** تمثيل «عندما يكتمل الجميع» بشكل قابل للاختبار (دلالة join). **المصطلحات:** `عندما يكتمل الجميع` · `all complete` · `join semantics` · `wait for all`. **الأمر:** `grep -rInE 'عندما يكتمل الجميع|all complete|join semantics|wait for all' …`.
- **النتيجة:** **0** حاكم (admin.workflows جدول حالات/انتقالات لكن بلا دلالة join «all-of» قابلة للاختبار). **⇒ لا حسم حاكم؛ نوع البند المجمَّد `DQ` ⇒ يبقى Owner Decision / تعارض يحتاج قراراً → SA (R5 pending decision).**
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_RUN_EXECUTION_MODEL.md:48` — **لماذا بدت قريبة:** «الفروع المتوازية تُعرض مسارات متجاورة **تلتقي بعقدة دمج** (تعميم مؤسسي لنمط fan-out/fan-in)» — المصطلح العربي المقابل لـjoin. **لماذا استُبعدت:** قاعدة **عرض** في الخط الزمني لـ`runs.detail`، ونصّها صريح في أنها تخصّ ما «**تُعرض**». **لا تحسم السؤال:** لا تعرّف المحمول ولا الشرط ولا معيار النجاح الذي يجعل «اكتمال الجميع» **قابلاً للاختبار** — وهو موضوع السؤال.

---
**ملاحظات إنفاذ:** 20 مرساة صريحة فريدة (`absence-b1-rar001-001..020`) · كل مرساة تخدم بنداً واحداً · لا إعادة استخدام مرساة واحدة لعدة صفوف · كل استشهاد في `RECONCILIATION-RAR-001.md` يحلّ إلى مرساة موجودة هنا · لا مرجع مكسور · لا مرساة يتيمة. **قاعدة (تصحيح §4):** العمود الرابع لا يستشهد بـ`references/analysis-inputs/**` إطلاقاً — دليل حاكم أو مرساة غياب أو سلطتان متعارضتان فقط.

**قاعدة إنفاذ إلزامية (تسري على RAR-002 · RAR-003 · B2):** أي بند مصنَّف في normalization بوصفه **`DQ`** لا يجوز تحويله في reconciliation إلى «دلتا حقيقية» لمجرد **غياب التقنين**. غياب الحسم يثبت **بقاء سؤال القرار**. التحويل إلى دلتا يتطلب **قرار مالك صريحاً**، أو **دليلاً حاكماً** يثبت أن الالتزام نفسه مستقر وأن الناقص هو التقنين فقط. واستثناء «مقنَّن» لا يُستوفى إلا بدليل حاكم **مباشر وصريح** بمسار وسطر يحسم **الخيار أو السلوك محل السؤال** — لا تشابه مصطلحات ولا استنتاج ضمني ولا مطابقة تقريبية ولا نص عام.

**درس تدقيقي:** النتيجة الصفرية تثبت غياب **المصطلح المبحوث** ضمن نطاق البحث المُعلَن؛ وقد يوجد نصٌّ حاكم بمصطلح آخر (عربي مقابل إنجليزي) دون أن يحسم السؤال — لذلك وُثِّقت المطابقات القريبة أعلاه بمسارها وسطرها وسبب استبعادها، بدل إغفالها أو التوسّع بها.
