# ABSENCE-EVIDENCE-B2-RAR004 — B2-A (RAR-004 · Governed Extension Store)

> **مخرج traceability/تحليلي غير حاكم** — لا سلطة موازية ولا بديل عن الأصل الحاكم. حزمة أدلة غياب **قابلة لإعادة التنفيذ** تسند صفوف `RECONCILIATION-RAR-004.md`. كل حالة بمرساة HTML صريحة بأحرف صغيرة. **HEAD المنفَّذ عليه البحث:** `7f05cafd8318e1b106a73bf3fbc806deae28a865` · **تاريخ البحث:** 2026-07-26.
> **نطاق مشترك مستثنى من «الحاكم»:** `references/analysis-inputs/**` (مدخل تحليلي غير حاكم خارج authority ladder) · `traceability/rar-reconciliation/**` (مخرجات المصالحة نفسها — لا تُثبت ذاتها) · `superseded/**` (تاريخي منسوخ — محظور إلا بتكليف صريح، `AUTHORITY.md:22`). **المسارات المشمولة:** كل ملف `.md` متعقَّب خارج المستثنى — **100 ملفاً** من أصل 143 عند HEAD (`git ls-files '*.md' | grep -vE '<المستثنى>' | wc -l`).
> **الأمر القياسي:** `grep -rInE "<terms>" . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
>
> ### قاعدتان إلزاميتان مطبَّقتان في كل حالة أدناه
> **(أ) ثنائية اللغة —** كل حزمة نُفِّذت **بأمرين منفصلين**: مصطلحات إنجليزية ومقابلها العربي، ونتيجتاهما مسجَّلتان معاً. نتيجة صفرية بلغة واحدة **لا تثبت الغياب**. *(السابقة الموجِبة: مطابقات عربية خالصة ظهرت هنا في `absence-b2-rar004-009` و`-015` و`-016` بعد نتيجة إنجليزية صفرية أو شبه صفرية.)*
> **(ب) الأسطر الكاملة —** المستودع يحوي أسطراً طويلة تجمع عدة التزامات مفصولة بـ`·` أو `؛` (أطولها `catalogs/Feature_Technical_Architecture_Catalog.md:30` و`:94` و`phases/designs/phase-8-monitoring-offline-production.md:13`). **كل فحص أدناه قرأ السطر بتمامه** — لا مقتطف مقصوص ولا معاينة محدودة الأحرف — قبل الحكم بالغياب أو بالتغطية.
>
> **ممنوع** وصف مطابقة بأنها «الوحيدة» ما لم يُرجع الأمر الموثَّق ذلك العدد بالضبط. النتيجة الصفرية تثبت غياب **المصطلح المبحوث** ضمن نطاق البحث المُعلَن — لذلك يرافق كل حالة قسم «مطابقات قريبة فُحصت واستُبعدت» حيثما وُجدت مطابقات.

> **أهلية الاستشهاد (حالة INDEX تحكم):** `knowledge/BUSINESS_GLOSSARY.md` حالته في `INDEX.md:57` **`Proposed (G1)`** — فلا يُستشهد به لإثبات سياسة نافذة. ويُستعمل أدناه **حصراً** لتمييز مصطلحين عند استبعاد مطابقة قريبة (`Connector` مقابل `Extension` — `:17`/`:18`)، ولم يُبنَ عليه أي تصنيف. أما `ui/UI_ADMIN_CONSOLE_MODEL.md` فحالته `Current` (`INDEX.md:127`) فيصلح استشهاداً مساعداً ضمن المرتبة 8 (`AUTHORITY.md:17`).
>
> **ملاحظة بنيوية تسري على الحزمة كلها:** البحث `absence-b2-rar004-000` أدناه يثبت أن **كل** ذِكر حاكم لـ«Governed Extension Store / حوكمة الحزم» في المستودع محصورٌ في **صفَّين قائمَين اثنين** (`decisions/DEFERRED_IMPLEMENTATION.md:23` · `methodology/RECONCILIATION_ROADMAP.md:18`). لذلك أغلب حالات الغياب أدناه تُسند تصنيف **`مؤجَّل بصف قائم`** لا `دلتا حقيقية`: الغياب هنا غيابُ **مضمون** وثيقة الجاهزية التي يملكها R10، لا غيابُ صفٍّ يحكمها.

---

<a id="absence-b2-rar004-000"></a>
### ABSENCE-B2-RAR004-000 — حصر الذِكر الحاكم للمتجر المحكوم
- **الغرض:** إثبات أن حوكمة الحزم/المتجر المحكوم لا تُذكر في المستودع الحاكم إلا في صفَّي التأجيل القائمين — وهو الأساس الذي تُبنى عليه بقية الحالات.
- **السؤال:** هل يوجد عند HEAD أي نص حاكم يقنِّن المتجر المحكوم أو حوكمة الحزم خارج صفَّي التأجيل؟
- **المصطلحات (EN):** `Governed Extension Store` · `extension store` · `package governance`. **(AR):** `حوكمة الحزم` · `متجر الامتدادات` · `متجر محكوم`.
- **الأمر (EN):** `grep -rInE 'Governed Extension Store|extension store|package governance' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 1**
- **الأمر (AR):** `grep -rInE 'حوكمة الحزم|متجر الامتدادات|متجر محكوم' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 2**
- **المطابقات الثلاث (بعد إزالة التكرار = ملفان):**
  - `decisions/DEFERRED_IMPLEMENTATION.md:23` — صف تأجيل رسمي: «Governed Extension Store — واجهة/تنزيل/ترقية | حوكمة الحزم توثَّق جاهزيةً بلا اختيار تقنية | **R10 → P7** | لا بناء قبل جاهزية R10 وبوابة P7».
  - `methodology/RECONCILIATION_ROADMAP.md:18` — صف R10: «Real-time & Extension-Store Readiness | هياكل Event Contracts وحوكمة الحزم — توثيقاً بلا اختيار تقنية | **وثيقتا جاهزية** | P6/P7».
- **ما يثبته هذا:** الموضوع كله **محجوز بصفَّين قائمَين** (لا بعبارة «لاحقاً» ولا بتوصية مستقبلية): سجل المؤجلات الرسمي (`decisions/DEFERRED_IMPLEMENTATION.md:4` — «سجل رسمي للأمور المؤجلة عمداً … أي وكيل يصادف حاجة لبند مؤجل: يتوقف ويحيل إلى صف البند») وخارطة R المعتمدة. وR10 **غير مُفعَّلة** (`methodology/RECONCILIATION_ROADMAP.md:5` — «كل مرحلة R تُفعَّل بأمر مالك صريح»).
- **مطابقة قريبة فُحصت واستُبعدت:**
  - `phases/BACKLOG_DEFERRED_SCOPE.md:15` — **قريبة:** «تطبيقات أدوات أمنية في المتجر … بعد P7». **مستبعَدة:** تؤجِّل **محتوى** المتجر (فئة تطبيقات) لا حوكمته. **لا تحسم البنود:** لا تقنِّن سياسة ترقية ولا تراجعاً ولا حدود تنفيذ — لكنها تؤكد أن المتجر بأسره خلف بوابة P7.

---

<a id="absence-b2-rar004-001"></a>
### ABSENCE-B2-RAR004-001
- **البند [01]:** Version policy — «قاعدة إصدار قابلة للتدقيق» للحزم.
- **السؤال:** هل يقنِّن المستودع قاعدة إصدار (ترقيم/دلالة/تدقيق) للحزم عند HEAD؟
- **المصطلحات (EN):** `version policy` · `versioning policy` · `SemVer` · `semantic version`. **(AR):** `سياسة الإصدار` · `قاعدة إصدار` · `قاعدة الإصدار` · `ترقيم الإصدار`.
- **الأمر (EN):** `grep -rInE 'version policy|versioning policy|SemVer|semantic version' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'سياسة الإصدار|قاعدة إصدار|قاعدة الإصدار|ترقيم الإصدار' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** الـ`versioning` الحاضر في المستودع (`constitution.md:18` · `phases/phase-0-foundation-full-stack-skeleton.md:39` · `phases/designs/phase-3-workflows-approvals-actions.md:35`) هو **تنسيخ تعريفات داخلية** (migrations · config · workflow definitions) بدورة اعتماد وتدقيق — لا قاعدة ترقيم لحزمة خارجية تُوزَّع وتُرقَّى. فالبند غير مقنَّن، ويسكن داخل صف `decisions/DEFERRED_IMPLEMENTATION.md:23` ⇒ **مؤجَّل بصف قائم** لا دلتا.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_FIELD_NAMING.md:58` — **قريبة:** «OD-NM-2 | بادئة إصدار الـ API | `/api/v1` ثابتة؛ كسر التوافق ⇒ v2 بقرار ADR». **مستبعَدة:** (١) قرارٌ **مفتوح** في جدول §8 Open Decisions بعمود «التوصية» لا سياسة نافذة؛ (٢) نطاقه **API المنصة** لا إصدار الحزمة. **لا تثبت البند.**
  - `decisions/license-review.md:4` — **قريبة:** «لا يدخل أي tool / package / container image … قبل صف معتمد هنا **للإصدار/الصورة المحددة**. **الترقية تعيد المراجعة**.» (قُرئ السطر كاملاً). **مستبعَدة:** بوابةُ **دخول** مربوطة بإصدار بعينه — تُلزم بمراجعة عند كل ترقية، لكنها **لا تعرّف** قاعدة ترقيم ولا دلالة كسر التوافق. **لا تثبت البند** وإن قيّدته.

<a id="absence-b2-rar004-002"></a>
### ABSENCE-B2-RAR004-002
- **البندان [02] و[17]:** Platform/API/spec compatibility · Compatibility tests — إعلان توافق الحزمة مع المنصة/الـAPI/المواصفة وفحصه.
- **السؤال:** هل يوجد إعلان توافق أو مصفوفة توافق أو اختبار توافق **للحزم** عند HEAD؟
- **المصطلحات (EN):** `platform compatibility` · `API compatibility` · `spec compatibility` · `compatibility matrix`. **(AR):** `توافق الحزمة` · `مصفوفة التوافق` · `توافق المنصة` · `إعلان التوافق`.
- **الأمر (EN):** `grep -rInE 'platform compatibility|API compatibility|spec compatibility|compatibility matrix' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'توافق الحزمة|مصفوفة التوافق|توافق المنصة|إعلان التوافق' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **أمر مساند (EN):** `grep -rInE 'compatibility test|compat test|contract test' …` — **النتيجة: 5** · **(AR):** `grep -rInE 'اختبار توافق|اختبارات التوافق|اختبارات العقود' …` — **النتيجة: 3**
- **لماذا لا يثبت الموجود البند:** مطابقات «اختبارات العقود» الخمس/الثلاث كلها تخصّ **استخراج module إلى خدمة مستقلة** لا توافق حزمة: `methodology/testing-strategy.md:32` («اختبارات العقود شرط مسبق إلزامي لأي استخراج module») و`decisions/adr/ADR-0026…:13` و`:22` و`methodology/Spec_Driven_Modular_Monolith_Methodology.md:52` و`phases/BACKLOG_DEFERRED_SCOPE.md:11`. لا نوع اختبار «توافق حزمة» في جدول `methodology/testing-strategy.md:9–29` (قُرئ الجدول كاملاً — **21 نوعاً**). البند غير مقنَّن ⇒ يسكن صف `decisions/DEFERRED_IMPLEMENTATION.md:23`.
- **مطابقة قريبة فُحصت واستُبعدت:**
  - `methodology/testing-strategy.md:31` — **قريبة:** «**قاعدة إدارة:** يوسَّع هذا الجدول عند تصميم كل مرحلة بحالات القبول التفصيلية لمهامها؛ ولا يُدمج أي feature قبل اجتياز الأنواع المنطبقة عليه». **مستبعَدة:** قاعدة **توسيع** الجدول مستقبلاً، لا نوع اختبار قائم. **لا تثبت البند** — لكنها تحدد المكان الذي يدخل منه لاحقاً (تصميم P7).

<a id="absence-b2-rar004-003"></a>
### ABSENCE-B2-RAR004-003
- **البند [04]:** Maintenance windows لترقية الحزم.
- **السؤال:** هل يقنِّن المستودع نوافذ صيانة (تعريفاً/سياسةً/إخطاراً) تُجرى فيها الترقيات؟
- **المصطلحات (EN):** `maintenance window` · `scheduled maintenance`. **(AR):** `نافذة صيانة` · `نوافذ صيانة` · `صيانة مجدولة` · `فترة الصيانة`.
- **الأمر (EN):** `grep -rInE 'maintenance window|scheduled maintenance' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'نافذة صيانة|نوافذ صيانة|صيانة مجدولة|فترة الصيانة' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 1**
- **لماذا لا يثبت الموجود البند:** المطابقة العربية الوحيدة التي أرجعها الأمر قصةُ مستخدم لا سياسة (أدناه) ⇒ لا نافذة صيانة مقنَّنة، لا للمنصة ولا للحزم.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-8-monitoring-offline-production.md:42` — **قريبة:** «بصفتي موظفاً: لا يتغير شيء في تجربتي بعد الترقية — نفس النظام أسرع/أثبت · **أرى إشعار صيانة مجدولة قبلها**» (قُرئ السطر كاملاً). **مستبعَدة:** **User Story** في §8 لا متطلب FR ولا سياسة؛ وموضوعها ترقية **المنصة** (P8) لا ترقية حزمة داخل المتجر. **لا تثبت البند:** لا تعرّف النافذة ولا مالكها ولا شرط إجراء الترقية داخلها.

<a id="absence-b2-rar004-004"></a>
### ABSENCE-B2-RAR004-004
- **البند [05]:** Backup before upgrade — نسخة احتياطية قبل الترقية.
- **السؤال:** هل يشترط المستودع نسخة احتياطية/لقطة **قبل** أي ترقية؟
- **المصطلحات (EN):** `backup before` · `pre-upgrade backup` · `snapshot before`. **(AR):** `نسخة احتياطية قبل` · `نسخ احتياطي قبل` · `لقطة قبل`.
- **الأمر (EN):** `grep -rInE 'backup before|pre.upgrade backup|snapshot before' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'نسخة احتياطية قبل|نسخ احتياطي قبل|لقطة قبل' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** النسخ الاحتياطي مقنَّن **قدرةً تشغيلية** مملوكة لـOps (`constitution.md:22` م14 · `phases/designs/phase-8-monitoring-offline-production.md:31` FR-8.5 «Backup مجدول (config) للقواعد الأربع … وRestore إجراء مُختبر بتمرين حقيقي» — قُرئ السطر كاملاً · `methodology/testing-strategy.md:25`)، لكن **لا شرط قبلية** يربطه بترقية. وجود القدرة ≠ اشتراطها قبل الترقية ⇒ البند غير مقنَّن ويسكن صف التأجيل.

<a id="absence-b2-rar004-005"></a>
### ABSENCE-B2-RAR004-005
- **البند [06]:** Dependency conflicts — تعارض تبعيات الحزم.
- **السؤال:** هل توجد سياسة كشف/حسم لتعارض التبعيات عند تثبيت أو ترقية حزمة؟
- **المصطلحات (EN):** `dependency conflict` · `conflicting dependenc` · `version conflict`. **(AR):** `تعارض التبعيات` · `تعارض تبعيات` · `تضارب التبعيات`.
- **الأمر (EN):** `grep -rInE 'dependency conflict|conflicting dependenc|version conflict' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'تعارض التبعيات|تعارض تبعيات|تضارب التبعيات' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** حوكمة التبعيات القائمة **بناءُ** المنتج نفسه لا تركيبُ حزم وقت التشغيل: `constitution.md:27` (م19 lockfiles + digest pinning) · `catalogs/Feature_Technical_Architecture_Catalog.md:269` («lockfiles + digest pinning لكل الاعتماديات والصور» — قُرئ السطر كاملاً) · `decisions/open-decisions.md:22` (D3: uv + pnpm + lockfiles + pinned). الـlockfile يحسم التعارض **عند البناء**، ولا يعالج حزمتين مثبَّتتين تطلبان إصدارين متعارضين في منصة تعمل ⇒ البند غير مقنَّن ويسكن صف التأجيل.

<a id="absence-b2-rar004-006"></a>
### ABSENCE-B2-RAR004-006
- **البندان [07] و[10]:** Downgrade restrictions · Rollback evidence.
- **السؤال:** هل تقنَّن قيودُ خفض إصدار حزمة، أو شكلُ الدليل الذي ينتجه تراجعها؟
- **المصطلحات (EN):** `package upgrade` · `upgrade path` · `rollback evidence` · `downgrade`. **(AR):** `ترقية الحزمة` · `ترقية حزمة` · `تراجع الحزمة` · `خفض الإصدار` · `دليل التراجع`.
- **الأمر (EN):** `grep -rInE 'package upgrade|upgrade path|rollback evidence|downgrade' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'ترقية الحزمة|ترقية حزمة|تراجع الحزمة|خفض الإصدار|دليل التراجع' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** الـ`rollback` الحاضر يخصّ كائنات داخلية بدورات حياتها الخاصة: تراجع migration (`methodology/testing-strategy.md:14`) · تراجع نسخة workflow (`phases/designs/phase-3-workflows-approvals-actions.md:35` FR-3.10) · تراجع قيم config (`catalogs/Feature_Technical_Architecture_Catalog.md:218`). ولا واحد منها **حزمةٌ في متجر**، ولا يعرّف أيٌّ منها منعَ خفض إصدار ولا «دليل تراجع» قابلاً للتدقيق للحزمة. أما م3 (`constitution.md:11`) فتُلزم بتدقيق append-only لكل عملية مهمة — قيدٌ يسري لاحقاً على تراجع الحزمة، لكنه **لا يعرّف** الدليل المطلوب ⇒ البندان غير مقنَّنين ويسكنان صف التأجيل.

<a id="absence-b2-rar004-007"></a>
### ABSENCE-B2-RAR004-007
- **البند [08]:** Configuration migration — ترحيل إعداد الحزمة بين الإصدارات.
- **السؤال:** هل يقنِّن المستودع ترحيل الإعدادات عند ترقية؟
- **المصطلحات (EN):** `configuration migration` · `config migration` · `settings migration`. **(AR):** `ترحيل الإعداد` · `ترحيل الإعدادات` · `ترحيل التهيئة`.
- **الأمر (EN):** `grep -rInE 'configuration migration|config migration|settings migration' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'ترحيل الإعداد|ترحيل الإعدادات|ترحيل التهيئة' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** Config Registry مقنَّن بـ**تنسيخ وتدقيق وتراجع** (`phases/phase-0-foundation-full-stack-skeleton.md:39` FR-0.6 · `catalogs/Feature_Technical_Architecture_Catalog.md:218`) — أي **حفظ نسخ القيم**، لا **تحويل شكل الإعداد** من إصدار حزمة إلى آخر. الفرق جوهري: التنسيخ يحفظ ما كان؛ الترحيل يحوّل ما كان إلى ما صار ⇒ البند غير مقنَّن ويسكن صف التأجيل.

<a id="absence-b2-rar004-008"></a>
### ABSENCE-B2-RAR004-008
- **البند [09]:** Data ownership at uninstall — مصير بيانات الحزمة عند إلغاء تثبيتها.
- **السؤال:** هل يقنِّن المستودع ملكية/مصير البيانات عند إزالة حزمة؟
- **المصطلحات (EN):** `uninstall` · `un-install` · `data ownership` · `DATA_OWNERSHIP`. **(AR):** `إلغاء التثبيت` · `إزالة الحزمة` · `إلغاء تثبيت` · `ملكية البيانات`.
- **الأمر (EN):** `grep -rInE 'uninstall|un-install|Uninstall' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 1** · `grep -rInE 'data ownership|DATA_OWNERSHIP' …` — **النتيجة: 4**
- **الأمر (AR):** `grep -rInE 'إلغاء التثبيت|إزالة الحزمة|إلغاء تثبيت' …` — **النتيجة: 0** · `grep -rInE 'ملكية البيانات' …` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** لا مفهوم «إلغاء تثبيت حزمة» في المستودع أصلاً؛ ومطابقات `DATA_OWNERSHIP` الأربع تحيل إلى وثيقة في **مستودع `platform`** (المرتبة 10 — `AUTHORITY.md:19`: «لا يكرر نص قرار ولا ينشئه») ⇒ لا سند حاكم داخل `specs`، والبند يسكن صف التأجيل.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `FLUTTER_ROLLBACK_AND_OPENWEBUI_BRIDGE_HANDOFF.md:102` — **قريبة:** المطابقة الإنجليزية الوحيدة لـ`uninstall`. **مستبعَدة:** موضوعها إزالة Flutter/Dart SDK من **جهاز SA**، خارج المنتج كلياً. **لا تثبت البند.**
  - `decisions/DEFERRED_IMPLEMENTATION.md:10` — **قريبة:** «المعرفات مملوكة للمنصة أصلاً (DATA_OWNERSHIP)». **مستبعَدة:** سياقها **مزامنة/استيراد المحادثات** من Open WebUI، لا إزالة حزمة. **لا تثبت البند.**
  - `SPECIFICATIONS_CLOSEOUT_HANDOFF.md:33` — **قريبة:** صف يذكر `PLATFORM: DATA_OWNERSHIP.md` «ملكية المعرفات الصريحة (رسائل/مرفقات/مشاريع/…)». **مستبعَدة:** إحالةُ جردٍ إلى ملف في مستودع آخر، وموضوعها كيانات المحادثة لا الحزم. **لا تثبت البند.**

<a id="absence-b2-rar004-009"></a>
### ABSENCE-B2-RAR004-009
- **البند [11]:** Orphaned workflows/actions — ما يبقى معلَّقاً بعد إزالة/تعطيل حزمة.
- **السؤال:** هل يقنِّن المستودع مصير الـWorkflows/Actions التي تشير إلى قدرة أُزيلت؟
- **المصطلحات (EN):** `orphan` · `dangling` · `left-behind` · `broken reference`. **(AR):** `يتيم` · `يتيمة` · `مراجع مكسورة` · `إشارات معلقة` · `كائنات متروكة`.
- **الأمر (EN):** `grep -rInE 'orphan|dangling|left.behind|broken reference' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'يتيم|يتيمة|مراجع مكسورة|إشارات معلقة|كائنات متروكة' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 2**
- **لماذا لا يثبت الموجود البند:** المطابقتان العربيتان تخصّان **حوكمة الوثائق** لا كائنات وقت التشغيل (أدناه). أقرب التزام تشغيلي — `methodology/testing-strategy.md:16` («تعطيل قدرة يخفيها **دون كسر خدمة**؛ الأعلام المحميّة ترفض التعطيل») و`phases/designs/phase-4-advanced-permissions-feature-management.md:33` («تعطيل قدرة يُخفيها ويُعطّل مساراتها **بلطف**») — يضمن **التعطيل اللطيف**، ولا يقرّر شيئاً عن تعريفات تشير إلى قدرة **مُزالة** (والإزالة نفسها غير مقنَّنة — قارن `absence-b2-rar004-008`) ⇒ البند غير مقنَّن ويسكن صف التأجيل.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_GATE_REVIEW_CHECKLIST.md:34` — **قريبة:** «كل screen_id … موجود في الجرد (**لا شاشة يتيمة**)». **مستبعَدة:** بند مراجعة بوابة على **اتساق الوثائق** (جرد الشاشات)، لا على كائنات منفَّذة. **لا تثبت البند.**
  - `methodology/PRE_IMPLEMENTATION_CONSOLIDATION_REVIEW.md:78` — **قريبة:** «ملفات «يتيمة» خارج الفهرس تضعف قابلية الاكتشاف للوكلاء». **مستبعَدة:** موضوعها **ملفات المستودع** خارج `INDEX.md`. **لا تثبت البند.**

<a id="absence-b2-rar004-010"></a>
### ABSENCE-B2-RAR004-010
- **البند [14]:** LTS channels — قنوات دعم طويل الأمد للحزم.
- **السؤال:** هل يعرف المستودع نموذجَ قنوات إصدار/دعم متعددة تُوزَّع عبرها الحزم؟
- **المصطلحات (EN):** `LTS` · `long-term support` · `release channel` · `support channel`. **(AR):** `قناة دعم` · `قنوات الإصدار` · `دعم طويل الأمد` · `قناة إصدار`.
- **الأمر (EN):** `grep -rInE 'LTS|long[- ]term support|release channel|support channel' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 1**
- **الأمر (AR):** `grep -rInE 'قناة دعم|قنوات الإصدار|دعم طويل الأمد|قناة إصدار' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند — ولماذا هو بلا أساس لا مؤجَّل:** «قنوات LTS» تفترض **منظومة توزيع متعددة القنوات من مورّدين**، وهي مستبعَدة نصاً وبنيةً عند HEAD: (١) `phases/designs/phase-7-integrations-app-store.md:16` يضع «**متجر عام/طرف ثالث**» في **Out of Scope** (قُرئ السطر كاملاً: «أي SaaS خارجي … · موصلات تكتب في قواعد بيانات أنظمة أخرى مباشرة … · أتمتة بلا موافقة للأفعال المُغيِّرة · متجر عام/طرف ثالث.»)؛ (٢) نموذج التوزيع المعتمد **قناة واحدة**: حزمة offline موقّعة ومتحقَّقة إلى الشبكة المغلقة (`constitution.md:27` م19 · `catalogs/Feature_Technical_Architecture_Catalog.md:94` D7 · `phases/designs/phase-8-monitoring-offline-production.md:13` Offline Bundle Builder). فالبند لا يصف حاجةً مؤجَّلة داخل صف R10، بل يفترض **بنية توزيع لا وجود لها ولا مسار لها** ⇒ **مصطنع أو بلا أساس**.
- **مطابقة قريبة فُحصت واستُبعدت:**
  - `methodology/coding-standards.md:43` — **قريبة:** المطابقة الإنجليزية الوحيدة التي أرجعها الأمر: «Python: **uv** … · Frontend: **Node LTS** + **pnpm**» (قُرئ السطر كاملاً). **مستبعَدة:** اختيارُ **إصدار أداة تطوير** (Node) لا قناةُ دعمٍ لمنتج المتجر. **لا تثبت البند.**

<a id="absence-b2-rar004-011"></a>
### ABSENCE-B2-RAR004-011
- **البند [15]:** End-of-support — إعلان نهاية دعم حزمة.
- **السؤال:** هل يقنِّن المستودع حالةَ «انتهى دعمها» لأي أصل، بأثرها على المستخدمين والترقية؟
- **المصطلحات (EN):** `end-of-support` · `end-of-life` · `sunset` · `deprecation policy`. **(AR):** `انتهاء الدعم` · `إيقاف الدعم` · `نهاية العمر` · `سياسة الإهمال`.
- **الأمر (EN):** `grep -rInE 'end.of.support|end.of.life|sunset|deprecation policy' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'انتهاء الدعم|إيقاف الدعم|نهاية العمر|سياسة الإهمال' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** الحالات المعلنة للأصول محصورة في `constitution.md:23` (م15: enabled/disabled/lightweight/demo/production/optional/protected) — وليس فيها حالة «انتهى الدعم». وخلافاً لـ[14]، البند **لا يفترض** بنية توزيع غائبة: إعلان نهاية الدعم ممكن لمالك المتجر الداخلي نفسه ⇒ حاجةٌ صالحة غير مقنَّنة، تسكن صف `decisions/DEFERRED_IMPLEMENTATION.md:23`.
- **مطابقة قريبة فُحصت واستُبعدت:**
  - `methodology/RECONCILIATION_ROADMAP.md:14` — **قريبة:** «R6 … ملكية i18n وnamespaces و**deprecation**» (ظهرت في بحث موسَّع بمصطلح `deprecat` وحده). **مستبعَدة:** نطاقها **إهمال مفاتيح الترجمة والنطاقات الاسمية**، لا دعم حزمة. **لا تثبت البند.**

<a id="absence-b2-rar004-012"></a>
### ABSENCE-B2-RAR004-012
- **البند [16]:** Partial failure — الترقية التي تنجح جزئياً.
- **السؤال:** هل يقنِّن المستودع سلوك النظام عند فشل ترقية جزئياً (تراجع؟ تجميد؟ حالة مختلطة معلنة؟).
- **المصطلحات (EN):** `partial failure` · `partially applied` · `rollback on failure`. **(AR):** `فشل جزئي` · `تطبيق جزئي` · `تراجع عند الفشل`.
- **الأمر (EN):** `grep -rInE 'partial failure|partially applied|rollback on failure' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'فشل جزئي|تطبيق جزئي|تراجع عند الفشل' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** «النجاح الجزئي» مقنَّن **حالةَ عرض** لتشغيلٍ لا لترقية: `ui/UI_COMPONENT_STATES.md:384` (S18 `partial` — «ما نجح يُعرض + لوحة «لم يكتمل: n بند» … وزر إعادة للفاشل فقط») و`ui/UI_RUN_EXECUTION_MODEL.md:84–88` (§7 دلالات الاكتمال الجزئي — تقرير جزئي بـtyped-ack). موضوعهما **تشغيل خطوات** (runs/reports)، لا تطبيق ترقية على منصة ⇒ البند غير مقنَّن ويسكن صف التأجيل.

<a id="absence-b2-rar004-013"></a>
### ABSENCE-B2-RAR004-013
- **البنود [18]–[28] (كتلة §12):** تعداد أنواع التبعية الواجب تغطيتها في تحليل الأثر — Workflows · Agents · Actions · Schedules · Connectors · Screens · Reports · Data ingestion jobs · Permissions · Stored configuration · Active runs.
- **السؤال:** هل يعدّد أي مصدر حاكم **أنواع الكائنات** التي يجب أن يغطيها تحليل الأثر قبل Disable/Upgrade/Remove؟
- **المصطلحات (EN):** `what is affected` · `affected objects` · `affected by`. **(AR):** `ما يتأثر` · `الكائنات المتأثرة` · `ما الذي يتأثر`.
- **الأمر (EN):** `grep -rInE 'what is affected|affected objects|affected by' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'ما يتأثر|الكائنات المتأثرة|ما الذي يتأثر' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **أمر مساند (EN):** `grep -rInE 'impact analysis|impact preview|dependency graph|blast radius' …` — **النتيجة: 1** · **(AR):** `grep -rInE 'تحليل الأثر|معاينة الأثر|أثر التبعيات|مخطط تبعيات|رسم تبعيات' …` — **النتيجة: 10**
- **لماذا لا يثبت الموجود البنود:** المطابقات العشر تثبت أن **آلية** أثر التبعيات مقنَّنة (وهي أساس تصنيف [12] `مقنَّن`): `phases/designs/phase-4-advanced-permissions-feature-management.md:33` · `:46` · `:59` · `phases/phase-roadmap.md:46` · `ui/UI_ADMIN_CONSOLE_MODEL.md:23` · `catalogs/Feature_Technical_Architecture_Catalog.md:363`. لكن **لا واحدة منها تعدّد نوعاً واحداً** من الأنواع الأحد عشر: `FR-4.8` يبني المخطط «**من manifests**» (تبعيات قدرة↔قدرة)، و`ui/UI_ADMIN_CONSOLE_MODEL.md:23` يذكر مواضع الاستخدام (Features P4 · Connectors P7 · تغيير embedding P5) لا محتوى التغطية. فالبنود الأحد عشر مطالبُ **تغطية** لا آلية، وهي غير مقنَّنة وتسكن صف `decisions/DEFERRED_IMPLEMENTATION.md:23`.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-3-workflows-approvals-actions.md:35` (FR-3.10) — **قريبة:** «الـ instances الجارية تُعامل بسياسة معلنة (تكمل على نسختها)» — أقرب نص إلى «Active runs». **مستبعَدة:** يعالج تغيّر **نسخة تعريف workflow**، لا إزالة/ترقية حزمة؛ ولا يُدرج التشغيلات الجارية ضمن تحليل أثرٍ يُعرض قبل التأكيد. **لا تثبت البند [28].**
  - `ui/UI_STITCH_REFINED_PROMPTS.md:206` — **قريبة:** المطابقة الإنجليزية الوحيدة (`dependency graph collapse`). **مستبعَدة:** توجيهُ **تصميم بصري** لأداة نماذج (Stitch)، ومخرجات Stitch «مرجع بصري فقط» (`ui/UI_STITCH_REFINED_PROMPTS.md:296`). **لا تثبت البند.**

<a id="absence-b2-rar004-014"></a>
### ABSENCE-B2-RAR004-014
- **البنود [29]–[32]:** حدود تنفيذ كود الحزمة — داخل Process · داخل Container مستقل · كخدمة داخل LAN · كConfiguration فقط.
- **السؤال:** هل يحسم المستودع نموذجَ تنفيذ كود الحزم، أو يقنِّن Invariants صريحة له؟
- **المصطلحات (EN):** `code execution` · `execution boundar` · `in-process execution` · `sandbox`. **(AR):** `حدود تنفيذ` · `تنفيذ الكود` · `تنفيذ كود الحزمة` · `حدود التنفيذ` · `صندوق رملي` · `عزل تنفيذ`.
- **الأمر (EN):** `grep -rInE 'code execution|execution boundar|run in.process|in-process execution' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0** · `grep -rInE 'sandbox|sandboxed|seccomp|namespace isolation' …` — **النتيجة: 1**
- **الأمر (AR):** `grep -rInE 'حدود تنفيذ|تنفيذ الكود|تنفيذ كود الحزمة|حدود التنفيذ' …` — **النتيجة: 0** · `grep -rInE 'صندوق رملي|عزل تنفيذ|بيئة معزولة' …` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البنود:** لا نص حاكم يختار نموذج تنفيذ ولا يعدّد invariants له. القيود الحاكمة **المحيطة** قائمة وتُقيّد أي خيار لاحق، ولا تحسمه: `constitution.md:12` (م4 — لا اتصال خارجي؛ أي tool/model/connector يحتاج الخارج ممنوع ما لم يُنص ويُعتمد صراحةً) · `constitution.md:22` (م14 — Operations وحدها تشغّل/توقف services وcontainers) · `constitution.md:15` (م7 — License Review لكل صورة/إصدار) · `decisions/open-decisions.md:69` («فئة 3 … قبل Docker Compose للصورة: Valkey · MinIO أو SeaweedFS · أي خادم مراقبة · **أي صورة جديدة مستقبلاً**» — قُرئ السطر كاملاً). فالخيار نفسه (Process/Container/LAN/Config-only) **لم يُحسم**، وهو ما يملكه صف R10 نصاً («توثيقاً بلا اختيار تقنية») ⇒ **مؤجَّل بصف قائم**، لا `تعارض يحتاج قراراً`: شرط «لا صف تأجيل قائم» **غير متحقق**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-7-integrations-app-store.md:13` — **قريبة:** المطابقة الإنجليزية الوحيدة لـ`sandbox`: «… · **dry-run/sandbox للموصلات حيث أمكن** · حدود معدل لكل connector/مستخدم · …» (قُرئ السطر كاملاً). **مستبعَدة:** نطاقها **Connector** (تكاملٌ مع نظام خارجي — `knowledge/BUSINESS_GLOSSARY.md:17` يميّزه صراحةً عن Extension)، ومشروطة بـ«حيث أمكن» فلا تُنشئ التزاماً. **لا تثبت البنود.**
  - `phases/designs/phase-4-advanced-permissions-feature-management.md:26` (FR-4.1) · `ui/UI_ACTION_BUTTON_MODEL.md:20` · `ui/UI_ADMIN_CONSOLE_MODEL.md:70` — **قريبة:** ثلاثة نصوص تمنع «كود حر/تعبيرات/سكربتات». **مستبعَدة:** نطاقها **ما يؤلّفه الأدمن من ميتاداتا** (سياسات ABAC · أثر الأزرار · معرّفات الـConsole)، لا كودٌ تجلبه حزمة. **لا تثبت البنود** — لكنها تُظهر ميل المستودع للنموذج التصريحي.

<a id="absence-b2-rar004-015"></a>
### ABSENCE-B2-RAR004-015
- **البندان [33] و[34]:** Network/File/System access · Resource limits لكود الحزمة.
- **السؤال:** هل تقنَّن حدود وصول الحزمة (شبكة/ملفات/نظام) وحدود مواردها؟
- **المصطلحات (EN):** `resource limits` · `CPU limit` · `memory limit` · `rate limit per package`. **(AR):** `حدود الموارد` · `حدود موارد` · `سقف الموارد`.
- **الأمر (EN):** `grep -rInE 'resource limits|CPU limit|memory limit|rate limit per package' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'حدود الموارد|حدود موارد|سقف الموارد' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 2**
- **لماذا لا يثبت الموجود البندين:** **شقّ الشبكة محسوم بالفعل** حداً أعلى: `constitution.md:12` (م4) يمنع أي مكوّن يحتاج خارج الشبكة المغلقة، ويُختبر آلياً (`methodology/testing-strategy.md:18` — «إدخال نداء خارجي تجريبي ⇒ فشل الاختبار»)، ويؤكده `catalogs/Feature_Technical_Architecture_Catalog.md:30` («قاعدة الشبكة المغلقة: أي tool أو model أو connector لا يعمل خارج الشبكة المغلقة إلا إذا نُصّ عليه ووفق عليه صراحةً» — قُرئ السطر كاملاً). لكن **شقَّي الملفات والنظام** بلا نص، و**حدود الموارد للحزمة** بلا نص ⇒ البندان غير محسومين كاملَين ويسكنان صف التأجيل. *(الحسم الجزئي مسجَّل في عمود «الحالة عند HEAD» ولم يُبنَ عليه تصنيف `مقنَّن`.)*
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/phase-0-foundation-full-stack-skeleton.md:55` — **قريبة:** «**حدود موارد demo لكل حاوية**» ضمن NFRs (قُرئ السطر كاملاً). **مستبعَدة:** حدودُ **حاويات المنصة** في بيئة العرض، لا حدود كود حزمة. **لا تثبت البند.**
  - `ui/UI_SCREEN_GOVERNANCE_STANDARD.md:47` — **قريبة:** «**Canvas:** وسم «تجريبي/أدمن» ظاهر · لا يكتب على بيانات إنتاج · نتائجه قابلة للنسخ/التصدير المحكوم · **حدود الموارد معلنة**» (قُرئ السطر كاملاً). **مستبعَدة:** قاعدةُ حوكمة **سطح Canvas** في الواجهة، لا الحزم. **لا تثبت البند.**

<a id="absence-b2-rar004-016"></a>
### ABSENCE-B2-RAR004-016
- **البندان [35] و[36]:** تسجيل نداءات الحزمة · منع الحزمة من تجاوز صلاحياتها.
- **السؤال:** هل يقنَّن سجلُّ نداءاتٍ للحزم، أو آليةُ حصر صلاحياتها؟
- **المصطلحات (EN):** `invocation_log` · `call log` · `tool call log` · `least privilege` · `privilege escalation` · `capability confinement`. **(AR):** `تسجيل النداءات` · `سجل النداءات` · `تسجيل الاستدعاءات` · `أدنى صلاحية` · `تجاوز الصلاحيات` · `حصر الصلاحيات`.
- **الأمر (EN):** `grep -rInE 'invocation_log|call log|tool call log' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 1** · `grep -rInE 'least privilege|privilege escalation|capability confinement' …` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'تسجيل النداءات|سجل النداءات|تسجيل الاستدعاءات' …` — **النتيجة: 0** · `grep -rInE 'أدنى صلاحية|تجاوز الصلاحيات|حصر الصلاحيات' …` — **النتيجة: 8**
- **لماذا لا يثبت الموجود البندين:** القيود العامة قائمة وتسري لاحقاً: م3 (`constitution.md:11` — تدقيق append-only لكل عملية مهمة) · م2 (`constitution.md:10` — الصلاحية قبل الجلب بـdefault-deny). لكنهما **عامّتان**: م3 تعدّد عمليات (قراءة حرجة/إنشاء/تعديل/حذف/تصدير/اعتماد/تغيير إعداد أو علم أو نموذج أو profile) ليس منها «نداء حزمة»، وم2 تحكم قرار الصلاحية لا **حصر** مكوّنٍ يحمل كوداً. ولا كتالوج أحداثٍ يعرّف حدث نداء حزمة (فُحص **§11 Audit Events Catalog** كاملاً — `catalogs/Feature_Technical_Architecture_Catalog.md:322–357`) ⇒ البندان غير مقنَّنين ويسكنان صف التأجيل.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-7-integrations-app-store.md:53` — **قريبة:** المطابقة الإنجليزية الوحيدة: «… · `delegation_log` · `invocation_log`(مدخلات مُقنَّعة/نتيجة/زمن/قرار البوابة) · …» (قُرئ السطر كاملاً). **مستبعَدة:** جدولُ تدقيقٍ لنداءات **الموصلات** (Connector — كيان متمايز عن Extension بنص `knowledge/BUSINESS_GLOSSARY.md:17–18`)، لا للحزم. **لا تثبت البند [35]** — لكنها النمط الأقرب الذي قد تبني عليه وثيقة R10.
  - `phases/designs/phase-7-integrations-app-store.md:62` — **قريبة:** «مبدأ مزدوج: **صلاحية المنصة ∩ صلاحية الحساب المربوط** — الأضيق يسود · لا invocation بلا هوية مستخدم نهائي مسجّلة … · **تعقيم مخرجات الأدوات قبل دخولها سياق النموذج (حقن)** · …» (قُرئ السطر كاملاً — سطرٌ يجمع ستة التزامات بفواصل `·`). **مستبعَدة:** يحصر **صلاحية نداء موصل**، لا صلاحية حزمة تحمل كوداً داخل المنصة. **لا تثبت البند [36]**.
  - المطابقات العربية الثماني لـ«أدنى صلاحية» (منها `constitution.md:27` · `catalogs/Feature_Technical_Architecture_Catalog.md:30`) — **قريبة:** مبدأ least-privilege عام. **مستبعَدة:** موضوعها **مستخدمو قواعد البيانات** ومبدأٌ عام في الصلاحيات، لا حصر حزمة. **لا تثبت البند.**

<a id="absence-b2-rar004-017"></a>
### ABSENCE-B2-RAR004-017
- **البند [37]:** «كيف يختلف ذلك حسب Package Type؟» — اختلاف حدود التنفيذ بحسب **نوع الحزمة**.
- **السؤال:** هل يعرّف المستودع تصنيفاً لأنواع الحزم أصلاً؟
- **المصطلحات (EN):** `Package Type` · `package type` · `package_type`. **(AR):** `نوع الحزمة` · `أنواع الحزم` · `تصنيف الحزم`.
- **الأمر (EN):** `grep -rInE 'Package Type|package type|package_type' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'نوع الحزمة|أنواع الحزم|تصنيف الحزم' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند — ولماذا هو بلا أساس لا مؤجَّل:** البند **مشروط بمقدّمة غير قائمة**: يسأل عن اختلاف الحدود «حسب Package Type» فيفترض وجود تصنيف أنواع للحزم. البحث الثنائي يرجع **صفراً في اللغتين** — لا تصنيف، ولا حتى مفردة «نوع حزمة» في أي مصدر حاكم. وخلافاً لبقية بنود §14 (التي تسأل أسئلة صالحة عن موضوع محجوز بصف R10)، هذا البند يسأل عن **تفريع** لتصنيف معدوم، فلا يملك R10 حتى مادةً يؤجّلها ⇒ **مصطنع أو بلا أساس** بموجب قاعدة `DQ` («مبنيٌّ على افتراض غير قائم في المستودع»). *(حكمٌ على البند مقيساً بالمستودع الحاكم عند HEAD — لا نفيٌ لصلاحية الفكرة إن عُرِّفت الأنواع لاحقاً في وثيقة R10.)*

---

**Related:** `RECONCILIATION-RAR-004.md` · `CANONICAL-ITEM-UNIVERSE.md` (`188ad37`) · `NORMALIZATION-AND-ATOMIC-MAP.md` (`188ad37`) · `decisions/DEFERRED_IMPLEMENTATION.md` · `methodology/RECONCILIATION_ROADMAP.md`
