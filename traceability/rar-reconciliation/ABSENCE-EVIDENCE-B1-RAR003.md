# ABSENCE-EVIDENCE-B1-RAR003 — B1 Commit 4

> **مخرج traceability/تحليلي غير حاكم** — لا سلطة موازية. حزمة أدلة غياب **قابلة لإعادة التنفيذ** لبنود RAR-003 المصنَّفة «دلتا حقيقية» أو «مصطنع أو بلا أساس». كل حالة بمرساة HTML صريحة بأحرف صغيرة. **HEAD المنفَّذ عليه البحث:** `55413d639e526fba2aa4299952edebdb94ca3009` · **تاريخ البحث:** 2026-07-26.
> **نطاق مشترك مستثنى من «الحاكم»:** `references/analysis-inputs/**` (مدخل غير حاكم خارج authority ladder) · `traceability/rar-reconciliation/**` (مخرجات B1 نفسها — لا تُثبت ذاتها) · `superseded/**` (تاريخي منسوخ). **المسارات المشمولة:** كل ملف `.md` متعقَّب خارج المستثنى.
> **الأمر القياسي:** `grep -rInE "<terms>" . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
>
> ### قاعدتان إلزاميتان مطبَّقتان في كل حالة أدناه
> **(أ) ثنائية اللغة —** كل حزمة نُفِّذت **بأمرين منفصلين**: مصطلحات إنجليزية ومقابلها العربي، ونتيجتاهما مسجَّلتان معاً. نتيجة صفرية بلغة واحدة **لا تثبت الغياب**. *(السابقة الموجِبة لهذه القاعدة: «تعقيم مخرجات الأدوات» في `phases/designs/phase-7-integrations-app-store.md:62` لم يكن ليظهر ببحث إنجليزي — راجع Commit 3.)*
> **(ب) الأسطر الكاملة —** المستودع يحوي أسطراً طويلة تجمع عدة التزامات مفصولة بـ`·` أو `؛`. **كل فحص أدناه قرأ السطر بتمامه** (لا مقتطف مقصوص ولا معاينة محدودة الأحرف) قبل الحكم بالغياب أو بالتغطية.
>
> **ممنوع** وصف مطابقة بأنها «الوحيدة» ما لم يُرجع الأمر الموثَّق ذلك العدد بالضبط. النتيجة الصفرية تثبت غياب **المصطلح المبحوث** ضمن نطاق البحث المُعلَن — لذلك يرافق كل حالة قسم «مطابقات قريبة فُحصت واستُبعدت» حين وُجدت مطابقات.

---

<a id="absence-b1-rar003-001"></a>
### ABSENCE-B1-RAR003-001
- **البند [01]:** وثيقة `UI Constitution` التاريخية — موضوع التوصية «قارن المبادئ مع ADR الحالي».
- **السؤال:** هل توجد وثيقة UI Constitution داخل المستودع الحاكم ليجري قياسها على ADR الحالي؟
- **المصطلحات (EN):** `UI Constitution` · `UI_CONSTITUTION`. **(AR):** `دستور الواجهة` · `دستور واجهة`.
- **الأمر (EN):** `grep -rInE 'UI Constitution|UI_CONSTITUTION' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'دستور الواجهة|دستور واجهة' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** حوكمة الواجهة في المستودع قائمة بحزمة `ui/` الملزمة-معيارياً (`ui/UI_SCREEN_GOVERNANCE_STANDARD.md:10`) لا بوثيقة «دستور». فالتوصية تخاطب مادةً **خارج** المستودع الحاكم ولا سند نافذ لها داخله ⇒ **مصطنع أو بلا أساس**. *(حكمٌ على البند مقيساً بالمستودع الحاكم — لا نفيٌ لوجود الوثيقة التاريخية خارجه.)*

<a id="absence-b1-rar003-002"></a>
### ABSENCE-B1-RAR003-002
- **البند [11]:** استثناءات موثقة للـInline Computed Styles عند React Flow أو التموضع الديناميكي.
- **السؤال:** هل يمنع المستودع الأنماط المضمّنة/المحسوبة أصلاً حتى يلزم استثناء موثق؟
- **المصطلحات (EN):** `inline styles` · `inline computed` · `React Flow` · `xyflow`. **(AR):** `أنماط مضمّنة` · `أنماط مضمنة` · `تنسيق مضمّن`.
- **الأمر (EN):** `grep -rInE 'inline styles?|inline computed|React Flow|xyflow' …` — **النتيجة: 3**
- **الأمر (AR):** `grep -rInE 'أنماط مضمّنة|أنماط مضمنة|تنسيق مضمّن' …` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** لا قاعدة تمنع الأنماط المضمّنة/المحسوبة في أي ملف حاكم ⇒ **لا منعَ يُستثنى منه**، فالتوصية بلا أساس نافذ ⇒ **مصطنع أو بلا أساس**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-3-workflows-approvals-actions.md:87` · `:57` — **قريبة:** تذكران `xyflow` بوصفها مكتبة canvas مرشَّحة. **مستبعَدتان:** موضوعهما **صف ترخيص مكتبة رسم** ضمن `OD-P3-2`، لا أنماط مضمّنة. **لا تثبتان البند:** لا تقرّران شيئاً عن CSS المضمّن.
  - `catalogs/Feature_Technical_Architecture_Catalog.md:63` — **قريبة:** صف ستاك `React + Vite`. **مستبعَدة:** اختيار إطار لا قاعدة تنسيق. **لا تثبت البند.**

<a id="absence-b1-rar003-003"></a>
### ABSENCE-B1-RAR003-003
- **البند [12]:** «عدم فرض منع CSS داخل Screens مطلقاً؛ الممنوع القيم الاعتباطية والهوية لكل شاشة».
- **السؤال:** هل يوجد منعٌ حاكم لـCSS داخل الشاشات، أو قاعدة «قيم اعتباطية»، حتى يلزم تعديله؟
- **المصطلحات (EN):** `arbitrary values` · `no custom CSS` · `CSS-in-`. **(AR):** `قيم اعتباطية` · `منع CSS` · `لا CSS داخل`.
- **الأمر (EN):** `grep -rInE 'arbitrary values?|no custom CSS|CSS-in-' …` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'قيم اعتباطية|منع CSS|لا CSS داخل' …` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** لا منع ولا قاعدة قيم اعتباطية في أي ملف حاكم ⇒ التوصية تعالج قاعدةً غير قائمة ⇒ **مصطنع أو بلا أساس**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_UX_ASSUMPTIONS.md:5` — **قريبة:** «وثيقة تخطيط UX — لا كود، لا CSS، لا تنفيذ». **مستبعَدة:** قاعدة على **محتوى هذه الوثيقة نفسها** (ما يُكتب فيها) لا على شاشات المنتج. **لا تثبت البند.**
  - `methodology/coding-standards.md:22` — **قريبة:** «لا hardcoded إطلاقاً». **مستبعَدة:** نطاقها الأسرار وendpoints وأسماء النماذج والمسارات — إعدادٌ لا تنسيق. **لا تثبت البند.**

<a id="absence-b1-rar003-004"></a>
### ABSENCE-B1-RAR003-004
- **البند [13]:** تحديد Version Canonical واحدة لوثيقة UI Constitution.
- **السؤال:** هل يقنِّن المستودع «نسخة كنسية» لتلك الوثيقة؟
- **المصطلحات (EN):** `canonical version`. **(AR):** `نسخة كنسية` · `إصدار كنسي` · `نسخة canonical`.
- **الأمر (EN):** `grep -rInE 'canonical version' …` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'نسخة كنسية|إصدار كنسي|نسخة canonical' …` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** نظام الإصدارات القائم (`INDEX.md` بعمود Version لكل ملف بقرار OD-IDX-1) يحكم **ملفات المستودع**، ولا يمتد إلى وثيقة خارجه — والوثيقة غير موجودة أصلاً (قارن `absence-b1-rar003-001`) ⇒ **مصطنع أو بلا أساس**.

<a id="absence-b1-rar003-005"></a>
### ABSENCE-B1-RAR003-005
- **البند [24]:** Exit Criteria **قابلة للقياس** لخروج Open WebUI.
- **السؤال:** هل يقنِّن المستودع معايير خروج مقيسة قابلة للفحص (لا وصفاً نوعياً)؟
- **المصطلحات (EN):** `exit criteria`. **(AR):** `معايير الخروج` · `شروط الخروج` · `معايير خروج`.
- **الأمر (EN):** `grep -rInE 'exit criteri' …` — **النتيجة: 1**
- **الأمر (AR):** `grep -rInE 'معايير الخروج|شروط الخروج|معايير خروج' …` — **النتيجة: 1**
- **لماذا لا يثبت الموجود البند:** المطابقتان تثبتان **وجود** شرط خروج لا **قابليته للقياس**؛ الشرط المعلن نوعيٌّ («الحد الأدنى … متاح») بلا عتبة ولا قائمة فحص ولا مالك قياس. المطلب ثابت المعنى والناقص تقنينه ⇒ **دلتا حقيقية**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `decisions/adr/ADR-0030-openwebui-temporary-chat-interface.md:11` — **قريبة:** «شروط الخروج (Decommission): الحد الأدنى من واجهة React البديلة متاح ثم تسلسل الخروج الموثق…». **مستبعَدة:** تسلسلٌ إجرائي ومعيار **نوعي**؛ «الحد الأدنى» غير معرَّف قياسياً، والتفصيل مُحال إلى `docs/DECOMMISSION_PLAN.md` **خارج مستودع المواصفات**. **لا تثبت البند:** لا معيار قابلاً للقياس داخل النطاق الحاكم المفحوص.
  - `phases/PHASE_MASTER_PLAN.md:30` — **قريبة:** عمود «Exit criteria» في ترويسة جدول المراحل. **مستبعَدة:** ترويسة عمود لمراحل P0–P8، لا علاقة لها بخروج الجسر. **لا تثبت البند.**

<a id="absence-b1-rar003-006"></a>
### ABSENCE-B1-RAR003-006
- **البند [25]:** تحديد الوظائف التي يجب أن توفرها AQL UI **قبل الإزالة**.
- **السؤال:** هل يعدّد المستودع الوظائف الواجب توفرها في الواجهة البديلة قبل إزالة الجسر؟
- **المصطلحات (EN):** `minimum viable UI` · `before removal` · `minimum replacement`. **(AR):** `الحد الأدنى من واجهة` · `قبل الإزالة`.
- **الأمر (EN):** `grep -rInE 'minimum viable UI|before removal|minimum replacement' …` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'الحد الأدنى من واجهة|الوظائف .*قبل الإزالة|قبل الإزالة' …` — **النتيجة: 2**
- **لماذا لا يثبت الموجود البند:** المطابقتان تذكران «الحد الأدنى» **دون تعداده**؛ لا قائمة وظائف ولا مرجع داخلي لها ⇒ **دلتا حقيقية**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `decisions/adr/ADR-0030-openwebui-temporary-chat-interface.md:11` — **قريبة:** تجعل توفّر «الحد الأدنى من واجهة React البديلة» شرط بدء الخروج. **مستبعَدة:** تثبت **الحاجة** ولا تعدّد **المحتوى**. **لا تثبت البند.**
  - `phases/phase-roadmap.md:11` — **قريبة:** تعلن المسار المؤقت وشروط خروجه. **مستبعَدة:** إعلان مسار لا تعداد وظائف. **لا تثبت البند.**

<a id="absence-b1-rar003-007"></a>
### ABSENCE-B1-RAR003-007
- **البند [29]:** «Chat workspace في AQL يغطي الاستخدامات المطلوبة» بوصفه معياراً.
- **السؤال:** هل يعدّد المستودع «الاستخدامات المطلوبة» التي يجب أن يغطيها سطح المحادثة قبل الخروج؟
- **المصطلحات (EN):** `chat workspace coverage` · `workspace covers`. **(AR):** `يغطي الاستخدامات` · `تغطية الاستخدامات`.
- **الأمر (EN):** `grep -rInE 'chat workspace coverage|workspace covers' …` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'يغطي الاستخدامات|تغطية الاستخدامات' …` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** `ws.main` مقنَّن كسطح مركزي من P6، لكن **لا قائمة استخدامات** تُقاس التغطية عليها. **سند تحويل SUG إلى دلتا:** الحاجة نفسها **مستقرة في مصدر حاكم** — `decisions/adr/ADR-0030-openwebui-temporary-chat-interface.md:11` يجعل توفّر الحد الأدنى من الواجهة البديلة شرطَ خروج ⇒ **دلتا حقيقية** لا «مصطنع». *(هذا هو الاستثناء الوحيد المطبَّق على كتلة SUG في RAR-003.)*

<a id="absence-b1-rar003-008"></a>
### ABSENCE-B1-RAR003-008
- **البند [38]:** Semantic HTML.
- **السؤال:** هل يقنِّن المستودع استعمال عناصر HTML الدلالية/الـlandmarks معياراً للإتاحة؟
- **المصطلحات (EN):** `semantic html` · `landmark` · `<main>` · `<nav>`. **(AR):** `عناصر دلالية` · `HTML دلالي` · `بنية دلالية`.
- **الأمر (EN):** `grep -rInE 'semantic html|landmark|<main>|<nav>' …` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'عناصر دلالية|HTML دلالي|بنية دلالية' …` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** قُرئ **سطر الإتاحة بتمامه** `ui/UI_DESIGN_SYSTEM.md:111` — يقنِّن خمسة معايير (تباين AA · focus ring · تنقّل لوحة مفاتيح · إعلان قارئ الشاشة · لمس ≥40px) و**ليس** بينها بنية دلالية. المطلب قابل للفحص ومعناه ثابت والناقص تقنينه ⇒ **دلتا حقيقية → R3**.

<a id="absence-b1-rar003-009"></a>
### ABSENCE-B1-RAR003-009
- **البند [40]:** Focus order.
- **السؤال:** هل يقنِّن المستودع ترتيب التركيز/تسلسل الانتقال؟
- **المصطلحات (EN):** `focus order` · `tab order` · `tabindex`. **(AR):** `ترتيب التركيز` · `تسلسل التركيز` · `ترتيب الانتقال`.
- **الأمر (EN):** `grep -rInE 'focus order|tab order|tabindex' …` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'ترتيب التركيز|تسلسل التركيز|ترتيب الانتقال' …` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** الموجود يخص **مظهر** حلقة التركيز لا **ترتيبه** ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_DESIGN_SYSTEM.md:111` — **قريبة:** «focus ring واضح 2px بلون primary». **مستبعَدة:** معيار **بصري** للحلقة. **لا تثبت البند:** لا تقرّر تسلسل الانتقال بين العناصر.
  - `ui/UI_DESIGN_SYSTEM.md:17` — **قريبة:** رمز اللون `primary` يذكر «focus». **مستبعَدة:** تعريف رمز لون. **لا تثبت البند.**

<a id="absence-b1-rar003-010"></a>
### ABSENCE-B1-RAR003-010
- **البند [42]:** Labels and descriptions (اسم/وصف متاح للعناصر).
- **السؤال:** هل يقنِّن المستودع اسماً ووصفاً قابلين للقراءة الآلية لعناصر الواجهة؟
- **المصطلحات (EN):** `accessible name` · `aria-label` · `aria-describedby` · `labels and descriptions`. **(AR):** `اسم متاح` · `وصف متاح` · `تسمية العنصر` · `تسميات وأوصاف`.
- **الأمر (EN):** `grep -rInE 'accessible name|aria-label|aria-describedby|labels and descriptions' …` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'اسم متاح|وصف متاح|تسمية العنصر|تسميات وأوصاف' …` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_SCREEN_GOVERNANCE_STANDARD.md:42` — **قريبة:** «مفاتيح i18n لكل نص (صفر نص مضمّن)». **مستبعَدة:** التزام **ترجمة** غرضه i18n؛ اعتباره دليل إتاحة **تشابه كلمات** لا دليلاً مباشراً. **لا تثبت البند:** لا تضمن اسماً متاحاً لعنصر بلا نص ظاهر (زر أيقونة).
  - `ui/UI_ACTION_BUTTON_MODEL.md:35` — **قريبة:** «أيقونة دلالية + label i18n» للزر المصغّر. **مستبعَدة:** قاعدة عرض لنمط زر واحد. **لا تثبت البند:** ليست معياراً عاماً ولا تذكر الوصف.

<a id="absence-b1-rar003-011"></a>
### ABSENCE-B1-RAR003-011
- **البند [43]:** Error association (ربط رسالة الخطأ بحقلها آلياً).
- **السؤال:** هل يقنِّن المستودع ربطاً قابلاً للقراءة الآلية بين الخطأ وحقله؟
- **المصطلحات (EN):** `error association` · `aria-invalid` · `aria-errormessage`. **(AR):** `ربط الخطأ` · `اقتران الخطأ` · `ربط رسالة الخطأ`.
- **الأمر (EN):** `grep -rInE 'error association|aria-invalid|aria-errormessage' …` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'ربط الخطأ|اقتران الخطأ|ربط رسالة الخطأ' …` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_AI_WORKSPACE_MODEL.md:29` — **قريبة:** «أخطاء **مثبتة على الحقول** داخل البطاقة» (حالة S5). **مستبعَدة:** التزام **تثبيت بصري** بموضع الخطأ. **لا تثبت البند:** الموضع البصري ليس اقتراناً آلياً يقرأه التقني المساعد.
  - `phases/designs/phase-1-governed-core-screen-builder.md:36` — **قريبة:** كل خطأ endpoint بحقل `error_code` + `request_id`. **مستبعَدة:** عقد أخطاء **خادمي** للتشخيص. **لا تثبت البند.**

<a id="absence-b1-rar003-012"></a>
### ABSENCE-B1-RAR003-012
- **البند [44]:** Dialog focus trapping.
- **السؤال:** هل يقنِّن المستودع حصر التركيز داخل الحوارات/الأدراج؟
- **المصطلحات (EN):** `focus trap` · `focus trapping` · `modal focus`. **(AR):** `حصر التركيز` · `حبس التركيز` · `احتجاز التركيز`.
- **الأمر (EN):** `grep -rInE 'focus trap|focus trapping|modal focus' …` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'حصر التركيز|حبس التركيز|احتجاز التركيز' …` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** نمطا `drawer`/`modal` معتمدان في حزمة الشاشات، ومع ذلك لا قاعدة لحصر التركيز داخلهما ⇒ **دلتا حقيقية → R3**.

<a id="absence-b1-rar003-013"></a>
### ABSENCE-B1-RAR003-013
- **البند [46]:** Reduced motion.
- **السؤال:** هل يقنِّن المستودع احترام تفضيل تقليل الحركة؟
- **المصطلحات (EN):** `reduced motion` · `prefers-reduced-motion`. **(AR):** `تقليل الحركة` · `تخفيف الحركة` · `إيقاف الحركة`.
- **الأمر (EN):** `grep -rInE 'reduced motion|prefers-reduced-motion' …` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'تقليل الحركة|تخفيف الحركة|إيقاف الحركة' …` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_DESIGN_SYSTEM.md:120` — **قريبة:** «انتقالات وظيفية ≤200ms» و«لا رسوم متحركة استعراضية». **مستبعَدة:** قاعدة **طابع بصري** تحدّ من الزخرفة. **لا تثبت البند:** لا تربط السلوك بتفضيل المستخدم ولا توجب تعطيل الحركة عند طلبه.

<a id="absence-b1-rar003-014"></a>
### ABSENCE-B1-RAR003-014
- **البند [48]:** Data table semantics.
- **السؤال:** هل يقنِّن المستودع دلالات جدول البيانات (رؤوس/نطاقات/عنوان)؟
- **المصطلحات (EN):** `table semantics` · `<th` · `scope="col"` · `scope="row"` · `<caption`. **(AR):** `دلالات الجدول` · `رؤوس الأعمدة` · `عنوان الجدول`.
- **الأمر (EN):** `grep -rInE 'table semantics|<th|scope="col"|scope="row"|<caption' …` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'دلالات الجدول|رؤوس الأعمدة|عنوان الجدول' …` — **النتيجة: 0**
- **فحص إضافي للتحقق من عدم إغفال مطابقة:** `grep -rInE '\bcaption\b' …` — **النتيجة: 5**، وكلها **إيجابيات كاذبة** (`vision-caption` قدرةَ نموذجٍ في P5، وسطر الطباعة في نظام التصميم) — لا واحدة منها عن دلالات الجداول.
- **لماذا لا يثبت الموجود البند:** نمط `List` أساسٌ في جرد الشاشات وقواعده سلوكية (فرز · ترقيم · تنقّل بلوحة المفاتيح للصفوف) بلا أي دلالة جدول ⇒ **دلتا حقيقية → R3**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_SCREEN_GOVERNANCE_STANDARD.md:43` — **قريبة:** حزمة `List` تُلزم بفرز أعمدة RTL سليم وتنقّل لوحة مفاتيح للصفوف. **مستبعَدة:** التزامات **سلوكية**. **لا تثبت البند:** لا تقرّر بنية رؤوس/نطاقات/عنوان يقرؤها التقني المساعد.

---
**ملاحظات إنفاذ:** 14 مرساة صريحة فريدة (`absence-b1-rar003-001..014`) · كل مرساة تخدم بنداً واحداً · لا إعادة استخدام مرساة لعدة صفوف · كل استشهاد في `RECONCILIATION-RAR-003.md` يحلّ إلى مرساة موجودة هنا · لا مرجع مكسور · لا مرساة يتيمة. **عدّ المراسي يشمل `<a id=...>` الصريحة وحدها.**

**قواعد سارية:** (1) العمود الرابع لا يستشهد بـ`references/analysis-inputs/**` إطلاقاً. (2) نصٌّ حالته `Proposed` غير نافذ **منفرداً** لإثبات سياسة — و`ui/UI_DESIGN_SYSTEM.md` (`Proposed`، `INDEX.md:127`) يُستشهد به **مقروناً** بـ`ui/UI_SCREEN_GOVERNANCE_STANDARD.md` (`Accepted`، `INDEX.md:138`) الذي يجعله ملزماً-معيارياً بالإحالة (`:10` · `:68`). (3) بند نوعه `COV` يُختبر صدقه لا غيابه، و`SUG` لا يصير دلتا إلا بإثبات استقرار الحاجة في مصدر حاكم مع الاستشهاد.
