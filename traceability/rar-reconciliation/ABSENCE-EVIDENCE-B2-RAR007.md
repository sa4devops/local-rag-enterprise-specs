# ABSENCE-EVIDENCE-B2-RAR007 — B2-D (RAR-007 · Strategic Capability Backlog)

> **مخرج traceability/تحليلي غير حاكم** — لا سلطة موازية ولا بديل عن الأصل الحاكم. حزمة أدلة غياب **قابلة لإعادة التنفيذ** تسند صفوف `RECONCILIATION-RAR-007.md`. كل حالة بمرساة HTML صريحة بأحرف صغيرة. **HEAD المنفَّذ عليه البحث:** `1a3ee79e6f3b5b806550446e28838f41da8bd19f` · **تاريخ البحث:** 2026-07-27.
> **نطاق مشترك مستثنى من «الحاكم»:** `references/analysis-inputs/**` (مدخل تحليلي غير حاكم خارج authority ladder) · `traceability/rar-reconciliation/**` (مخرجات المصالحة نفسها — لا تُثبت ذاتها) · `superseded/**` (تاريخي منسوخ — محظور إلا بتكليف صريح، `AUTHORITY.md:22`). **المسارات المشمولة:** كل ملف `.md` متعقَّب خارج المستثنى — **100 ملفاً** من أصل 149 عند HEAD (`git ls-files '*.md' | grep -vE '<المستثنى>' | wc -l`).
> **الأمر القياسي:** `grep -rInE "<terms>" . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
>
> ### قاعدتان إلزاميتان مطبَّقتان في كل حالة أدناه
> **(أ) ثنائية اللغة —** كل حزمة نُفِّذت **بأمرين منفصلين**: مصطلحات إنجليزية ومقابلها العربي، ونتيجتاهما مسجَّلتان معاً. نتيجة صفرية بلغة واحدة **لا تثبت الغياب**. *(السابقة الحاسمة في هذه الحزمة: `absence-b2-rar007-001` — البحث الإنجليزي أرجع **1** فقط بينما أرجع العربي **3** ومنها **نصّ المادة م22 الدستورية** الذي حسم تصنيف `[01]`؛ ولولا القاعدة لسُجِّل «لا تعريف في المستودع» بدل «تعريفٌ نافذ مغاير». وكذلك `absence-b2-rar007-012`: الإنجليزية **0** والعربية **1** هي سطر ADR-0035 الحاسم.)*
> **(ب) الأسطر الكاملة —** المستودع يحوي أسطراً طويلة تجمع عدة التزامات مفصولة بـ`·` أو `؛`. **كل فحص أدناه قرأ السطر بتمامه**، وكل اختصار `· :NNN` وُسِّع قبل أي فحص آلي. الأسطر الجامعة المقروءة كاملةً: `constitution.md:30` · `decisions/DEFERRED_IMPLEMENTATION.md:16` و`:20` و`:25` · `decisions/adr/ADR-0035-contracts-layer-single-source.md:14` و`:18` و`:20` · `decisions/adr/ADR-0027-provider-capability-registry-architecture.md:7` · `decisions/adr/ADR-0033-automation-levels-git-merge-policy.md:10` · `architecture/README.md:12` · `PACKAGE_CHECKLIST.md:63` · `catalogs/Feature_Technical_Architecture_Catalog.md:56` · `methodology/agent-execution-model.md:59` و`:76` · `phases/designs/phase-8-monitoring-offline-production.md:36`.
>
> **ممنوع** وصف مطابقة بأنها «الوحيدة» ما لم يُرجع الأمر الموثَّق ذلك العدد بالضبط. *(استُعملت الصفة أدناه في `-010` و`-012` فقط — حيث أرجع الأمر **1** بالضبط.)*

> **أهلية المصادر — بالصيغة الهوياتية (لا بأرقام أسطر الفهرس):** لم يُستشهد أدناه بمصدرٍ غير نافذ لإثبات التزام، ولا يظهر في هذا الملف ولا في ملف المصالحة استشهادٌ بصيغة `INDEX.md:NNN` ولا مرة. المصادر غير النافذة التي وردت ذُكرت **مطابقةً قريبة أو سياقاً** مع الإفصاح:
> - ``INDEX.md → row for `knowledge/BUSINESS_GLOSSARY.md` → status `Proposed (G1)``` — مطابقة قريبة في `absence-b2-rar007-006` فقط. *(وهو المصدر موضوع القيد المؤجَّل `SOURCE-ELIGIBILITY REVIEW REQUIRED` على الصف `[01]` في `RECONCILIATION-RAR-001.md` — لم يُمَسّ ولم تُفترَض نتيجته.)*
> - ``INDEX.md → row for `traceability/TRACEABILITY_MATRIX.md` → status `Proposed (G1)``` — سياقٌ في `absence-b2-rar007-008`؛ وجودُ المسار أُثبت بـ`decisions/adr/ADR-0035-contracts-layer-single-source.md:13` (‏`Accepted`) لا به.
> - ``INDEX.md → row for `contracts/enums/ENUMS_DICTIONARY.md` → status `Proposed (G1)``` — مطابقة قريبة في `absence-b2-rar007-008`.
> - ``INDEX.md → row for `ui/UI_PROVIDER_MODEL_MANAGEMENT.md` → status `Proposed (v0.6)``` — مطابقة قريبة في `absence-b2-rar007-006`.
> - ``INDEX.md → row for `phases/BACKLOG_DEFERRED_SCOPE.md` → status `Accepted (v0.3)``` — **نافذ بحدّ ذاته مقيَّد** بنص الوثيقة («لا تُستخدم للتنفيذ المباشر» — `phases/BACKLOG_DEFERRED_SCOPE.md:3`) ⇒ سياقٌ ومطابقة قريبة في `absence-b2-rar007-005` فقط.
> - `ui/UI_STITCH_PROMPTS_BY_PHASE.md` — **تاريخي-informative** (`ui/UI_SCREEN_GOVERNANCE_STANDARD.md:14` — «لا تُقرأ كمتطلبات») ⇒ **لم يُستشهد به**، واستُبعدت مطابقته في `absence-b2-rar007-011` من كل حكم.

---

<a id="absence-b2-rar007-000"></a>
### ABSENCE-B2-RAR007-000 — حصر الذِكر الحاكم لخط توليد الوثائق (DGP) وقائمة مخرجاته
- **الغرض:** تحديد ما إذا كانت وثائق المشروع الأربع عشرة مقنَّنةً أم محجوزةً بصفٍّ قائم — **أساسُ تصنيف §14 كاملةً**، وتحديد **حدود القائمة** لئلا يُسحب الصف على بندٍ خارجها.
- **السؤال:** هل يقنِّن المستودع مولّد وثائق، وما **الحصر الدقيق** لمخرجاته المؤجَّلة؟
- **المصطلحات (EN):** `Documentation Generation Pipeline` · `FP-DOCGEN` · `GENERATED`. **(AR):** `خط أنابيب توليد` · `توليد الوثائق` · `مولَّد` · `المخرجات المولَّدة`.
- **الأمر (EN):** `grep -rInE 'Documentation Generation Pipeline|FP-DOCGEN|GENERATED' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 12**
- **الأمر (AR):** `grep -rInE 'خط أنابيب توليد|توليد الوثائق|مولَّد|المخرجات المولَّدة' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 20**
- **المطابقات الحاكمة الحاسمة (قُرئت بأسطرها كاملة):**
  - `decisions/DEFERRED_IMPLEMENTATION.md:20` — صفٌّ في **سجل المؤجلات الرسمي** (المرتبة 4): «**بناء Documentation Generation Pipeline** (مولّد BRD/FRD/SRS/الكتالوجات/الأدلة من المصادر القانونية) | متطلب مالك ملزم وُثِّق تعريفه كاملاً … في ADR-0035 بند 5 — **البناء لا التوثيق هو المؤجل** | **FP-DOCGEN ضمن نافذة P2** … | لا مصادر كافية للتوليد قبل P1».
  - `decisions/adr/ADR-0035-contracts-layer-single-source.md:14` — **الحصر الحرفي للمخرجات المولَّدة**: «‏BRD · ‏FRD · ‏SRS · ‏Data Dictionary · ‏Screen/Action/Workflow Catalogues · ‏Permission Matrix · **أدلة المستخدم والأدمن والتشغيل** · المواد التعريفية/التسويقية».
  - `decisions/adr/ADR-0035-contracts-layer-single-source.md:11` — بند 4: «كل مشتق (كتالوجات/BRD/FRD/أدلة/مواد تعريفية) **GENERATED**: يُصحَّح في مصدره فقط ويُعاد توليده — **التحرير اليدوي فيه مخالفة**».
  - `decisions/adr/ADR-0035-contracts-layer-single-source.md:18` — البوابة: «لا يُبنى المولد في G1؛ يُبنى كحزمة ميزات مستقلة (FP-DOCGEN) **ضمن نافذة Phase 2** … **يُسجَّل بنداً في `decisions/DEFERRED_IMPLEMENTATION.md`**» — إحالةٌ متبادلة تُغلق الدائرة بين الـADR والصف.
- **ما يثبته هذا:** عشرةٌ من بنود §14 **محجوزة بصفٍّ فعلي قائم** بمسار وسطر ووجهة (`FP-DOCGEN` → نافذة P2) — لا بعبارة «لاحقاً» ولا بتوصية ولا بوسم «Future Roadmap».
- **ما لا يثبته — حدُّ الحصر (بالغ الأهمية):** القائمة **لا تشمل** `SDS / Architecture Description` ولا `API Documentation` ولا `Test Plan` ولا `Release Documentation`. فامتناعُ سحب الصف عليها ليس اجتهاداً بل **قراءة حرفية لحصرٍ نصّي** — وهذه الأربعة فُحصت فردياً في `-014` و`[25]` و`[29]` و`-013`.
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-001"></a>
### ABSENCE-B2-RAR007-001
- **البند [01]:** تعريف **Future Capability** بأنه «اتجاه منتج معتمد **أو مرشح**، لكن قد لا يكون التنفيذ مقرراً».
- **السؤال:** هل يثبت المستودع **المعنى نفسه**؟ وتحديداً: هل يعرف فئةَ «قدرة مستقبلية **مرشّحة** قد لا يكون تنفيذها مقرراً»؟
- **المصطلحات (EN):** `Future Capability` · `future capability`. **(AR):** `قدرة مستقبلية` · `القدرات المستقبلية` · `قدرات مستقبلية` · `اتجاه منتج`.
- **الأمر (EN):** `grep -rInE 'Future Capabilit|future capabilit' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 1**
- **الأمر (AR):** `grep -rInE 'قدرة مستقبلية|القدرات المستقبلية|قدرات مستقبلية|اتجاه منتج' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 3**
- **أمرٌ ثانٍ مؤكِّد (EN):** `grep -rInE 'candidate capability|Approved Future Capability|Prepare Now|product direction' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **أمرٌ ثانٍ مؤكِّد (AR):** `grep -rInE 'اتجاه منتج|قدرة مرشحة|قدرات مرشحة|فكرة غير معتمدة' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **المطابقات (ثلاثة ملفات بعد إزالة التكرار) — كلها تعريفٌ نافذٌ **مغاير**:**
  - `constitution.md:30` — **المادة م22 (المرتبة 1)**، قُرئت بتمامها: «القدرات المستقبلية **التزامات معمارية**. *Future capabilities are architectural commitments. They shall not be implemented prematurely, and they shall not be removed merely because they are scheduled for later phases.* لا تُبنى قدرة قبل مرحلتها ولا يُحذف موطؤها المعماري لأنها مؤجلة؛ **سلّم الجاهزية** وشروط الـabstraction المبكر في `methodology/agent-execution-model.md`».
  - `methodology/agent-execution-model.md:76` — قاعدة الحراسة الأولى (المرتبة 2)، تكرار م22 نصاً بالإنجليزية والعربية.
  - `PACKAGE_CHECKLIST.md:10` — إشارةٌ توثيقية إلى إدراج م22 في `constitution v1.1`؛ لا تعريفَ إضافياً فيها.
- **لماذا لا يثبت الموجود البند — ولماذا هو بلا أساس:** المستودع يجعل القدرة المستقبلية **التزاماً** (commitment) لا **مرشّحاً**؛ ومنطقُه كله مبنيٌّ على ذلك: موطؤها المعماري **لا يُحذف**، ولها **سلّم جاهزية** بستّ درجات، ودخولُها السجل **بقرار SA** حصراً. فشقُّ «أو مرشح … قد لا يكون التنفيذ مقرراً» **لا سند له**، بل يناقض ما تُرتّبه م22 من آثار. أما الشقّ المطابق («معتمد + تنفيذٌ مؤجَّل») فهو بعينه **Deferred Implementation** — إذ السجل نصاً «**مرآة تنفيذية للمادة م22**» (`decisions/DEFERRED_IMPLEMENTATION.md:3`) — فالوصف لا يُنتج **قاعدةً فاصلة** بل يدمج الطرفين ويضيف ثالثاً بلا أصل ⇒ **مصطنع أو بلا أساس** بقاعدة `STATE`.
- **حدُّ صريح — لا إعادة فتح:** هذه النتيجة **لا تمسّ** أي تصنيف في `RECONCILIATION-RAR-001..006.md`؛ تلك استندت إلى **صفوف تأجيل فعلية** لا إلى تعريفٍ نظري، ونتيجةُ هذه الحزمة **تعزّز** سلطة السجل الذي تستند إليه.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `methodology/agent-execution-model.md:66`–`:73` (سلّم الجاهزية الستّ الدرجات: `Architecture-ready` … `Production-scaled`) — **قريبة:** أقربُ ما في المستودع إلى «قاعدة فاصلة» بين درجات نضج القدرة. **مستبعَدة:** سلّمُ **جاهزيةِ تنفيذٍ** لقدرةٍ **ملتزَمٍ بها**، لا فرزٌ بين «معتمد» و«مرشّح»؛ ولا درجةَ فيه لقدرةٍ غير مقرَّرة. **لا تثبت البند.**
  - `methodology/agent-execution-model.md:77` (قاعدة منع الهندسة الزائدة: «القدرات البعيدة تكتفي بـ: حدود موثقة · عقود متوقعة · تبعيات · مرحلة تفعيل · **قرارات مؤجلة مسماة**») — **قريبة:** تتحدث عن قدرات «بعيدة». **مستبعَدة:** تشترط لها **مرحلة تفعيل وقرارات مسماة** — أي التزاماً مجدولاً، وهو نقيض «قد لا يكون التنفيذ مقرراً». **لا تثبت البند.**
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-002"></a>
### ABSENCE-B2-RAR007-002
- **البند [13]:** خيار **إنشاء Strategic Registry مستقل**.
- **السؤال:** هل تبنّى المستودع سجلاً استراتيجياً مستقلاً للقدرات؟
- **المصطلحات (EN):** `Strategic Registry` · `Strategic Capability Backlog` · `Capability Registry`. **(AR):** `سجل استراتيجي` · `سجل قدرات استراتيجي` · `سجل القدرات الاستراتيجية`.
- **الأمر (EN):** `grep -rInE 'Strategic Registry|Strategic Capability Backlog|Capability Registry' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 2**
- **الأمر (AR):** `grep -rInE 'سجل استراتيجي|سجل قدرات استراتيجي|سجل القدرات الاستراتيجية' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** المطابقتان الإنجليزيتان **ملفٌ واحد** بعينه — `decisions/adr/ADR-0027-provider-capability-registry-architecture.md:1` و`:7` — ونطاقُه **تشغيليٌّ داخل الـMonolith**: «سلسلة التنفيذ الموحدة: **Capability → Provider → Model/Engine → Configuration → Execution Profile → Normalized Result → Audit** … وحدات سجلات منطقية … **Capability Registry · Provider Registry · Model Registry · Configuration · Health · Execution**». هذا سجلُّ **قدرات المزوّدات والنماذج** يُستهلك وقت التشغيل، لا سجلُّ **اتجاهات منتجٍ استراتيجية**. وصفرُ العربية يقطع احتمالَ وجود مقابلٍ عربي غير مرصود.
- **والقرار المالكي القائم يسير عكس الخيار:** `decisions/DEFERRED_IMPLEMENTATION.md:25` (قُرئ بتمامه): «Data/File Portability + الخطة الخمسية + restore drills | **قرار مالك: لا وثيقة مستقلة الآن — هذا الصف بديلها** | R9 → P5/P8 | …» — سابقةٌ صريحة لاختيار **صفٍّ في سجلٍّ قائم** بدل وثيقةٍ/سجلٍّ مستقل ⇒ **لم يُتبنَّ**.
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-003"></a>
### ABSENCE-B2-RAR007-003
- **البند [14]:** خيار **إنشاء Idea Inbox خارج Tier-0**.
- **السؤال:** هل يوجد في المستودع وارد أفكار (Idea Inbox) داخل Tier-0 أو خارجها؟
- **المصطلحات (EN):** `Idea Inbox` · `idea inbox` · `Idea Register` · `ideas backlog`. **(AR):** `صندوق الأفكار` · `صندوق أفكار` · `سجل الأفكار` · `وارد الأفكار`.
- **الأمر (EN):** `grep -rInE 'Idea Inbox|idea inbox|Idea Register|ideas backlog' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'صندوق الأفكار|صندوق أفكار|سجل الأفكار|وارد الأفكار' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **صفرٌ في اللغتين معاً** ⇒ الغياب مُثبت لا مُفترض.
- **لماذا لا يثبت الموجود البند:** لا يوجد موجودٌ أصلاً. ويُضاف أن **Tier-0 نفسها قائمة محصورة** بستة مدخلات مسمّاة نصاً — `constitution.md` · نموذج التشغيل · `decisions/open-decisions.md` · `decisions/DEFERRED_IMPLEMENTATION.md` · آخر إدخال handoff · إصدار المواصفات في `platform/docs/SPEC_SOURCE.md` (`methodology/agent-execution-model.md:59`، قُرئ بتمامه) — فلا وارد أفكار داخلها ولا نصَّ يُنشئ نظيراً له خارجها ⇒ **لم يُتبنَّ**.
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-004"></a>
### ABSENCE-B2-RAR007-004
- **البند [15]:** خيار **استخدام Structured Source مع Markdown مولد** لموطن السجل.
- **السؤال:** هل تبنّى المستودع مصدراً مهيكلاً (YAML/JSON) يُولَّد منه Markdown لسجلاته؟
- **المصطلحات (EN):** `index.yaml` · `manifest` · `Structured Source` · `generated Markdown` · `YAML` · `JSON canonical`. **(AR):** `مصدر مهيكل` · `مصدر موازٍ` · `Markdown مولد` · `مولَّد يدوياً`.
- **الأمر (EN):** `grep -rInE 'index\.yaml|manifest|Structured Source|structured source|generated Markdown|YAML|JSON canonical' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 27**
- **الأمر (AR):** `grep -rInE 'مصدر مهيكل|مصدر موازٍ|مصدر موازي|Markdown مولد|مولَّد يدوياً' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 2**
- **لماذا لا يثبت الموجود البند — بل يرفضه:** الـ27 مطابقة الإنجليزية أغلبُها `manifest` بمعنى **بيان وحدةٍ برمجية أو حزمةِ نقل** (`service_manifest` · `integrity manifest` · `model manifest` · `bundle_manifests` · `K8s manifests` · `feature manifests`) — نطاقٌ تنفيذيٌّ لا علاقة له بموطن سجلٍّ حوكمي. أما المطابقات المنطبقة على السؤال فثلاثٌ، وكلها **رفضٌ صريح**:
  - `decisions/open-decisions.md:126` — صفٌّ في **المرجع الملزم الوحيد للمفتوح/المقفل** (المرتبة 4): «‏OD-IDX-1 | **إنشاء manifest آلي للمواصفات (index.yaml)** | — | **مرفوض الآن بقرار SA (2026-07-10)** — لا يُعاد فتحه إلا عند وجود مستهلك آلي فعلي (أداة/خط CI يقرأه)؛ حينها ADR/قرار جديد».
  - `decisions/adr/ADR-0035-contracts-layer-single-source.md:20` — بند 7 (‏`Accepted`): «احترام OD-IDX-1 … لا index.yaml ولا manifest آلي **ولا مصدر موازٍ** في هذه الدفعة — إعادة الفتح تستلزم مستهلكاً آلياً فعلياً وقراراً جديداً».
  - `decisions/open-decisions.md:152` — «‏OD-IDX-1 | **محترم كما هو** … لا index.yaml/manifest في هذه الدفعة | ADR-0035 بند 7».
- **ولماذا ليس `تعارض يحتاج قراراً`:** المحفّز الثالث يشترط «مصدر حاكم **يحجز القرار** للمالك **ولم يصدر**». **القرار صدر** — رفضاً مؤرّخاً بتاريخه — وما هو محجوزٌ إنما **إعادة الفتح** المشروطة بمستهلك آلي فعلي. قرارٌ صادرٌ لا معلَّق ⇒ `مصطنع أو بلا أساس` بقاعدة `SUG`.
- **مطابقة قريبة فُحصت واستُبعدت:**
  - `decisions/adr/ADR-0035-contracts-layer-single-source.md:11` و`:14` (المشتقات GENERATED من مصادر التأليف) — **قريبة:** نمطُ «مصدرٌ واحد ← مخرجٌ مولَّد» بعينه. **مستبعَدة:** نطاقُها **المستندات المشتقة** (BRD/FRD/كتالوجات/أدلة)، والسجلُّ الحوكمي **ليس مشتقاً** بل موطنَ تأليف؛ وبند 7 من الـADR نفسه يقطع سريانها على الفهرسة والسجلات. **لا تثبت البند.**
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-005"></a>
### ABSENCE-B2-RAR007-005
- **البند [16]:** خيار **دمج بعض السجلات**.
- **السؤال:** هل تبنّى المستودع سياسة دمج أو توحيد سجلاته الحوكمية؟
- **المصطلحات (EN):** `merge registers` · `consolidate registers` · `single register`. **(AR):** `دمج السجلات` · `دمج سجلات` · `توحيد السجلات` · `سجل موحّد`.
- **الأمر (EN):** `grep -rInE 'merge registers|consolidate registers|single register' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'دمج السجلات|دمج سجلات|توحيد السجلات|سجل موحّد' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **صفرٌ في اللغتين معاً** ⇒ الغياب مُثبت لا مُفترض.
- **لماذا لا يثبت الموجود البند — والواقع المقنَّن عكسه:** `AUTHORITY.md:13` (قُرئ بتمامه) يُبقي داخل **المرتبة 4 وحدها** أربعة سجلات متمايزة عمداً: `decisions/open-decisions.md` (المرجع الملزم للمفتوح/المقفل) + `decisions/adr/**` (سجل وملفات ADR) + `decisions/DEFERRED_IMPLEMENTATION.md` + `decisions/license-review.md`. ويتعايش معها سجلُّ نطاقٍ مؤجَّل ثانٍ خارج المرتبة — `phases/BACKLOG_DEFERRED_SCOPE.md` — بقاعدته الخاصة («كل بند هنا **خارج مسار التنفيذ الأول** عمداً؛ تفعيله لاحقاً يتطلب قرار مالك + ADR + بواباته» — `phases/BACKLOG_DEFERRED_SCOPE.md:6`) **بلا أي نص يدمجه** بسجل المؤجلات.
- **مطابقة قريبة فُحصت واستُبعدت:**
  - `decisions/adr/ADR-0035-contracts-layer-single-source.md:10` («‏`ui/UI_SCREEN_INVENTORY.md` هو السجل المركزي الكنسي للشاشات والمسارات — **لا سجل موازٍ**») — **قريبة:** لغةُ «سجل واحد». **مستبعَدة:** قاعدةُ **منع ازدواجٍ داخل موضوعٍ واحد** (الشاشات)، لا قرارَ **دمج سجلات مختلفة الموضوع**؛ ولو قُرئت دمجاً لناقضت `AUTHORITY.md:13` الذي يُبقيها متمايزة. **لا تثبت البند.**
  - `decisions/DEFERRED_IMPLEMENTATION.md:25` («لا وثيقة مستقلة الآن — هذا الصف بديلها») — **قريبة:** استيعابُ موضوعٍ داخل سجلٍّ قائم. **مستبعَدة:** استيعابُ **وثيقةٍ مفردة** في صفٍّ، لا **دمجُ سجلٍّ بسجل**؛ وهي حجّة `[12]` لا `[16]`، و«قرب الاقتراح من بنية قائمة لا يثبت تبنّيه». **لا تثبت البند.**
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-006"></a>
### ABSENCE-B2-RAR007-006
- **البند [11]:** موضع الفحص «**أي Capability Catalogue قائم**» — وحدُّ ما يُعدّ قائماً.
- **السؤال:** هل يوجد كتالوج قدرات قائم في المستودع، وما نطاقه بالضبط؟
- **المصطلحات (EN):** `Capability Catalog` · `Capability Catalogue` · `Product Capability`. **(AR):** `كتالوج القدرات` · `كتالوج قدرات` · `سجل القدرات` · `فهرس القدرات`.
- **الأمر (EN):** `grep -rInE 'Capability Catalog|Capability Catalogue|capability catalog' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'كتالوج القدرات|كتالوج قدرات|سجل القدرات|فهرس القدرات' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 2**
- **أمرٌ ثانٍ مؤكِّد (EN):** `grep -rInE 'Product Capability|capability catalogue|Capability Catalogue' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **ما يثبته الفحص — إيجاباً:** البند فحصٌ مشروطٌ بـ«قائم»، والشرط **مستوفى** بمصدرين نافذين لا بالمصطلح:
  - `catalogs/Feature_Technical_Architecture_Catalog.md:13` (المرتبة 3 — `AUTHORITY.md:12`؛ الحالة `Current / Accepted`): «**Purpose:** المرجع الرسمي **للميزات والخدمات والشاشات والتقنيات** وسيناريوهات التدفق وأحداث التدقيق، قبل أي تصميم مرحلة أو تنفيذ».
  - `decisions/adr/ADR-0027-provider-capability-registry-architecture.md:7` (المرتبة 4 — `Accepted`): سجل قدراتٍ مقنَّن بـADR في نطاق المزوّدات/النماذج.
- **وما لا يثبته — حدٌّ مُعلَن:** **لا يوجد Strategic Capability Backlog** ولا كتالوج قدراتٍ منتجيّ بالمعنى الذي يقترحه المدخل، ولا يُدَّعى وجودُه. المصطلح `Capability Catalogue` نفسه **صفرٌ بالإنجليزية** في أمرين مستقلين.
- **مطابقات قريبة فُحصت واستُبعدت (المطابقتان العربيتان):**
  - `ui/UI_PROVIDER_MODEL_MANAGEMENT.md:93` («القدرات المطلوبة (اختيار من **كتالوج القدرات**)» في نموذج طلب مزوّد) — **قريبة:** المصطلح حرفياً. **مستبعَدة:** نطاقُها **قدرات النماذج/المزوّدات** لا قدرات المنتج، وحالة الملف ``INDEX.md → row for `ui/UI_PROVIDER_MODEL_MANAGEMENT.md` → status `Proposed (v0.6)`` ⇒ غير نافذة وحدها. **لا تثبت البند** ولم تدخل العمود الرابع.
  - `knowledge/BUSINESS_GLOSSARY.md:18` («Extension (امتداد) … | **سجل القدرات**» في عمود موضع الظهور) — **قريبة:** تذكر «سجل القدرات» اسماً. **مستبعَدة:** حالتها ``INDEX.md → row for `knowledge/BUSINESS_GLOSSARY.md` → status `Proposed (G1)`` ⇒ غير نافذة وحدها، و**لا ملف بهذا الاسم في المستودع** فالإشارة تسمية مفهوم لا إثبات وجود. **لا تثبت البند** ولم تدخل العمود الرابع.
  - `catalogs/Feature_Technical_Architecture_Catalog.md:56` (صف OKF: «Knowledge Catalog / **Data Dictionary** / Glossary / agent-instructions … **ليس مصدر حقيقة**») — **قريبة:** يذكر كتالوجات معرفة. **مستبعَدة:** صفُّ **تقييم مكوّن OSS** يقرّر اعتماده بدور محدود وينفي عنه مصدرية الحقيقة صراحةً. **لا تثبت البند.**
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-007"></a>
### ABSENCE-B2-RAR007-007
- **البنود [17] [18] [19]:** توليد **BRD** · **FRD** · **SRS**.
- **السؤال:** هل توجد أيٌّ من هذه الوثائق الثلاث في المستودع، أم أن كل ذِكرٍ لها ذكرُ حجزٍ لا وجود؟
- **المصطلحات (EN):** `BRD` · `FRD` · `SRS`. **(AR):** `وثيقة متطلبات` · `وثيقة المتطلبات` · `مواصفة متطلبات النظام`.
- **الأمر (EN):** `grep -rInE '\bBRD\b|\bFRD\b|\bSRS\b' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 6**
- **الأمر (AR):** `grep -rInE 'وثيقة متطلبات|وثيقة المتطلبات|مواصفة متطلبات النظام' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر البنيوي:** `git ls-files | grep -iE 'brd|frd|srs|sds|guide|runbook|dictionary|manual'` — **النتيجة: 1** (`contracts/enums/ENUMS_DICTIONARY.md` فقط) ⇒ **لا ملف** باسم أيٍّ من الثلاث.
- **لماذا لا يثبت الموجود البنود:** المطابقات الستّ كلها في **ثلاثة ملفات حوكمية** وتتحدث عن الوثائق بوصفها **مخرجاتٍ ستُولَّد**، لا بوصفها موجودة: `decisions/adr/ADR-0035-…:3` و`:11` و`:14` و`:22` · `decisions/DEFERRED_IMPLEMENTATION.md:20` · `decisions/open-decisions.md:151` («طبقة العقود + المشتقات المولَّدة + DGP | contracts/ موطن التأليف؛ **BRD/FRD/… GENERATED**؛ **المولد يُبنى في P2** | ADR-0035»). بل إن `ADR-0035:22` يُدرج «**تحرير BRD/FRD يدوياً**» في **البدائل المرفوضة** («انجراف مضمون ومصدر حقيقة ثانٍ») ⇒ فالوجود اليدوي ممنوعٌ أصلاً، والوجود المولَّد محجوزٌ بصفٍّ فعلي.
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-008"></a>
### ABSENCE-B2-RAR007-008
- **البند [21]:** توليد **Data Dictionary**.
- **السؤال:** هل يوجد قاموس بيانات (حقول/كيانات) قائم في المستودع؟
- **المصطلحات (EN):** `Data Dictionary` · `data dictionary`. **(AR):** `قاموس البيانات` · `قاموس بيانات` · `معجم البيانات`.
- **الأمر (EN):** `grep -rInE 'Data Dictionary|data dictionary' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 2**
- **الأمر (AR):** `grep -rInE 'قاموس البيانات|قاموس بيانات|معجم البيانات' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **أمرٌ موسَّع للمطابقات القريبة (EN+AR):** `grep -rInE 'Dictionary|قاموس' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 19**
- **لماذا لا يثبت الموجود البند:** المطابقتان المباشرتان هما `decisions/adr/ADR-0035-…:14` (قائمة المخرجات المولَّدة — أي **حجز** لا وجود) و`catalogs/Feature_Technical_Architecture_Catalog.md:56` (صفُّ تقييم OKF الذي ينفي عنه مصدرية الحقيقة). وصفرُ العربية يقطع احتمال وجود مقابلٍ عربي.
- **مطابقات قريبة فُحصت واستُبعدت (من الأمر الموسَّع):**
  - `contracts/enums/ENUMS_DICTIONARY.md:1` و`:25` («**القاموس المركزي للتعدادات**» · «لا يضيف أي عميل/وكيل قيمة تعداد خارج هذا القاموس») — **قريبة:** قاموسٌ مركزي قائم فعلاً في `contracts/`. **مستبعَدة:** نطاقُه **قيم التعدادات** حصراً (`contracts/NAMING_AND_CONTRACTS_STANDARD.md:20`)، لا الحقول والكيانات وأنواعها ومصادرها؛ وحالته ``INDEX.md → row for `contracts/enums/ENUMS_DICTIONARY.md` → status `Proposed (G1)`` ⇒ غير نافذ وحده. **لا تثبت البند** ولم يدخل العمود الرابع.
  - `knowledge/BUSINESS_GLOSSARY.md` (`Proposed (G1)`) و`traceability/TRACEABILITY_MATRIX.md:15` (`Proposed (G1)`) — **قريبتان:** معجمُ أعمالٍ وسجلُّ تتبعٍ يُشيران إلى القاموس. **مستبعَدتان:** حالتاهما غير نافذتين وحدهما، والمعجم مصطلحاتُ أعمالٍ لا قاموسَ بيانات. **لا تثبتان البند** ولم تدخلا العمود الرابع.
  - `ui/UI_FIELD_NAMING.md` (المصدر الكنسي للتسمية — `ADR-0035:9`) — **قريبة:** تحكم تسمية الحقول. **مستبعَدة:** **معيارُ تسميةٍ** لا جردَ حقولٍ بأنواعها ودلالاتها؛ والـADR يجعلها **مصدر تأليف** يُولَّد منه القاموس لا القاموسَ نفسه. **لا تثبت البند.**
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-009"></a>
### ABSENCE-B2-RAR007-009
- **البند [22]:** توليد **Screen Catalogue**.
- **السؤال:** هل الكتالوج المطلوب موجودٌ مولَّداً، أم أن الموجود **موطنُ تأليفٍ** يُولَّد منه؟
- **المصطلحات (EN):** `Screen Catalog` · `Screen Catalogue` · `Action Catalog`. **(AR):** `كتالوج الشاشات` · `كتالوج الشاشة` · `فهرس الشاشات`.
- **الأمر (EN):** `grep -rInE 'Screen Catalog|Screen Catalogue|Workflow Catalog|Workflow Catalogue|Action Catalog' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 1**
- **الأمر (AR):** `grep -rInE 'كتالوج الشاشات|كتالوج الشاشة|فهرس الشاشات' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 1**
- **لماذا لا يثبت الموجود البند:** المطابقة الإنجليزية هي `decisions/adr/ADR-0035-…:14` نفسها (قائمة المخرجات المولَّدة المؤجَّلة)، والمطابقة العربية `phases/designs/phase-1-governed-core-screen-builder.md:58` عنوانُ قسمٍ يحيل إلى الجرد («UI Notes … **من كتالوج الشاشات**») لا كتالوجاً.
- **مطابقة قريبة فُحصت واستُبعدت — وهي الأقوى في هذه الحزمة:**
  - `ui/UI_SCREEN_INVENTORY.md:1` («**جرد الشاشات الشامل**» · الحالة ``INDEX.md → row for `ui/UI_SCREEN_INVENTORY.md` → status `Current``) — **قريبة جداً:** جردٌ شاملٌ قائم للشاشات والمسارات، ويرقّيه `decisions/adr/ADR-0035-…:10` إلى «**السجل المركزي الكنسي للشاشات والمسارات — لا سجل موازٍ**». **مستبعَدة بنصٍّ من الـADR نفسه:** بند 4 (`:11`) يفصل الطبقتين — المشتق **GENERATED** «يُصحَّح **في مصدره فقط** ويُعاد توليده»، والجرد هو **المصدر**. واعتبارُ الجرد هو الكتالوجَ المطلوب يجعل المخرج المولَّد **سجلاً موازياً** يمنعه بند 3 صراحةً. فالجرد **لا يُسقط** الحجز بل يفسّره. **لا تثبت البند.**
  - `contracts/NAMING_AND_CONTRACTS_STANDARD.md:27` («السجل المركزي للشاشات والمسارات يبقى `ui/UI_SCREEN_INVENTORY.md` (ADR-0035 بند 3) — **عقود الشاشات تكوّد سلوكها ولا تنافس الجرد على المعرفات**») — **قريبة:** تثبّت التمايز. **مستبعَدة:** تؤكّد التمايز ولا تُنشئ كتالوجاً. **لا تثبت البند.**
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-010"></a>
### ABSENCE-B2-RAR007-010
- **البند [23]:** توليد **Workflow Catalogue**.
- **السؤال:** هل يوجد كتالوج تدفقات عمل قائم؟
- **المصطلحات (EN):** `Workflow Catalog` · `Workflow Catalogue`. **(AR):** `كتالوج التدفقات` · `كتالوج سير العمل` · `كتالوج مسارات العمل`.
- **الأمر (EN):** `grep -rInE 'Workflow Catalog|Workflow Catalogue' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 1**
- **الأمر (AR):** `grep -rInE 'كتالوج التدفقات|كتالوج سير العمل|كتالوج مسارات العمل' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** المطابقة الإنجليزية **الوحيدة** (الأمر أرجع `1` بالضبط) هي `decisions/adr/ADR-0035-…:14` — أي سطرُ **الحجز** نفسه ضمن `Screen/Action/Workflow Catalogues`؛ وصفرُ العربية يقطع وجود مقابل. فلا كتالوج تدفقاتٍ قائماً يُنازع الحجز.
- **مطابقة قريبة فُحصت واستُبعدت:**
  - `phases/designs/phase-3-workflows-approvals-actions.md` (تصميم مرحلة التدفقات والاعتمادات) — **قريبة:** يعرّف متطلبات التدفقات ومحرّكها. **مستبعَدة:** **تصميمُ مرحلةٍ** في المرتبة 7 يعرّف الـFRs، لا كتالوجاً مولَّداً يجرد التدفقات المعرَّفة في المنصة. **لا تثبت البند.**
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-011"></a>
### ABSENCE-B2-RAR007-011
- **البند [24]:** توليد **Permission Matrix**.
- **السؤال:** هل توجد وثيقةُ مصفوفة صلاحيات قائمة، أم أن الموجود **شاشةٌ** بهذا الاسم؟
- **المصطلحات (EN):** `Permission Matrix` · `permission matrix` · `permissions matrix`. **(AR):** `مصفوفة الصلاحيات` · `مصفوفة صلاحيات`.
- **الأمر (EN):** `grep -rInE 'Permission Matrix|permission matrix|permissions matrix' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 2**
- **الأمر (AR):** `grep -rInE 'مصفوفة الصلاحيات|مصفوفة صلاحيات' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 2**
- **لماذا لا يثبت الموجود البند:** المطابقة الإنجليزية الحاكمة هي `decisions/adr/ADR-0035-…:14` (سطر الحجز)، والثانية `ui/UI_STITCH_PROMPTS_BY_PHASE.md:42` **مصدرٌ تاريخي-informative مستبعَد**. والمطابقتان العربيتان **شاشةُ أدمن** لا وثيقة.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_SCREEN_INVENTORY.md:33` (`admin.permissions | مصفوفة الصلاحيات | /admin/permissions | … | G6`) و`ui/UI_SCREEN_BEHAVIOR_CARDS.md:163` (بطاقة سلوك `admin.permissions`) — **قريبتان:** الاسم حرفياً. **مستبعَدتان:** **سطحُ عرضٍ وتحريرٍ تفاعلي** بمسارٍ ومعرّف شاشة وبوابة، لا **وثيقةً مولَّدة** تجرد الصلاحيات مقابل الأدوار. **لا تثبتان البند.**
  - `ui/UI_STITCH_PROMPTS_BY_PHASE.md:42` — **قريبة:** تذكر `roles + permission matrix`. **مستبعَدة:** الملف مصنَّف **تاريخي-informative** («لا تُقرأ كمتطلبات» — `ui/UI_SCREEN_GOVERNANCE_STANDARD.md:14`) ⇒ **غير مؤهَّل أصلاً** ولم يُستشهد به. **لا تثبت البند.**
  - `methodology/RECONCILIATION_ROADMAP.md:12` (‏R4 — «عقد الصلاحيات الكامل (التقاطع الرباعي · سياسة إعادة حساب الموروث · معايير قبول المنح المنطاق) | **Permission Contract + مواصفة اختبار** | P4») — **قريبة:** حجزٌ ثانٍ قائم في نطاق الصلاحيات. **مستبعَدة من كونها الصفَّ الحاكم لهذا البند:** موضوعُها **عقد الصلاحيات** لا **الوثيقة المولَّدة**؛ سُجّلت في العمود الرابع **مساندةً** لا بديلاً عن `decisions/DEFERRED_IMPLEMENTATION.md:20`. *(وR4 غير مُفعَّلة — `methodology/RECONCILIATION_ROADMAP.md:5`.)*
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-012"></a>
### ABSENCE-B2-RAR007-012
- **البنود [26] [27] [28]:** توليد **User Guide** · **Admin Guide** · **Operations Guide**.
- **السؤال:** هل يوجد أيٌّ من الأدلة الثلاثة قائماً في المستودع؟
- **المصطلحات (EN):** `User Guide` · `Admin Guide` · `Operations Guide`. **(AR):** `دليل المستخدم` · `دليل الأدمن` · `دليل التشغيل` · `أدلة المستخدم`.
- **الأمر (EN):** `grep -rInE 'User Guide|Admin Guide|Operations Guide' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'دليل المستخدم|دليل الأدمن|دليل التشغيل|أدلة المستخدم' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 1**
- **مثالٌ حيٌّ على ثنائية اللغة:** الإنجليزية **صفر** والعربية **واحد** — والمطابقة العربية **الوحيدة** (الأمر أرجع `1` بالضبط) هي `decisions/adr/ADR-0035-…:14`، أي **سطر الحجز** الذي يسمّي «أدلة المستخدم والأدمن والتشغيل». لولا الأمر العربي لسُجِّل «صفرٌ مطلق» وضاع سندُ الحجز نفسه.
- **لماذا لا يثبت الموجود البنود:** لا دليل قائماً — والذِكر الوحيد ذِكرُ حجز.
- **مطابقات قريبة فُحصت واستُبعدت (خاصة بـ[28]):**
  - `phases/designs/phase-8-monitoring-offline-production.md:36` (**FR-8.10** — «Runbooks الأربعة مكتملة ومختبرة **بمن ينفّذها غير كاتبها**») و`:92` («**Handoff نهائي للتشغيل** (وثيقة تسليم Ops)») و`:13` (تعداد الـrunbooks الأربعة: troubleshooting / backup-restore / offline-install / promotion) — **قريبة:** مطلبٌ مقنَّن بمعيار قبولٍ ومرحلةٍ لأدلة التشغيل. **حكمُها:** لا تُسقط الحجز بل **تُحدّد مرحلة استهلاكه** — المولّد يُبنى في نافذة P2 والاستهلاك في P8، فلا تعارض ولا ازدواج؛ سُجّلت في العمود الرابع **مساندةً** لا بديلاً عن `decisions/DEFERRED_IMPLEMENTATION.md:20`.
  - `methodology/Spec_Driven_Modular_Monolith_Methodology.md:56` (مجموعة ملفات المواصفات الكنسية تتضمن `runbooks/ (troubleshooting, backup-restore, offline-install)`) — **قريبة:** تُدرج الـrunbooks في البنية المعيارية (المرتبة 2). **مستبعَدة:** تُعرّف **موضعاً في البنية** لا وثيقةً قائمة. **لا تثبت البنود.**
  - `PACKAGE_CHECKLIST.md:63` («**تُنشأ لاحقاً (ليست نقصاً):** ملفات المعمارية المستخرجة · ملفات ADR الفردية · handoff/archive · **runbooks** · prototypes/design-notes») — **قريبة:** تذكر الـrunbooks مؤجَّلةً. **مستبعَدة:** «تُنشأ لاحقاً» **ليست صف تأجيل** بنص القاعدة النافذة على هذه المصالحة؛ والسطر متقادم جزئياً (ملفات ADR الفردية موجودة اليوم). **لم تُستعمل سنداً لأي تصنيف.**
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-013"></a>
### ABSENCE-B2-RAR007-013
- **البند [30]:** توليد **Release Documentation** — وتحديد ما إذا كان قائماً أم محجوزاً أم بلا أصل.
- **السؤال:** هل يقنِّن المستودع توثيقاً للإصدار، وهل يحجزه صفٌّ قائم؟
- **المصطلحات (EN):** `Release Documentation` · `Release Notes` · `Release Evidence`. **(AR):** `وثائق الإصدار` · `وثيقة الإصدار` · `ملاحظات الإصدار` · `توثيق الإصدار`.
- **الأمر (EN):** `grep -rInE 'Release Documentation|Release Notes|Release Evidence' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'وثائق الإصدار|وثيقة الإصدار|ملاحظات الإصدار|توثيق الإصدار' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **أمرٌ ثانٍ موسَّع (EN):** `grep -rInE '\bRelease\b|\brelease\b' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 67** في **16 ملفاً**.
- **ما يثبته هذا — إثباتٌ إيجابيّ لا غياب:** **المصطلح** غائبٌ في اللغتين، لكن **الممارسة مقنَّنة ومنفَّذة** — وهذا بالضبط سبب إجراء الأمر الموسَّع بدل الاكتفاء بالصفرين:
  - `AUTHORITY.md:18` — **المرتبة 9: سجل التسليم `handoff/handoff.md`**.
  - `constitution.md:28` — م20: «… **كل جلسة تنفيذ تنتهي بتحديث `handoff.md`**» ⇒ التزامٌ دستوري لا توصية.
  - `SPECIFICATIONS_CLOSEOUT_HANDOFF.md:88` — «**§12) Release Addendum — ملحق الإصدار** (GATE M — نُفِّذ بأمر SA الصريح، 2026-07-11)» ⇒ وثيقةُ إصدارٍ فعليّة منفَّذة، بسجلٍّ يشمل الوسم والحزمة وبصمة sha256 والتحقق.
  - `decisions/adr/ADR-0033-automation-levels-git-merge-policy.md:10` — «المسارات المحمية — **دمج بشري دائماً** … **عمليات Tag/Release** …» ⇒ الإصدار إجراءٌ محكوم لا حرّ.
  - وحقلُ `Release:` متكرّر في إدخالات `handoff/handoff.md` (`:192` · `:224` · `:266`) بأدلته.
- **مطابقة قريبة فُحصت واستُبعدت — وهي مفصليّة:**
  - `decisions/DEFERRED_IMPLEMENTATION.md:16` (قُرئ بتمامه): «**التحديث الدوري لـ PROJECT_EVOLUTION.md** | الملف أُنشئ (ليس مؤجلاً)؛ المؤجل هو التزام تحديثه عند كل وسم | عند كل وسم/إصدار جديد | **توصية تشغيلية لا التزام** — تُنفَّذ مع كل إغلاق» — **قريبة:** صفٌّ في سجل المؤجلات يذكر الإصدار. **مستبعَدة لسببين مستقلين:** (١) موضوعُه **تحديث `PROJECT_EVOLUTION.md`** لا إنتاجُ توثيق إصدار؛ (٢) يصف نفسه نصاً «**توصية تشغيلية لا التزام**» ⇒ سحبُه على هذا البند يكون **استعمالَ صفٍّ عامٍّ لتغطية التزامٍ يتجاوز موضوعه** — وهو ممنوع. **لم يُستعمل صفَّ تأجيل لهذا البند**، وبقي البند `مقنَّن` بأدلته الأربعة أعلاه.
  - `decisions/adr/ADR-0035-…:15` و`:18` («إعادة توليد **كاملة عند كل Release**» · «التوليد الكامل **شرط بوابة لكل Release**») — **قريبة:** تربط الإصدار بالتوثيق. **مستبعَدة:** تحكم **إعادة توليد المشتقات** عند الإصدار، لا تُنشئ وثيقةَ إصدارٍ مستقلة ولا تحجزها. **لا تثبت الحجز.**
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

<a id="absence-b2-rar007-014"></a>
### ABSENCE-B2-RAR007-014
- **البند [20]:** توليد **SDS / Architecture Description** — وتحديد ما إذا كان قائماً أم محجوزاً.
- **السؤال:** هل يوجد وصفٌ معماري قائم ومقنَّن، وهل يحجزه صفٌّ قائم؟
- **المصطلحات (EN):** `Architecture Description` · `System Design Specification` · `SDS`. **(AR):** `وصف المعمارية` · `وثيقة تصميم النظام` · `توصيف المعمارية`.
- **الأمر (EN):** `grep -rInE 'Architecture Description|System Design Specification|\bSDS\b' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'وصف المعمارية|وثيقة تصميم النظام|توصيف المعمارية' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **أمرٌ ثانٍ موسَّع (AR):** `grep -rInE 'استخراج وثائق المعمارية|المعمارية المستخرجة|وثائق المعمارية' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 4**
- **ما يثبته هذا — إثباتٌ إيجابيّ لا غياب:** المصطلحان الاصطلاحيان غائبان في اللغتين، لكن **المضمون قائمٌ ومقنَّنٌ وموضعُه مُعلَن نصاً** — ولهذا أُجري الأمر الموسَّع:
  - `architecture/README.md:6`–`:10` (قُرئت كاملة؛ الحالة ``INDEX.md → row for `architecture/README.md` → status `Current / Accepted``): «## أين تعيش المعمارية الآن (المصادر المعتمدة) — المحتوى المعماري **الكامل موحَّد حالياً** في: (1) `catalogs/Feature_Technical_Architecture_Catalog.md` (v2.1) — الطبقات، الخدمات، البيانات، الصلاحيات، الوكلاء، RAG/SQL/Media، الإعداد/البروفايلات، التكاملات، التدقيق/المراقبة، الأمن؛ (2) `methodology/Spec_Driven_Modular_Monolith_Methodology.md` (v2.1) — الحدود، الـModules، مصفوفة الاعتماد، النشر والنقل؛ (3) **المخطط المعتمد بصرياً** `architecture/diagrams/Full_Stack_Architecture.svg`».
  - `architecture/README.md:4` — «**Purpose:** دليل موقع محتوى المعمارية الآن، وخريطة استخراج الوثائق الفرعية لاحقاً».
  - `catalogs/Feature_Technical_Architecture_Catalog.md:13` — الغرض: «**المرجع الرسمي** للميزات والخدمات والشاشات والتقنيات وسيناريوهات التدفق وأحداث التدقيق» (المرتبة 3).
  - `constitution.md:29` — م21: «**خط الأساس المعماري** … المعمارية مجمّدة للتنفيذ اعتباراً من الإصدار الموسوم `v1.0-architecture-baseline`؛ أي تغيير معماري لاحق لا يمر إلا عبر ADR» ⇒ لا يُجمَّد إلا موصوفٌ قائم.
- **وما لا يثبت الحجز — «لاحقاً» ليست صف تأجيل:** المطابقتان الباقيتان من الأمر الموسَّع تتحدثان عن **إعادة تنظيم** محتوى قائم لا عن غيابه، وكلتاهما بصيغة توصية مستقبلية:
  - `architecture/README.md:12` (نهاية السطر، قُرئ بتمامه): «… تحديثها البصري مُدرج مع **استخراج وثائق المعمارية لاحقاً** (لن نرسم رسوماً شكلية لمجرد الإكمال)».
  - `PACKAGE_CHECKLIST.md:63`: «**تُنشأ لاحقاً (ليست نقصاً):** ملفات المعمارية المستخرجة · …».
  كلتاهما **«لاحقاً»/توصية مستقبلية — ليست صف تأجيل** بنص القاعدة النافذة، ولم تُستعملا سنداً لأي تصنيف. ويؤكّده أن `SDS / Architecture Description` **غائبٌ عن حصر `decisions/adr/ADR-0035-contracts-layer-single-source.md:14`** (انظر `absence-b2-rar007-000`) ⇒ لا يشمله صفُّ `decisions/DEFERRED_IMPLEMENTATION.md:20`، ولا صفَّ تأجيل آخر له.
- **SHA (HEAD):** `1a3ee79e6f3b5b806550446e28838f41da8bd19f`.

---

**Related:** `RECONCILIATION-RAR-007.md` · `CANONICAL-ITEM-UNIVERSE.md` (`188ad37`) · `NORMALIZATION-AND-ATOMIC-MAP.md` (`188ad37`) · `ABSENCE-EVIDENCE-B2-RAR004.md` · `ABSENCE-EVIDENCE-B2-RAR005.md` · `ABSENCE-EVIDENCE-B2-RAR006.md` · `decisions/DEFERRED_IMPLEMENTATION.md` · `decisions/adr/ADR-0035-contracts-layer-single-source.md` · `constitution.md` (م20/م21/م22)
