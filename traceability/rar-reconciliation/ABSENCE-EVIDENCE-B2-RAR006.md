# ABSENCE-EVIDENCE-B2-RAR006 — B2-C (RAR-006 · Realtime AI Readiness)

> **مخرج traceability/تحليلي غير حاكم** — لا سلطة موازية ولا بديل عن الأصل الحاكم. حزمة أدلة غياب **قابلة لإعادة التنفيذ** تسند صفوف `RECONCILIATION-RAR-006.md`. كل حالة بمرساة HTML صريحة بأحرف صغيرة. **HEAD المنفَّذ عليه البحث:** `a8129d6b700020393ab5dc42809cc5800424e7a2` · **تاريخ البحث:** 2026-07-27.
> **نطاق مشترك مستثنى من «الحاكم»:** `references/analysis-inputs/**` (مدخل تحليلي غير حاكم خارج authority ladder) · `traceability/rar-reconciliation/**` (مخرجات المصالحة نفسها — لا تُثبت ذاتها) · `superseded/**` (تاريخي منسوخ — محظور إلا بتكليف صريح، `AUTHORITY.md:22`). **المسارات المشمولة:** كل ملف `.md` متعقَّب خارج المستثنى — **100 ملفاً** من أصل 147 عند HEAD (`git ls-files '*.md' | grep -vE '<المستثنى>' | wc -l`).
> **الأمر القياسي:** `grep -rInE "<terms>" . --include='*.md' | grep -viE 'references/analysis-inputs/|traceability/rar-reconciliation/|superseded/'`
>
> ### قاعدتان إلزاميتان مطبَّقتان في كل حالة أدناه
> **(أ) ثنائية اللغة —** كل حزمة نُفِّذت **بأمرين منفصلين**: مصطلحات إنجليزية ومقابلها العربي، ونتيجتاهما مسجَّلتان معاً. نتيجة صفرية بلغة واحدة **لا تثبت الغياب**. *(السابقة الحاسمة في هذه الحزمة: `absence-b2-rar006-002` — البحث الإنجليزي أرجع **صفراً** بينما أرجع العربي **الرفض البنيوي الثلاثي** الذي حسم تصنيف البند `[07]`؛ ولولا القاعدة لسُجِّل «غياب» بدل «رفض».)*
> **(ب) الأسطر الكاملة —** المستودع يحوي أسطراً طويلة تجمع عدة التزامات مفصولة بـ`·` أو `؛`. **كل فحص أدناه قرأ السطر بتمامه**. الأسطر الجامعة المقروءة كاملةً: `methodology/Spec_Driven_Modular_Monolith_Methodology.md:52` و`:125` · `phases/designs/phase-7-integrations-app-store.md:13` و`:16` و`:62` و`:71` و`:77` · `ui/UI_AI_WORKSPACE_MODEL.md:5` و`:27` · `ui/UI_SCREEN_GOVERNANCE_STANDARD.md:49` · `decisions/adr/ADR-0018-cache-queue-lock-valkey-scale-profile.md:15` · `ui/UI_SCREEN_INVENTORY.md:103`.
>
> **ممنوع** وصف مطابقة بأنها «الوحيدة» ما لم يُرجع الأمر الموثَّق ذلك العدد بالضبط.

> **أهلية المصادر — بالصيغة الهوياتية (لا بأرقام أسطر الفهرس):** لم يُستشهد أدناه بمصدرٍ غير نافذ لإثبات التزام. المصادر غير النافذة التي وردت ذُكرت **مطابقةً قريبة أو سياقاً** مع الإفصاح:
> - `INDEX.md → row for `ui/UI_RUN_EXECUTION_MODEL.md` → status `Proposed (v0.6)`` — مطابقة قريبة في `absence-b2-rar006-001`؛ وحيثما لزم إثبات، قُرن بـ`ui/UI_SCREEN_GOVERNANCE_STANDARD.md:10` (`INDEX.md → row for `ui/UI_SCREEN_GOVERNANCE_STANDARD.md` → status `Accepted``) الذي يصنّفه **ملزم-معيارياً** نصاً.
> - `INDEX.md → row for `ui/UI_V06_DELTA_AMENDMENTS.md` → status `Proposed (v0.6)`` — مطابقة قريبة فقط.
> - `INDEX.md → row for `ui/UI_STITCH_PROMPTS_BY_PHASE.md` / `ui/UI_STITCH_REFINED_PROMPTS.md`` — مصنَّفان **تاريخي-informative** («لا تُقرأ كمتطلبات» — `ui/UI_SCREEN_GOVERNANCE_STANDARD.md:14`) ⇒ **لم يُستشهد بهما إطلاقاً**، واستُبعدت مطابقاتهما من كل حكم.

---

<a id="absence-b2-rar006-000"></a>
### ABSENCE-B2-RAR006-000 — حصر الذِكر الحاكم لجاهزية الزمن الحقيقي وعقود الأحداث
- **الغرض:** تحديد ما إذا كانت جاهزية الزمن الحقيقي (Ordering/Replay/Live Views) مقنَّنة أم محجوزة بصفوف قائمة — أساسُ تصنيف §14 كاملةً وبقية §12.
- **السؤال:** هل يقنِّن المستودع عقود أحداث أو ضمانات ترتيب/إعادة تشغيل، أم يحجزها بصفٍّ قائم؟
- **المصطلحات (EN):** `Event Contract` · `Real-time AI` · `Live View` · `event contract`. **(AR):** `جاهزية توثيقية` · `عقود الأحداث` · `العرض الحي` · `الوقت الفعلي`.
- **الأمر (EN):** `grep -rInE 'Event Contract|Real.time AI|Live View|event contract' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 2**
- **الأمر (AR):** `grep -rInE 'جاهزية توثيقية|عقود الأحداث|العرض الحي|الوقت الفعلي' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 1**
- **المطابقات (ملفان بعد إزالة التكرار) — كلاهما صفُّ حجز لا تقنين:**
  - `decisions/DEFERRED_IMPLEMENTATION.md:24` — صفٌّ في **سجل المؤجلات الرسمي**: «**Real-time AI & Live Views readiness** | جاهزية توثيقية (**Event Contracts**) بلا اختيار تقنية | **R10 → P6/P8** | لا اختيار تقنية قبل جاهزية R10».
  - `methodology/RECONCILIATION_ROADMAP.md:18` — صف **R10**: «Real-time & Extension-Store Readiness | **هياكل Event Contracts** وحوكمة الحزم — توثيقاً بلا اختيار تقنية | وثيقتا جاهزية | P6/P7».
- **صفٌّ ثالث مؤكِّد لا يُرجعه هذا الأمر:** `methodology/PHASE_EXECUTION_STANDARD.md:23` (المرتبة 2 · `Accepted`) يدرج «**Event-Contract skeleton (توثيق)**» ضمن قائمة إغلاق قرارات **P6**.
- **ما يثبته هذا:** موضوع §14 كله (وبقية مصادر Ingestion غير المقنَّنة) **محجوز بصفوف فعلية قائمة** — لا بعبارة «لاحقاً» ولا بتوصية. وقاعدة السجل نصاً: «أي وكيل يصادف حاجة لبند مؤجل: **يتوقف ويحيل إلى صف البند**» (`decisions/DEFERRED_IMPLEMENTATION.md:4`)، وسندها الدستوري م22 (`constitution.md:30`). وR10 **غير مُفعَّلة** (`methodology/RECONCILIATION_ROADMAP.md:5`).
- **مطابقة القيد المجمَّد:** نصّ المدخل «المطلوب **Invariants**، لا اختيار أداة» ينطبق حرفياً على تفويض الصفّين «**توثيقاً بلا اختيار تقنية**» — فالتطابق تامٌّ في الموضوع والقيد معاً.

<a id="absence-b2-rar006-001"></a>
### ABSENCE-B2-RAR006-001
- **البند [02]:** نمط **Acknowledge** — إقرار بشري بلا قبول ولا رفض.
- **السؤال:** هل تبنّى المستودع «الإقرار» نمطاً مستقلاً من أنماط التدخّل البشري في مسار فعل مقترَح؟
- **المصطلحات (EN):** `acknowledge` · `acknowledgement` · `typed-ack` · `read receipt`. **(AR):** `إقرار مكتوب` · `إقرار المستخدم` · `تمييز كمقروء` · `إشعار مقروء`.
- **الأمر (EN):** `grep -rInE 'acknowledge|acknowledgement|typed.ack|read receipt' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 7**
- **الأمر (AR):** `grep -rInE 'إقرار مكتوب|إقرار المستخدم|تمييز كمقروء|إشعار مقروء' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** المطابقات السبع كلها **مثيلٌ واحد** بعينه — `typed-ack` عند اعتماد **تقرير جزئي** — لا نمطُ تدخّلٍ في فعل مقترَح؛ وهو فوق ذلك **قرارٌ مفتوح** لا سياسة نافذة: `ui/UI_RUN_EXECUTION_MODEL.md:113` يسجّله «OD-RUN-2 | اعتماد تقرير جزئي | typed-ack إلزامي من المراجع (**توصية**)». ولا يوجد في المستودع نمطٌ يقف عند الإقرار دون قبول أو رفض؛ خط الأفعال ينتقل من `PROPOSED` إلى `CONFIRMED` مباشرةً (`ui/UI_AI_WORKSPACE_MODEL.md:27` · `:31`) بلا حالة إقرار وسيطة ⇒ **لم يُتبنَّ**.
- **مطابقات قريبة فُحصت واستُبعدت:**
  - `ui/UI_SCREEN_BEHAVIOR_CARDS.md:322` (`Current` · ملزم-معياري) — **قريبة:** «اعتماد تقرير **جزئي** مع placeholder ⇒ **typed-ack**». **مستبعَدة:** إقرارٌ مكتوب مرافق لقرار **اعتماد** (قبول)، لا نمطاً بديلاً عن القبول/الرفض؛ ونطاقه مراجعة تقرير لا فعل مقترَح من نموذج. **لا تثبت البند.**
  - `ui/UI_V06_DELTA_AMENDMENTS.md:50` و`:53` — **قريبة:** النص نفسه. **مستبعَدة:** حالتها `Proposed (v0.6)` (‏`INDEX.md → row for `ui/UI_V06_DELTA_AMENDMENTS.md` → status `Proposed (v0.6)``) ⇒ لا تثبت نفاذاً، وهي مكرِّرة للسابقة. **لا تثبت البند.**
  - `phases/designs/phase-3-workflows-approvals-actions.md:31` — **قريبة:** FR-3.6 «مركز قراءة/تمييز» للإشعارات. **مستبعَدة:** تمييز إشعارٍ مقروءاً حالةُ عرضٍ لصندوق الوارد، لا إقرارٌ بفعلٍ اقترحه نموذج. **لا تثبت البند.**

<a id="absence-b2-rar006-002"></a>
### ABSENCE-B2-RAR006-002
- **البند [07]:** **Auto-action within safe threshold** — تنفيذ تلقائي تحت عتبة آمنة.
- **السؤال:** هل يسمح المستودع بتنفيذ فعلٍ مُغيِّر للحالة بلا موافقة بشرية عند انخفاض المخاطرة؟
- **المصطلحات (EN):** `auto-action` · `autonomous action` · `safe threshold` · `act without approval`. **(AR):** `فعل تلقائي` · `تنفيذ تلقائي` · `عتبة آمنة` · `بلا موافقة`.
- **الأمر (EN):** `grep -rInE 'auto.action|autonomous action|safe threshold|act without approval' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'فعل تلقائي|تنفيذ تلقائي|عتبة آمنة|بلا موافقة' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 3**
- **لماذا لا يثبت الموجود البند — ولماذا هو بلا أساس:** المطابقات العربية الثلاث ليست سنداً بل **رفضاً بنيوياً ثلاثي الطبقات**، وكلها قُرئت بأسطرها كاملة:
  - `phases/designs/phase-7-integrations-app-store.md:16` — **Out of Scope**: «… · **أتمتة بلا موافقة للأفعال المُغيِّرة** · متجر عام/طرف ثالث.»
  - `:62` — **Security & Permissions**: «… · **act بلا موافقة = مرفوض بنيوياً** · تعقيم مخرجات الأدوات قبل دخولها سياق النموذج (حقن) · …»
  - `:77` — **Testing**: «**act بلا موافقة يفشل بنيوياً (اختبار سلبي إلزامي)** · تنفيذ باسم مستخدم غير مفوَّض يفشل · …»
  ويعضدها الثابت الأول لسطح العمل الذكي: «الـ Workspace **لا ينفّذ شيئاً بنفسه**» (`ui/UI_AI_WORKSPACE_MODEL.md:5`)، وFR-7.6 الذي يمرّر أفعال `act` **إلزامياً** ببوابة human-in-the-loop (`phases/designs/phase-7-integrations-app-store.md:32`). فالنمط **لم يُتبنَّ — بل رُفض نصاً واختباراً** ⇒ **مصطنع أو بلا أساس** بقاعدة `SUG`.
- **مطابقة قريبة فُحصت واستُبعدت:**
  - `ui/UI_ACTION_BUTTON_MODEL.md:44`–`:48` (جدول `risk_level`) — **قريبة:** المستوى `low` عموده «الاعتماد الافتراضي = **لا**». **مستبعَدة:** الغياب هنا غيابُ **اعتمادٍ ثانٍ**، لا غيابُ إنسان — الفعل يبقى **بادئه مستخدمٌ** يمرّ بـ`visible_when → permission → validation → confirmation` (`:40`). تدرّجُ المخاطرة ينظّم **الموافقة**، لا يمنح **استقلالية**. **لا تثبت البند.**

<a id="absence-b2-rar006-003"></a>
### ABSENCE-B2-RAR006-003
- **البند [11]:** **File drops** مصدرَ استيعاب.
- **السؤال:** هل يقنِّن المستودع مدخلاً غير مراقَبٍ للملفات (مجلد إسقاط) بعقد/محوِّل محكوم بهوية وصحة وصلاحية وتدقيق؟
- **المصطلحات (EN):** `file drop` · `watched folder` · `drop folder` · `manual event`. **(AR):** `مجلد مراقَب` · `إسقاط ملفات` · `حدث يدوي` · `إدخال يدوي للحدث`.
- **الأمر (EN):** `grep -rInE 'file drop|watched folder|drop folder|manual event' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'مجلد مراقَب|إسقاط ملفات|حدث يدوي|إدخال يدوي للحدث' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** دخول الملفات مقنَّن **فعلاً بشرياً محكوماً** لا مدخلاً غير مراقَب: `ui/UI_ACTION_BUTTON_MODEL.md:63` يعرّف `file.ingest` بصلاحية `files.upload (+نطاق)` وتصنيفٍ موروث وتدقيق `FILE_UPLOADED` وتوجيهٍ إلى خط P5 (قُرئ السطر كاملاً). وخط المعالجة نفسه مقنَّن بشاشة ومهام وأحداث (`ui/UI_SCREEN_INVENTORY.md:103`: «admin.ingestion | خط المعالجة | `ingestion_jobs+steps` | `job.retry/cancel` | `admin.knowledge.manage` | `INGESTION_RETRIED/FAILED`»). لكن **مجلد الإسقاط مدخلٌ بلا هوية عند لحظة الدخول**، وهو ما يستلزمه البند صراحةً («يخضع **للهوية** والHealth والصلاحيات والتدقيق») — ولا نص يقنّنه ⇒ يسكن صف `decisions/DEFERRED_IMPLEMENTATION.md:24`.

<a id="absence-b2-rar006-004"></a>
### ABSENCE-B2-RAR006-004
- **البند [12]:** **Manual event** مصدرَ استيعاب.
- **السؤال:** هل يقنِّن المستودع حقن حدثٍ يدوياً في طبقة الأحداث عبر عقد محكوم؟
- **المصطلحات (EN):** `manual event` · `inject event` · `publish event manually`. **(AR):** `حدث يدوي` · `حقن حدث` · `نشر حدث يدوياً`.
- **الأمر (EN):** `grep -rInE 'manual event|inject event|publish event manually' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'حدث يدوي|حقن حدث|نشر حدث يدوياً' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** الأفعال البشرية مقنَّنة **عقوداً محكومة** (`ui/UI_ACTION_BUTTON_MODEL.md:40` — سلسلة التقييم الخادمية كاملة) وتولّد أحداث تدقيق، لكن **لا مسار يحوّل فعلاً بشرياً إلى حدثٍ داخل طبقة أحداث الزمن الحقيقي**؛ وكتالوج الأفعال المغلق (`ui/UI_FIELD_NAMING.md:43`) لا يضم فعل نشر حدث. يسكن الصف القائم ⇒ **مؤجَّل بصف قائم**.

<a id="absence-b2-rar006-005"></a>
### ABSENCE-B2-RAR006-005
- **البند [14]:** **Message bus** مصدرَ استيعاب لاحقاً.
- **السؤال:** هل للمستودع موقفٌ حاكم من ناقل الرسائل، وهل يقنّن دخوله عبر عقد محكوم؟
- **المصطلحات (EN):** `message bus` · `event bus` · `Kafka` · `RabbitMQ`. **(AR):** `ناقل أحداث` · `ناقل رسائل` · `بث أحداث`.
- **الأمر (EN):** `grep -rInE 'domain event|in.process bus|message bus|event bus|Kafka|RabbitMQ' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 3**
- **الأمر (AR):** `grep -rInE 'أحداث داخلية|ناقل أحداث|ناقل in-process|بث أحداث' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 2**
- **لماذا لا يثبت الموجود البند تقنيناً — بل يثبت حجزاً:** المطابقات تُظهر **موقفاً حاكماً قائماً** لا تقنيناً لمصدر استيعاب:
  - `decisions/adr/ADR-0018-cache-queue-lock-valkey-scale-profile.md:15` (قُرئ السطر كاملاً) — «**البدائل:** Redis (مرفوض نهائياً …) · **RabbitMQ/Kafka (مرفوضان الآن — أثقل من الحاجة؛ أي حاجة مستقبلية لبث أحداث مؤسسي = ADR جديد)** · Valkey من اليوم الأول (مرفوض …)».
  - `methodology/Spec_Driven_Modular_Monolith_Methodology.md:52` — «… · **Events** عبر ناقل in-process **قابل للترقية إلى message bus** · …».
  فالبند محجوزٌ ببوابة صريحة (**ADR جديد**) ومسارِ ترقيةٍ معلن ⇒ **مؤجَّل بصف قائم** لا دلتا ولا مصطنع.

<a id="absence-b2-rar006-006"></a>
### ABSENCE-B2-RAR006-006
- **البند [15]:** **Database CDC** مصدرَ استيعاب لاحقاً.
- **السؤال:** هل يقنِّن المستودع التقاط التغيير من قواعد البيانات مصدرَ أحداث؟
- **المصطلحات (EN):** `CDC` · `change data capture` · `binlog` · `logical replication`. **(AR):** `التقاط التغيير` · `تغذية التغييرات` · `نسخ منطقي`.
- **الأمر (EN):** `grep -rInE 'CDC|change data capture|binlog|logical replication' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'التقاط التغيير|تغذية التغييرات|نسخ منطقي' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** المستودع يقنّن موصّل قاعدة بيانات **قراءةً فقط** ضمن allow-list وبمستخدم أدنى صلاحية (`phases/designs/phase-7-integrations-app-store.md:36` — FR-7.10)، وهو **استعلامٌ عند الطلب** لا تدفّقُ تغييرات. فالنطاق (قواعد بيانات داخلية) **قائم**، والآلية (CDC) **غير مقنَّنة** ⇒ حاجةٌ صالحة داخل نطاق قائم، تسكن صف `decisions/DEFERRED_IMPLEMENTATION.md:24` ⇒ **مؤجَّل بصف قائم**. *(يُقابَل بـ`absence-b2-rar006-007` حيث النطاق نفسه معدوم.)*

<a id="absence-b2-rar006-007"></a>
### ABSENCE-B2-RAR006-007
- **البند [16]:** **Device/IoT gateway** مصدرَ استيعاب لاحقاً.
- **السؤال:** هل يوجد في المستودع نطاقُ أجهزة أو مستشعرات أصلاً ليكون له مصدر استيعاب؟
- **المصطلحات (EN):** `IoT` · `device gateway` · `sensor` · `telemetry ingest`. **(AR):** `إنترنت الأشياء` · `بوابة أجهزة` · `مستشعرات` · `أجهزة طرفية`.
- **الأمر (EN):** `grep -rInE 'IoT|device gateway|sensor|telemetry ingest' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'إنترنت الأشياء|بوابة أجهزة|مستشعرات|أجهزة طرفية' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند — ولماذا هو بلا أساس لا مؤجَّل:** **صفرٌ في اللغتين معاً** ولا نطاق أجهزة في أي مستوى من السلّم: الرؤية تحصر المستفيدين في موظفين ومديرين وأدمن وعمليات وفرق أمن (`vision.md:17`)، والكتالوج المعماري لا يعرّف أي مكوّن جهازي، ونطاق P7 المعتمد أنواعُ موصلات أربعة فقط (MCP/REST/Webhook/DB-ro — `phases/designs/phase-7-integrations-app-store.md:71`). فالبند **لا يطلب آليةً داخل نطاق قائم** (بخلاف `[15]`)، بل يفترض **نطاقاً معدوماً بالكامل**؛ وصفُّ R10 يحجز **عقود الأحداث** لا يُنشئ نطاق أجهزة ⇒ **مصطنع أو بلا أساس**. *(حكمٌ على البند مقيساً بالمستودع عند HEAD — لا نفيٌ لصلاحيته في منتج آخر.)*

<a id="absence-b2-rar006-008"></a>
### ABSENCE-B2-RAR006-008
- **البنود [17] و[18] و[19]:** نطاق الترتيب (per source أم per subject) · معالجة الحدث خارج الترتيب · وجود رقم تسلسلي.
- **السؤال:** هل يقنِّن المستودع أي ضمان ترتيبٍ للأحداث؟
- **المصطلحات (EN):** `per-source ordering` · `per-subject` · `out-of-order` · `sequence number`. **(AR):** `ترتيب لكل مصدر` · `ترتيب لكل موضوع` · `رقم تسلسلي` · `أحداث متأخرة`.
- **الأمر (EN):** `grep -rInE 'per.source ordering|per.subject|out.of.order|sequence number' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'ترتيب لكل مصدر|ترتيب لكل موضوع|رقم تسلسلي|أحداث متأخرة' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البنود:** أقرب ما في المستودع ترتيبٌ **داخل تشغيل واحد** (خط زمني للخطوات · `retry_step` · فروع متوازية بعقدة دمج) لا ترتيبَ **أحداث بين مصادر**؛ ولا حقل تسلسل في أي عقد. وثلاثتها **أسئلة Invariants** لا أسئلة أداة، فينطبق عليها تفويض R10 حرفياً ⇒ **مؤجَّل بصف قائم** (`absence-b2-rar006-000`).

<a id="absence-b2-rar006-009"></a>
### ABSENCE-B2-RAR006-009
- **البند [20]:** كيف تعرض Live View بيانات متأخرة؟
- **السؤال:** هل يقنِّن المستودع سلوك العرض الحي عند وصول بيانات متأخرة؟
- **المصطلحات (EN):** `late data` · `late arriving` · `stale data indicator` · `catch-up`. **(AR):** `بيانات متأخرة` · `وصول متأخر` · `تحديث متأخر`.
- **الأمر (EN):** `grep -rInE 'late data|late arriving|stale data indicator|catch.up' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'بيانات متأخرة|وصول متأخر|تحديث متأخر' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** **آلية** البث الحي مقنَّنة: `decisions/adr/ADR-0025-api-contract-rest-openapi-sse.md:9` («**SSE للبث**: ردود النماذج المتدفقة، تقدم المهام الطويلة، تحديثات الحالة»)، ونمط `Monitor` يوجب «بث/تحديث حي **معلن الآلية (SSE)** مع مؤشر اتصال (S16)» (`ui/UI_SCREEN_GOVERNANCE_STANDARD.md:49` — قُرئ السطر كاملاً). لكن **لا قاعدة لسلوك التأخّر**: لا وسم «متأخر»، ولا سياسة إدراج، ولا حالة عرض للبيانات الواصلة بعد أوانها. والصف القائم يحمل اسم «**Live Views** readiness» صراحةً ⇒ **مؤجَّل بصف قائم**.

<a id="absence-b2-rar006-010"></a>
### ABSENCE-B2-RAR006-010
- **البنود [21] و[22] و[23]:** لماذا يعاد التشغيل · من يملك الصلاحية · ما النطاق الزمني.
- **السؤال:** هل يوجد مفهوم إعادة تشغيل الأحداث أصلاً، وله غرضٌ وصلاحيةٌ ونطاق؟
- **المصطلحات (EN):** `replay` · `reprocess` · `event store` · `event log` · `replay permission` · `replay window`. **(AR):** `إعادة تشغيل الأحداث` · `إعادة المعالجة` · `سجل الأحداث` · `صلاحية إعادة التشغيل` · `نطاق إعادة التشغيل`.
- **الأمر (EN):** `grep -rInE 'replay|re.process|reprocess|event store|event log' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0** · `grep -rInE 'replay permission|replay window|replay scope|replay authority' …` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'إعادة تشغيل الأحداث|إعادة المعالجة|سجل الأحداث|مخزن الأحداث' …` — **النتيجة: 0** · `grep -rInE 'صلاحية إعادة التشغيل|نطاق إعادة التشغيل|من يعيد التشغيل' …` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البنود:** **صفرٌ مطلق في اللغتين** — لا مخزن أحداث ولا سجل أحداث ولا مفهوم replay. وإعادة التنفيذ المقنَّنة (`run.retry` · `run.retry_step` · `job.retry`) نطاقها **تشغيلٌ أو مهمة بعينها**، لا إعادة بثّ أحداث تاريخية. ولا صلاحية باسم replay في أي كتالوج. والصفّ القائم يحجز عقود الأحداث التي تقرّر وجود replay من عدمه ⇒ **مؤجَّل بصف قائم** لا `مصطنع`: المفهوم **غير محسوم داخل مؤجَّل**، لا مبنيٌّ على افتراض يناقضه المستودع.
- **مطابقة قريبة فُحصت واستُبعدت:**
  - `ui/UI_SCREEN_INVENTORY.md:103` — **قريبة:** «admin.ingestion | خط المعالجة | `ingestion_jobs+steps` | **`job.retry/cancel`** | `admin.knowledge.manage` | `INGESTION_RETRIED/FAILED`» (قُرئ السطر كاملاً)؛ وسمّاها الـsitemap «مراقبة/**إعادة معالجة**». **مستبعَدة:** إعادةُ **مهمة فهرسة فاشلة** بصلاحية إدارة المعرفة — عمليةٌ على وحدة عمل قائمة، لا إعادةُ بثِّ أحداث بنطاق زمني. **لا تثبت البنود** — لكنها أقرب سابقة صلاحية قد تبني عليها وثيقة R10.

<a id="absence-b2-rar006-011"></a>
### ABSENCE-B2-RAR006-011
- **البند [24]:** كيف تمنع Side Effects المكررة عند Replay؟
- **السؤال:** هل يقنِّن المستودع منع تكرار الأثر عند إعادة بثّ حدث؟
- **المصطلحات (EN):** `idempotent` · `idempotency key` · `deduplication` · `exactly.once`. **(AR):** `منع الازدواج` · `عدم التكرار` · `مفتاح تفرد`.
- **الأمر (EN):** `grep -rInE 'idempotent|idempotency|deduplicat|exactly.once' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 6**
- **الأمر (AR):** `grep -rInE 'منع الازدواج|عدم التكرار|مفتاح تفرد' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 7** *(أغلبها «قاعدة عدم التكرار» في حوكمة الواجهة — منعُ تكرار المحتوى بين الأسطح، لا منعُ تكرار أثرٍ تنفيذي؛ مستبعَدة موضوعياً.)*
- **لماذا لا يثبت الموجود البند — والخريطة المجمَّدة تحسم ذلك:** `SEMANTIC-MERGE-MAP.md:26` (مجمَّد عند `188ad37`) يفصل الحالتين نصاً: «Idempotency/Dedup | RAR001-S12-010 (:250) مقابل **RAR006-S14-008 (:322)** | **scope مختلف**: إعادة محاولة تنفيذ مهمة ≠ **منع تكرار أثر عند Replay حدث**». فالمطابقات القائمة — `ui/UI_ACTION_BUTTON_MODEL.md:21` («تنفيذ **idempotent** بمفتاح للبطاقات») و`ui/UI_RUN_EXECUTION_MODEL.md:87` («نتائج upstream تُعاد بالاستخدام (idempotent)») و`phases/designs/phase-3-workflows-approvals-actions.md:75` («at-least-once للتذكيرات مع **منع الازدواج المنطقي**») — كلها في النطاق الأول الذي **استبعدته الخريطة المجمَّدة صراحةً** عن هذا البند ⇒ لا يجوز البناء عليها ⇒ **مؤجَّل بصف قائم**.

<a id="absence-b2-rar006-012"></a>
### ABSENCE-B2-RAR006-012
- **البند [25]:** هل يُعاد AI Inference عند Replay؟
- **السؤال:** هل يقنِّن المستودع حتمية استدعاء النموذج أو إعادته عند إعادة المعالجة؟
- **المصطلحات (EN):** `rerun inference` · `re-run inference` · `deterministic inference` · `inference replay`. **(AR):** `إعادة الاستدلال` · `إعادة تشغيل النموذج` · `حتمية النموذج`.
- **الأمر (EN):** `grep -rInE 'rerun inference|re.run inference|deterministic inference|inference replay' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'إعادة الاستدلال|إعادة تشغيل النموذج|حتمية النموذج' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** القيود المحيطة قائمة ولا تحسم: م11 يحصر كل استدعاء نموذج في الـGateway (`constitution.md:19`)، وم16 يمنع final-response cache لبيانات الأعمال ويوجب **إعادة تحقق live** قبل أي تعديل/اعتماد/تصدير (`constitution.md:24`) — قيدٌ يفرض الطزاجة عند القرار، **لا** قاعدةً لإعادة الاستدلال عند إعادة بثّ حدث تاريخي. يسكن الصف القائم ⇒ **مؤجَّل بصف قائم**.

<a id="absence-b2-rar006-013"></a>
### ABSENCE-B2-RAR006-013
- **البند [26]:** كيف تميّز Replay عن Live Processing؟
- **السؤال:** هل يوجد وسمٌ أو حالةٌ تميّز المعالجة المعادة عن الحيّة؟
- **المصطلحات (EN):** `replay mode` · `live processing` · `shadow mode` · `dry.run` (كوسم تمييز). **(AR):** `وضع الإعادة` · `معالجة حية` · `تمييز الإعادة`.
- **الأمر (EN):** `grep -rInE 'replay mode|live processing|shadow mode|backfill' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'وضع الإعادة|معالجة حية|تمييز الإعادة' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند:** المستودع يعرف **تمييزاً لسياقات لا-أثر** في مواضع أخرى — لوحات المحاكاة «صلاحية خاصة ومدقَّقة» (`ui/UI_ADMIN_CONSOLE_MODEL.md:22`) ووسم «تجريبي/أدمن» لسطح Canvas مع «لا يكتب على بيانات إنتاج» (`ui/UI_SCREEN_GOVERNANCE_STANDARD.md:47`) — لكن **لا وسم للإعادة**، ولا مفهوم replay أصلاً (`absence-b2-rar006-010`). يسكن الصف القائم ⇒ **مؤجَّل بصف قائم**.
- **مطابقة قريبة فُحصت واستُبعدت:**
  - `ui/UI_INTERACTION_MODEL.md:57` — **قريبة:** وسم «**محاكاة — لا أثر**» على درج Policy Studio. **مستبعَدة:** حالتها `Proposed (v0.5)` (`INDEX.md → row for `ui/UI_INTERACTION_MODEL.md` → status `Proposed (v0.5)``) فلا تثبت نفاذاً وحدها؛ ونطاقها **محاكاة سياسة** لا إعادة معالجة أحداث. **لا تثبت البند.**

<a id="absence-b2-rar006-014"></a>
### ABSENCE-B2-RAR006-014
- **البند [27]:** كيف يُسجَّل تدقيق Replay؟
- **السؤال:** هل يقنِّن المستودع تدقيقاً خاصاً بإعادة التشغيل؟
- **المصطلحات (EN):** `replay audit` · `audit of replay` · `REPLAY_` (بادئة حدث). **(AR):** `تدقيق الإعادة` · `تدقيق إعادة التشغيل` · `حدث إعادة`.
- **الأمر (EN):** `grep -rInE 'replay audit|audit of replay|REPLAY_' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'تدقيق الإعادة|تدقيق إعادة التشغيل|حدث إعادة' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا لا يثبت الموجود البند — والخريطة المجمَّدة تحسم ذلك:** `SEMANTIC-MERGE-MAP.md:24` (مجمَّد عند `188ad37`) ينص حرفياً: «Audit | RAR001-S12-015 (:255) · RAR002-S14-013 (:419) · **RAR006-S14-011 (:325)** | **التزامات متمايزة**: كتالوج أحداث مهام ≠ خاصية عدم إنكار لوكيل ≠ **تدقيق Replay**؛ **مرساة م3 لا تكفي**». فالاستناد إلى م3 (`constitution.md:11`) **ممنوع صراحةً** لهذا البند بموجب المدخل المجمَّد، ولا حدث `REPLAY_*` في **§11 Audit Events Catalog** (`catalogs/Feature_Technical_Architecture_Catalog.md:322–357` — فُحص كاملاً) ⇒ **مؤجَّل بصف قائم**.

<a id="absence-b2-rar006-015"></a>
### ABSENCE-B2-RAR006-015
- **البند [09] — تحديد نطاق:** **Connector polling** بوصفه *أسلوب سحبٍ دوري*.
- **السؤال:** هل يقنِّن المستودع السحب الدوري (polling) أسلوبَ استدعاءٍ للموصلات؟
- **المصطلحات (EN):** `polling` · `poll` · `scheduled fetch` · `pull-based`. **(AR):** `استطلاع دوري` · `سحب دوري` · `جلب مجدول`.
- **الأمر (EN):** `grep -rInE 'polling|poll |scheduled fetch|pull.based' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **الأمر (AR):** `grep -rInE 'استطلاع دوري|سحب دوري|جلب مجدول' . --include='*.md' | grep -viE '<المستثنى>'` — **النتيجة: 0**
- **لماذا سُجِّل هذا رغم تصنيف `[09]` مقنَّناً:** القيد المجمَّد للكتلة نصُّه «**هذه ليست تقنيات مختارة**؛ كل مصدر يجب أن يدخل عبر Adapter/Contract ويخضع للهوية والHealth والصلاحيات والتدقيق» — فالمطلب هو **الإطار المحكوم** لا الأسلوب. والإطار مقنَّن كاملاً للموصلات (Registry + طبقة Connector-Adapter لكل نوع + Connector Contract بفصل `admin_status/op_health` + تقاطع الصلاحيتين + تدقيق كامل). لذلك صُنِّف `[09]` **مقنَّناً بالإطار**، وسُجِّل هنا صراحةً أن **أسلوب السحب الدوري نفسه غير مقنَّن** — كي لا يُقرأ التصنيف أوسع مما يحتمل. هذا **تحديد نطاق لا ادعاء غياب** يغيّر التصنيف.

---

**Related:** `RECONCILIATION-RAR-006.md` · `CANONICAL-ITEM-UNIVERSE.md` (`188ad37`) · `NORMALIZATION-AND-ATOMIC-MAP.md` (`188ad37`) · `SEMANTIC-MERGE-MAP.md` (`188ad37`) · `decisions/DEFERRED_IMPLEMENTATION.md` · `methodology/RECONCILIATION_ROADMAP.md` · `methodology/PHASE_EXECUTION_STANDARD.md`
