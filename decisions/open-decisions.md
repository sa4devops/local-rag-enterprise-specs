# تصنيف القرارات المفتوحة — Open Decisions Classification

> **Document Title:** Open Decisions Classification · **Version:** 3.8 — (Δ 2026-08-05 R3 — **مواءمة عقود المرشَّح المتبقّية — `BATCH_A`**: +«سجل مواءمة عقود المرشَّح المتبقّية — `OD-R3-CONTRACTS-2`» بمواءمة `contracts/screens/admin.record_types.md` و`contracts/screens/admin.workflows.md` مع القالب الحاكم **v1.1**، و**تسجيل تنازع إسناد `wf.approve`/`wf.activate`/`wf.rollback` تبعيةً لاحقة لا حسماً**، و**جرد التبعيات اللاحقة المسمّاة**. **الحالتان تبقيان `Candidate`** · **لا ترقية إلى `Approved`** · **لا عقد جديد** · **`admin.workflow_detail` لم يُنشأ ويبقى منفصلاً محجوباً عن التأليف حتى بطاقة سلوك/أمر مالك** · **لا معيار إتاحة `PASS`** · **`ui/**` لم تُمسّ** ومنها الجرد وبطاقات السلوك و§9 · **`SCREEN_CONTRACT_TEMPLATE.md` و`NAMING_AND_CONTRACTS_STANDARD.md` و`contracts/enums/**` و`contracts/identity/**` و`run.list.md` و`admin.actions.md` لم تُمسّ** · **لا `contracts/fields/`** · **`OD-GOV-2` مفتوح** · **`OD-P3-1` مفتوح** · **`OD-P3-2` مفتوح و`CC-WF-1` غير محسوم** · **`FP-0001` `BLOCKED`** · **التجميد `ACTIVE` وشروط الرفع بلا تعديل ولا إعادة عدّ** · **`R4` `NOT_ACTIVATED`** · **`R3` لم تُغلق** · **لا وسم ولا إصدار**) (3.7: Δ 2026-08-04 R3 — **توسيع عقود الشاشات ذات الأولوية**: +«سجل توسيع عقود الشاشات ذات الأولوية — `OD-R3-CONTRACTS-1`» بمواءمة `contracts/screens/run.list.md` و`contracts/screens/admin.actions.md` مع القالب الحاكم **v1.1**، و**تسجيل تعارض مسار اعتماد العقود تبعيةً لاحقة لا مانعاً**، و**جرد التبعيات اللاحقة**، و**السجل الحاكم السابق للدمج — PR #17** وفق **ADR-0033 بند 12**. **الحالتان تبقيان `Candidate`** · **لا ترقية إلى `Approved`** · **لا معيار إتاحة `PASS`** · **`ui/**` ومنها §9 لم تُمسّ** · **`SCREEN_CONTRACT_TEMPLATE.md` و`NAMING_AND_CONTRACTS_STANDARD.md` لم يُمسّا** · **لا `contracts/fields/`** · **لا عقد شاشة آخر** · **لا `UI-4` ولا `UI-5`** · **`FP-0001` `BLOCKED`** · **التجميد `ACTIVE`** · **`R4` غير مفعَّلة** · **`R3` لم تُغلق** · **لا وسم ولا إصدار**) (3.6: Δ 2026-08-04 GOV — **تبسيط إتمام أثر الدمج**: +«(ل) تبسيط إتمام أثر الدمج + السجل الحاكم السابق للدمج — PR #16» داخل **`OD-GOV-MERGE-1`** — **بند 12 مُعاد صياغته**: السجل الحاكم **يكتمل داخل الدفعة قبل الدمج**، **ولا تحديثَ مستودعياً بعد الدمج ولا PR لاحق**، **ولا تُكرَّر الوقائع التي يملكها Git** (الرأس المدموج = **الأب الثاني للـMerge Commit**) · **بند 11** +**استثناء دورة التصحيح** بشرط مراجعةٍ مستقلة كاملة و**عدم دمج رأسٍ لم يُعتمد** · +**حدود المراجع المستقل** · +**الاكتفاء الذاتي**: `.git` جزءٌ من وحدة التاريخ الحاكم المحمولة ولا اعتماد حاكم على خدمة خارجية · +**نصّ رسالة Merge Commit مثبَّتاً حرفياً**. **ونموذجُ إغلاق الحلقة المطروح في مسودة PR #16 حُذف قبل الدمج ولم ينفذ قط** · **لا `PR #17`** · **لا سياسة تُضعِّف فصل الأدوار · شروط بند 10 السبعة بلا مساس · لا مساس بـ§9 ولا `ui/**` ولا `contracts/**` ولا `traceability/**` ولا `AUTHORITY.md` ولا `constitution.md` · `FP-0001` `BLOCKED` · التجميد `ACTIVE` · `R4` غير مفعَّلة · لا وسم ولا إصدار**) (3.5: Δ 2026-08-03 GOV — **استكمال أثر تنفيذ دمج PR #14** بموجب **ADR-0033 بند 12** و**§(د)-7**: +«(ك) سجل أثر تنفيذ الدمج — PR #14» داخل **`OD-GOV-MERGE-1`** بالأبعاد الأربعة و`Merge SHA` وخط الأساس الجديد وهوية المراجعة المستقلة ونطاقها، **وبالنصّ على أن الحساب التقني `sa4devops` هوية بيانات اعتماد لا دليلَ تنفيذٍ يدويٍّ من المالك**. **توثيقُ ما بعد الدمج فقط — لا سياسة جديدة · لا `ADR` جديد · لا `OD` جديد · لا تعديل لـ`OD-GOV-MERGE-1` القائم ولا لشروطه السبعة · لا مساس بـ§9 ولا بأي ملف `ui/**` · لا معيار `PASS` · لا معالجة ولا اختبارات · `FP-0001` `BLOCKED` · التجميد `ACTIVE` · شروط الرفع بلا تعديل ولا إعادة عدّ · `R3` لم تُغلق · `R4` غير مفعَّلة · لا وسم ولا إصدار**) (3.4: Δ 2026-08-03 R3 — **مصالحة ما قبل الدمج لـPR #14 بأمر مالك**: +«(هـ) حكم الاستثناء التنفيذي المحدود» **يغلق** التوتّر مع `R3-ACT-01` — الأخير **صحيحٌ عند صدوره**، والاستثناء **محصور بتقنين §9 وبملفات PR #14**، و**`R3-ACT-01` لا يُوسَم `Superseded`** وبقية قيوده نافذة · +«(و) واقعة تسرّب `replace_all` إلى §4» **دليلاً على فجوة الحماية القائمة** لا شرطاً جديداً · +تصحيح سلطوي: **حالة `ui/UI_DESIGN_SYSTEM.md` تبقى `Proposed`**، وعند الدمج يصير §9 **المرجعَ المنشور** ضمن تلك الحالة، **والترقية قرار مالك مستقل** · وحُذفت مسألةُ وسم حالة `R3` من الفجوات بعد إغلاقها. **لا `OD` جديد · لا `ADR` · لا تعديل لـ`R3-ACT-01` · لا معيار `PASS` · لا معالجة · لا اختبارات · `FP-0001` `BLOCKED` · التجميد `ACTIVE` · `R4` غير مفعَّلة**) (3.3-GOV: ‏**Δ 2026-08-03 · جولة تصحيح مراجعة مستقلة قبل الدمج — `C1`/`C2`/`C3`**: توحيد شروط الدمج على **سبعةٍ مجتمعة** في كل الأوعية · تصويب وصف إضافة `methodology/**` بوصفها **توسيعاً مقصوداً** لا تفسيراً · **تقييد الاستثناء الانتقالي بـPR #15 والمستودع والرأس صراحةً**. **لا تغيير جوهري في أي قاعدة · التجميد `ACTIVE` و`FP-0001` `BLOCKED` و`R4` غير مفعَّلة بلا مساس** — التفصيل في §(ي)) (Δ 2026-08-03 · أمر مالك «‏Agent-Authorized Merge Governance Amendment»: +«**سجل تعديل حوكمة الدمج**» (‏**`OD-GOV-MERGE-1`** — `AMENDED — EFFECTIVE UPON MERGE`) بمقابلة القديم/الجديد وجدول الأدوار الأربعة وشروط `Owner-Authorized Agent Merge` السبعة و**الاستثناء الانتقالي الضيق لمرة واحدة** وأثر التعديل على `L1`/`L2` وقيود عدم التوسّع. **الوعاء المعياري ADR-0033 بنود 8–14 و`agent-execution-model` §18** · **فصل الأدوار مُشدَّد لا مُخفَّف** · **لا auto-merge غير مشروط للمسارات الحساسة** · **PR #14 لم يُمسّ** · **لا وسم ولا إصدار** · **`FP-0001` `BLOCKED` · التجميد `ACTIVE` · شروط الرفع بلا تعديل ولا إعادة عدّ · `R3` لم تُغلق · `R4` غير مفعَّلة · جرد `P3` `28 = 13 + 15 + 0` بلا تغيير · `ui/**` و`contracts/**` و`traceability/**` و`AUTHORITY.md` و`constitution.md` لم تُمسّ**) (3.3: Δ 2026-08-02 R3 — **سجل تقنين معايير الإتاحة السبعة**: +«سجل تقنين معايير الإتاحة السبعة — `ui/UI_DESIGN_SYSTEM.md` §9» (‏`OD-R3-CODIFY-1`) بحالته **`CODIFIED — EFFECTIVE UPON MERGE`** + تسجيل أثر التقنين على شروط رفع `OD-FP-0001-FREEZE` **بلا تعديل ولا إضافة ولا إعادة عدّ** + إحدى عشرة فجوةً/مسألةً مسجَّلة لا مَحسومة. **لا معيار `PASS` · لا معالجة · لا بناء اختبارات · لا رفع تجميد · `FP-0001` `BLOCKED` · التجميد `ACTIVE` · `R3` لم تُغلق ولم يُعَد تعريفها · `R4` غير مفعَّلة · جرد `P3` `28 = 13 + 15 + 0` بلا تغيير · `traceability/**` بقي غير حاكم ولم يُمسّ**) (3.2: Δ 2026-08-02 R3 — **سجل النقل الحاكم لقرارات `OD-R3-UI-1` و`OD-R3-UI-2` و`OD-R3-UI-3`**: +«سجل النقل الحاكم — قرارات واجهة `R3`» بمصدره التاريخي (‏`Issue #11` وتعليقاته الثلاثة ببصماتها) وأوعيته الثلاثة و`commit` SHAs. **الحالة: `OWNER APPROVED · DURABLY RECORDED · GOVERNING TRANSFER PREPARED · EFFECTIVE UPON MERGE` — ولا تُستعمل `TRANSFERRED` قبل الدمج** · `Issue #11` **لم يُمسّ** · لا تقنين ولا معالجة ولا اختبارات · `FP-0001` `BLOCKED` · التجميد `ACTIVE` · `R4` غير مفعَّلة) (3.1: Δ 2026-08-02 R3 — **سند استمرار Accessibility Sub-track بأمر مالك — استثناء تنفيذي محدود**: +«سجل استمرار Accessibility Sub-track» (‏`OD-R3-SCOPE-1`) + تطبيق مختصر لقاعدة **عدم توسّع شروط رفع `OD-FP-0001-FREEZE` تلقائياً بتوسّع Enterprise UI Governance**. **لا تعديل لـ`methodology/RECONCILIATION_ROADMAP.md` · لا ادّعاء بأن الإتاحة جزء أصلي من ميثاق `R3` · لا إعادة تعريف لـ`R3` · لا مسار `R` جديد · لا شرط جديد على التجميد · لا حسم لتعارض المرتبتين 2 و4 · لم تُعدَّل شروط الرفع القائمة ولم يُعَد عدُّها · `FP-0001` يبقى `BLOCKED` · التجميد يبقى `ACTIVE` · `R3` تبقى `ACTIVE — SPECIFICATION ONLY` · `R4` غير مفعَّلة · جرد `P3` `28 = 13 + 15 + 0` بلا تغيير**) (3.0: Δ 2026-08-01 P3 — **قرار إغلاق نطاق `P3` بعد Phase Exit Review**: +«سجل قرار إغلاق نطاق `P3`» (‏`OD-P3-CLOSURE`) + قاعدة دلالة الإغلاق (§3.9) + خطوات المالك المتبقية. **نتيجة §3.1–§3.9 = PASS · المعادلة `28 = 13 + 15 + 0` · `UNCLASSIFIED = 0` · `BLOCKING REMAINDER = 0`. و`OD-FP-0001-FREEZE` يبقى `ACTIVE` · `FP-0001` يبقى `BLOCKED` · `R3` تبقى `ACTIVE — SPECIFICATION ONLY` · لا merge ولا tag ولا release ولا تفعيل `R4`**) (2.9: Δ 2026-07-31 R3 — **تفعيل مسار `R3` بأمر مالك — تخصيص ومعايير قبول فقط**: +«سجل تفعيل مسار `R3`» (‏`R3-ACT-01`) + تسجيل المعرّفات الثابتة للمعايير السبعة `R3-A11Y-01`..`07` + شروط رفع التجميد وإغلاق `R3` + رفع تعارض ميثاق `R3` إلى المالك. **`FP-0001` يبقى `BLOCKED` · التجميد يبقى `ACTIVE` · لا معيار `PASS` · الدلتا السبعة لم تُعالَج ولم تُقنَّن · `ui/UI_DESIGN_SYSTEM.md` لم يُمسّ · `R3` لم تُغلق · `P3` لم تُغلق · جرد `P3` `28 = 12 + 16 + 0` بلا تغيير · لا `P3-CI-*` جديد**) (2.8: Δ 2026-07-31 P3 — **معالجة `OD-GOV-3`**: +«مجموعة قراءة مسار `R3`» (وعاء حاكم Tier-0 يوصل مصدر المصالحة والدلتا إلى منفِّذ `R3`) + **`OD-GOV-3` ⇒ مُعالَج/مغلق** بسجل إغلاق · +تسجيل **`GOV-OBS-03`** (غياب نص §18 المرجعي) قيداً مفتوحاً · +استكمال حقول §3.4 الناقصة لـ`P3-CI-02` و`P3-CI-26` · +تدقيق إحالات الخطوتين 1 و5. **لم تبدأ R3 · لم تُعالَج الدلتا السبعة · لم يُرفع التجميد · لم تُغلق P3**) (2.7: Δ 2026-07-30 P3 — **تنفيذ §18**: +سجل أثر الحجب `OD-FP-0001-FREEZE` (‏`ACTUAL NARROW FREEZE` · `ACTIVE`) + تسجيل الدلتا السبعة الحاجبة بوجهاتها · بلا تغيير لأي قرار قائم ولا لأي تصنيف تاريخي) (2.6: Δ 2026-07-30 P3 — Phase Exit Review: +فهرس ترحيل إغلاق P3 · +`OD-GOV-1` `OD-GOV-2` `OD-GOV-3` مفتوحة · بلا تغيير لأي قرار قائم) (2.5: Δ 2026-07-29 P3/R2: +فهرس قرارات المالك التسعة OD-RAR-01..09 — مقفلة) (2.4: Δ 2026-07-19: +فهرس دفعة G1 — Governance Foundation Baseline) (2.3: D1–D9 مدمجة؛ C1–C5 مُغلقة؛ +فهرس v0.8؛ +قسم إغلاق خط الأساس v1.0) · **Status:** Current / Accepted · **Date:** 2026-07-10
> **Supersedes:** v2.0 + قسم Open Decisions في الكتالوج (§13) والمنهجية (§19) — هذه الوثيقة هي **المرجع المُلزم الوحيد** للقرارات المفتوحة.
> **Authority Order:** المرتبة **4**. · **Related:** license-review.md · phase-roadmap.md · adr/README.md · coding-standards.md
> **Purpose:** تصنيف كل قرار إلى فئة توقيت واضحة حتى لا يوقف أي قرارٍ التصميمَ بلا داعٍ، ولا يدخل أي شيء التنفيذَ بلا حسم.

## الفئات الخمس (القاعدة المعتمدة)
| الفئة | المعنى | الأثر |
|---|---|---|
| **1 — Blockers قبل التصميم** | يمنع تصميم المراحل | **فارغة** — لا شيء يوقف التصميم |
| **2 — Blockers قبل التنفيذ** | يُحسم قبل كتابة أول كود للمرحلة المعنية | بوابة لكل مرحلة عند تنفيذها |
| **3 — Blockers قبل Docker Compose** | يُحسم قبل سحب/تفعيل الصورة المعنية | **بوابة متكررة** لكل صورة جديدة/مفعَّلة في أي مرحلة |
| **4 — Blockers قبل التوزيع النهائي** | يُحسم قبل أول release خارج الفريق | لا يوقف التطوير الداخلي |
| **5 — Configurable** | يبقى مفتاح إعداد/سياسة | لا يوقف شيئاً |

## المحسوم حديثاً — قرارات ما قبل التنفيذ D1–D9 (2026-07-04)
| # | القرار المحسوم | المرجع |
|---|---|---|
| D1 | **Frontend = React + Vite + TypeScript + Tailwind + shadcn/ui-style** (SPA مستقلة تستهلك OpenAPI/SDK؛ لا Next.js — لا SSR/SEO داخلياً؛ SvelteKit رُفض) | ADR-0017 |
| D2 | **Backend مؤكَّد** = Python + FastAPI + Pydantic + SQLAlchemy + Alembic + PostgreSQL + workers | ADR-0001/0003 |
| D3 | **C5 محسوم:** uv + pnpm + lockfiles + pinned + reproducible + offline caches + install/verify scripts | coding-standards §7 |
| D4 | **Cache/Queue/Lock:** ثلاث واجهات بافتراضي **memory/PostgreSQL**؛ Valkey **optional-ready** (ليس إلزامياً في P0/P1) | ADR-0018 |
| D5 | **ObjectStorageProvider** بافتراضي **local filesystem**؛ المزوّد (MinIO/SeaweedFS/S3) كان يُحسم بمرحلة الملفات — **حُسم افتراضي الإنتاج الموزَّع في v1.0-baseline: SeaweedFS؛ MinIO مشروط Legal** | ADR-0019 |
| D6 | **Observability Baseline:** Audit-PG + JSON logs خارج PG + Log Viewer + Health + Error Catalog + IDs + /metrics اختياري؛ لا Grafana/Loki/Graylog الآن | ADR-0020 |
| D7 | **GitHub الآن** + قناة نقل مغلقة موقّعة؛ Git داخلي (Gitea/GitLab) = **Backlog** | ADR-0022 |
| D8 | **Reference-aware clean implementation** — سياسة مُغلقة مُلزمة (ليست قراراً مفتوحاً) | ADR-0021 + coding-standards §9 |
| D9 | **حدود Admin UI** مؤكدة (capabilities من الواجهة؛ البنية الحساسة config/Ops؛ audit + approval حسب السياسة) | ADR-0008 |

---

## الجدول التفصيلي — القرارات المفتوحة والمُغلقة

> الأعمدة: القرار · الفئة · لماذا · يوقف التصميم؟ · يوقف التنفيذ؟ · متى يُحسم · الخيارات · التوصية/الحالة · License Review؟ · Configurable؟

| القرار | الفئة | لماذا | يوقف التصميم؟ | يوقف التنفيذ؟ | متى يُحسم | الخيارات | التوصية/الحالة | License Review؟ | Configurable؟ |
|---|---|---|---|---|---|---|---|---|---|
| **Frontend stack** | **مُغلق (D1)** | قرار لا-رجعة عملياً | لا | لا | مُغلق 2026-07-04 | — | **React + Vite + TS + Tailwind + shadcn-style**؛ Next.js غير معتمد؛ SvelteKit مرفوض | نعم (صفوف شكلية قبل أول كود) | — |
| **Backend stack** | **مؤكَّد (D2)** | AI/RAG ecosystem | لا | لا | مُغلق | — | Python + FastAPI + Pydantic + SQLAlchemy + Alembic + PostgreSQL | نعم (شكلية) | — |
| مراجعة تراخيص ستاك الكود المعتمد | 2 (لمرحلة 0) | قاعدة «لا dependency بلا صف» | لا | **تنفيذ Phase 0** | قبل أول كود | — | تعبئة/إقرار صفوف: React/Vite/TS/Tailwind/shadcn/FastAPI/Pydantic/SQLAlchemy/Alembic/uv/pnpm | **نعم (شكلية متوقعة)** | — |
| **Git host (C4)** | **مُغلق (D7)** | التطوير online أولاً | لا | لا | مُغلق | GitHub / Gitea / GitLab | **GitHub الآن**؛ Git داخلي = **Backlog** (فقط عند نشوء تطوير داخل الشبكة)؛ قناة نقل كود موقّعة ضمن offline bundle | نعم (لأداة Git الداخلية إن فُعّلت) | — |
| **معايير الأدوات (C5)** | **مُغلق (D3)** | تثبيت قبل أول كود | لا | لا | مُغلق | uv/poetry · pnpm/npm | **uv + pnpm**؛ الإصدارات تُثبَّت عند التنفيذ في coding-standards | لا | تُوثَّق كمعيار |
| **Redis vs Valkey (C1)** | **مُغلق نهائياً (D4 + D9c/ADR-0018 — v1.0-baseline)**؛ التفعيل = فئة 3 | لا حاجة فعلية في P0/P1 | لا | لا (يوقف فقط تفعيل الصورة) | مُغلق؛ التفعيل عند بلوغ محفزات ADR-0018 | Redis (RSAL/SSPL) / **Valkey (BSD)** | الافتراضي **memory/PostgreSQL** عبر CacheProvider/QueueProvider/LockProvider؛ **Valkey افتراضي بروفايل التوسع — مسألة Redis مقفلة لا تُطرح مجدداً** | **نعم قبل تفعيل الصورة** | نعم — تبديل بالإعداد |
| **Object Storage (C2): MinIO / SeaweedFS / S3** | **مُغلق (D5 + D8/ADR-0019 — v1.0-baseline)**؛ التفعيل = فئة 3 (+4 للتوزيع) | التخزين ≠ OCR/استخراج؛ لا ملفات كبيرة قبل P5 | لا | لا (يوقف تفعيل الصورة فقط) | مُغلق؛ التفعيل قبل compose مرحلة الملفات | MinIO (AGPL) / SeaweedFS (Apache-2) / S3-compatible آخر | الافتراضي **local filesystem**؛ **الإنتاج الموزَّع: SeaweedFS افتراضاً؛ MinIO adapter مشروط باعتماد قانوني**؛ لا ربط بالكود باسم أداة | **نعم (MinIO = Legal)** | نعم |
| **Monitoring stack (C3)** | **مُغلق (D6)** — baseline | enterprise-auditability بلا وزن مبكر | لا | لا | مُغلق؛ التوسعة P8 | baseline / Grafana+Loki / VictoriaMetrics | **الخط الأساسي**: Audit-PG + JSON logs + Log Viewer + Health + Error Catalog + IDs + /metrics اختياري؛ OTel adapter مستقبلاً | **نعم عند التوسعة (Grafana/Loki = Legal)** | نعم |
| محرك OCR العربي | 5 (+2 لترخيص المحرك عند P5) | capability في الـ Registry | لا | صف مراجعة عند تنفيذ P5 | اختيار افتراضي بتصميم/تنفيذ P5 | Tesseract / **PaddleOCR** / docTR | قياس جودة عربي ثم الاختيار | نعم (شكلية Apache-2) | **نعم** — يبدَّل من الأدمن |
| PDF tooling (PyMuPDF؟) | 2 (لمرحلة 5) | dependency كود عند تنفيذ media | لا | تنفيذ Phase 5 | قبل تنفيذ P5 | PyMuPDF (AGPL) / **pdfplumber+pypdf** / Tika | الافتراضي pdfplumber/pypdf + Tika؛ PyMuPDF بعد Legal فقط | **نعم (Legal لـ PyMuPDF)** | نعم — parser قابل للتبديل |
| ffmpeg (بناء الصورة) | 2 (لمرحلة 5) + 3 عند دخوله | الترخيص حسب خيارات البناء | لا | تنفيذ Phase 5 | قبل تنفيذ P5 | بناء LGPL-only / GPL | بناء LGPL-only مفضّل + توثيق الخيارات | **نعم** | جزئياً |
| تراخيص أوزان النماذج | **2 (P5) + 4** — بوابة مزدوجة | تشغيل = dependency؛ تضمين = توزيع | لا | تنفيذ P5 (تشغيل)؛ release (تضمين) | قبل تشغيل كل نموذج + قبل أول release | حسب النموذج | صف مستقل لكل نموذج | **نعم** | اختيار النموذج: نعم (Registry) |
| Graph: Neo4j / Apache AGE / تأجيل | 5 | Optional-off | لا | لا | عند الحاجة الفعلية | تأجيل / **AGE** / Neo4j (GPLv3) | يبقى off؛ إن لزم: Apache AGE | نعم (عند التفعيل) | **نعم** |
| Open WebUI usage status | محسوم (مرجع فقط) | Enterprise UI هي الواجهة | لا | لا | مُغلق | مرجع / طبقة داخلية (مشروط Legal) | **مرجع دراسة فقط** خاضع لسياسة Reference-aware (D8)؛ v0.6.6+ ممنوع white-label | نعم (إن استُخدم فعلياً) | — |
| Qdrant + OpenSearch | محسوم + مراجعة شكلية | **حاضران في Local Demo** (constitution م12) | لا | لا (صف شكلي) | مُغلق (ADR-0004) | — | pgvector/PG-FTS **fallback ضمن low-resource bundle فقط** | نعم (شكلية Apache-2) | إعدادات الحجم فقط |
| IdP (LDAP/AD/Keycloak/OIDC/SAML) | 5 | Local Auth + IdP-adapter | لا | لا | عند ربط المؤسسة (بعد P1) | LDAP / AD / Keycloak / OIDC / SAML | Local الآن؛ الربط لاحقاً adapter بلا تعديل جوهر | نعم (لمكوّن الربط) | **نعم** |
| Production sizing | 5 | قياسات وإعدادات | لا | لا | قبل نشر الإنتاج (P8) | حسب المؤسسة | افتراض تزامن 2–5% من 20–30k للتحقق؛ الأحجام config | لا | **نعم** |
| Compose الآن / K8s-OpenShift لاحقاً | محسوم الآن + 5 مستقبلاً | Compose يكفي dev/staging | لا | لا | عند الحاجة (P8+) | Compose / K8s / OpenShift | Compose الآن؛ K8s لاحقاً **manifests فقط دون كود** | لا | **نعم** |
| بنية Postgres في Local (C6) | 5 | DatabaseRouter يجعلها إعداداً | لا | لا | افتراض معتمد | 1×4 / 4×1 | **instance واحد ×4 قواعد** في Local؛ الفصل بالإعداد في الإنتاج | لا | **نعم** |
| Ollama/vLLM في compose مبكراً (C7) | 5 | تعريف مطفأ لا تشغيل | لا | لا | مع P0.4 | تعريف مطفأ / تأجيل | تعريفهما بعلم مطفأ (حضور معماري) | نعم (شكلية) | **نعم** |
| TLS/PKI + توقيع حزمة النقل | 4 (تُقدَّم إلى «قبل أول Staging شبكي» إن وُجد) | تشغيلي/أمني قبل النشر | لا | لا | قبل أول بيئة شبكية أو release | PKI داخلي / شهادات مؤسسة | يُحسم مع فريق العمليات | لا | جزئياً |
| موقف AGPL النهائي عند التوزيع | 4 | استخدام داخلي ≠ توزيع | لا | لا | قبل أول release | رأي قانوني / بدائل Apache-2 | رأي قانوني قبل أي توزيع (يشمل MinIO/Grafana/Loki إن اعتُمدت) | **نعم (Legal)** | — |
| اكتمال Attribution/About | 4 | شرط إغلاق للتوزيع | لا | لا | قبل أول release | — | تُبنى تدريجياً من license-review | — | — |
| مصفوفات الاعتماد + SoD التفصيلية | 5 | سياسات في platform_db | لا | لا | مع P3/P4 كإعدادات | حسب المؤسسة | قابلة للضبط من الأدمن | لا | **نعم** |
| جرد الأنظمة الداخلية + عقودها | 2 (لمرحلة 7) | لا تكامل بلا جرد | لا | تنفيذ Phase 7 | قبل تنفيذ P7 | حسب المؤسسة | يبدأ حصره مبكراً | نعم (لكل connector) | — |

---

## خلاصة الفئات (بعد D1–D9)
- **فئة 1:** فارغة — لا شيء يوقف التصميم (بما فيه تصميم Phase 1).
- **فئة 2 (قبل أول كود لكل مرحلة):** P0 = صفوف ترخيص الستاك المعتمد فقط · P5 = PDF-tooling + ffmpeg + ترخيص محرك OCR + أوزان النماذج (بوابة 1) · P7 = جرد الأنظمة وعقودها.
- **فئة 3 (بوابة تفعيل متكررة — قبل Docker Compose للصورة):** Valkey · MinIO أو SeaweedFS · أي خادم مراقبة · أي صورة جديدة مستقبلاً. **لا بوابة فئة 3 على P0.4** (الصور الفعّالة Postgres/Qdrant/OpenSearch تمرّ بصف شكلي).
- **فئة 4 (قبل التوزيع/الإنتاج):** موقف AGPL · أوزان النماذج (بوابة 2) · TLS/PKI + توقيع الحزمة · اكتمال Attribution.
- **فئة 5 (Configurable):** OCR engine · Graph · النماذج لكل capability · IdP · sizing · بنية Postgres · SoD/مصفوفات الاعتماد · K8s لاحقاً.

**ملاحظات حاكمة:** «Reference-aware clean implementation» **سياسة مُغلقة** (ADR-0021) وليست قراراً مفتوحاً · بنود الفئتين 2/3 تُحسم عبر License Review Workflow داخل نافذتها · بنود 5 تُوثَّق في configuration.md.

## فهرس القرارات المفتوحة لحزم v0.5–v0.7 (Δ ما-بعد الدمج 2026-07-08)
> هذه القرارات تُدار **نصاً داخل ملفاتها الحاضنة** (المصدر المعياري)؛ هذا الجدول فهرس تصنيفي بفئة التوقيت فقط، على نسق هذا السجل.
| OD | الملف الحاضن | فئة التوقيت | خلاصة سطرية |
|---|---|---|---|
| OD-IX-1..3 | ui/UI_INTERACTION_MODEL.md | عند تفعيل P1 (Clarify) | حفظ حالة drawer/فتح تلقائي للتبويبات/زر «فتح كامل» |
| OD-VZ-1..3 | ui/UI_VISIBILITY_RULES.md | عند تفعيل P1 (Clarify) | كتالوج أسباب disabled/عتبة مرجع التدقيق/استثناء عدّاد admin.audit |
| OD-PD-1..3 | ui/UI_PROGRESSIVE_DISCLOSURE.md | عند تفعيل P1 (Clarify) | مؤشر الحقول المطوية/تذكّر التبويب/سقف أزرار البطاقات |
| OD-MX-1 | ui/UI_PROVIDER_MODEL_MANAGEMENT.md | عند تفعيل P6 (Clarify) | إظهار محدد Model Profiles للمستخدم (التوصية: مخفي افتراضياً) |
| OD-MX-3 | ui/UI_PROVIDER_MODEL_MANAGEMENT.md | Ops/config لاحقاً | تحكم يدوي warm/unload — قراءة فقط في v1 |
| OD-MX-4 | ui/UI_PROVIDER_MODEL_MANAGEMENT.md | Ops/config (P5/P6) | مفتاح عتبة slow |
| OD-RUN-1 | ui/UI_RUN_EXECUTION_MODEL.md | Ops/config (P6) | عتبة auto-offload لحجم المخرجات |
| OD-RUN-2 | ui/UI_RUN_EXECUTION_MODEL.md | عند تفعيل P6 (Clarify) | إقرار typed-ack لاعتماد تقرير جزئي |
| OD-RUN-3 | ui/UI_RUN_EXECUTION_MODEL.md | Ops/config | مدة احتفاظ/أرشفة قائمة التشغيلات |
| OD-TPL-1 | ui/UI_RUN_EXECUTION_MODEL.md | **مؤجل بقرار المالك (غير معتمد)** | template.create — لا يدخل إلا بقرار صريح |
| OD-AB-4 | ui/UI_ACTION_BUTTON_MODEL.md | مرآة OD-TPL-1 | نفس الحكم على مستوى كتالوج الأفعال |
| OD-NM-1 (المعدَّل) | ui/UI_FIELD_NAMING.md | محسوم جزئياً + Clarify P3 | run/escalate/compare معتمدة (2026-07-08)؛ بقية الكتالوج تُثبَّت في P3 |
| OD-PSC-1 | architecture/PROVIDER_MODEL_SECRET_CONFIG_SPEC.md | عند تفعيل P5/P6 (Clarify) | مخزن الأسرار لأول تنفيذ (التوصية: env + ملفات محلية) |
| OD-PSC-2 | architecture/PROVIDER_MODEL_SECRET_CONFIG_SPEC.md | عند تفعيل P5/P6 (Clarify) | حساسية endpoint_ref (التوصية: حساس افتراضياً) |
| OD-PSC-3 | architecture/PROVIDER_MODEL_SECRET_CONFIG_SPEC.md | Ops/config (P5/P6) | مدة كاش الاكتشاف |

## فهرس قرارات دفعة v0.8 — Conversational & Workflow (Δ 2026-07-10)
> **كلها مقفلة بقرار SA (2026-07-10)** — المصدر المعياري نصاً داخل ملفاتها الحاضنة؛ هذا فهرس تصنيفي فقط.
| OD | الملف الحاضن | الحالة | خلاصة سطرية |
|---|---|---|---|
| OD-WS-4 | ui/UI_AI_WORKSPACE_MODEL.md §14 | **مقفل** | وضعا عرض ws.main (بطاقات/محادثة) كتفضيل مستخدم — عرضيّ صرف، لا تغيير سلوكياً خادمياً (ورد في دفعة SA بمعرّف OD-WS-2؛ عُدِّل الترقيم لتفادي التصادم) |
| OD-BLD-1 | ui/UI_ADMIN_CONSOLE_MODEL.md §4-C | **مقفل** | أوضاع الباني الثلاثة (تقليدي/محادثي/مدمج) فوق خط draft→migration→checks→approval→apply بلا تغيير؛ الـ LLM يكتب metadata فقط |
| OD-WF-1 | phases/designs/DELTA_V08_FR_WORKFLOW_ORG.md §4 | **مقفل** | الرفض النهائي والإرجاع للمعالجة خياران متمايزان في سطح القرار |
| OD-WF-2 | phases/designs/DELTA_V08_FR_WORKFLOW_ORG.md §4 | **مقفل** | الإرجاع/التوجيه للمكلَّف الحالي فقط (`workflow.return_route`) |
| OD-ORG-1 | phases/designs/DELTA_V08_FR_WORKFLOW_ORG.md §4 | **مقفل** | الشجرة التنظيمية للتوجيه/التكليف فقط — لا RLS مشتقاً منها في v1 · **ملاحظة حدّية (R1):** القفل يمنع الاشتقاق التلقائي؛ المنح الإداري المنطاق الصريح يحكمه ADR-0037 — القفل قائم. |

---

## إغلاق خط الأساس المعماري v1.0-architecture-baseline (2026-07-10)

### (أ) قرارات أُغلقت عند خط الأساس (المرجع المعياري: ملف الـ ADR المذكور)
| القرار المُغلق | الخلاصة | ADR |
|---|---|---|
| افتراضي تخزين الكائنات | filesystem محلياً؛ **SeaweedFS افتراضي الإنتاج الموزَّع**؛ MinIO adapter مشروط باعتماد قانوني | ADR-0019 |
| Cache/Queue/Lock | الآن memory/PostgreSQL (SKIP LOCKED + advisory locks)؛ **بروفايل التوسع Valkey — مسألة Redis مقفلة** + محفزات انتقال موثقة | ADR-0018 |
| طوبولوجيا البحث | PG مصدر الحقيقة · Qdrant متجهي · OpenSearch نصي/هجين؛ pgvector/PG-FTS بروفايل low_resource اختياري فقط | ADR-0004 |
| عقد الواجهة/الخادم | REST أساسي + OpenAPI عقد رسمي + SSE للبث؛ types مولّدة + غلاف fetch رفيع؛ WebSocket بشرط ADR | ADR-0025 |
| موضع الواجهة | `/frontend` داخل مستودع المنصة (monorepo-lite)؛ الفصل لاحقاً عبر ADR | ADR-0017 |
| سياسة تغليف الوحدات | حدود منطقية لا عدد نهائياً؛ packaging map أثر تنفيذي يتغيّر دون ADR؛ ملكية الجداول لكل وحدة دائماً؛ شروط الاستخراج الخمسة | ADR-0026 |
| الهوية والتجميد | Enterprise AI Platform with Progressive Implementation + تجميد المعمارية عند الوسم (م21/م22) | ADR-0023 · ADR-0028 |

### (ب) قيم تشغيلية لا قرارات معمارية
تُضبط **بالقياس لكل نشر** ولا تُعامل قرارات معمارية مفتوحة ولا تدخل هذا السجل كبنود: أعداد النسخ (replicas) · أحجام النماذج · مدد الاحتفاظ النهائية · أحجام التخزين · حدود التزامن · تحجيم GPU للإنتاج. (المرجع: agent-execution-model §11 — توضيح القيم التشغيلية.)

### (ج) قرار مسجَّل — فهرس مواصفات قابل للقراءة الآلية
| OD | الموضوع | الفئة | الحالة |
|---|---|---|---|
| OD-IDX-1 | إنشاء manifest آلي للمواصفات (index.yaml) | — | **مرفوض الآن بقرار SA (2026-07-10)** — لا يُعاد فتحه إلا عند وجود مستهلك آلي فعلي (أداة/خط CI يقرأه)؛ حينها ADR/قرار جديد |

- F-3 (استعادة حزمة v0.3): مغلقة 2026-07-10 — الملفات العشرة مستعادة بمساراتها بلافتة حالة، والأصل الكامل مؤرشف في superseded/ ببصمته؛ نسخة agent-execution-model v0.3 متجاوَزة بخليفتها.

## فهرس قرارات دفعة «تنحية Flutter + جسر Open WebUI» (Δ 2026-07-11)
> المصدر المعياري نصاً في ملفَي الـ ADR؛ هذا فهرس تصنيفي فقط (قاعدة عدم التكرار).
| القرار | الملف الحاضن | الحالة | خلاصة سطرية |
|---|---|---|---|
| مبدأ العميل القابل للاستبدال | decisions/adr/ADR-0029-replaceable-client-principle.md | **مقفل — دُستر (م23، ‏2026-07-11)** | كل عميل يستهلك العقود فقط؛ لا حقيقة/صلاحيات/قواعد عمل/DB في أي عميل؛ الخادم يعيد التحقق ويملك القرار؛ اختبار الاستبدال معيار قبول مؤجل (سجل المؤجلات) |
| ‏Open WebUI واجهة مؤقتة | decisions/adr/ADR-0030-openwebui-temporary-chat-interface.md | **مقفل** (التفعيل خلف بوابات؛ الخروج بخطة) | ‏interim حصراً ضمن شرط م6؛ React خط الأساس بلا إعادة فتح؛ لا اعتماد على جداول Open WebUI الداخلية |


## فهرس قرارات دفعة G1 — Governance Foundation Baseline (Δ 2026-07-19)
> قرارات المالك في G0 (أمر 2026-07-19) قُنّنت كالتالي — **لا إعادة ترقيم لأي قرار قائم، ولا تغيير في حالة أي OD معلق** (التسعة Clarify لمرحلة P1 وبنود Ops-config تبقى كما هي بمواطنها في `methodology/PHASE_EXECUTION_STANDARD.md §2`).

| القرار | الحسم | التقنين |
|---|---|---|
| ‏Q1 عميل الواجهة | ‏Next.js client-only عبر Rocket بالحراس الأربعة | **ADR-0031** (‏ADR-0017 ‏Superseded-in-scope) |
| ‏Q2 تخزين السجلات الديناميكية | هجين: نواة + JSONB + ترويج مقنن؛ ‏UUIDv7 (توليد app/DB) | **ADR-0032** |
| ‏Q3 مستوى الأتمتة | ‏L1 لأول حزمتين ثم L2 افتراضي للمنخفض؛ مسارات محمية | **ADR-0033** |
| ‏Q4 ظهور المستودعات | عامة مؤقتاً؛ خصخصة = بوابة G6 | **ADR-0036** |
| ‏Q5 الهوية | ‏AQL Enterprise AI Platform — by AQLORA | ‏`knowledge/BUSINESS_GLOSSARY.md §1` |
| ‏DR-1 الترخيص | إشعار **Proprietary — All Rights Reserved © 2026 AQLORA**؛ لا Open Source؛ مراجعة قانونية قبل الإطلاق (مؤجلات) | **ADR-0036 بند 2** |
| ‏DR-2 أول حزمتين | ‏FP-0001 = ‏Retrofit ‏`admin.record_types` · ‏FP-0002 = بناء `admin.org` | **ADR-0033 بند 2** + عقود البذرة |
| مهارات التطوير | ‏subset مثبت من Superpowers ‏v6.1.1@d884ae0 | **ADR-0034** |
| طبقة العقود + المشتقات المولَّدة + ‏DGP | ‏contracts/ موطن التأليف؛ ‏BRD/FRD/… ‏GENERATED؛ المولد يُبنى في P2 | **ADR-0035** |
| ‏OD-IDX-1 | **محترم كما هو**: ‏`INDEX.md` بشري القراءة هو مصدر الفهرسة الحاكم — لا index.yaml/manifest في هذه الدفعة | ‏ADR-0035 بند 7 |

## فهرس قرار دفعة R1 — Requirements Reconciliation (Δ 2026-07-21)
> المصدر المعياري نصاً في `decisions/adr/ADR-0037-scoped-administration-model.md`؛ هذا فهرس تصنيفي فقط (قاعدة عدم التكرار).
| OD | الملف الحاضن | الحالة | خلاصة سطرية |
|---|---|---|---|
| OD-ADM-1 | decisions/adr/ADR-0037-scoped-administration-model.md | **محسوم — ADR-0037 (R1)** | نموذج الإدارة المنطاقة: منح صريح؛ تقاطع رباعي؛ وراثة SELF_ONLY افتراضاً |
| ‏OD-P3-2 (canvas) | يبقى مؤجلاً كما هو؛ ‏canvas تجربة aql مؤشَّر **CC-WF-1** يُحسم ببوابته لا صمتاً | عقد `contracts/screens/admin.workflows.md §6` |


## فهرس قرارات المالك — حزمة RAR (‏Δ 2026-07-29 · P3/R2)
> **Decision date:** 2026-07-29 · **Decision authority:** Owner (‏SA) · **Baseline المراجَع عليه:** `934a8fc76965091d6b7cce10fd0e1cc0c2bab796`.
> هذا **فهرس تصنيفي حاكم** لقرارات مقفلة (قاعدة عدم التكرار) — التفصيل الكامل غير الحاكم في `traceability/rar-reconciliation/OWNER-DECISION-PACKAGE.md` → قسم `SA OWNER DECISIONS — RAR NINE DECISIONS`.
> **قيد نافذ على القرارات التسعة:** هذه القرارات **تحسم الاتجاه ولا تكتب العقود التنفيذية**؛ صياغة العقود وschemas والأسماء التقنية تبقى لمسار R المذكور وبوابته.
> **`OD-P3-2` remains OPEN and is not closed by the RAR owner decisions.** ولم تتغيّر حالته ولا بوابته ولا مصدره المعياري.

| OD | الحالة | الاختيار | الملخص الحاكم ونطاقه | الوجهة | القيود المؤجَّلة |
|---|---|---|---|---|---|
| **OD-RAR-01** | **مقفل — قرار مالك (2026-07-29)** | **ج** | أربعة أنماط: Workflow حاكم · Workflow داخلي · مهام مستقلة أو مرتبطة · Workflow فرعي بطلب أب. والداخلي **لا يتحوّل إلى سلطة حاكمة ضمنياً** ولا يغيّر المسار الرسمي أو الاعتمادات بلا صلاحية | **R3** | عقد `Task` وحقل التمييز ومصفوفة صلاحياته |
| **OD-RAR-02** | **مقفل — قرار مالك (2026-07-29)** | **ج** | جذور المشغّلات: **Manual · Schedule · Event**؛ وWebhook وExternal وCondition **سمات** لا جذور. **تجاوز مالك محدود النطاق** لتأجيل `DEFERRED_IMPLEMENTATION.md:24` — التصنيف يُحسم الآن | **R10** | Event Contracts التفصيلية · payload · delivery · transport · retry · webhook security |
| **OD-RAR-03** | **مقفل — قرار مالك (2026-07-29)** | **ب** | فصل: المحادثة · السجل الحوكمي · Audit metadata · مخزن النص الخام — مع retention وdeletion وredaction وlegal hold والبصمة والمرجع. **سجل التدقيق ليس مخزناً إلزامياً للنص الخام** | **R3** | أسماء الحقول ومدد الاحتفاظ وتصنيف الحساسية |
| **OD-RAR-04** | **مقفل — قرار مالك (2026-07-29)** | **ب** | **لا تخزين لـChain of Thought ولا reasoning حرّ**؛ ميتاداتا مغلقة ومحكومة فقط | **R3** | حقول الميتاداتا وصلاحية الاطّلاع عليها |
| **OD-RAR-05** | **مقفل — قرار مالك (2026-07-29)** | **ب** | Validation **حتمي** (schema + قواعد أعمال خادمية معلَنة) مع **إعادة تحقق مباشرة قبل التنفيذ** | **R3** | مجموعة القواعد الخادمية ومعيار قبولها |
| **OD-RAR-06** | **مقفل — قرار مالك (2026-07-29)** | **ب** | Preview مثبتة على نسخ وسياق محدّدين؛ وعند تغيّر مؤثر: **تبطل القديمة · Revalidation · Preview جديدة · عرض الفرق · تأكيد جديد** | **R3** | حقول التثبيت وشكل الفرق وسلوك التعارض |
| **OD-RAR-07** | **مقفل — قرار مالك (2026-07-29)** | **ب** | التفريق بين **Draft Revision** و**Governing Version** و**Runtime Instance State**؛ الجاري يبقى على نسخته، والترحيل صريح ومحكوم. **ولا تُعتمد عبارة «نسخة عند كل انتقال حالة» بلا تحديد الكيان** | **R3** | محفّز إنشاء النسخة وسياسة المسودات والترحيل |
| **OD-RAR-08** | **مقفل — قرار مالك (2026-07-29)** | **ب** | **Dry Run read-only** — بلا كتابة ولا أثر مالي ولا اعتماد ولا أثر غير قابل للعكس | **R3** | نطاق القدرة وصلاحيتها ومحرّك الأثر المتوقَّع |
| **OD-RAR-09** | **مقفل — قرار مالك (2026-07-29)** | **ب** *(تصحيح نهائي لخطأ نقل سابق كان «أ»)* | **Threshold join `m-of-n`** · `all-of-n` عند `m=n` · حالة **`Threshold Impossible`** · **لا اعتماد صامت** · **لا خفض تلقائي للعتبة** · المعالجة **بصلاحية موثَّقة** | **R5** | عقدة Join وقواعد صحّتها في لوحة التحقّق |

**قرار مرتبط — الحدّ المؤسسي (سند الصف `RAR-002 [08]`):** النظام **مطلوب منه دعم عدّة مؤسسات**؛ **ولا يُفرض المصطلح `tenant`**؛ **ولا تُختزل المؤسسة في `org_unit`** — المؤسسة **حدٌّ أعلى مستقل** عن الوحدات التنظيمية التابعة لها. و**غياب الحدّ المؤسسي الحاكم يمثّل Delta حقيقية** وجهتها **`R4 → P4`**. **ولا يُعتمد هنا اسم حقل ولا جدول ولا API**، **ولا يُحسم `GATE-DEFINITION`**.

**قيد عام على هذا الفهرس:** لا يُعيد ترقيم أي قرار قائم، ولا يغيّر حالة أي `OD` آخر، ولا يُنشئ ADR، ولا يُعدّل `AUTHORITY.md`.


## فهرس ترحيل إغلاق P3 — Phase Exit Review (‏Δ 2026-07-30 · P3)
> **السند:** قرار مالك (‏SA) بتاريخ 2026-07-30: «لا يجوز الاعتماد على ذاكرة المالك أو أي مساعد لتذكّر التوصيات والقرارات والالتزامات لاحقاً؛ وقبل إغلاق أي مرحلة يجب أن يكون كل عنصر إمّا مكتملاً، أو منقولاً حاكماً بوعاء ومرحلة مالكة وشرط تفعيل ومالك وأثر حجب، أو مصنَّفاً حاجباً يمنع الإغلاق».
> **الوظيفة:** هذا **تسجيل حاكم** لعناصر `GOVERNED TRANSFER` من دفعة P3 التي كانت تعيش في تقارير غير حاكمة. المعيار المركزي: `methodology/PHASE_EXECUTION_STANDARD.md` §3 (نافذ عند دمج دفعته). الجرد التفصيلي **غير الحاكم**: `traceability/rar-reconciliation/P3-CLOSURE-INVENTORY.md` — **وثيقة إحالة لا وعاء**.
> **قيد على هذا الفهرس:** لا يُعيد ترقيم أي قرار قائم · لا يغيّر حالة أي `OD` آخر · لا يُغلق `OD-P3-2` · لا يُنشئ ADR · لا يُعدّل `AUTHORITY.md` · لا يحسم أيّاً من القيود المسجَّلة أدناه · **ولا يُطبّق §18**.

| OD | الحالة | المسألة | الوجهة الحاكمة | المرحلة المالكة | سلطة القرار | سبب عدم التنفيذ الآن | شرط التفعيل | أثر الحجب | شرط الإغلاق | Traceback |
|---|---|---|---|---|---|---|---|---|---|---|
| **OD-GOV-1** | **مفتوح** | هل يُفوَّض `phases/BACKLOG_DEFERRED_SCOPE.md` **سجلاً حاكماً**، وبأي **مرتبة** و**نطاق**؟ حالته `Accepted` في الفهرس **لا تمنح مرتبة** (‏`AUTHORITY.md:28`)، وهو غير مرتَّب في المرتبتين 6 و7 | هذا السجل ⇒ ثم دفعة تملك `AUTHORITY.md` | دفعة مخوَّلة لاحقة | Owner (‏SA) | حلُّه يستلزم **تعديل `AUTHORITY.md`** وهو محظور نصاً في دفعة الترحيل | قرار مالك صريح بمرتبة ونطاق | **غير حاجب** — 11 صفاً من 12 لا يتأثر تصنيفها بأيٍّ من الفرضين | تفويض نافذ بمرتبة ونطاق، أو رفض صريح | `traceability/rar-reconciliation/OWNER-DECISION-PACKAGE.md` → `GOV-OBS-01` |
| **OD-GOV-2** | **مفتوح** | **المرتبة 5 خالية فعلياً**: لا عقد واحد بحالة `Approved` عند HEAD (‏4 بذور `Candidate` + 3 معايير `Proposed`) — فما أثر ذلك على أي إثبات «مقنَّن» يستند إلى عقد؟ | هذا السجل ⇒ **R3** (توسيع العقود) وبوابة الاعتماد | R3 → بوابات العقود | Owner (‏SA) / Specification Architect | ترقية العقود وتعديل عقود `Candidate` **محظوران** في دفعة الترحيل | أمر تفعيل R3 | **غير حاجب** — أثره على المصالحة **صفر** بعد تمريرة الأهلية | عقد `Approved` واحد على الأقل، أو قرار مالك بإبقاء المرتبة خالية | `traceability/rar-reconciliation/OWNER-DECISION-PACKAGE.md` → `GOV-OBS-02` · `AUTHORITY.md:14` |
| **OD-GOV-3** | **~~مفتوح~~ ⇒ مُعالَج (Δ 2026-07-31)** | مخرجات المصالحة موسومة «خارج authority ladder وReading Sets» ⇒ **الدلتا الـ46 لا تصل إلى أي منفِّذ** ما لم تُستوعَب في مصادر حاكمة عبر مسارات R، أو يُدرَج مخرَج جامع في Reading Set بقرار مالك. أيُّ المسارين؟ | هذا السجل ⇒ مسارات R المالكة (‏R3/R4/R5/R10) أو قرار Reading Sets | مسارات R المعنية | Owner (‏SA) | لا تعديل لمصفوفة القراءة ولا تنفيذ لأي دلتا في دفعة الترحيل | استيعاب الدلتا في أصولها المالكة، أو قرار مالك بالإدراج | **غير حاجب لـP3**؛ خطر ضياع تنفيذي إن أُغفل | كل دلتا مستوعَبة في أصل حاكم أو مغلقة بقرار | `traceability/rar-reconciliation/B1-CHECKPOINT-REPORT.md` §7 |
| **`GOV-OBS-03`** *(جديد · Δ 2026-07-31)* | **مفتوح** | **نصّ §18 المرجعي غير موجود في المستودع** بينما تحيل إليه نصوصٌ حاكمة ونُفِّذ أثره بقرار مالك مستقل (2026-07-30). ويتفاقم بوجود **ثلاثة مدلولات متمايزة** للرمز «§18» في المستودع (التفصيل في «سجل `GOV-OBS-03`» أدناه) ⇒ خطر **ambiguity / wrong-source execution** | هذا السجل ⇒ قرار حوكمي مستقل (إنشاء أو استعادة أو إعادة تسمية النص المرجعي) | دفعة مخوَّلة لاحقة | Owner (‏SA) | إنشاء نصٍّ مرجعي مفقود **محظور نصاً** في أمر هذه الدفعة؛ ولا يُخترع نصّ حاكم | **قرار مالك صريح** بأحد المسارات الثلاثة | **غير حاجب لـP3** ولا لـ`FP-0001`؛ **مُخفَّف حالياً** بأمر المالك 2026-07-30 و`OD-FP-0001-FREEZE` | وجود نصٍّ مرجعي حاكم مسمّى بلا التباس، أو قرار مالك صريح بعدم إنشائه مع قاعدة تسمية فارقة | `traceability/rar-reconciliation/SECTION-18-BLOCKING-EFFECT.md` §11 · `handoff/handoff.md` H-0023 (`:490`) |

**تأكيدات ترحيل على قيود قائمة (بلا تغيير لأي منها):**
- **`OD-P3-2`** يبقى **`OPEN`** بصياغته وبوابته ومصدره المعياري كما هي — **لم يُغلق ولم يُعدَّل**؛ وهو **غير حاجب** لإغلاق P3 (لا دلتا من الـ46 معلَّقة عليه). المالك: Owner (‏SA) · شرط الإغلاق: حسم `CC-WF-1` ببوابة عقد `contracts/screens/admin.workflows.md §6` لا صمتاً.
- **الحدّ المؤسسي** (سند `RAR-002 [08]`) يبقى **`دلتا حقيقية`** بوجهة **`R4 → P4`** — و**شرط إغلاقه المسجَّل الآن حاكماً:** يُحسم **داخل R4 قبل إغلاق بوابة P4**؛ شرط تفعيله: **أمر تفعيل R4**؛ أثره: **غير حاجب لـP3 · حاجب لإغلاق بوابة P4**. **ولا يُعتمد اسم حقل ولا جدول ولا API، ولا يُفرض `tenant`، ولا تُختزل المؤسسة في `org_unit`، ولا يُحسم `GATE-DEFINITION`.**
- **`BLOCKING REMAINDER` القائم** (‏§18 غير منفَّذة · الدلتا السبعة · تسجيل النطاق المتأثر) **ليس مسجَّلاً هنا بوصفه منقولاً** — لأن وعاءه الحاكم هو **مخرج §18** نفسه؛ ويبقى **مانعاً لإغلاق P3**.
  > **Δ 2026-07-30 (يتجاوز هذا السطر ولا يمحوه):** صدر **أمر §18 المستقل** ونُفِّذ. مخرج §18 هو القسم التالي **«سجل أثر الحجب §18»**، وفيه سُجِّلت الثلاثة حاكمياً. **الأثر `ACTIVE`** — و«مانع لإغلاق P3» لم يعد التوصيف النافذ لهذه العناصر الثلاثة؛ إغلاق P3 يبقى **قراراً مستقلاً للمالك** لم يُتخذ.


## سجل أثر الحجب §18 — بوابة `FP-0001` (‏Δ 2026-07-30 · P3 — تنفيذ §18)
> **السند:** `§13b: DETERMINED — BLOCKING DELTA DETECTED` بنطاق `FP-0001` حصراً (`traceability/rar-reconciliation/FINAL-B-13B-DETERMINATION.md:198`..`:202`) + **أمر SA المستقل المؤرَّخ 2026-07-30 «P3 — §18 GOVERNED NARROW-FREEZE EXECUTION»**، وقرار المالك فيه: **`ACTUAL NARROW FREEZE`**.
> **الوظيفة:** هذا **الوعاء الحاكم** لأثر الحجب وفق `methodology/PHASE_EXECUTION_STANDARD.md` §3.4 («آثار الحجب ⇒ الوعاء الذي يحدده النص الحاكم للبوابة المعنية») — ونصّ بوابة `FP-0001` لا يسمّي وعاءً، فحدّده أمر المالك ضمن هذا السجل المرتَّب (المرتبة 4). الجرد التفصيلي **غير الحاكم**: `traceability/rar-reconciliation/SECTION-18-BLOCKING-EFFECT.md` — **وثيقة إحالة لا وعاء**.
> **قيد على هذا القسم:** لا يعدّل نص بوابة `FP-0001` ولا `Gate Criteria` (المرتبة 2 لم تُمسّ) · لا يعيد `FINAL-B` ولا §13b · لا يفتح جدول المصالحة ولا يغيّر تصنيفاته ولا توزيعه المجمَّد `116/80/46/18/0` · لا يعالج الدلتا السبعة ولا يكتب المعايير · لا يصلح Reading Sets ولا ينفّذ `OD-GOV-3` · لا يبدأ R3 · **ولا يغلق P3**.

### `OD-FP-0001-FREEZE` — أثر الحجب

| الحقل | القيمة |
|---|---|
| **الحالة** | **`FP-0001 = BLOCKED`** · حالة الأثر: **`ACTIVE`** |
| **القرار** | **مقفل — قرار مالك (2026-07-30):** `ACTUAL NARROW FREEZE` — تجميد فعلي **محدود النطاق**، لا تجميد للمشروع ولا للمراحل ولا للتحليل ولا للعمل التحضيري ولا لمسار رفع الحجب |
| **طبيعة البوابة** | **`FAIL-CLOSED`** — الخطوة 5 من بوابة الأساس توجب اختبار الوصولية، والنص يقفل بـ«لا يُعلن Core قبل اجتياز البوابة كاملة» (`methodology/PHASE_EXECUTION_STANDARD.md` — بوابة الأساس · بنداً 1 و5) |
| **تاريخ التفعيل** | **2026-07-30** |
| **مصدر الحكم** | §13b DETERMINED — `FINAL-B-13B-DETERMINATION.md:198`..`:202` (**نتيجة تاريخية مجمَّدة لا تُعاد**) |
| **سبب الحجب** | سبع دلتا إتاحة معلَنة **بلا صيغة مقنَّنة قابلة للاختبار** — الجدول أدناه |
| **النطاق المتأثِّر** | `FP-0001 itself and any downstream acceptance, readiness declaration, or dependency that requires FP-0001 to have passed` — أي: **بوابة `FP-0001` نفسها، وكل قبول أو إعلان جاهزية أو اعتماد لاحق يشترط اجتيازها**. **لا تُستخدم `P12`** — لا ملف ولا مرحلة بهذا الرقم في المستودع (`phases/designs/**` ينتهي عند `phase-8`)، ولا تُنشأ مرحلة من مرفق خارجي. ولا توجد في النص الحاكم عند HEAD **قائمة اعتمادات صريحة بعينها** — فلم يُخترع أيٌّ منها |
| **سلطة الرفع** | Owner (‏SA) — بأدلة حاكمة؛ **لا رفع صامتاً ولا بانقضاء وقت** |
| **Traceback** | `traceability/rar-reconciliation/SECTION-18-BLOCKING-EFFECT.md` · `B1-CHECKPOINT-REPORT.md:87` · `handoff/handoff.md` H-0021 بند 6 · H-0023 |

**محظور ما دام `ACTIVE`:** إعلان `FP-0001` مجتازة · قبولها · اعتماد أي مرحلة أو نطاق على أساس اجتيازها · استخدام مخرجاتها بوصفها `Accepted baseline` · رفع التجميد دون دليل حاكم · وصف الاعتمادات التابعة لها بأنها جاهزة نهائياً.

**مسموح رغم التجميد:** التحليل · العمل التحضيري · إصلاح Reading Sets (‏`OD-GOV-3`) · تفعيل مسار معالجة الدلتا **بعد أمر مستقل** · تقنين المعايير السبعة في مسارها المالك · تنفيذ اختبارات البوابة · كل عمل لا يعتمد على قبول `FP-0001`.

**شروط الرفع لاحقاً (تراكمية — لم يُرفع شيء الآن):** (1) تقنين المعايير السبعة في وعائها الحاكم بصياغة **قابلة للاختبار** (`ui/UI_DESIGN_SYSTEM.md` §9 عبر **R3**) · (2) إدخال المعايير والدلتا اللازمة في **Reading Sets** التي يستهلكها المنفِّذ (= صميم `OD-GOV-3`، ويسبق التنفيذ الفعلي للتقنين) · (3) تنفيذ فحوص بوابة `FP-0001` في `methodology/PHASE_EXECUTION_STANDARD.md` — وخصوصاً الخطوة 1 (‏verify) والخطوة 5 (اختبارات متعددة الأنماط · RTL/LTR · Light/Dark · **وصولية**) · (4) أدلة نجاح قابلة للتدقيق · (5) تحديث حالة هذا السجل من **`ACTIVE`** إلى **`LIFTED`** · (6) ربط الرفع بالأدلة والـcommit والاختبارات · (7) تحديث `P3 Closure Inventory`.
**ولا يجوز** أن يُدرَج ضمن شروط الرفع: إعادة `FINAL-B` · إعادة §13b · إعادة فتح جدول المصالحة · إعادة تصنيف صفوفه · تغيير التوزيع المجمَّد. **ورفع الأثر لا يُبطل الحكم التاريخي ولا يُعيد كتابته — يرفع أثره فقط.** وإن تبيّن أن الرفع يحتاج **حكماً حاكماً جديداً** يتجاوز التحديث إلى `LIFTED`، **فلا يُخترع الحكم**: تُسجَّل الحاجة إلى **قرار مالك مستقل قبل الرفع**.

### الدلتا السبعة الحاجبة — تسجيل حاكم (‏لا معالجة)

> **لم تُعالَج ولم تُكتب صيغتها المقنَّنة.** «الاختبار المطلوب» يصف **صنف الاختبار** لا نصّ المعيار — صياغته مملوكة لـ**R3**. وتصنيفها في الجدول التاريخي يبقى **`دلتا حقيقية`** بوجهة **`R3`** بلا تغيير.

| الدلتا | المصدر | العلاقة بـ`FP-0001` | الوجهة الحاكمة | المسار المالك | شرط التفعيل | الاختبار المطلوب | دليل الإغلاق | أثر بقائها مفتوحة |
|---|---|---|---|---|---|---|---|---|
| **`RAR-003 [38]`** Semantic HTML | `RAR-003:373` · `#absence-b1-rar003-008` | تماس مباشر | `ui/UI_DESIGN_SYSTEM.md` §9 — صيغة قابلة للاختبار | **R3** (غير مُفعَّلة) | **أمر تفعيل R3** | اختبار الوصولية — الخطوة 5 من `FP-0001` | نتيجة اختبار قابلة للتدقيق + مرجع الصيغة المقنَّنة | **حاجبة** — تُبقي `FP-0001` `BLOCKED` |
| **`RAR-003 [40]`** Focus order | `RAR-003:375` · `#absence-b1-rar003-009` | تماس مباشر | كما أعلاه | **R3** | **أمر تفعيل R3** | كما أعلاه — ترتيب التركيز | كما أعلاه | **حاجبة** |
| **`RAR-003 [42]`** Labels and descriptions | `RAR-003:377` · `#absence-b1-rar003-010` | تماس مباشر | كما أعلاه | **R3** | **أمر تفعيل R3** | كما أعلاه — الاسم/الوصف الآلي | كما أعلاه | **حاجبة** |
| **`RAR-003 [43]`** Error association | `RAR-003:378` · `#absence-b1-rar003-011` | تماس مباشر | كما أعلاه | **R3** | **أمر تفعيل R3** | كما أعلاه — ربط الخطأ بحقله | كما أعلاه | **حاجبة** |
| **`RAR-003 [44]`** Dialog focus trapping | `RAR-003:379` · `#absence-b1-rar003-012` | تماس مباشر | كما أعلاه | **R3** | **أمر تفعيل R3** | كما أعلاه — حصر التركيز | كما أعلاه | **حاجبة** |
| **`RAR-003 [46]`** Reduced motion | `RAR-003:381` · `#absence-b1-rar003-013` | تماس مباشر | كما أعلاه | **R3** | **أمر تفعيل R3** | كما أعلاه — تقليل الحركة | كما أعلاه | **حاجبة** |
| **`RAR-003 [48]`** Data table semantics | `RAR-003:383` · `#absence-b1-rar003-014` | تماس مباشر | كما أعلاه | **R3** | **أمر تفعيل R3** | كما أعلاه — دلالات الجدول | كما أعلاه | **حاجبة** |

**ملاحظة ترتيب إلزامية:** `OD-GOV-3` **مفتوح** ولم يُنفَّذ؛ ومخرجات المصالحة **ليست حالياً** ضمن Reading Sets التي يستهلكها المنفِّذ — فقد **لا يرى** منفِّذُ التقنين هذه الدلتا. **معالجة `OD-GOV-3` تسبق** التنفيذ الفعلي لمسار التقنين. **§18 لا تعالج هذه الفجوة**، والعمل التحضيري لإصلاحها **مسموح** رغم التجميد.

> **Δ 2026-07-31 — يتجاوز الفقرة أعلاه ولا يمحوها:** `OD-GOV-3` **عولج** بإنشاء «مجموعة قراءة مسار `R3`» أدناه داخل هذا الوعاء الحاكم **Tier-0**. الفقرة أعلاه صحيحةٌ في تاريخها. **وما زال قائماً بلا تغيير:** «معالجة `OD-GOV-3` تسبق التنفيذ الفعلي لمسار التقنين» — والمعالجة **ليست** بدءاً لـ`R3`.

## مجموعة قراءة مسار `R3` — Reading Set (‏Δ 2026-07-31 · P3 — معالجة `OD-GOV-3`)

> **السند:** `PHASE_EXECUTION_STANDARD.md` §3.4 «التوجيه المبدئي للأوعية: القرارات المفتوحة وقرارات المالك ⇒ `decisions/open-decisions.md`» (‏`origin/main` = `7611ed9…` · `methodology/PHASE_EXECUTION_STANDARD.md:71`) + أمر SA المؤرَّخ 2026-07-31 «‏P3 — OD-GOV-3 READING-SET DELIVERY AND §18 TRACEABILITY COMPLETION».
> **لماذا هذا الوعاء بالذات (أقل تغيير صحيح حاكمياً):** هذا الملف **مقروء إلزاماً في Tier-0** لكل مهمة بنص `methodology/agent-execution-model.md:59` («`decisions/open-decisions.md` (كفهرس قرارات)»). فوضْعُ مجموعة القراءة هنا يجعلها **واصلةً إلى كل منفِّذ محتمل** بلا تعديل مصفوفة القراءة نفسها — و`methodology/agent-execution-model.md` **ممنوع تعديله** بنص `decisions/DEFERRED_IMPLEMENTATION.md:26` (المرتبة 4) و`handoff/handoff.md:499` (المرتبة 9).
> **قيد قاطع:** هذا القسم **لا يبدأ `R3`** ولا يفعّلها ولا يأذن بأي تنفيذ. `R3` **لا تُفعَّل إلا بأمر مالك صريح** (`methodology/RECONCILIATION_ROADMAP.md:5`). ووجود مجموعة قراءة **ليس** تفعيلاً.
> **قيد قاطع ثانٍ:** لا يُنسخ محتوى المصالحة هنا. هذا **فهرس إحالات** فقط؛ والمحتوى يبقى في مواضعه بلا تكرار.

### (أ) ما يجب أن يقرأه منفِّذ `R3` — إحالات قابلة للحل

| # | المرجع | الموضع (قابل للحل عند `bdd5929`) | الحالة الحاكمة | ماذا يعطي المنفِّذ |
|---|---|---|---|---|
| 1 | **سجل أثر الحجب** | `decisions/open-decisions.md` → `OD-FP-0001-FREEZE` (‏`:209`..`:228`) | **حاكم — المرتبة 4** | `FP-0001 = BLOCKED` · الأثر `ACTIVE` · المحظور والمسموح · شروط الرفع السبعة |
| 2 | **الدلتا السبعة الحاجبة** | `decisions/open-decisions.md` → «الدلتا السبعة الحاجبة — تسجيل حاكم» (‏`:230`..`:243`) | **حاكم — المرتبة 4** | الصفوف السبعة بأسمائها ومصادرها ومراسيها ووجهتها واختبارها ودليل إغلاقها |
| 3 | **صف التأجيل** | `decisions/DEFERRED_IMPLEMENTATION.md:27` | **حاكم — المرتبة 4** | التزام التقنين · الوجهة · شرط التفعيل · الأثر |
| 4 | **وجهة المعالجة** | `ui/UI_DESIGN_SYSTEM.md` §9 «الإتاحة (Accessibility)» — العنوان `:110` والمضمون `:111` | **حاكم — المرتبة 8** | الوعاء الذي تُكتب فيه الصيغة القابلة للاختبار — **وهو خالٍ منها الآن، ولا يُكتب فيه شيء بهذه الدفعة** |
| 5 | **قاعدة تفعيل المسار** | `methodology/RECONCILIATION_ROADMAP.md:5` · صف `R3` `:11` | **حاكم — المرتبة 2** | لا `R3` بلا أمر مالك صريح |
| 6 | **هذا القيد نفسه** | `decisions/open-decisions.md` → `OD-GOV-3` (‏`:194`) + هذا القسم | **حاكم — المرتبة 4** | أن الوصول صار مضموناً، وأن ذلك ليس بدءاً لـ`R3` |
| 7 | **مصدر المصالحة — الجدول المجمَّد** | `traceability/rar-reconciliation/FINAL-RECONCILIATION-TABLE.md` (‏`:26` صف التوزيع · `:29` صف المجموع) | **غير حاكم — إحالة** | جدول **260** والتوزيع **`116/80/46/18/0`** والدلتا **46** |
| 8 | **حكم §13b** | `traceability/rar-reconciliation/FINAL-B-13B-DETERMINATION.md` (‏`:104` صف `FP-0001` · `:198`..`:202` الحكم) | **غير حاكم — إحالة** | لماذا الحجب، وبأي نطاق |
| 9 | **سجل تنفيذ §18** | `traceability/rar-reconciliation/SECTION-18-BLOCKING-EFFECT.md` | **غير حاكم — إحالة** | التفصيل الكامل لأثر الحجب وشروط الرفع وجداول §3.4 |
| 10 | **جرد إغلاق P3** | `traceability/rar-reconciliation/P3-CLOSURE-INVENTORY.md` | **غير حاكم — إحالة** | موضع `P3-CI-02` و`P3-CI-09` و`P3-CI-26` من الجرد |
| 11 | **سجلات `RAR-003` السبعة** | `traceability/rar-reconciliation/RECONCILIATION-RAR-003.md` — الصفوف `:67` `[38]` · `:69` `[40]` · `:71` `[42]` · `:72` `[43]` · `:73` `[44]` · `:75` `[46]` · `:77` `[48]` | **غير حاكم — إحالة** | نص كل دلتا ومصدرها وتصنيفها ووجهتها |
| 12 | **أدلة الغياب** | `traceability/rar-reconciliation/ABSENCE-EVIDENCE-B1-RAR003.md` — المراسي `#absence-b1-rar003-008`..`-014` | **غير حاكم — إحالة** | إثبات الغياب لكل معيار من السبعة |

> **قاعدة الرتب داخل هذه المجموعة:** البنود **1–6 حاكمة** وتُلزم؛ والبنود **7–12 إحالات تحليلية غير حاكمة** تُقرأ للفهم والتتبّع **ولا تُشتق منها سلطة** — عملاً بالقاعدة القاطعة في `PHASE_EXECUTION_STANDARD.md` §3.4 («وجود العنصر في وثيقة جرد أو تقرير `traceability/**` وحده لا يُعدّ Governed Transfer»). **وتزويد المنفِّذ بها مسموح** — قيد «لا تُزوَّد لوكلاء التنفيذ» يخص `references/analysis-inputs/rar-2026-07/**` وحدها (‏`MANIFEST.md:73` · صفوف `INDEX.md` لذلك المجلد)، **لا** مخرجات `traceability/rar-reconciliation/**`.

### (ب) حدود ملزمة على منفِّذ `R3` — تُقرأ مع المجموعة

1. **`FP-0001` تبقى `BLOCKED`** و`OD-FP-0001-FREEZE` يبقى **`ACTIVE`** حتى تُستوفى **الشروط السبعة كاملةً** (‏`:227`) — ولا يُرفع صمتاً ولا بانقضاء وقت.
2. **ممنوع** إعادة `FINAL-B` أو إعادة حساب §13b أو إعادة فتح الجدول المجمَّد أو إعادة تصنيف صفوفه.
3. **ممنوع** تغيير التوزيع التاريخي **`116 / 80 / 46 / 18 / 0`** أو مجموع **260**.
4. **ممنوع** المساس بخط الأساس المجمَّد `188ad379d04b07ca9e1b4eeee38dc68ecca29914`.
5. **تصنيف الصفوف السبعة يبقى `دلتا حقيقية` بوجهة `R3`** بلا تغيير.
6. **وجود هذه المجموعة ليس أمر تفعيل** — ولا يُستنتج منه إذنٌ ببدء `R3` ولا بكتابة أي معيار.

> **Δ 2026-07-31 (ب) — إحالة أمامية تُضاف ولا تنسخ ولا تنقض:** صدر لاحقاً **أمر مالك صريح مستقل** فُعِّلت به `R3` **للتخصيص ومعايير القبول فقط** — والسجل الحاكم في قسم **«سجل تفعيل مسار `R3`»** أدناه (‏`R3-ACT-01`). **البنود الستة أعلاه تبقى نافذة كلها** بلا استثناء: `FP-0001` ما زال `BLOCKED` والتجميد `ACTIVE`، والصفوف السبعة ما زالت `دلتا حقيقية` بوجهة `R3`، والجدول المجمَّد وتوزيعه وخط الأساس بلا مساس. **وصحيحٌ أنّ هذه المجموعة لم تكن أمر تفعيل** — التفعيل جاء بأمر مستقل لا منها.

### (ج) سجل إغلاق `OD-GOV-3`

| الحقل | القيمة |
|---|---|
| **Root cause** | مصفوفة القراءة (`methodology/agent-execution-model.md` §9 — `:56`..`:63`) لها **ثلاثة صفوف فقط**: Tier-0 (`:59`) · «لكل مرحلة» (`:60`) · «لكل مهمة» (`:61`). وصفُّ «لكل مرحلة» مُفتاحُه **مرحلة**، بينما `methodology/RECONCILIATION_ROADMAP.md:5` ينصّ على أن مسار R «**منفصل تماماً** عن مراحل تطوير المنتج P0–P8». ولا يرد في `agent-execution-model.md` ذكرٌ لأي «مسار R». ⇒ **منفِّذ `R3` يسقط من المصفوفة** فلا يستهلك إلا Tier-0 + بطاقة المهمة؛ ومصدر المصالحة يقع خارج كليهما |
| **Reading Set المتأثرة** | مصفوفة القراءة §9 — **صفّ Tier-0** (`agent-execution-model.md:59`)، عبر البند الثالث فيه: «`decisions/open-decisions.md` (كفهرس قرارات)» |
| **الملفات/المراجع المضافة** | لا ملف جديد. أُضيف **قسم واحد** في وعاء حاكم قائم Tier-0 (هذا القسم) يحمل **12 إحالة** + **6 حدود ملزمة** |
| **المستهلك المتوقع** | منفِّذ **أمر تفعيل `R3`** (وكيل تنفيذ أو Specification Architect) — ويلزمه Tier-0 قبل أي عمل بنص `agent-execution-model.md:59` |
| **Evidence** | `agent-execution-model.md:59` (‏Tier-0 يشمل هذا الملف) · `PHASE_EXECUTION_STANDARD.md:71` على `origin/main` (توجيه الأوعية) · `RECONCILIATION_ROADMAP.md:5` (قاعدة التفعيل) · الإحالات الاثنتا عشرة في (أ) — **كلها قابلة للحل عند `bdd5929`** |
| **Commit SHA** | **`e6c5b345702ad4c6c9e479f7bf1183fd9a0ecc88`** — هو الـcommit الذي نفَّذ إغلاق `OD-GOV-3` فعلياً (أبوه `bdd5929825e5050d7cb8b39ffcdae8e07e81c3cf`)، ودُفِع إلى فرع PR #9 بتاريخ 2026-07-31. **مسجَّل كذلك في** `handoff/handoff.md` → **H-0024** («رأس الفرع بعد») — وتثبيتُه في الموضعين تمَّ بجولة تصحيح لاحقة، فلا يُقرأ هذا الحقل إحالةً ذاتية إلى commit التصحيح |
| **Verification method** | (1) كل مرجع في (أ) يُفتح ويُطابَق سطراً؛ (2) `agent-execution-model.md:59` يذكر هذا الملف نصاً؛ (3) لا إحالة دائرية — لا يشير أيٌّ من البنود 7–12 إلى هذا القسم بوصفه سلطة؛ (4) لا مجموعة قراءة ثانية متعارضة — `grep` يثبت أن §9 هو التعريف الوحيد |
| **Closure date** | **2026-07-31** |
| **Residual risks** | (1) **البنية لم تُصلَح**: §9 ما زالت بلا صفٍّ لمسارات R — الوصول مضمون عبر **Tier-0** لا عبر صفٍّ مخصَّص؛ إصلاحه يحتاج دفعة تملك `agent-execution-model.md` (‏`P3-CI-08` · `DEFERRED_IMPLEMENTATION.md:26`). (2) **مصطلح «Reading Sets» غير معرَّف** في أي نص حاكم — يُستعمل مرادفاً لمصفوفة §9 بلا تعريف. (3) البند السادس من Tier-0 (`platform/docs/SPEC_SOURCE.md`) **غير موجود في هذا المستودع**. (4) الدلتا الـ39 الأخرى وجهاتُها مسجَّلة في العمود السادس من الجدول غير الحاكم؛ هذا القسم يوصل **مصدرها** لا يمنحها صفوفاً حاكمة فردية |
| **تأكيد** | **`R3` لم تبدأ** · لم تُعالَج الدلتا السبعة · لم تُكتب المعايير · لم يُرفع التجميد · **P3 لم تُغلق** |

## سجل `GOV-OBS-03` — غياب نص §18 المرجعي (‏Δ 2026-07-31 · P3)

> **لم يُنشأ نصُّ §18 المفقود في هذه الدفعة، ولن يُخترع.** هذا **تسجيل ملاحظة** لا معالجة.

| الحقل | القيمة |
|---|---|
| **الموضوع** | يحتوي المستودع إحالات حاكمة إلى «**§18**»، ولا يوجد فيه **النص المرجعي المفصَّل** لتلك §18؛ ومع ذلك **نُفِّذ أثرها** بقرار مالك مستقل مؤرَّخ **2026-07-30** |
| **Evidence** | `traceability/rar-reconciliation/SECTION-18-BLOCKING-EFFECT.md:16` («**نصّ §18 نفسه** — **غير موجود داخل المستودع**») · `handoff/handoff.md:490` («**نصّ §18 ذاته:** **غير موجود في المستودع**») · `methodology/PHASE_EXECUTION_STANDARD.md` ينتهي عند «بوابة الأساس» ولا يبلغ §18 (‏`:29` على الفرع · `:95` على `origin/main`) |
| **مواضع الإحالات** | الرمز «§18» يرد في **22 ملفاً** — أبرزها: `traceability/rar-reconciliation/SECTION-18-BLOCKING-EFFECT.md` · `P3-CLOSURE-INVENTORY.md` · `FINAL-B-13B-DETERMINATION.md:30` · `:205` · `:207` · `B1-CHECKPOINT-REPORT.md:87` · `handoff/handoff.md:449` وما بعده · `decisions/open-decisions.md:204`..`:205` · `methodology/RECONCILIATION_ROADMAP.md:3` · `decisions/DEFERRED_IMPLEMENTATION.md:27` · `:29` |
| **ثلاثة مدلولات متمايزة — جوهر الالتباس** | **(1) §18 «خريطة الحجب»** — المقصودة في سياق المصالحة و`FP-0001`؛ نصُّها **غير موجود**. **(2) §18 «حوكمة الشاشات»** — قائمة ومُغطّاة حاكمياً بجدول EC-4 في `ui/UI_SCREEN_GOVERNANCE_STANDARD.md:58`..`:74` ولها بنود فرعية **§18-د** و**§18-هـ**، و`traceability/TRACEABILITY_MATRIX.md:13` · `:16` · `:20`؛ ونصُّها هي الأخرى خارج المستودع. **(3) §18 داخل وثائق `RAR`** — «الأسئلة المعمارية المفتوحة». **والالتباس حادّ لأن `§18-د` من المدلول (2) يُحيل إلى `methodology/PHASE_EXECUTION_STANDARD.md` — وهو الملف نفسه الذي يستضيف بوابة `FP-0001` موضوعَ المدلول (1)** |
| **§18 داخل `RAR` — غير حاكمة ومحظورة** | `references/analysis-inputs/rar-2026-07/MANIFEST.md:73`: «قد تحتوي وثائق RAR (خصوصاً أقساماً مثل §16/§17/**§18**) نصوصاً موجَّهة إلى وكلاء … محفوظة **مادةً تاريخية للتحليل فقط، لا تُعامَل تعليماتٍ تنفيذية**، **ولا تُزوَّد لوكلاء التنفيذ**» — مؤكَّداً في `handoff/handoff.md:490`. **لا يجوز استخدامها مصدراً** |
| **الأثر المحتمل** | **ambiguity / wrong-source execution** — قارئٌ لاحق قد يسند «§18» إلى المدلول الخطأ، أو يستقيها من وثائق `RAR` المحظورة، أو يفترض وجود نص حاكم غير موجود |
| **الوضع الحالي** | **Mitigated for P3** — بأمر المالك المؤرَّخ 2026-07-30 (`P3 — §18 GOVERNED NARROW-FREEZE EXECUTION`) الذي صدر عنه مضمون الخريطة، وبـ`OD-FP-0001-FREEZE` الذي يحمل الأثر في وعاء حاكم مرتَّب. **التخفيف يخص P3 وحدها ولا يمتد لما بعدها** |
| **الإجراء المستقبلي المطلوب** | **قرار حوكمي مستقل** بأحد ثلاثة: (أ) **إنشاء** نص §18 مرجعي حاكم؛ (ب) **استعادته** من مصدره التاريخي وتوثيق سلسلة الحيازة؛ (ج) **إعادة تسميته** برمز فارق يرفع الالتباس مع مدلولَي «حوكمة الشاشات» و`RAR`. **ولا يُنفَّذ أيٌّ منها بلا أمر مالك** |
| **المالك** | Owner (‏SA) — **حصراً** |
| **التوقيت** | **قبل** أي دفعة لاحقة تستشهد بـ«§18» بوصفها سنداً حاكماً مستقلاً؛ **وليس** شرطاً لإغلاق P3 ولا لرفع `OD-FP-0001-FREEZE` |
| **شرط الإغلاق** | وجود **نصٍّ مرجعي حاكم مسمّى بلا التباس** ومُحال إليه من مواضع الاستشهاد، **أو** قرار مالك صريح بعدم إنشائه مع **قاعدة تسمية فارقة** تمنع الخلط بين المدلولات الثلاثة |
| **ملاحظة ترقيم** | سُجِّل بالمعرّف **`GOV-OBS-03`** كما ورد في أمر المالك. **ولم يُمنَح رقم `OD-GOV-*`** — منحُ رقمٍ في سلسلة القرارات فعلٌ حوكمي لم يأذن به هذا الأمر |

## استكمال حقول §3.4 الناقصة — `P3-CI-02` و`P3-CI-26` (‏Δ 2026-07-31)

> **السند:** `PHASE_EXECUTION_STANDARD.md` §3.4 (`origin/main:69`) يوجب **ثلاثة عشر حقلاً** لكل `GOVERNED TRANSFER`. تدقيقُ الحقول (الجدولان في `traceability/rar-reconciliation/SECTION-18-BLOCKING-EFFECT.md` §9) أظهر **أربعة حقول** بلا قيمة في أي وعاء حاكم. **تُستكمل هنا — في الوعاء الحاكم — لا في `traceability/**`.**
> **لا يُنشئ هذا القسم التزاماً جديداً** ولا يغيّر تصنيفاً ولا وجهةً ولا شرط تفعيل؛ يسجّل حقولاً **مستنبَطة من السجل القائم** لا مخترَعة.

| العنصر | الحقل الناقص (‏§3.4) | القيمة المسجَّلة الآن | مستندها في السجل القائم |
|---|---|---|---|
| **`P3-CI-02`** | **Stable ID** | `P3-CI-02` | `handoff/handoff.md:491` (كان يرد في تعداد الإحصاء فقط) |
| **`P3-CI-02`** | **Source phase/batch** | **P3 / B2-FINAL-B** — ثبتت بحساب §13b من الجدول المكتمل | `FINAL-B-13B-DETERMINATION.md:84`..`:90` · `:198`..`:202` |
| **`P3-CI-02`** | **Owner or decision authority** | **Owner (‏SA)** — بأمر تفعيل `R3`. (‏«**R3**» في جدول الدلتا السبعة هو **المسار المالك** لا سلطةَ القرار؛ وقاعدة التفعيل تُسند القرار للمالك حصراً) | `methodology/RECONCILIATION_ROADMAP.md:5` («كل مرحلة R تُفعَّل **بأمر مالك صريح**») |
| **`P3-CI-26`** | **Source phase/batch** | **P3 / §18** — اكتُشف بتنفيذ §18 بتاريخ 2026-07-30 | `SECTION-18-BLOCKING-EFFECT.md` §5 · `handoff/handoff.md:487`..`:488` |

**بعد هذا الاستكمال:** `P3-CI-02` = **13/13** · `P3-CI-26` = **13/13** — بأوعية حاكمة لكل حقل. التفصيل حقلاً-بحقل مع الموضع والدليل: `traceability/rar-reconciliation/SECTION-18-BLOCKING-EFFECT.md` §9 (**وثيقة إحالة لا وعاء**).

## سجل تفعيل مسار `R3` — تخصيص ومعايير قبول فقط (‏Δ 2026-07-31 · أمر مالك)

> **السند:** **أمر مالك صريح** صادر 2026-07-31 — «‏OWNER ACTIVATION ORDER — R3 (specification and acceptance-criteria design only)» — وفق قاعدة التفعيل الحاكمة في `methodology/RECONCILIATION_ROADMAP.md:5` («كل مرحلة R تُفعَّل **بأمر مالك صريح**»). وهذا **الوعاء الحاكم** للتفعيل بمقتضى `PHASE_EXECUTION_STANDARD.md` §3.4 («القرارات المفتوحة وقرارات المالك ⇒ `decisions/open-decisions.md`» — `origin/main:71`)، ويرافقه `handoff/handoff.md` → **H-0026** (المرتبة 9).
> **قيد قاطع أول:** هذا التفعيل **لا يرفع التجميد ولا يفكّ حجب `FP-0001`**. `OD-FP-0001-FREEZE` يبقى **`ACTIVE`** و`FP-0001` يبقى **`BLOCKED`**، وشروط الرفع السبعة (`:227`) **لم يُستوفَ منها إلا الثاني** (Reading Sets — بإغلاق `OD-GOV-3`). **واكتمال التخصيص وحده لا يرفع التجميد.**
> **قيد قاطع ثانٍ:** **لم تُعالَج الدلتا السبعة ولم تُقنَّن.** `ui/UI_DESIGN_SYSTEM.md` **لم يُمسّ** في هذه الدفعة، و§9 ما زال على حاله. وتصنيف الصفوف السبعة يبقى **`دلتا حقيقية`** بوجهة **`R3`** بلا تغيير.
> **قيد قاطع ثالث:** **`P3` لم تُغلق**، ومعادلة جرد `P3` **`28 = 12 + 16 + 0`** و`UNCLASSIFIED = 0` **بلا تغيير**؛ ولم يُنشأ أي `P3-CI-*` جديد لعمل `R3` الاعتيادي. و`traceability/rar-reconciliation/P3-CLOSURE-INVENTORY.md` **لم يُستعمل وعاءً لتتبّع `R3`** ولم يُعدَّل.

### (أ) سجل التفعيل — `R3-ACT-01`

| الحقل | القيمة |
|---|---|
| **المعرّف** | **`R3-ACT-01`** — سجل تفعيل مسار `R3` |
| **تاريخ التفعيل** | **2026-07-31** |
| **السلطة المفعِّلة** | **Owner (‏SA)** — حصراً |
| **commit البداية** | **`00c358712be9d5d952b64b82edfa2ac9d9154193`** (رأس فرع PR #9 عند إصدار الأمر) · **`origin/main`** = `7611ed9531660f5f4762f3e6a60f05cdf0f419b4` |
| **الحالة النافذة لـ`R3`** | **`ACTIVE — SPECIFICATION ONLY`** (كانت **`NOT STARTED`**) |
| **النطاق المأذون** | **تفعيل `R3` + تخصيص الإتاحة + تصميم معايير القبول** — دفعةُ تخصيصٍ تحضيرية مسموحة رغم التجميد (‏`:225` «مسموح رغم التجميد: التحليل · العمل التحضيري … تقنين المعايير السبعة في مسارها المالك») |
| **شروط الدخول المستوفاة** | (1) `OD-GOV-3` **مغلق** بـ`e6c5b345702ad4c6c9e479f7bf1183fd9a0ecc88` ⇒ «مجموعة قراءة مسار `R3`» (‏`:248`) واصلة عبر **Tier-0** (`methodology/agent-execution-model.md:59`) · (2) الدلتا السبعة مسجَّلة حاكمياً (‏`:230`..`:243`) · (3) صفّ التأجيل قائم (`decisions/DEFERRED_IMPLEMENTATION.md:27`) · (4) `P3-CI-02` و`P3-CI-26` = 13/13 · (5) صفر `placeholder` غير محلول عند `00c3587` · (6) **أمر المالك الصريح** |
| **المخرجات المتحقّقة** | (أ) هذا السجل · (ب) `handoff/handoff.md` → **H-0026** · (ج) **سبعة معايير بمعرّفات ثابتة `R3-A11Y-01`..`07`** بحقولها الكاملة · (د) **مصفوفة اختبارات** بـ46 اختباراً `R3-A11Y-T-001`..`046` · (هـ) نموذج حوكمة «Labels and Descriptions» بأقسامه الأربعة · (و) تصنيف حقول ميتاداتا الشاشات الديناميكية بأدلة · (ز) شروط رفع التجميد وإغلاق `R3` كاملةً |
| **وعاء التتبّع** | **`traceability/r3-accessibility/R3-ACCESSIBILITY-SPECIFICATION.md`** + **`…/R3-ACCESSIBILITY-TEST-MATRIX.md`** — **مخرجان تحليليان غير حاكمين** ومنفصلان تماماً عن جرد إغلاق `P3` |
| **`FP-0001`** | **`BLOCKED`** — بلا تغيير |
| **`OD-FP-0001-FREEZE`** | **`ACTIVE`** — بلا تغيير |
| **حالة المعايير السبعة** | **لا معيار واحد `PASS`.** الحالة الموحَّدة: **`SPECIFIED — NOT CODIFIED — NOT REMEDIATED — NOT TESTED`** |
| **`P3`** | **مفتوحة** — لم تُغلق؛ وإغلاقها **قرار مالك مستقل لم يُتخذ** |
| **جرد `P3`** | **`28 = 12 + 16 + 0`** · `UNCLASSIFIED = 0` — **بلا تغيير** |
| **الأفعال المحظورة (نافذة ما دامت الدفعة تخصيصاً)** | تنفيذ معالجة إتاحة · تعديل كود تطبيق أو مكوّنات UI · تعديل `ui/UI_DESIGN_SYSTEM.md` · نشر أو اعتماد تسمية مولَّدة · رفع التجميد · فكّ حجب `FP-0001` · إغلاق `R3` · إغلاق `P3` · إضافة عمل `R3` إلى `P3-CLOSURE-INVENTORY.md` · تغيير معادلة الجرد · تعديل `FINAL-B` أو §13b أو الجدول المجمَّد أو توزيعه · تعديل `AUTHORITY.md` أو `PHASE_EXECUTION_STANDARD.md` أو `agent-execution-model.md` أو `RECONCILIATION_ROADMAP.md` · حسم `GOV-OBS-03` أو `P3-CI-28` صامتاً · merge · rebase · amend · force-push · وسم · إصدار |
| **سلطة الإغلاق** | Owner (‏SA) — **ولم يُغلق `R3`** |

### (ب) المعرّفات الثابتة للمعايير السبعة (تسجيل حاكم — بلا تقنين)

> **مسوّغ مساحة الأسماء:** فحصٌ آليّ عند `00c3587` أثبت أن `R3-` و`A11Y` و`AX-` و`ACC-` **غير مستعملة إطلاقاً** في المستودع ولا في أي فرع؛ والنمط البيتي `<FAMILY>-<AREA>-<zero-padded-n>` (‏`OD-GOV-1` · `P3-CI-28` · `GOV-OBS-03`). **ولا تُنشئ هذه المعرّفات سلسلةَ قراراتٍ جديدة ولا تُرقَّم في `OD-*`.**

| المعرّف | الاسم | الدلتا الحاكمة | الحالة |
|---|---|---|---|
| **`R3-A11Y-01`** | Semantic Structure | `RAR-003 [38]` | `SPECIFIED — NOT CODIFIED — NOT REMEDIATED — NOT TESTED` |
| **`R3-A11Y-02`** | Focus Order | `RAR-003 [40]` | كما أعلاه |
| **`R3-A11Y-03`** | Labels and Descriptions | `RAR-003 [42]` | كما أعلاه |
| **`R3-A11Y-04`** | Error Association | `RAR-003 [43]` | كما أعلاه |
| **`R3-A11Y-05`** | Dialog Focus Trapping | `RAR-003 [44]` | كما أعلاه |
| **`R3-A11Y-06`** | Reduced Motion | `RAR-003 [46]` | كما أعلاه |
| **`R3-A11Y-07`** | Data Table Semantics | `RAR-003 [48]` | كما أعلاه |

**اختبارات القبول:** `R3-A11Y-T-001`..`R3-A11Y-T-046` — **مصمَّمة لا منفَّذة** (`DESIGNED — NOT BUILT — NOT EXECUTED`).

### (ج) أثر التفعيل على `P3-CI-02` و`P3-CI-26` (تسجيل — بلا تعديل الجرد)

| العنصر | التصنيف قبل | التصنيف بعد | ما تغيَّر |
|---|---|---|---|
| **`P3-CI-02`** | `GOVERNED TRANSFER` | **`GOVERNED TRANSFER` — بلا تغيير** | **شرط التفعيل المسجَّل («أمر تفعيل `R3`») صار مستوفى**؛ أمّا الالتزام نفسه — **التقنين في `ui/UI_DESIGN_SYSTEM.md` §9** — **فلم يُنفَّذ**، فيبقى التصنيف كما هو |
| **`P3-CI-26`** | `GOVERNED TRANSFER` | **`GOVERNED TRANSFER` — بلا تغيير** | الشرط الأول من شروط الرفع السبعة **لم يُستوفَ**؛ والثاني كان مستوفى قبل هذه الدفعة |

> **لذلك لم يُعدَّل `P3-CLOSURE-INVENTORY.md`:** لا تصنيفَ عنصرٍ تغيَّر، ولا عنصرَ `P3` جديداً نشأ. وعملُ `R3` الاعتيادي **لا يُسجَّل** في جرد إغلاق `P3` بنص أمر المالك.

### (د) شروط رفع التجميد وإغلاق `R3` — **تفصيلٌ للشروط السبعة القائمة (`:227`) لا استبدالٌ لها**

**لرفع `FP-0001` من `BLOCKED`** (تراكمية): (1) اعتماد المعايير السبعة كاملةً من المالك · (2) **تقنينها في `ui/UI_DESIGN_SYSTEM.md` §9 بصيغة قابلة للاختبار** · (3) إيصالها إلى Reading Sets — **مستوفى تشغيلياً** بإغلاق `OD-GOV-3`، ويشمل المعاييرَ المقنَّنة بعد وقوع التقنين · (4) **تنفيذ المعالجة** على الشاشات والمكوّنات · (5) **نجاح** الاختبارات الآلية · (6) **نجاح** الاختبارات اليدوية المطلوبة · (7) تغطية مسارات الواجهات **الديناميكية والمُعانة بالـAI** · (8) تغطية **`ar`/`en`** و**RTL/LTR** · (9) تغطية **الوضعين الفاتح/الداكن** حيث ينطبق · (10) تنفيذ فحوص بوابة `FP-0001` (الخطوة 1 verify والخطوة 5 وصولية) · (11) **أدلة قابلة للتدقيق** مؤرشفة ومتعقَّبة · (12) **صفر `BLOCKING REMAINDER` إتاحة**.

**لانتقال `OD-FP-0001-FREEZE` إلى `LIFTED`:** ما سبق كاملاً، ثم (13) ربط الرفع بالأدلة والـcommit والاختبارات · (14) تحديث حالة السجل هنا إلى `LIFTED` · (15) تحديث `P3 Closure Inventory` · (16) **قرار مالك صريح** — لا رفع صامتاً ولا بانقضاء وقت.

**لإغلاق `R3`:** ما سبق كاملاً، ثم (17) إغلاق حِمل `R3` الآخر المسجَّل في الخارطة — **توسيع تغطية العقود** (`RECONCILIATION_ROADMAP.md:11`) بما فيه أثر `OD-GOV-2` · (18) تحديث `P3-CI-02` و`P3-CI-26` بدليل · (19) `Phase Exit Review` على `R3` بالتصنيف الثلاثي و`UNCLASSIFIED = 0` · (20) **قرار مالك مخوَّل بالإغلاق**. **ولا يُغلق `R3` بإغلاق شقّ الإتاحة وحده.**

**ولا يجوز** أن يُدرَج ضمن أيٍّ من هذه الشروط: إعادة `FINAL-B` · إعادة §13b · إعادة فتح الجدول المجمَّد · إعادة تصنيف صفوفه · تغيير التوزيع `116/80/46/18/0`.

### (هـ) تعارض حوكمي مرفوع إلى المالك — **لم يُحسم**

**ميثاق `R3` لا يطابق حِمله الفعلي.** `methodology/RECONCILIATION_ROADMAP.md:11` يعرّف `R3` بأنها **«Contracts Coverage Expansion»** مخرجُها «عقود Candidate إضافية». وتقنينُ الإتاحة أُلحق بـ`R3` لاحقاً بنصوص **المرتبة 4** (هذا الملف · `DEFERRED_IMPLEMENTATION.md:27`) **دون تعديل صفّ `R3`** في الخارطة — ونصّ v1.3 يقرّ صراحةً أنه «لم يُعدَّل صفُّ أيّ مسار R ولا هدفُه ولا مخرجُه». ⇒ **`R3` تحمل اليوم حِملين غير متجانسين**، والمرتبة 2 لا تعكس الثاني.
**ولم يُعدَّل `RECONCILIATION_ROADMAP.md`** — تعديله **محظور نصاً** في أمر هذه الدفعة، والمرتبة 2 لا تُعدَّل من داخل دفعة تخصيص. **المطلوب:** قرار مالك مستقل إمّا بتوسيع صفّ `R3` نصاً، أو بفصل شقّ الإتاحة في مسارٍ مسمّى. **الأثر الحالي: غير حاجب** لهذه الدفعة — التفعيل صحيح لأن الدلتا السبعة مسجَّلة حاكمياً بوجهة `R3` في المرتبة 4، والخارطة تُسند التفعيل للمالك لا لنص الصفّ.

### (و) ملاحظات مسجَّلة لا تُحسم هنا

1. **`ui/UI_SCREEN_GOVERNANCE_STANDARD.md:68`** (المرتبة 8) يصف إحالة قبول الإتاحة إلى `DESIGN_SYSTEM §§9–11` بأنها **«صيغة قابلة للاختبار»**، بينما البند 4 من مجموعة القراءة (‏`:262` — المرتبة 4) يقرّر أن §9 **«خالٍ منها الآن»**. **المرتبة 4 تعلو** ⇒ العلاج **ملءُ §9 لا تعديلُ `:68`** — وكلاهما خارج نطاق هذه الدفعة.
2. **تعارض الوضع الداكن:** `ui/UI_DESIGN_SYSTEM.md:117` يؤجّله (`OD-UX-3`) بينما ثلاثة نصوص توجب اختبار/لقطات Light/Dark. **قائم قبل `R3` وليس من الدلتا السبعة — ولا يُحسم هنا.**
3. **فجوات إنفاذ:** `methodology/testing-strategy.md` بلا نوع اختبار إتاحة · `ui/UI_GATE_REVIEW_CHECKLIST.md` بلا بند إتاحة · `ui/UI_COMPONENT_STATES.md` بلا بُعد إتاحة في `S1`–`S20` · `ui/UI_UX_ASSUMPTIONS.md` بلا فرضية إتاحة · `contracts/screens/SCREEN_CONTRACT_TEMPLATE.md` §8 بلا حدٍّ أدنى للإتاحة. **كلها خارج نطاق هذه الدفعة** ومواضعُها محظورة التعديل فيها.
4. **لا عقد حقلٍ في المستودع** (`contracts/fields/` مؤجَّل والمجلد غير موجود) ⇒ ستة من ثلاثة عشر حقلاً لميتاداتا الشاشات الديناميكية **`MISSING`** وثلاثة **`PARTIAL`** وواحد **`CONFLICTING`**. **ولم يُنشأ العقد** — إنشاؤه يمسّ المرتبة 5 ويحتاج أمراً مستقلاً.
5. **`GOV-OBS-03`** و**`P3-CI-28`** و**`OD-GOV-1`** و**`OD-GOV-2`** و**`OD-P3-2`** تبقى **مفتوحة بلا مساس**، و**`P3-CI-08`** غير منفَّذ.

**قيد عام على هذا القسم:** لا يُعيد ترقيم أي قرار قائم · لا يغيّر حالة أي `OD` آخر · لا يُنشئ ADR · لا يُعدّل `AUTHORITY.md` · لا يُنشئ طبقة سلطة جديدة · ولا يمنح `traceability/**` أي سلطة.

## سجل قرار إغلاق نطاق `P3` — Phase Exit Review (‏Δ 2026-08-01 · أمر مالك)

> **السند:** **أمر مالك صريح** صادر 2026-08-01 — «‏P3 — Phase Exit Review and Closure Preparation» — ومراجعة الخروج مُجراة وفق `methodology/PHASE_EXECUTION_STANDARD.md` **§3.1–§3.9** كما هي **نافذة على `origin/main` = `7611ed9531660f5f4762f3e6a60f05cdf0f419b4`** (بعد دمج PR #10).
> **الوعاء:** هذا القسم هو **الوعاء الحاكم** لقرار إغلاق نطاق `P3` بمقتضى §3.2 («كل **Owner Decision** مسجَّل في وعاء حاكم» · «**لا قرار يعيش في تقرير أو محادثة فقط. التقرير إحالة، لا وعاء**») و`AUTHORITY.md:13` (المرتبة 4). السجل التفصيلي **غير الحاكم**: `traceability/rar-reconciliation/P3-CLOSURE-INVENTORY.md`، والمراجعة المفصَّلة في `handoff/handoff.md` → **H-0027** (المرتبة 9).
> **قيد تنفيذي مسجَّل (‏§2 من أمر المالك):** §3 **غير موجودة في شجرة فرع PR #9** (`methodology/PHASE_EXECUTION_STANDARD.md` هنا **39 سطراً** وينتهي عند «بوابة الأساس»؛ وعلى `main` **105 أسطر**). فقُرِئت §3.1–§3.9 **من `origin/main`** لا من الفرع، وهذا **ليس عيباً في الفرع**: فرعُ PR #9 أُنشئ قبل PR #10، وتقاطع ملفات الشقّين = **صفر**. **ووراثة §3 تتحقق عند دمج PR #9.**

### (أ) سجل القرار — `OD-P3-CLOSURE`

| الحقل | القيمة |
|---|---|
| **المعرّف** | **`OD-P3-CLOSURE`** — قرار إغلاق نطاق `P3` |
| **التاريخ** | **2026-08-01** |
| **السلطة** | **Owner (‏SA)** — حصراً |
| **الحالة** | **مقفل — نطاق `P3` مُغلق** بمعنى §3.9-1 (**إغلاق نطاق المرحلة**) حصراً |
| **رأس الفرع عند المراجعة** | `8a5b2f6ba66462042c988987bf056e541e1095b0` · **`origin/main`** = `7611ed9531660f5f4762f3e6a60f05cdf0f419b4` (**غير مستوعَب في الفرع**) |
| **نتيجة §3.1–§3.9** | **PASS** — بلا `BLOCKING REMAINDER` وبلا `UNCLASSIFIED` |
| **معادلة الجرد** | **`28 = 13 + 15 + 0`** · **`UNCLASSIFIED = 0`** |
| **ما لا يعنيه هذا القرار** | انظر (ب) أدناه — **قاعدة دلالة الإغلاق**، وهي **جزء من القرار لا تعليقٌ عليه** |

**قرارات المالك الخمسة المسجَّلة حاكمياً (نصّ الأمر):**
1. **يجوز إغلاق `P3` مع بقاء `OD-FP-0001-FREEZE` `ACTIVE`.**
2. **`P3-CI-02` و`P3-CI-26` يُعدّان منقولَين حاكمياً بنجاح لأغراض نطاق `P3`** — لأن التزاماتهما **مصنَّفة**، وحقول النقل الحاكم لديهما **كاملة (13/13)**، ولهما **مسار مالك مسمّى**، وهما **محفوظان لـ`R3`**. **وهذا لا يُنفِّذ التزامَيهما.**
3. **إغلاق `P3` لا يفعل شيئاً ممّا يلي:** إكمال التزامات الإتاحة السبعة · رفع `OD-FP-0001-FREEZE` · فكّ حجب `FP-0001` · إغلاق `R3` · إعلان جاهزية المنتج · الإذن بمعالجة الإتاحة · محو أو إضعاف أي التزام منقول.
4. **محتوى تفعيل `R3` القائم على PR #9 مقبولٌ بوصفه توسعةَ نطاقٍ مسجَّلة غير حاجبة** — يُفصح عنه في مراجعة الخروج، **ولا يُحذف ولا يُنقل ولا يُعاد تأسيسه ولا يُعدَّل ولا يُعاد كتابته**، **ولا يُضاف عملُ `R3` جديدٌ إلى PR #9**.
5. **‏GitHub Issue #11 خارج نطاق `P3`** — لا يُضاف إلى `P3-CLOSURE-INVENTORY.md` · لا يُعدَّل · لا يُصنَّف بنداً حاكمَ النقل جديداً في `P3`.

### (ب) دلالة الإغلاق — قاعدة ملزمة (‏§3.9)

```text
P3 scope closure confirms completion of reconciliation, classification, and governed
transfer only. It does not assert completion of transferred obligations, lift
OD-FP-0001-FREEZE, unblock FP-0001, close R3, or declare product readiness.
```

**وبالعربية — التمييز الثلاثي في §3.9 لا يُخلط:** (1) **إغلاق نطاق `P3`** = استيفاء §3.1–§3.8 لهذه المرحلة وحدها — **وقع**؛ (2) **اكتمال المشروع** — **حكمٌ مستقل لم يقع ولا يُستنتج من (1)**؛ (3) **عدم فقدان الالتزامات** — **ضمانٌ مستقل يبقى نافذاً بعد الإغلاق** بالتسجيل الحاكم القائم.

### (ج) الحالات النافذة بعد هذا القرار — بلا تغيير

| العنصر | الحالة النافذة |
|---|---|
| **`OD-FP-0001-FREEZE`** | **`ACTIVE`** — لم يُرفع |
| **`FP-0001`** | **`BLOCKED`** — لم يُفكّ حجبه |
| **`R3`** | **`ACTIVE — SPECIFICATION ONLY`** — لم تُغلق · الدلتا السبعة `NOT REMEDIATED` · لا معيار `PASS` |
| **`P3-CI-02` · `P3-CI-26`** | **`GOVERNED TRANSFER`** — 13/13 · التزاماهما **لم يُنفَّذا** ويبقيان محفوظَين لـ`R3` |
| **`P3-CI-13`** (‏merge/tag/release) | **`GOVERNED TRANSFER`** — **حاجب لأي إعلان اكتمال، لا لإغلاق نطاق `P3`**؛ خطوات **مالك حصراً** لم تُنفَّذ |
| **`GOV-OBS-03`** · **`OD-GOV-1`** · **`OD-GOV-2`** · **`OD-P3-2`** · **`P3-CI-08`** · **`P3-CI-28`** | **مفتوحة/غير منفَّذة بلا مساس** — منقولة حاكمياً لا مُسقَطة |
| **`ui/UI_DESIGN_SYSTEM.md` §9** | **لم يُمسّ** — التقنين لم يقع |
| **جدول §10.4** | **260** · التوزيع **`116/80/46/18/0`** · خط الأساس `188ad37` — **بلا مساس** |

### (د) خطوات المالك المتبقية — **لا تختفي من السجل** (‏§3.7)

**غير منفَّذة في هذا القرار ولا مأذون بها فيه:** (1) **دمج PR #9** · (2) **الوسم (tag)** · (3) **الإصدار (release)** · (4) **تفعيل `R4`**. وكلها **سلطة مالك حصراً**، ومسجَّلة أصلاً في `P3-CI-13` و`handoff/handoff.md` → H-0022 `:467`. **وإغلاق نطاق `P3` لا يُنفِّذ أيّاً منها ولا يأذن بها.**

**قيد عام على هذا القسم:** لا يُعيد ترقيم أي قرار قائم · لا يغيّر حالة أي `OD` آخر عدا تسجيل هذا القرار · لا يُنشئ ADR · لا يُعدّل `AUTHORITY.md` ولا `methodology/**` · لا يمسّ أي مخرج مجمَّد · لا يُنشئ طبقة سلطة جديدة · ولا يمنح `traceability/**` أي سلطة.

## سجل استمرار Accessibility Sub-track — استثناء تنفيذي محدود (‏Δ 2026-08-02 · R3 — أمر مالك)

> **السند:** **أمر مالك صريح** صادر 2026-08-02 — «‏R3 — Governing Transfer Preparation». وهذا **الوعاء الحاكم** للسند بمقتضى `methodology/PHASE_EXECUTION_STANDARD.md` §3.4 («القرارات المفتوحة وقرارات المالك ⇒ `decisions/open-decisions.md`») و`AUTHORITY.md:13` (المرتبة **4**)، ويرافقه `handoff/handoff.md` → **H-0028** (المرتبة 9).
> **موضعه من السجل:** يُقرأ **بعد** «سجل تفعيل مسار `R3`» (‏`R3-ACT-01` · `:333`) و**مع** §(هـ) منه («تعارض حوكمي مرفوع إلى المالك — **لم يُحسم**» · `:396`). وهو **لا ينسخهما ولا يتجاوزهما ولا يحسم ما تركاه مفتوحاً**.
> **قيد قاطع:** هذا السند **إذنُ استمرارٍ لعملٍ قائم**، وليس تفعيلاً جديداً ولا توسيعَ نطاقٍ ولا حسماً لسلطة.

### (أ) سجل السند — `OD-R3-SCOPE-1`

| الحقل | القيمة |
|---|---|
| **المعرّف** | **`OD-R3-SCOPE-1`** — سند استمرار Accessibility Sub-track داخل أعمال `R3` |
| **التاريخ** | **2026-08-02** |
| **السلطة** | **Owner (‏SA)** — حصراً |
| **الحالة** | **مقفل بوصفه سنداً** · **الاستثناء `ACTIVE`** — ونطاقُه ما هو منصوص عليه أدناه حرفياً لا أوسع |
| **نصّ الإذن** | **المالك يأذن باستمرار Accessibility Sub-track الحالي داخل أعمال `R3` بوصفه استثناءً تنفيذياً محدوداً تحت تعارض سلطويّ معلوم وغير محلول.** |
| **الوظيفة الوحيدة** | **السماح باستمرار العمل الحالي على الدلتا السبعة** — لا غير |
| **الدلتا السبعة** | **`RAR-003 [38]` · `[40]` · `[42]` · `[43]` · `[44]` · `[46]` · `[48]`** (المسجَّلة حاكمياً في `:230`..`:243` — **بلا إعادة تصنيف ولا تغيير وجهة**) |
| **انتهاء الاستثناء** | **ينتهي عند إغلاق الدلتا السبعة.** انقضاؤه **تلقائيّ بانتهاء موجبه**، ولا يُمدَّد صمتاً ولا يتحوّل إلى ميثاق |
| **التسوية النهائية لاسم `R3` ونطاقه** | **`AUTHORITY CONFLICT — SEPARATE OWNER ACTION`** — تبقى معلَّقة، وتُراجَع **ديناً حوكمياً في `Phase Exit Review`** |
| **`FP-0001`** | **`BLOCKED`** — بلا تغيير |
| **`OD-FP-0001-FREEZE`** | **`ACTIVE`** — بلا تغيير · **ولا شرط رفعٍ أُضيف ولا عُدِّل ولا أُعيد عدُّه** |
| **`R3`** | **`ACTIVE — SPECIFICATION ONLY`** — بلا تغيير |
| **`R4`** | **غير مفعَّلة** — بلا تغيير |
| **جرد `P3`** | **`28 = 13 + 15 + 0`** · `UNCLASSIFIED = 0` — **بلا تغيير**؛ ولم يُنشأ `P3-CI-*` جديد |

### (ب) ما **لا** يفعله هذا السند — قيود ملزمة

1. **لا يعدّل `methodology/RECONCILIATION_ROADMAP.md`** — المرتبة **2** لم تُمسّ، وصفُّ `R3` («Contracts Coverage Expansion» · `RECONCILIATION_ROADMAP.md:11`) **كما هو حرفياً**.
2. **لا يدّعي أن الإتاحة جزء أصليّ من ميثاق `R3`.** إلحاقُها بـ`R3` وقع بنصوص **المرتبة 4** لاحقاً، وهذا السند **يقرّ بذلك ولا يصحّحه**.
3. **لا يعيد تعريف `R3`** — لا اسماً ولا هدفاً ولا مخرجاً ولا عمودَ «تغذّي».
4. **لا يُنشئ مسار `R` جديداً** ولا يُرقّم `R12` ولا يفصل شقّ الإتاحة في مسارٍ مسمّى.
5. **لا يضيف شرطاً إلى `OD-FP-0001-FREEZE`** — شروط الرفع تبقى كما هي في `:227` وفي «(د)» من سجل `R3-ACT-01` (`:386`)، **بلا تعديل ولا إعادة عدّ في هذه الجولة**.
6. **لا يحلّ تعارض المرتبتين 2 و4** المرفوع في `:396`–`:399` — التعارض **قائم كما هو**.
7. **لا يرفع التجميد · لا يفكّ حجب `FP-0001` · لا يغلق `R3` · لا يفعّل `R4` · لا يمسّ أي مخرج مجمَّد · لا يعدّل جرد `P3`.**

> **قاعدة قراءة:** «استثناء تنفيذي محدود» تعني **إذناً بالاستمرار في عملٍ جارٍ تحت تعارضٍ معلوم**، ولا تعني **صحّةَ الإسناد** ولا **سكوتاً عن التعارض** ولا **سبقاً حوكمياً** يُستشهد به لتوسيع أي نطاق آخر.

### (ج) تطبيق مختصر — توسّع Enterprise UI Governance لا يوسّع شروط الرفع

```text
Expansion of Enterprise UI Governance does not automatically expand the conditions
for lifting OD-FP-0001-FREEZE. Any new lift condition requires an explicit Owner
decision that amends the freeze record itself.
```

**وبالعربية:** كلُّ ما يُضاف إلى حوكمة واجهة المؤسسة — عقوداً أو حقولاً أو قواعد أسطح أو أنماط حالات — **لا يصير شرطَ رفعٍ بمجرّد إضافته**. شروط رفع `OD-FP-0001-FREEZE` **مغلقة على المنصوص عليه** في `:227` وتفصيلِه في `:386`؛ وأيّ شرط جديد **يحتاج قرار مالك صريحاً يعدّل سجل التجميد نفسه** (‏`:209`..`:228`). **ولم يُعدَّل شيء منها في هذه الجولة.**

**قيد عام على هذا القسم:** لا يُعيد ترقيم أي قرار قائم · لا يغيّر حالة أي `OD` آخر عدا تسجيل هذا السند · لا يُنشئ ADR · لا يُعدّل `AUTHORITY.md` ولا `methodology/**` ولا `catalogs/**` ولا `decisions/adr/**` · لا يمسّ أي مخرج مجمَّد ولا `traceability/**` · لا يُنشئ طبقة سلطة جديدة · ولا يمنح أي وثيقة غير حاكمة سلطةً.

## سجل النقل الحاكم — قرارات واجهة `R3`: `OD-R3-UI-1` · `OD-R3-UI-2` · `OD-R3-UI-3` (‏Δ 2026-08-02 · R3)

> **السند:** أمر المالك «‏R3 — Governing Transfer Preparation» (2026-08-02) · و`OD-R3-SCOPE-1` أعلاه · و`handoff/handoff.md` → **H-0028**.
> **وظيفة هذا السجل:** **تسجيلُ النقل وحدَه** — ماذا نُقل، من أين، إلى أي وعاء، وبأي دليل. **ولا يعيد كتابة نصوص القرارات** — نصوصها النافذة في أوعيتها المذكورة أدناه.

### (أ) المصدر التاريخي غير الحاكم

**`GitHub Issue #11` — «‏R3 Working Decisions — Owner Approved, Not Yet Governing»** (‏<https://github.com/sa4devops/local-rag-enterprise-specs/issues/11>).

**حالته الحاكمة: مصدر تاريخي دائم غير حاكم.** يُقرأ **دليلاً على أصل القرار وتاريخه**، **ولا يُقرأ وعاءً** ولا يُستشهد به سنداً تنفيذياً بدل الوعاء. وهذا مطابق لقرار المالك الخامس في `OD-P3-CLOSURE` (‏`:435`): **`Issue #11` خارج نطاق `P3`**. **ولم يُمسّ `Issue #11` في هذه الجولة** — لا تعديل ولا إغلاق ولا تعليق.

**بصمات التعليقات الثلاثة عند النقل** — `SHA-256` محسوبة على **نصّ جسم التعليق كما تُرجعه `GitHub REST API` (`.body`) بلا محرف سطر مُضاف**:

| القرار | `Comment ID` | `created_at` | `updated_at` | `SHA-256` |
|---|---|---|---|---|
| **`OD-R3-UI-1`** | `5156528819` | `2026-08-02T08:26:18Z` | `2026-08-02T08:26:18Z` | `df92080cad347046ce935bf30cc5f08e2cd5bdab976ba8f27e5a3bdcccbeb843` |
| **`OD-R3-UI-2`** | `5156530178` | `2026-08-02T08:26:33Z` | `2026-08-02T08:26:33Z` | `e79b5ab4f69f7c9a7c1244bc452e89981a2616be6bf0e0ec345ff90cc1d31e9d` |
| **`OD-R3-UI-3`** | `5156531912` | `2026-08-02T08:26:51Z` | `2026-08-02T08:26:51Z` | `78ec5c6a19daec7a22ce48354fed479f8c7d47448fdf688dad8a4827eb160edd` |

**`updated_at` = `created_at` في الثلاثة** ⇒ **لا تعديل بعد اعتماد المالك**. وأُعيد حساب البصمات **مباشرةً قبل تنفيذ النقل** فطابقت بصمات بداية الجولة **بايتاً ببايت** ⇒ **لا تغيير في المصدر أثناء التنفيذ**.

### (ب) الأوعية التي أُعدّت لاستقبال القرارات

| القرار | الوعاء الحاكم | الموضع |
|---|---|---|
| **`OD-R3-UI-1`** — تفكيك حالة التعطيل | **`ui/UI_COMPONENT_STATES.md`** | «‏Δ v1.2 — تفكيك حالة التعطيل: أنماط `S8` الإلزامية» |
| **`OD-R3-UI-2`** — إغلاق تعداد أنواع الأسطح | **`contracts/screens/SCREEN_CONTRACT_TEMPLATE.md`** | **§3.1** (ضمن قسم §3 الجديد) |
| **`OD-R3-UI-3`** — الاسم الدلالي المحكوم مقابل النص الظاهر | **المبدأ:** `ui/UI_FIELD_NAMING.md` («‏Δ v1.2») · **الحقول:** `contracts/screens/SCREEN_CONTRACT_TEMPLATE.md` **§3.2** و**§3.3** | كما هو مبيَّن |

**وسندُ الاستمرار** الذي أذن بهذا العمل مسجَّل في **هذا الملف** → **`OD-R3-SCOPE-1`**.

### (ج) `commit` SHAs — الفرع `docs/r3-governing-transfer` (من `origin/main` = `04bdbb32ab610b4f33df7332fe010da6f099ef2c`)

| # | `commit` | الأب | الرسالة | الملفات |
|---|---|---|---|---|
| **1** | **`5eed4b52eb1e1d379c3cabd773331ac9aed7744a`** | `04bdbb32ab610b4f33df7332fe010da6f099ef2c` | `docs(r3): record bounded accessibility sub-track authorization` | `decisions/open-decisions.md` · `INDEX.md` |
| **2** | **`826a51eac971e379b065cba8f21caa1d023c34c1`** | `5eed4b52eb1e1d379c3cabd773331ac9aed7744a` | `docs(ui): transfer approved surface, naming, and disabled-state rules` | `ui/UI_COMPONENT_STATES.md` · `ui/UI_FIELD_NAMING.md` · `contracts/screens/SCREEN_CONTRACT_TEMPLATE.md` · `INDEX.md` |
| **3** | **هذا الـcommit** — أبوه `826a51eac971e379b065cba8f21caa1d023c34c1` | `826a51eac971e379b065cba8f21caa1d023c34c1` | `docs(r3): record governing-transfer preparation and handoff` | `decisions/open-decisions.md` · `handoff/handoff.md` · `INDEX.md` |

**كل commit بأبٍ واحد · صفر merge commit · لا rebase ولا amend ولا force-push ولا وسم ولا إصدار.** وSHA الـcommit الثالث مثبَّت في `handoff/handoff.md` → **H-0028** بعد الدفع، وفي وصف الـPR.

### (د) حالة النقل الحالية

```text
OWNER APPROVED
DURABLY RECORDED
GOVERNING TRANSFER PREPARED
EFFECTIVE UPON MERGE
```

**قاعدة لفظية ملزمة:** **لا تُستعمل الصفة `TRANSFERRED` قبل الدمج.** ما دام الـPR غير مدموج، فالنقل **مُعدٌّ لا واقع**، والأوعية الثلاثة **لا تنفذ** — وسلطتها تبدأ **عند الدمج في `main`** حصراً.

### (هـ) ما لا يفعله هذا النقل

**لا يقنّن** الدلتا السبعة (‏`RAR-003 [38] [40] [42] [43] [44] [46] [48]` — تبقى **`NOT REMEDIATED`**) · **لا معيار `PASS`** ولا تغيير في حالة `R3-A11Y-01`..`07` · **لا يبني اختباراً** ولا يعدّل `traceability/r3-accessibility/**` · **لا يمسّ** `ui/UI_DESIGN_SYSTEM.md` ولا `ui/UI_ACTION_BUTTON_MODEL.md` ولا `methodology/**` ولا `AUTHORITY.md` ولا `constitution.md` ولا `decisions/adr/**` ولا `catalogs/**` ولا `traceability/**` · **لا يرفع** `OD-FP-0001-FREEZE` (يبقى **`ACTIVE`**) ولا يفكّ حجب **`FP-0001`** (يبقى **`BLOCKED`**) · **لا يضيف شرط رفعٍ** ولا يعدّل الشروط القائمة ولا يعيد عدّها · **لا يغلق `R3`** (تبقى **`ACTIVE — SPECIFICATION ONLY`**) · **لا يفعّل `R4`** · **لا يعدّل جرد `P3`** (**`28 = 13 + 15 + 0`** · `UNCLASSIFIED = 0`) ولا يُنشئ `P3-CI-*` · **لا يحسم تعارض المرتبتين 2 و4** · **لا merge ولا tag ولا release ولا إغلاق `Issue`**.

---

## سجل تقنين معايير الإتاحة السبعة — `ui/UI_DESIGN_SYSTEM.md` §9 (‏Δ 2026-08-02 · R3)

> **السند:** **أمر مالك صريح** صادر 2026-08-02 — «‏R3 Accessibility §9 Codification» — جولة تنفيذ حوكمي محدودة غايتها **تقنين** المعايير السبعة في وعائها الحاكم. وهذا **الوعاء الحاكم للقرار** بمقتضى `methodology/PHASE_EXECUTION_STANDARD.md` §3.4 («القرارات المفتوحة وقرارات المالك ⇒ `decisions/open-decisions.md`») و`AUTHORITY.md:13` (المرتبة **4**)، ويرافقه `handoff/handoff.md` → **H-0031** (المرتبة 9).
> **حدّ وظيفي:** **الوعاء المحدَّد للقواعد نفسها هو `ui/UI_DESIGN_SYSTEM.md` §9** (منظومة `ui/**` — المرتبة 8). وهذا السجل **يسجّل واقعة التقنين وأثرها على شروط الرفع فقط** — **لا يكرّر نصّ القواعد**، ولا يُنشئ سلطة موازية، ولا يُقرأ بديلاً عن §9.
> **قيد سلطوي دقيق (‏C2):** **عند الدمج يُنشر نصّ §9 المقنَّن داخل الوعاء المحدَّد له، ويصير المرجعَ المنشور للقواعد السبع ضمن حالة الملف الحالية `Proposed`.** أمّا **ترقية حالة `ui/UI_DESIGN_SYSTEM.md`** والأثرُ السلطوي النهائي المرتبط بها **فتحتاج قرار مالك مستقلاً** — **لا يُنشأ هنا ولا يُحسم**. **ولا يُقرأ الدمجُ وحده ترقيةً لحالة الملف.**
> **قاعدة مصدر غير حاكم:** `traceability/r3-accessibility/**` **مخرَج تحليلي غير حاكم**؛ استُعمل **استخراجاً وربطاً تاريخياً** لا سنداً. **ولم تُنقل سلطته ولم يرتفع تصنيفه ولم يُمسّ في هذه الجولة.**

### (أ) سجل الواقعة — `OD-R3-CODIFY-1`

| الحقل | القيمة |
|---|---|
| **المعرّف** | **`OD-R3-CODIFY-1`** — تقنين معايير الإتاحة السبعة في الوعاء المحدَّد لها |
| **الموضوع** | تقنين **`R3-A11Y-01`..`R3-A11Y-07`** في `ui/UI_DESIGN_SYSTEM.md` **§9** بصيغة **معيارية قابلة للاختبار وقابلة للفشل** — لكل قاعدة: معرّف · متطلب معياري · نطاق · سلوك مطلوب · سلوك محظور · شرط نجاح · شرط فشل · أسطح ومكوّنات منطبِقة · دليل مطلوب · استثناء · إحالات |
| **السلطة** | **Owner (‏SA)** — أمر مالك صريح 2026-08-02 · **Specification Architect** — الصياغة بتفويض |
| **الوعاء المحدَّد للقواعد** | **`ui/UI_DESIGN_SYSTEM.md` §9** — `v1.0` ⇒ **`v1.1`**. **حالة الملف تبقى `Proposed` ولم تُغيَّر في هذه الجولة**؛ وعند الدمج يصير §9 **المرجعَ المنشور للقواعد السبع ضمن تلك الحالة**، **والترقية قرارُ مالك مستقل** |
| **نقطة الأساس** | **`origin/main` = `765c4504a8e813e031ae0c9fb8b5eaad95f85da2`** (‏merge PR #13) |
| **الحالة** | **`CODIFIED — EFFECTIVE UPON MERGE`** — ‏§9 **لا تنفذ** حتى الدمج في `main`؛ **ولا تُستعمل صفة `CODIFIED` بلا قيد قبل الدمج** |
| **أثر التقنين على حالة المعايير** | حالتها المسجَّلة **`SPECIFIED — NOT CODIFIED — NOT REMEDIATED — NOT TESTED`** تصير **عند الدمج** ⇒ **`SPECIFIED — CODIFIED — NOT REMEDIATED — NOT TESTED`**. **ولا معيار `PASS`** — التقنين إنشاءُ قاعدةٍ قابلة للقياس لا نتيجةُ قياس |
| **حكم على السجلات السابقة** | **لم يُعدَّل سجل `R3-ACT-01`** ولا جدولُ حالة المعايير فيه. وما ورد هناك من **`NOT CODIFIED`** **صحيحٌ في تاريخه**، ويصير **`HISTORICAL / STALE EVIDENCE` عند دمج هذه الجولة** — وهو **الوسمُ المطلوب في `AUTHORITY.md` §2 القاعدة 2** لحسم التعارض داخل المرتبة الواحدة بدفعة موثقة، **مقصوراً على هذا الحقل وحده لا على السجل كلّه** (فلا يُوسَم `R3-ACT-01` كلُّه `Superseded`)؛ **والحالة النافذة بعدئذٍ هي المسجَّلة في هذا السجل**. **ولا يُعاد كتابة سجل سابق** — قاعدة السجل المتجدد. **والحكم الكامل للاستثناء التنفيذي المحدود في «(هـ)» أدناه** |
| **البنود الستة القائمة في §9** | **محفوظة نافذة** (تباين AA · حلقة تركيز 2px · تنقّل لوحة مفاتيح كامل · إعلان الحالة والشارات نصاً · أهداف لمس ≥40px · لا معنى باللون وحده) — نُقلت إلى **§9.1** بشرطَي نجاح وفشل، **بلا حذف ولا إضعاف**، **ولا تُستعمل بديلاً** عن السبعة |
| **`FP-0001`** | **`BLOCKED`** — بلا تغيير |
| **`OD-FP-0001-FREEZE`** | **`ACTIVE`** — بلا تغيير · **ولا شرط رفعٍ أُضيف ولا عُدِّل ولا أُعيد عدُّه** |
| **`R3`** | **لم تُغلق · ولم يُعَد تعريفها · ولم تُوسَّع ميثاقها.** و**حالتها المسجَّلة في `R3-ACT-01` (`ACTIVE — SPECIFICATION ONLY`) لم تُعدَّل**؛ و**`R3-ACT-01` كان صحيحاً عند صدوره**. وقد منح **أمرُ المالك اللاحق الخاص بتقنين §9** **استثناءً تنفيذياً محدوداً ومؤرَّخاً** من قيوده الخاصة بـ`ui/UI_DESIGN_SYSTEM.md` — **والحكم كاملاً في «(هـ)» أدناه، وبه أُغلق التوتّر فلم يعد مسألةً معلَّقة**. ويبقى **تعارض ميثاق `R3`** (المرتبتان 2 و4) **`AUTHORITY CONFLICT — SEPARATE OWNER ACTION`** — وهو **غير** مسألة الاستثناء |
| **`R4`** | **غير مفعَّلة** — بلا تغيير |
| **جرد `P3`** | **`28 = 13 COMPLETE + 15 GOVERNED TRANSFER + 0 BLOCKING REMAINDER`** · **`UNCLASSIFIED = 0`** — **بلا مساس**، و`P3-CLOSURE-INVENTORY.md` **لم يُمسّ**، ولم يُنشأ `P3-CI-*` جديد |
| **`P3-CI-02` و`P3-CI-26`** | **`GOVERNED TRANSFER` — بلا تغيير.** التقنين شرطٌ واحد من التزامهما؛ **والالتزام لا يكتمل بالتقنين وحده** (المعالجة والاختبارات والأدلة باقية) ⇒ **لا مبرّر لتعديل صفَّيهما** |

### (ب) أثر التقنين على شروط رفع `OD-FP-0001-FREEZE` — تسجيلٌ بلا تعديل

**قاعدة قاطعة:** الشروط تبقى **كما هي نصاً وعدداً** في هذا الملف (سجل `OD-FP-0001-FREEZE` — الشروط السبعة) وتفصيلِها في سجل `R3-ACT-01` (الشروط الاثنتا عشرة). **ولم يُضَف شرط، ولم يُعدَّل شرط، ولم يُعَد العدّ.**

| الشرط | الحالة بعد هذه الجولة |
|---|---|
| **(1)** تقنين المعايير السبعة في وعائها الحاكم بصيغة **قابلة للاختبار** | **الواقعة المسجَّلة:** التقنين وقع في وعائه الحاكم (`ui/UI_DESIGN_SYSTEM.md` §9 `v1.1`)، **ولا ينفذ قبل الدمج**. **وتقديرُ استيفاء الشرط يعود إلى المالك** بالأدلة الحاكمة — **ولا تُقرّر هذه الجولة استيفاءَ شرطِ رفعٍ لنفسها** |
| **(2)** إدخال المعايير والدلتا في **Reading Sets** | **مستوفى تشغيلياً** سابقاً بإغلاق `OD-GOV-3`؛ **ويجب أن تشمل المجموعةُ §9 المقنَّنة بعد الدمج** — وهذا **تفصيلٌ قائم** في الشرط الثالث من سجل `R3-ACT-01`، **لا شرط جديد** |
| **(3)** تنفيذ فحوص بوابة `FP-0001` (الخطوة 1 verify · الخطوة 5 وصولية) | **غير مستوفى** |
| **(4)** أدلة نجاح **قابلة للتدقيق** | **غير مستوفى** — لم يُنتَج دليل قياس واحد في هذه الجولة |
| **(5)** تحديث حالة سجل التجميد من **`ACTIVE`** إلى **`LIFTED`** | **غير مستوفى — ومحظور في هذه الجولة** |
| **(6)** ربط الرفع بالأدلة والـ`commit` والاختبارات | **غير مستوفى** |
| **(7)** تحديث `P3 Closure Inventory` | **غير مستوفى — ولم يُمسّ** |

**ومن الاثنتي عشرة المفصَّلة في `R3-ACT-01`:** الشرطان **(2) التقنين** و**(3) Reading Sets** حالتُهما مبيَّنة في الجدول أعلاه فلا تُعادان هنا؛ **وتبقى غير مستوفاة:** اعتماد المعايير السبعة كاملةً من المالك · **تنفيذ المعالجة** على الشاشات والمكوّنات · **نجاح** الاختبارات الآلية · **نجاح** الاختبارات اليدوية · تغطية مسارات الواجهات **الديناميكية والمُعانة بالـAI** · تغطية **`ar`/`en`** و**RTL/LTR** · تغطية **الوضعين الفاتح/الداكن** حيث ينطبق (وقبلها حسم تعارض الوضع الداكن) · فحوص البوابة · **الأدلة المؤرشفة** · **صفر `BLOCKING REMAINDER` إتاحة**.

**الخلاصة:** **التقنين شرطٌ واحد من شروطٍ تراكمية** — **ولا يرفع التجميد وحده**، ولا يُقرأ إذناً بالمعالجة ولا بإعلان جاهزية. **ويُلاحَظ ترتيبياً** أن الشرط «اعتماد المعايير السبعة من المالك» ما زال **غير مستوفى** بينما صارت قواعدُها **نافذةً عند الدمج**؛ **وهذا مقصود**: نفاذُ القاعدة يوجب الالتزام بها، **ولا يُغني عن اعتماد المالك** لها بوصفه شرطَ رفعٍ مستقلاً.

### (ج) ما لا يفعله هذا السجل

**لا يرفع** `OD-FP-0001-FREEZE` (يبقى **`ACTIVE`**) · **لا يفكّ حجب `FP-0001`** (يبقى **`BLOCKED`**) · **لا يعلن أي معيار `PASS`** · **لا ينفّذ معالجة** ولا يعدّل كود تطبيق أو مكوّن UI · **لا يبني اختباراً ولا ينفّذه** · **لا يضيف شرط رفعٍ ولا يعدّل الشروط القائمة ولا يعيد عدّها** · **لا يغلق `R3`** ولا يعيد تعريفها ولا يوسّع ميثاقها · **لا يفعّل `R4`** · **لا يعدّل جرد `P3`** ولا يُنشئ `P3-CI-*` · **لا يحسم** تعارض ميثاق `R3` (المرتبتان 2 و4) ولا تعارض الوضع الداكن ولا `GOV-OBS-03` ولا `P3-CI-28` ولا `OD-GOV-1/2` ولا `OD-P3-2` · **لا يرفع سلطة `traceability/**`** ولا ينقل محتواه حاكماً تلقائياً · **لا ينشئ عقد حقلٍ** ولا نوع سطحٍ ولا حالة `S` ولا حقلَ عقد · **لا ينشئ `UI-4` ولا `UI-5`** ولا يمسّ Rocket ولا Visual Baseline · **لا merge ولا rebase ولا amend ولا force-push ولا وسم ولا إصدار**.

### (د) فجوات إنفاذٍ ومسائل مسجَّلة — ظهرت أثناء التقنين ولا تُحسم هنا

> **تنبيه ملزم:** **تسجيلُ فجوةٍ ليس إضافةَ شرط رفعٍ.** شروط رفع `OD-FP-0001-FREEZE` **مغلقة على المنصوص عليه** في سجل التجميد وتفصيلِه في `R3-ACT-01`؛ وأيّ شرط جديد **يحتاج قرار مالك صريحاً يعدّل سجل التجميد نفسه**.

| # | الفجوة | الأثر على إنفاذ §9 | الوجهة |
|---|---|---|---|
| **1** | **لا يوجد عقد حقلٍ** — `contracts/fields/` مؤجَّل والمجلد غير موجود | `R3-A11Y-03` و`R3-A11Y-07` (م) **يفشلان مغلقاً** على المسار المولَّد من الميتاداتا حتى إنشائه — وهذا **أثرٌ مقصود** لا عيب في القاعدة | **أمر مالك مستقل** — يمسّ المرتبة 5 |
| **2** | `ui/UI_GATE_REVIEW_CHECKLIST.md` **بلا بند إتاحة** | §9 مقنَّنة **بلا نقطة إنفاذ** عند تسليم الواجهة | أمر مستقل — خارج نطاق هذه الجولة |
| **3** | `methodology/testing-strategy.md` **لا يعرّف نوع اختبار إتاحة** بينما الخطوة 5 من بوابة الأساس توجب اختبار الوصولية | لا تعريف لنوع الدليل الآلي المطلوب | أمر مستقل — `methodology/**` محظورة هنا |
| **4** | `contracts/screens/SCREEN_CONTRACT_TEMPLATE.md` §8 **بلا حدٍّ أدنى للإتاحة** | إعلان الاستثناءات وقيم `focus_return_target` مطلوبٌ من العقد **بلا بند اختبارٍ يقابله** | أمر مستقل — `contracts/**` محظورة هنا |
| **5** | `ui/UI_COMPONENT_STATES.md` **بلا بُعد إتاحة** لكل حالة من `S1`–`S20` | ربط الحالة بسلوك التركيز والإعلان يقع اليوم على §9 وحدها | أمر مستقل |
| **6** | **تعارض الوضع الداكن** — §11 يؤجّله (`OD-UX-3`) بينما نصوصٌ حاكمة توجب لقطتَي/اختبارَي Light/Dark | البعد التاسع من أبعاد الفحص **مشروط** ولا يُقاس قبل الحسم | **قائم قبل هذه الجولة — لا يُحسم هنا** |
| **7** | **ميثاق `R3`** في `methodology/RECONCILIATION_ROADMAP.md` لا يذكر الإتاحة | تعارض مرتبتين قائم | **`AUTHORITY CONFLICT — SEPARATE OWNER ACTION`** |
| **8** | **لا حقلَ عقدٍ يعلن «الترتيب المنطقي المتوقَّع»** لأسطح الشاشة | `R3-A11Y-02` يشتقّ الترتيب من ترتيب المحتوى المعلَن في عقد الشاشة وبطاقة سلوكها ومن ترتيب تعريف الحقول؛ **وحيث لا يكفي المُعلَن لاشتقاق ترتيبٍ وحيد فذلك عيبُ عقدٍ يُصحَّح فيه** (‏§9.3 (أ)) — **ولم يُنشأ حقلٌ هنا** | أمر مستقل — يمسّ المرتبة 5 إن رُئي إنشاء حقل |
| **9** | **`accessible_name_source` حقلُ عقدٍ على مستوى السطح** (`SCREEN_CONTRACT_TEMPLATE.md` §3.2)، بينما `R3-A11Y-03` يطلب إعلان مصدر الاسم **على مستوى العنصر** | §9.4 تستعمل **المفردات نفسها وصفاً** ولا تُنشئ حقلاً؛ وحبيبةُ العنصر تحتاج **توسيعاً مخوَّلاً للمخطط** | أمر مستقل — يمسّ المرتبة 5 |
| **10** | **حالة `ui/UI_DESIGN_SYSTEM.md` في ترويسته `Proposed`** بينما `ui/UI_SCREEN_GOVERNANCE_STANDARD.md` §(أ) يصنّفه **ملزم-معياري** | **تعارضٌ قائم قبل هذه الجولة ولم تُحدثه**؛ ولم تُغيَّر الحالة هنا — وترقيتها إلى `Accepted` **قرار مالك مستقل** | أمر مستقل — لا يُرقّى مستوى ملفٍّ صامتاً |
| **11** | **لا حماية آلية لمسارات المستودع** — لا `CODEOWNERS` ولا `Branch Protection` ولا `CI` تمنع تعديلاً عارضاً خارج نطاق الدفعة؛ والاعتماد اليوم على **مراجعة الـ`diff` البشرية** | **واقعة مثبتة في هذه الجولة** (‏«(و)» أدناه): تسرَّب تعديل `replace_all` إلى **§4** خارج النطاق، **فكُشف قبل الـcommit وأُعيد §4 مطابقاً لـ`origin/main`**. **الضمانة الحالية بشرية لا آلية** | **المسار المخطط القائم** (‏`G2` · حماية الفروع · `CODEOWNERS` · `CI`) — **خارج هذه الجولة**؛ **ولا يُنشأ هنا أمرُ `CI` ولا حمايةُ فروع، ولا يصير شرطَ رفعٍ ولا شرطاً سابقاً للدمج** |

### (هـ) حكم الاستثناء التنفيذي المحدود — إغلاق التوتّر مع `R3-ACT-01`

> **السند:** **أمر مالك صريح لاحق** — «‏OWNER CORRECTION ORDER — PR #14 Pre-Merge Reconciliation» (2026-08-03). **وهو نفسه قرارُ المالك المطلوب لتوثيق الاستثناء** ⇒ **لا يلزم `Decision Request` جديد** ولا `ADR` ولا `OD` جديد.
> **وظيفة هذا القسم:** **حسمُ** ما كان مسجَّلاً توتّراً غير محسوم بين `R3-ACT-01` وأمر تقنين §9 — **فلا يبقى معلَّقاً**.

| # | الحكم |
|---|---|
| **1** | **`R3-ACT-01` كان صحيحاً عند صدوره.** نصُّه أن `ui/UI_DESIGN_SYSTEM.md` **«لم يُمسّ»** وأن تعديله **محظور** كان صحيحاً **في نطاق جولة التفعيل** (تخصيص ومعايير قبول فقط) — **ولم يكن خطأً ولا يُعاد كتابته**. |
| **2** | **أمر المالك اللاحق الخاص بتقنين §9 منح استثناءً تنفيذياً محدوداً ومؤرَّخاً** من ذلك القيد. |
| **3** | **نطاق الاستثناء — مغلق:** **تقنين §9 وحده**، وفي **الملفات المخوَّلة في PR #14 وحدها**: `ui/UI_DESIGN_SYSTEM.md` · `decisions/open-decisions.md` · `handoff/handoff.md` · `INDEX.md`. |
| **4** | **لا يُعدّ `R3-ACT-01` كلُّه `Superseded`** ولا يُلغى. **وبقية قيوده تظل نافذة كاملةً** — ومنها حظر المعالجة وبناء الاختبارات وإعلان `PASS` ورفع التجميد وفكّ حجب `FP-0001` وإغلاق `R3` وتفعيل `R4`. |
| **5** | **الأثر النصّي المحصور:** العبارات — في `R3-ACT-01` وفي مداخل `handoff` السابقة — التي **تمنع تعديل `ui/UI_DESIGN_SYSTEM.md`** أو تقرّر أنه **«لم يُمسّ»** تصير **`HISTORICAL / STALE EVIDENCE`** **عند الدمج**، و**بالنسبة لهذه الجولة المحدَّدة وحدها**؛ **وتبقى نافذةً على كل ما عداها**. **ولم يُعدَّل سجل سابق ولا مدخل `handoff` سابق.** |
| **6** | **لا يعيد تعريف `R3`** · **لا يوسّع ميثاقها** · **لا يُنشئ مسار `R` جديداً**. |
| **7** | **لا يأذن بأي تعديل آخر في `ui/**`** — وما عدا `ui/UI_DESIGN_SYSTEM.md` §9 يبقى محظوراً. |
| **8** | **لا يغيّر التجميد** (`OD-FP-0001-FREEZE` = **`ACTIVE`**) · **لا يفكّ حجب `FP-0001`** (**`BLOCKED`**) · **لا يفعّل `R4`** · **لا يضيف شرط رفعٍ**. |
| **9** | **لا يفتح `UI-4` ولا `UI-5`** ولا يمسّ Screen Contracts ولا Rocket ولا Visual Baseline. |
| **10** | **لا يرقّي حالة `ui/UI_DESIGN_SYSTEM.md`** من `Proposed` — الترقية **قرار مالك مستقل** (الفجوة 10 في «(د)» تبقى مفتوحة). |

⇒ **التوتّر مغلق.** وقد **حُذف** من قائمة الفجوات في «(د)»، ولم يعد يُقرأ مسألةً معلَّقة. **وما يبقى مفتوحاً هو تعارض ميثاق `R3`** في `methodology/RECONCILIATION_ROADMAP.md` (الفجوة 7) — **وهو مسألةٌ أخرى غير الاستثناء**، ويظل **`AUTHORITY CONFLICT — SEPARATE OWNER ACTION`**.

### (و) واقعة تنفيذية مسجَّلة — تسرّب `replace_all` إلى §4

**ما وقع:** أثناء تصحيح إحالةٍ داخل §9، طُبِّق استبدالٌ شاملٌ (`replace_all`) على سلسلة نصية موجودة **أيضاً** في **§4** — فتغيّر سطرُ «الأزرار» في §4 **خارج نطاق الجولة**.

**ما جرى بعده:** كُشف التسرّب **بفحص شكل الـ`diff`** (عدّ الأسطر المحذوفة ومطابقة الأقسام مع `origin/main`) **قبل الـ`commit`**، وأُعيد سطرُ §4 **مطابقاً لـ`origin/main`**، ثم أُثبت بمقارنةٍ سطراً-بسطر أن الملف **لا يختلف عن `origin/main` إلا في سطر الترويسة و§9**.

**دلالة الواقعة — تُسجَّل ولا تُوسَّع:**
1. **التعديل غير المقصود وقع فعلاً** — ولا يُقلَّل من شأنه.
2. **اكتُشف قبل الـ`commit`** ولم يدخل التاريخ.
3. **أُعيدت §4 إلى مطابقة `origin/main`** — مثبَتٌ بالفحص.
4. **الواقعة دليلٌ على فجوة قائمة** (الفجوة 11 في «(د)»): **الاعتماد الحالي على مراجعة الـ`diff` البشرية** بلا حماية آلية للمسارات.
5. **معالجة الحماية الآلية تبقى ضمن مسارها المخطط** (‏`G2` · حماية الفروع · `CODEOWNERS` · `CI`) — **خارج هذه الجولة**.

**وما لا تفعله هذه الواقعة:** **لا تُنشئ شرط رفعٍ جديداً** · **ولا شرطاً سابقاً للدمج** · **ولا أمرَ إنشاء `CI`** · **ولا أمرَ حمايةِ فروع أو `CODEOWNERS`** · **ولا توسّعاً إلى `G2`**. وهي **تسجيلٌ للأثر** لا إذنٌ بعمل.

## سجل تعديل حوكمة الدمج — `OD-GOV-MERGE-1` (‏Δ 2026-08-03 · أمر مالك)

> **السند:** **أمر مالك صريح** صادر 2026-08-03 — «‏**OWNER ORDER — Agent-Authorized Merge Governance Amendment**» (‏Owner-Authorized Independent Merge Agent).
> **الوعاء:** هذا القسم هو **الوعاء الحاكم** للقرار بمقتضى `methodology/PHASE_EXECUTION_STANDARD.md` §3.2 («لا قرار يعيش في تقرير أو محادثة فقط. التقرير إحالة، لا وعاء») و`AUTHORITY.md:13` (المرتبة 4). **والوعاء المعياري للقاعدة نفسها** هو `decisions/adr/ADR-0033-automation-levels-git-merge-policy.md` **بنود 8–14** (المرتبة 4) و`methodology/agent-execution-model.md` **§18** (المرتبة 2).
> **الحالة:** **`AMENDED — EFFECTIVE UPON MERGE`** — ولا تُقرأ نافذةً قبل الدمج في `main`.
> **النطاق:** **سياسة الدمج وفصل الأدوار وحدهما.** **لا يمسّ** `constitution.md` ولا `AUTHORITY.md` ولا `contracts/**` ولا `ui/**` ولا `traceability/**` ولا `phases/**` ولا `catalogs/**`.
> **خط الأساس:** ‏`origin/main` = **`765c4504a8e813e031ae0c9fb8b5eaad95f85da2`**.

### (أ) سجل القرار — `OD-GOV-MERGE-1`

| الحقل | القيمة |
|---|---|
| **المعرّف** | `OD-GOV-MERGE-1` |
| **الموضوع** | استبدال قاعدة «الدمج البشري بواسطة SA حصراً» بنموذج **Owner-Authorized Independent Merge Agent** — مع **تشديد** فصل الأدوار لا تخفيفه |
| **مصدر السلطة** | **أمر المالك** 2026-08-03 · و**قرار الدمج يبقى للمالك وحده** في كل الأحوال |
| **الأوعية المعيارية** | ‏ADR-0033 بنود **8–14** + الاستثناء الانتقالي · `agent-execution-model.md` **§18** |
| **الحالة** | **`AMENDED — EFFECTIVE UPON MERGE`** |
| **الفئة** | **5 — Configurable** (سياسة تشغيل حوكمي؛ لا تحجب تصميماً ولا تنفيذاً) |

### (ب) القاعدة القديمة والقاعدة الجديدة — مقابلة نصية

| # | **القاعدة القديمة (`SUPERSEDED — 2026-08-03`)** | **القاعدة الجديدة (النافذة عند الدمج)** |
|---|---|---|
| **1** | ‏ADR-0033-1: «‏L1 … **والدمج بشري (SA)**» | ‏L1: المنفِّذ **لا يدمج عمله بنفسه**؛ والتنفيذ بشري **أو** بـMerge Agent مفوَّض |
| **2** | ‏ADR-0033-3: «المسارات المحمية — **دمج بشري دائماً في L≤2**» | المسارات المحمية: **لا auto-merge غير مشروط أبداً**؛ ‏Human Manual Merge **أو** Owner-Authorized Agent Merge بشروط بند 10 مجتمعةً |
| **3** | ‏`agent-execution-model` §2-7: «الدمج/الرفع **بيد SA حصراً**» | **قرار** الدمج للمالك؛ و**تنفيذه** وفق §18 |
| **4** | ‏`agent-execution-model` §8: «الدمج والوسوم تبقى **بيد SA في كل مستوى دون L3**» | الدمج: كما أعلاه · **والوسوم والإصدارات لم تُمسّ** وتبقى كما هي |
| **5** | ‏`agent-execution-model` §14: «‏L1 … **والدمج بشري**» | المستوى يحكم **الأتمتة غير المشروطة** لا **هوية المنفِّذ** |
| **6** | ‏ADR-0033-7: «المنفذ لا يدمج عمله بنفسه؛ المراجعة بجلسة/وكيل مستقل» | **`RETAINED + EXTENDED`** — **نافذ ومُشدَّد** بالبنود 8–14 و§18؛ **لم يُضعَّف** |

**قاعدة حفظ:** النصوص القديمة **محفوظة حرفياً** في ADR-0033 (جدول «النصوص المستبدَلة») — **ولم يُحذف قرارٌ ولا سجلٌ سابق**، ولا يُعاد كتابة أيّ `handoff` أو سجلٍّ استند إليها.

### (ج) جدول الأدوار — تسجيل حاكم

| الدور | يفعل | **لا يفعل** |
|---|---|---|
| **Execution Agent** | يكتب المحتوى · يدفع الـcommits · يفتح الـPR · يشغّل البوابات | **لا يدمج PR أنشأه أو عدّل محتواه** · لا يوافق على عمله |
| **Review Agent** | يراجع مستقلاً من المستودع والـPR **مباشرةً** · يثبّت النتيجة نصاً قبل الدمج | **لا يعدّل الـPR نفسه** إلا بجولة تصحيح منفصلة مُعلَنة (فيصير كاتب محتوى ويفقد استقلاله لتلك الدفعة) · لا يدمج |
| **Merge Agent** | جلسة مستقلة · يقرأ المستودع والـPR مباشرةً · يتحقق · **يرفض** عند التخلّف · ينفّذ أمر المالك · يسجّل الأثر | **لا يكتب المحتوى ولا يعدّله ولا يدفع إلى فرعه** · **لا يوافق لنفسه** · **لا يتجاوز الـSHA المثبَّت** · لا يخفّف بوابة · لا يحسم تعارضاً سلطوياً بالافتراض · لا يوسّع الأمر إلى PR آخر |
| **Owner** | يصدر قرار القبول/الرفض · يصدر أمر التفويض · يحسم التعارض السلطوي | — |

### (د) شروط `Owner-Authorized Agent Merge` — **السبعة** مجتمعةً (‏AND) لا تخييراً

> **مطابقة العدّ:** هذه الشروط السبعة **مطابقة نصاً وترتيباً** لـ**ADR-0033 بند 10 `(أ)`..`(ز)`** — الوعاء المعياري. ‏`1`=`(أ)` · `2`=`(ب)` · `3`=`(ج)` · `4`=`(د)` · `5`=`(هـ)` · `6`=`(و)` · `7`=`(ز)`. **ولا يوجد تعدادٌ ثالثٌ مختلف في أي وعاء.**

1. **أمر مالك صريح لهذا الـPR بعينه** بحقوله السبعة: `repository` · `PR number` · **`expected head SHA`** · `target branch` · `merge method` · نطاق الملفات/العمل · القيود الخاصة.
2. **وكيل دمج مستقل:** جلسة مستقلة · لم يكتب المحتوى · لم يدفع commits إلى الفرع · يقرأ المستودع والـPR **مباشرةً** لا من تقرير.
3. **مراجعة مستقلة موثقة** — و**المراجعة الذاتية ولو بوكيل مراجعة منفصل داخل الجلسة نفسها لا تستوفيها**، وتُوصف بما هي: `Structured self-review`.
4. **تثبيت الرأس:** ‏`head SHA` الفعلي **مطابقٌ حرفياً** للمثبَّت في الأمر؛ وتغيّره ⇒ **توقّف** وأمرُ مالكٍ جديد أو تحديثٌ صريح للتفويض.
5. **نجاح البوابات المطلوبة** بأدلة قابلة لإعادة الإنتاج.
6. **لا تعارض سلطوي غير محسوم** يمسّ نطاق الـPR.
7. **تسجيل أثر الدمج بالأبعاد الأربعة** (‏= ADR-0033 بند 10-`(ز)`): ‏Authorization Principal (المالك) · ‏Execution Actor (‏Merge Agent) · ‏Reviewer Identity · ‏Technical Credential — **والحساب التقني ليس دليلاً على تنفيذٍ يدويٍّ من المالك**. **وجودُ التسجيل شرطُ دمجٍ**؛ و**محتواه التفصيلي** — بما فيه `Merge SHA` وخط الأساس الجديد — **في ADR-0033 بند 12** ويُستكمل عقب الدمج مباشرةً.

**تخلّف أيّ شرط ⇒ لا دمج**، ويرفع الوكيلُ الحالةَ للمالك ولا يعالجها بالافتراض.

### (هـ) الاستثناء الانتقالي الضيق — لمرة واحدة (‏نصّ ملزم)

**المشكلة البنيوية المعالَجة:** السياسة **السابقة** تمنع وكيل دمج من تنفيذ دمج الـPR الذي يعدّل سياسة الدمج نفسها — فلا تنفذ الجديدة إلا بدمجٍ تمنعه القديمة.

**الموضوع — مثبَّتٌ حرفياً لا وصفاً عاماً:**

| الحقل | القيمة |
|---|---|
| **Repository** | `sa4devops/local-rag-enterprise-specs` |
| **PR** | **`#15`** — «‏GOV: Owner-Authorized Independent Merge Agent» |
| **Head SHA عند إنشاء الاستثناء** | **`8b741729d50116eebcbdbb25cc79a62cd4fba05d`** |
| **Target branch** | `main` |

**النص:** يجوز **لوكيل دمج مستقل — لم يكتب ولم يعدّل PR #15 ولم يدفع إلى فرعه** — دمجُ **PR #15 وحده** في المستودع أعلاه، بعد **مراجعة مستقلة** و**نجاح البوابات** و**تثبيت Head SHA**.

> **قاعدة الرأس النهائي:** ‏`8b741729…` هو الرأس **عند إنشاء الاستثناء**، **لا رأسُ الدمج النهائي بالضرورة**. وكلُّ تصحيحٍ معتمد على الفرع **يغيّر الرأس** ⇒ **يلزم أمرُ دمجٍ جديد من المالك يثبّت الرأس النهائي الجديد قبل التنفيذ** (‏ADR-0033 بند 11). **ولا يُدمج رأسٌ غير مثبَّت في أمر مالك سارٍ.**

| # | حدّ الاستثناء |
|---|---|
| **1** | **لا يسمح لوكيل التنفيذ بدمج عمله** — الحظر قائم كاملاً |
| **2** | **لا يطبَّق على أي PR آخر** — ومنه **PR #14** صراحةً، و**أيّ PR لاحق يعدّل سياسة الدمج** |
| **3** | **ينتهي فور** أيٍّ مما يلي: **دمج PR #15** · **رفضه أو إغلاقه** · **سحب المالك لأمره** — بلا إجراء إضافي، ولا يُقرأ بعدها سنداً لأي دمج |
| **4** | **مسجَّل هنا في الوعاء الحاكم** — لا يعيش في تقرير ولا محادثة |
| **5** | **لا يُنشئ سابقة عامة** مستقلة عن البنود 8–14 |
| **6** | **لا يخفّف أي شرط** من شروط §(د) السبعة — بل يستوفيها جميعاً |
| **7** | **لا يسري على أي وكيل كتب أو عدّل PR #15 أو دفع إلى فرعه** — ومنه وكيلُ المراجعة إذا نفّذ تصحيحاً على الفرع |

> **قيد سلطوي دقيق:** سندُ هذا الاستثناء هو **أمر المالك نفسه** (2026-08-03) لا النصُّ المدموج — إذ لا يُعقل أن يشترط النصُّ نفاذَه لإتاحة دمجه. **وهذا القسم تسجيلٌ دائم للأمر** بمقتضى §3.2، **لا مصدرُ إذنٍ لاحق**.

### (و) ما **لا** يفعله هذا القرار — قيود ملزمة

1. **لا يمنح أي وكيل قرارَ الدمج** — بل **تنفيذَه** بعد تحقق شروط المالك.
2. **لا يجعل auto-merge غير المشروط متاحاً للمسارات الحساسة** بأي مستوى.
3. **لا يخفّف فصل الأدوار** — بل يشدّده بالبنود 8–14 و§18.
4. **لا يمسّ PR #14** ولا يدمجه ولا يعدّله ولا يمنحه تفويضاً.
5. **لا يعدّل `ui/UI_DESIGN_SYSTEM.md` §9** ولا أي ملف في `ui/**`.
6. **لا يبدأ `UI-4` ولا `UI-5`** ولا يمسّ `contracts/**`.
7. **لا يرفع `OD-FP-0001-FREEZE`** — يبقى **`ACTIVE`** · **`FP-0001` يبقى `BLOCKED`** · **ولا يضيف ولا يحذف شرط رفعٍ ولا يعيد عدّها**.
8. **لا يفعّل `R4`** ولا يغلق `R3` ولا يعيد تعريفها ولا يحسم تعارض ميثاقها.
9. **لا وسم ولا إصدار** — والوسوم والإصدارات **خارج نطاق التعديل** (‏L3 غير مفعَّل) · **عدد الوسوم 16 بلا تغيير**.
10. **لا يُنشئ طبقة سلطة جديدة** ولا يعدّل `AUTHORITY.md` ولا `constitution.md`.
11. **لا يُنشئ `CI` ولا حماية فروع ولا `CODEOWNERS`** — مسارها المخطط (‏`G2`) **خارج هذه الجولة**؛ وقاعدةُ **تسجيل الأثر** (بند 12) **إجراءٌ توثيقي لا بوابةٌ آلية**.
12. **لا يعدّل جرد `P3`** (**`28 = 13 + 15 + 0`**) ولا يُنشئ `P3-CI-*` ولا يمسّ `R3-ACT-01` ولا `OD-P3-CLOSURE` ولا `OD-R3-SCOPE-1` ولا سجل النقل الحاكم.

### (ز) أثر التعديل على `L1` و`L2`

| المستوى | قبل | بعد |
|---|---|---|
| **L1** | فرع+commits+PR+بوابات · **والدمج بشري (SA)** — نقطةُ توقفٍ يدوية إلزامية | فرع+commits+PR+بوابات · **المنفِّذ لا يدمج عمله** · و**Owner-Authorized Agent Merge متاح على L1 نفسه** — **ولا يلزم بلوغ L2** |
| **L2** | دمج آلي للمسارات منخفضة المخاطر عند اخضرار البوابات · شرط الانتقال: مقاييس FP-0001 (بند 2) | **بلا تغيير** — و**Autonomous Merge يبقى محصوراً بـL2 وبالمسارات منخفضة المخاطر**؛ وشرط الانتقال **لم يُمسّ** |
| **L3** | وسم/إصدار آلي — **غير مفعَّل** | **بلا تغيير — غير مفعَّل** |

**قاعدة قاطعة:** ‏**Owner-Authorized Agent Merge ليس ترقيةً إلى L2 ولا مقدمةً لها**؛ ولا **يستهلك** شرط الانتقال في ADR-0033 بند 2 ولا يستبدله ولا يعجّله. **وبما أن `FP-0001` `BLOCKED` والتجميد `ACTIVE`، فإن L2 لم يُبلَغ ولا يُبلَغ بهذا القرار.**

### (ح) الخطوة التالية المسجَّلة بخصوص PR #14 — **تسجيلٌ لا تنفيذ**

**PR #14 لم يُمسّ في هذه الجولة ولا يُدمج بها.** وبعد دمج هذا الـPR ونفاذ السياسة الجديدة، **وبأمر مالك مستقل**، تُفتح **جلسة Merge Agent مستقلة** تقرأ PR #14 **من المستودع مباشرةً** وتتحقق من: بقاء الرأس **`52d88d0ea9b642f5b04def30a416f0a595bdaa10`** · **مراجعة مستقلة** · **الفحوص الاثني عشر** · ثم تنفّذ أمر المالك وتسجّل: ‏Owner authorization · ‏Merge Agent execution · ‏Technical credential · ‏Merge SHA · **خط الأساس الجديد**.

**ثلاثة قيود مثبَتة لا مفترَضة — تُسجَّل ولا تُحسم هنا:**

| # | القيد | الدليل | الحكم |
|---|---|---|---|
| **1** | **المراجعة المستقلة لـPR #14 غير مستوفاة حتى تاريخه** | ‏PR #14 يسجّل بنفسه (‏`C3` في مدخله) أن مراجعته هي **`Structured self-review using a separate review agent`** — **وليست مراجعة مستقلة** | **يجب استيفاء §(د)-3 قبل أي دمج له.** وهذا **تسجيلُ حالةٍ** لا شرطُ رفعٍ جديد ولا أمرُ تنفيذ |
| **2** | **PR #14 يمسّ مسارات محمية** — `decisions/**` و`INDEX.md` **من القائمة الكنسية** (‏ADR-0033 بند 3). أمّا `handoff/**` فيُعامَل هنا **حمايةً إضافية في سياق هذا الـPR وحده** (لأنه سجل التسليم — المرتبة 9) — **وليس جزءاً من القائمة الكنسية العامة**، ولا يصير كذلك إلا **بقرار مالك صريح يضيفه إلى ADR-0033 بند 3** | قائمة ملفاته الأربعة | **لا يدخل auto-merge بأي حال** — والسندُ فيه `decisions/**` و`INDEX.md` وحدهما وهو كافٍ · ويلزمه **أمر مالك صريح** مستوفٍ لشروط §(د) السبعة مجتمعةً |
| **3** | **تفاعل تسلسلي بين الـPRين — يحتاج قرار مالك** | **مُثبَت بالفحص لا مفترَض:** ‏`git merge-tree --write-tree` بين رأس هذا الفرع ورأس PR #14 (`52d88d0e…`) يُرجع **ثلاثة تعارضات محتوى**: `INDEX.md` · `decisions/open-decisions.md` · `handoff/handoff.md` — لأن الدفعتين تضيفان في **ذيل** الملفّين وتحدّثان **صفوف `INDEX.md` نفسها** | دمجُ أيٍّ منهما **يجعل الآخر غير قابل للدمج آلياً** (‏`mergeStateStatus` ينتقل من `CLEAN` إلى `DIRTY`). وحلُّ التعارض يستلزم **دفع commit** إلى فرع الآخر ⇒ **يغيّر رأسه** ⇒ **يُبطل الـSHA المثبَّت** (‏ADR-0033 بند 11) ويستوجب **أمر مالك جديداً أو تحديثاً صريحاً للتفويض بالـSHA الجديد**. **مسجَّل للمالك — ولا يُحسم بافتراض من وكيل، ولا يُقرأ سبباً لرفض أيٍّ من الـPRين** |

### (ط) أوعية فُحصت ولم تُعدَّل — **تسجيلٌ لمنع التوسّع**

| الوعاء | النص المفحوص | الحكم |
|---|---|---|
| `methodology/ROCKET_OPERATING_MODEL.md:21` | «ثم مسار الدمج **حسب مستوى المهمة (§14)**» | **إحالة لا قاعدة** — تَرِث التعديل تلقائياً · **لم يُعدَّل** |
| `decisions/adr/ADR-0034-…md:11` و`:15` | «قائمة خيارات الدمج … **تُستبدل بسياسة ADR-0033**» · «الامتناع عن أي دمج/إنهاء فرع **خارج سياسة ADR-0033**» | **إحالة لا قاعدة** — تَرِث التعديل تلقائياً · **لم يُعدَّل** |
| `methodology/PRE_IMPLEMENTATION_CONSOLIDATION_REVIEW.md:4` | «لا دمج/رفع GitHub **ضمن هذه الوثيقة**؛ كل الدمج بيد SA وفق ترتيب §3» | **مقيَّد بنطاق تلك الوثيقة** (دفعة تجميع v0.7 المنتهية) ولا يقرّر سياسة عامة · **لم يُعدَّل — ولا تبعية مثبتة تُوجب تعديله** |
| `methodology/coding-standards.md:47` | «لا دمج بدون CI أخضر» | **متسق مع الجديد** (شرط البوابات §(د)-5) · **لم يُعدَّل** |
| `AUTHORITY.md` · `constitution.md` · `README.md` | لا يحوي أيٌّ منها قاعدة دمج (‏`README.md:34` يحيل إلى **ADR-0033-6** — الوسوم، **ولم تُمسّ**) | **خارج النطاق** · **لم تُمسّ** |

**قيد عام على هذا القسم:** لا يعيد ترقيم أي قرار قائم · لا يغيّر حالة أي `OD` آخر · لا يُنشئ ADR جديداً (التعديل **في** ADR-0033 بنمط `Accepted (Amended)` وفق سابقة ADR-0029) · لا يمنح `traceability/**` أي سلطة · ولا يُقرأ إذناً بأي عمل خارج ما نصّ عليه.

### (ي) سجل جولة التصحيح بعد المراجعة المستقلة — ‏Δ 2026-08-03

> **السند:** «‏OWNER CORRECTION ORDER — PR #15 Independent Review Findings» (2026-08-03).
> **الحالة:** مراجعة مستقلة جرت على الرأس **`8b741729d50116eebcbdbb25cc79a62cd4fba05d`** وأصدرت **`REQUEST CHANGES`** بثلاث ملاحظات حاجبة؛ فنُفِّذت تصحيحاتها في **commit جديد فوق الرأس** — بلا `amend` ولا `rebase` ولا `force-push`.

| ‏ID | الملاحظة | التصحيح | الأوعية |
|---|---|---|---|
| **`C1`** | تناقض عدد الشروط: ‏ADR-0033 بند 10 = **ستة**، §(د) = **سبعة**، و`adr/README.md` ينسب «السبعة» إلى ADR-0033 | اعتُمد **سبعة مجتمعة**؛ أُضيف الشرط **`(ز)` تسجيل أثر الدمج بالأبعاد الأربعة** إلى بند 10، وبقي **بند 12** تفصيلاً للمحتوى والتنفيذ؛ ووُحِّد العدّ والإحالات | ‏ADR-0033 · open-decisions §(د) · `adr/README.md` · `agent-execution-model` §14 و§18.1 · `INDEX.md` · `handoff` |
| **`C2`** | ‏ADR-0033 بند 3 نسب إلى النص السابق عبارة «كل قرار حاكم» **غير الموجودة فيه** ووصف الإضافة بأنها «تثبيت لا توسيع» | حُذف الادّعاء؛ و`methodology/**` موصوفة الآن **إضافةً وتوسيعاً مقصوداً** بأمر المالك 2026-08-03؛ ونُصَّ أن السابق كان «**وكل قرار تجاري**» لا «كل قرار حاكم»، وحُفظ النص السابق حرفياً في جدول النصوص المستبدَلة | ‏ADR-0033 (بند 3 + جدول النصوص المستبدَلة) |
| **`C3`** | الاستثناء الانتقالي وصفٌ عام («PR تعديل سياسة الدمج») لا يسمّي PR #15، ولا يَرِد رأسه في أي وعاء | ثُبِّت الموضوع حرفياً (‏Repository · **PR #15** · ‏Head SHA · ‏Target branch)، وأُضيفت **قاعدة الرأس النهائي**، وحُصر عدم الانطباق في **PR #14** و**أي PR لاحق يعدّل السياسة** و**أي وكيل كتب أو عدّل PR #15**، وضُبط الانقضاء بثلاثة محفزات | ‏ADR-0033 · open-decisions §(هـ) |
| **`N1`** (غير حاجبة) | اختلاف قوائم المسارات الحساسة بين الأوعية، و`handoff/**` في وعاء واحد | **ADR-0033 بند 3** هو **القائمة الكنسية الوحيدة**؛ وما في غيره **إحالة لا تعريف**؛ و`handoff/**` **حمايةٌ إضافية في سياق PR #14 وحده** لا جزءٌ من القائمة العامة إلا بقرار مالك صريح. **ولم تُوسَّع القائمة** | `agent-execution-model` §18.2-6 · open-decisions §(ح) |

**أثر سلطوي لازم — يُسجَّل ولا يُطوى:** وكيلُ المراجعة الذي نفّذ هذه التصحيحات **صار كاتبَ محتوى لهذه الدفعة** بمقتضى `agent-execution-model` §18.1 (صفّ Review Agent) و§18.2-1 ⇒ **فقَدَ أهليةَ المراجعة المستقلة لها**، و**لا يجوز له دمجُها**. ويلزم قبل أي دمج: **(1) مراجعة مستقلة جديدة** بمراجعٍ لم يكتب ولم يعدّل · **(2) أمر مالك جديد يثبّت الرأس النهائي الجديد** (بند 11).

**ما لم يتغيّر في هذه الجولة:** `OD-FP-0001-FREEZE` **`ACTIVE`** · `FP-0001` **`BLOCKED`** · شروط الرفع السبعة **بلا تعديل ولا إعادة عدّ** · `R3` لم تُغلق · **`R4` غير مفعَّلة** · جرد `P3` **`28 = 13 + 15 + 0`** · **16 وسماً** · **PR #14 لم يُمسّ** · `ui/**` و`contracts/**` و`traceability/**` و`AUTHORITY.md` و`constitution.md` **لم تُمسّ** · **لا ملف جديد ولا محذوف**.

### (ك) سجل أثر تنفيذ الدمج — PR #14 (‏Δ 2026-08-03 · استكمالٌ إلزامي بموجب ADR-0033 بند 12)

> **السند:** **أمر مالك صريح** صادر 2026-08-03 — «‏**OWNER MERGE ORDER — PR #14**» (‏Owner-Authorized Independent Merge Agent)، ثم «‏**OWNER ORDER — Complete PR #14 Merge Audit Record**» الموجب لهذا التسجيل.
> **طبيعة هذا القسم:** **تسجيلُ أثرٍ بعد الدمج فقط** — **لا يُنشئ سياسة** ولا يعدّل شروط §(د) السبعة ولا يمسّ ADR-0033 ولا `agent-execution-model` ولا §9 ولا أي حالة تجميد.
> **الحالة:** **`EXECUTED — RECORDED`**.

#### (ك-1) الأبعاد الأربعة + بيانات التنفيذ — تسجيل حرفي

```text
Authorization Principal = Owner
Execution Actor = Owner-Authorized Independent Merge Agent
Technical Credential = GitHub token for account sa4devops used by the agent environment
PR = #14
Authorized Head SHA = 0020dfb6de7f383b3468518691a7b29da0cf7f1e
Base SHA Before Merge = 0c199d76f732357ac948fe6c6e50171e9250a04c
Merge SHA = d2c93d2c16b03d406a6b6fe70c0b0771687c9ff5
New Governing Baseline = origin/main @ d2c93d2c16b03d406a6b6fe70c0b0771687c9ff5
Merge Method = merge commit
Merge Result = SUCCESS
```

| الحقل | القيمة |
|---|---|
| **أمر المالك ومرجعه وتاريخه** | «‏OWNER MERGE ORDER — PR #14» · **2026-08-03** · مثبِّتاً `EXPECTED_HEAD_SHA` و`EXPECTED_BASE_SHA` و`MERGE_METHOD=merge` |
| **عنوان الـPR** | `R3 — Codify Accessibility Requirements in UI Design System §9` |
| **الفرع المصدر** | `docs/r3-accessibility-section-9-codification` |
| **أبوا merge commit** | `0c199d76f732357ac948fe6c6e50171e9250a04c` (‏base السابق) · `0020dfb6de7f383b3468518691a7b29da0cf7f1e` (رأس الـPR) |
| **الملفات المدموجة صافياً** | **أربعة فقط**: `INDEX.md` · `decisions/open-decisions.md` · `handoff/handoff.md` · `ui/UI_DESIGN_SYSTEM.md` |
| **وسوم/إصدارات** | **لا وسم ولا إصدار** — **16 وسماً** قبل الدمج وبعده بلا تغيير |

#### (ك-2) نصٌّ سلطوي قاطع — الحساب التقني ليس دليلَ تنفيذٍ يدويٍّ من المالك (‏ADR-0033 بندا 9 و12)

1. **الدمج نُفِّذ بواسطة Merge Agent مفوَّض** بأمر المالك — **ولم يُنفَّذ يدوياً بواسطة المالك**، ولم يضغط المالك زرّ الدمج بنفسه.
2. **ظهور `sa4devops` في واجهة GitHub** (‏حقل `mergedBy`) هو **هوية بيانات الاعتماد التقنية** التي تعمل بها بيئة الوكيل — **وليس دليلاً ولا قرينةً على تنفيذٍ يدويٍّ من المالك**. **ويُمنع** قراءتُه كذلك في أي تدقيق لاحق.
3. **قرار الدمج** صادر من **المالك** · **تنفيذه** جرى بـ**Merge Agent** · **والفصل بينهما مسجَّل هنا نصاً**.

#### (ك-3) استيفاء استقلال وكيل الدمج — §(د)-2 و§(ج) صفّ Merge Agent

| القيد | الحالة |
|---|---|
| جلسة **مستقلة** عن جلسة تنفيذ PR #14 | **مستوفى** |
| **لم يكتب محتوى PR #14** | **مستوفى** |
| **لم يدفع أي commit إلى فرع PR #14** | **مستوفى** |
| **لم يحلّ تعارضات PR #14** | **مستوفى** — حلُّ التعارضات جرى في جولة سابقة منفصلة أنتجت الرأس `0020dfb6…` |
| **لم يعدّل رأس الـPR ولا محتواه** | **مستوفى** |
| قرأ المستودع والـPR **مباشرةً** لا من تقرير | **مستوفى** — تحقُّقٌ محليّ على استنساخٍ جديد، و`merge` تجريبي فعليّ أُجهض بلا commit |
| **لم يوافق لنفسه** | **مستوفى** — التفويض من المالك، والوكيل نفّذ ولم يمنح الموافقة |

#### (ك-4) هوية المراجعة المستقلة ونتيجتها — §(د)-3 و§(ح)-1

```text
Review Actor = Independent Engineering Review Session
Review Date = 2026-08-03
Reviewed Head SHA = 0020dfb6de7f383b3468518691a7b29da0cf7f1e
Review Result = APPROVE / NO BLOCKING FINDINGS
Review Scope =
- verification of the three conflict resolutions
- confirmation that §9 remained unchanged
- confirmation that merge-policy files matched main
- confirmation that PR #14 net diff contained only four files
- successful local trial merge without conflicts
Review Artifact Identifier = Not persisted outside the review conversation
```

**نقص تتبّع مسجَّل — لا يُخترع له معرّف:** لا يوجد **معرّف تقني دائم** (‏رابط ولا مخرج محفوظ في المستودع) للمراجعة المستقلة أعلاه. **يُسجَّل هذا نقصاً في قابلية التتبّع**، و**لا يغيّر نتيجة المراجعة التي وقعت فعلاً**، و**لا يُعالَج بافتراض ولا باختراع معرّف**. ومعالجتُه — إن قرّرها المالك — **قرارٌ مستقل خارج هذه الجولة**.

**إغلاق القيد المسجَّل في §(ح)-1:** كان مسجَّلاً أن مراجعة PR #14 حتى 2026-08-03 هي **`Structured self-review using a separate review agent`** ⇒ **§(د)-3 غير مستوفٍ**. وقد جرت بعدها **مراجعةٌ هندسية مستقلة** على الرأس **`0020dfb6…`** بالنطاق أعلاه ⇒ **القيد مستوفى للرأس المدموج**. **والسجل السابق يبقى نافذاً كتاريخ ولا يُعاد كتابته.**

#### (ك-5) أثر تغيّر الرأس عن المسجَّل في §(ح) — تسجيلٌ لا تعديل

§(ح) سجّل — قبل دمج PR #15 — أن جلسة Merge Agent لـPR #14 تتحقق من **بقاء الرأس `52d88d0ea9b642f5b04def30a416f0a595bdaa10`**، وسجّل في §(ح)-3 أن حلّ التعارض التسلسلي **يغيّر رأس الآخر فيُبطل الـSHA المثبَّت** ويستوجب **أمر مالك جديداً**. **وهذا ما وقع فعلاً وبالترتيب المتوقَّع:**

| # | الواقعة | الأثر |
|---|---|---|
| **1** | دُمج PR #15 فصار `origin/main` = `0c199d76…` | انتقل PR #14 إلى `DIRTY` كما تنبّأ §(ح)-3 |
| **2** | جولة **حلّ تعارض منفصلة** على فرع PR #14 أنتجت **merge commit** أبواه `52d88d0e…` و`0c199d76…` | الرأس صار **`0020dfb6…`** ⇒ **الـSHA القديم `52d88d0e…` بطَل** (‏ADR-0033 بند 11) |
| **3** | **أمر مالك جديد** ثبّت الرأس **`0020dfb6…`** والأساس `0c199d76…` | **التفويض صحيح على الرأس المدموج فعلاً** — ولم يُدمج أي رأس غير مثبَّت |

**ولم يُنفَّذ الدمج بموجب الاستثناء الانتقالي الضيق §(هـ)** — فذاك **محصور بـPR #15** و**لا ينطبق على PR #14** بنصّه، وقد **انقضى** بدمج PR #15. **الدمج جرى بموجب القاعدة الجديدة النافذة** بعد نفاذ `OD-GOV-MERGE-1`.

#### (ك-6) ما لم يتغيّر بالدمج ولا بهذا التسجيل — تثبيت

- **`OD-FP-0001-FREEZE` = `ACTIVE`** · **`FP-0001` = `BLOCKED`** · **شروط الرفع بلا تعديل ولا إضافة ولا إعادة عدّ**.
- **`R4` غير مفعَّلة** · **`R3` لم تُغلق** ولم يُعَد تعريفها · جرد `P3` **`28 = 13 + 15 + 0`** بلا تغيير.
- **لا معيار Accessibility أُعلن `PASS`** — لا بالدمج ولا بهذا السجل. والدمج **نشرَ نصّ §9** ولم يقِس شيئاً ولم يعالج دلتا.
- **حالة `ui/UI_DESIGN_SYSTEM.md` تبقى `Proposed`** — **الدمج نشر §9 داخل هذه الحالة ولم يرقِّها**، **والترقية قرار مالك مستقل**.
- **`OD-GOV-MERGE-1` نفسه لم يُعدَّل** في أقسامه (أ)..(ي) — **هذا القسم إضافةُ سجلِّ أثرٍ فقط** بمقتضى بند 12.
- **لا وسم · لا إصدار · لا ADR جديد · لا OD جديد · لا ملف جديد ولا محذوف.**

### (ل) تبسيط إتمام أثر الدمج + السجل الحاكم السابق للدمج — PR #16 (‏Δ 2026-08-04 · أمر مالك)

> **السند:** **أمر مالك صريح** صادر **2026-08-04** — «‏**OWNER EXECUTION ORDER — PR #16 Final Merge Governance Simplification**».
> **الوعاء المعياري:** `decisions/adr/ADR-0033-…md` **بند 12** (مُعاد صياغته) و**بند 11** (+استثناء دورة التصحيح) و«حدود المراجع المستقل» و«الاكتفاء الذاتي» · `methodology/agent-execution-model.md` **§18.2-8/9/10**. **وهذا القسم الوعاء الحاكم.**
> **الحالة:** **`AMENDED — EFFECTIVE UPON MERGE`**.

#### (ل-1) القرار المعماري — أين يعيش كلُّ نوعٍ من المعلومة

| النوع | الموضع | القاعدة |
|---|---|---|
| **المعلومات الحاكمة** | `decisions/open-decisions.md` + `handoff/handoff.md` | تُسجَّل **قبل الدمج داخل الدفعة نفسها** |
| **الوقائع التقنية** | **Git** | **لا تُكرَّر في وثائق الحوكمة** — تُستخرج عند الحاجة |
| **واقعة التنفيذ** | **رسالة Merge Commit** | **إيصالُ تنفيذٍ لا وعاءُ سلطة** |

**الوقائع التي يملكها Git ولا تُكرَّر:** الرأس النهائي المدموج · `Merge SHA` · الأبوان · التاريخ · الشجرة الناتجة. **والرأس النهائي المدموج هو الأب الثاني للـMerge Commit**، يُستخرج بـ`git show -s --format='%P' <MERGE_SHA>`.

**أثرُ ذلك:** **لا يُطلب أيُّ تحديث مستودعي بعد الدمج، ولا PR توثيقي لاحق، بأي حال** — و**`PR #17` غير مطلوب**.

#### (ل-2) نموذج إغلاق الحلقة السابق — لم ينفذ قط

النموذج الذي طُرح في **مسودة PR #16** (‏بند 15 المقترح · فئةُ دفعات التوثيق · الوصف الانتقالي ذاتي الإغلاق · الاعتماد على **GitHub PR body/timeline** حاملاً دائماً للأثر · اشتراطُ PR توثيقي بعد الدمج) **حُذف بالكامل من المسودة قبل الدمج**:

| الحقيقة | الحكم |
|---|---|
| ظهر على **فرع PR #16 وحده** | **لم يُدمج إلى `main` قط** |
| **لم يصبح سياسة نافذة** ولا لحظةً واحدة | **`SUPERSEDED — NEVER IN FORCE`** |
| استُبدل **قبل النفاذ** بالنموذج المبسَّط | **بند 12 المُعاد صياغته** |

**وأثرُه التاريخي محفوظ في `H-0034` موسوماً `SUPERSEDED — NEVER IN FORCE`** — **ولا يُقرأ قاعدةً نافذة في أي وعاء.**

#### (ل-3) استثناء دورة التصحيح على بند 11 — لا يُقرأ تخفيفاً

تغيُّرُ الرأس الناتجُ **حصراً** عن **معالجة ملاحظات مراجعةٍ مستقلة**، **داخل نطاق أمر المالك نفسه**، و**بلا إضافة ملفات ولا موضوعات جديدة** ⇒ **لا يحتاج أمر مالك جديداً**، بشرطين: **مراجعة مستقلة كاملة للرأس الجديد** · و**عدم دمج رأسٍ لم يحصل هو نفسه على `APPROVE`**.

**وبند 11 يبقى نافذاً بكامل أثره** في: تغييرٍ **غير ناتج عن ملاحظة مراجعة** · **توسيع النطاق** · **إضافة ملف غير مخوَّل** · **تغيير الهدف أو السياسة** · **تعارض سلطوي** · **محاولة دمج رأسٍ لم يُراجَع**.

#### (ل-4) السجل الحاكم السابق للدمج — PR #16

```text
Authorization Principal      = Owner
Owner Order Reference        = OWNER EXECUTION ORDER — PR #16 Final Merge Governance Simplification
Owner Order Date             = 2026-08-04
PR                           = #16
Permitted Scope              = decisions/adr/ADR-0033-automation-levels-git-merge-policy.md
                               decisions/adr/README.md
                               methodology/agent-execution-model.md
                               decisions/open-decisions.md
                               handoff/handoff.md
                               INDEX.md
                               PR #16 title and description
Required Review Actor        = Independent Review Session (did not author or modify this PR)
Required Review Result       = APPROVE
Authorized Execution Actor   = Owner-Authorized Independent Merge Agent
                               (third session: not the author, not the reviewer)
Expected Technical Credential = GitHub token for account sa4devops used by the agent environment
Credential Statement         = The technical account sa4devops is NOT evidence of manual Owner execution
Merge Method                 = merge commit
Pending Merge SHA            = assigned automatically by Git
```

**نصّ رسالة Merge Commit المتوقَّع — مثبَّتٌ حرفياً:**

```text
Merge PR #16 — GOV — Record PR #14 Merge Provenance and Simplify Merge Audit Completion

Governance record: OD-GOV-MERGE-1 · H-0034
Executed by Owner-Authorized Independent Merge Agent.
Technical account sa4devops is not evidence of manual Owner execution.
```

**قواعد المطابقة:** تُقارن بعد **توحيد نهايات الأسطر إلى `LF`** · **حذف المسافات الطرفية من كل سطر** · **حذف الأسطر الفارغة الزائدة في البداية والنهاية**. **ولا يُسمح باختلاف الكلمات ولا ترتيب الأسطر.** ‏**واختلافٌ بعد التطبيع ⇒ `MERGE RECEIPT MISMATCH` ⇒ لا دمج.**

**ولا يُضاف `Authorized head` إلى الرسالة** — الرأس النهائي يُستخرج من **الأب الثاني للـMerge Commit**.

> **تغيّر نمط العنوان** عن `Merge pull request…` **مقصود ومعتمد لهذه السابقة** بأمر المالك.

#### (ل-5) حدود المراجع المستقل

**Review Agent يقرأ ولا يعدّل.** مخرجُه: **العيب** · **دليله** · **القاعدة المخالفة** · **نتيجة القبول المطلوبة** · ثم **`APPROVE`** أو **`REQUEST CHANGES`**. **ويُمنع عليه:** كتابةُ نصوصٍ بديلة جاهزة · تعديلُ أي ملف · دفعُ commits · حلُّ التعارضات. **واقتراحُ نصٍّ بديل أو تعديلُ الملفات يجعله مؤلفاً ⇒ يفقد أهلية المراجعة والدمج لهذه الدفعة.**

#### (ل-6) الاكتفاء الذاتي — لا اعتماد حاكم على خدمة خارجية

**لا تعتمد إعادةُ بناء قرار الدمج ولا أثرِه الحاكم على GitHub ولا أي خدمة خارجية**؛ والخدمات الخارجية **أدواتُ عملٍ أثناء التطوير فقط**.

**وحدة التاريخ الحاكم المحمولة = المستودع الكامل متضمناً `.git` وتاريخَ Git** — لا شجرةُ الملفات وحدها:

- **شجرةُ الملفات تكفي** لقراءة **الحالة الحالية**.
- **إعادةُ بناء تاريخ الدمج والوقائع التقنية تلزمها `.git`**.
- **النسخة المسلَّمة على الفلاش يجب أن تحتوي مستودعات المشروع كاملةً مع تاريخها.**
- **النسخُ للفلاش والتنصيبُ لا يتطلبان تعديل الملفات ولا إضافة بيانات حوكمة.**
- **الأحجام — تقريبية ومقيسة من المالك (‏2026-08-04):** شجرة الملفات **≈ 3.3 MB** · المستودع الكامل **≈ 7.2 MB** · الفرق **≈ 4 MB** · **ولا مبرر تشغيلي لإسقاط `.git`.** *(القيمة الفعلية لـ`.git` تتغير بحالة الحزم؛ فالفرق **حدٌّ أعلى تقريبي** لا ثابت.)*

**لا Bundle ولا Script ولا دليل تنصيب في هذه الجولة.**

#### (ل-7) ما لم يتغيّر — تثبيت

- **شروط بند 10 السبعة بلا تعديل ولا إعادة عدّ** · **فصل الأدوار مُشدَّد لا مُخفَّف** · **لا auto-merge غير مشروط** · **لا دمجَ وكيلٍ لعمله** · **لا قبولَ `Structured self-review` مراجعةً مستقلة**.
- **`OD-FP-0001-FREEZE` = `ACTIVE`** · **`FP-0001` = `BLOCKED`** · **شروط الرفع بلا تعديل** · **`R4` غير مفعَّلة** · **`R3` لم تُغلق**.
- **`ui/UI_DESIGN_SYSTEM.md` §9 لم تُمسّ** · **الحالة تبقى `Proposed`** · **لا معيار Accessibility أُعلن `PASS`**.
- **`contracts/**` و`traceability/**` و`AUTHORITY.md` و`constitution.md` لم تُمسّ** · **لا ADR جديد** · **لا OD جديد** · **لا ملف جديد ولا محذوف** · **لا وسم ولا إصدار**.

## سجل توسيع عقود الشاشات ذات الأولوية — `OD-R3-CONTRACTS-1` (‏Δ 2026-08-04 · R3 — أمر مالك)

> **السند:** **أمر مالك صريح** صادر **2026-08-04** — «‏**OWNER EXECUTION ORDER — R3 Priority Screen Contracts Expansion**».
> **الوعاء:** هذا القسم هو **الوعاء الحاكم** للقرار بمقتضى `methodology/PHASE_EXECUTION_STANDARD.md` §3.2 («لا قرار يعيش في تقرير أو محادثة فقط. التقرير إحالة، لا وعاء») و`AUTHORITY.md:13` (المرتبة **4**)، ويرافقه `handoff/handoff.md` → **H-0035** (المرتبة 9).
> **خط الأساس:** ‏`origin/main` = **`e81ea775d119971e2efed2646c5b006df1706388`** — **مطابقٌ حرفياً** للـSHA المثبَّت في أمر المالك.
> **الحالة:** **`RECORDED — EFFECTIVE UPON MERGE`** — ولا تُقرأ نافذةً قبل الدمج في `main`.

### (أ) سجل الواقعة — `OD-R3-CONTRACTS-1`

| الحقل | القيمة |
|---|---|
| **المعرّف** | **`OD-R3-CONTRACTS-1`** — توسيع تغطية عقود الشاشات ذات الأولوية |
| **التاريخ** | **2026-08-04** |
| **السلطة** | **Owner (‏SA)** — أمر صريح · **Specification Architect** — الصياغة بتفويض |
| **المخرج المنفَّذ** | **مخرج `R3` المسجَّل في `methodology/RECONCILIATION_ROADMAP.md:11`** — «‏Contracts Coverage Expansion · توسيع عقود الشاشات ذات الأولوية + تعبئة فهرس الأنواع · المخرج: **عقود Candidate إضافية**». **وهذه الجولة تقع مباشرةً داخل ذلك المخرج** — لا توسيعَ ميثاقٍ ولا إلحاقَ نطاق |
| **النطاق المأذون — الملفات** | **خمسة حصراً:** `contracts/screens/run.list.md` · `contracts/screens/admin.actions.md` · `decisions/open-decisions.md` · `handoff/handoff.md` · `INDEX.md` |
| **ما نُفِّذ** | مواءمة العقدين مع **`contracts/screens/SCREEN_CONTRACT_TEMPLATE.md` v1.1**: §3 **`surfaces[]` مهيكل** بالتعداد المغلق · **قاعدة التعامد** بين `archetype` و`surfaces[].type` · **أنماط `S8`** · **الترتيب المنطقي المعلَن** · **إعلان الحالات نصاً** · **جرد التبعيات اللاحقة** |
| **حالة العقدين** | **`Candidate` — بلا تغيير.** **ولم يُرقَّ أيُّ عقد إلى `Approved`**، ولم يُدَّعَ ذلك، و**المرتبة 5 تبقى خالية فعلياً** (‏`OD-GOV-2` مفتوح بلا مساس) |
| **`version`** | `run.list` **0.1 ⇒ 0.2** · `admin.actions` **0.1 ⇒ 0.2** — بسطر `delta` على سابقة القالب نفسه (‏v1.0 ⇒ v1.1)، **بلا وسم أي نسخة `Deprecated`** لأن التغيير **مواءمةٌ بنيوية لا كسرٌ لعقد** |
| **`FP-0001`** | **`BLOCKED`** — بلا تغيير |
| **`OD-FP-0001-FREEZE`** | **`ACTIVE`** — بلا تغيير · **ولا شرط رفعٍ أُضيف ولا عُدِّل ولا أُعيد عدُّه** |
| **`R3`** | **`ACTIVE — SPECIFICATION ONLY`** — **لم تُغلق**. وهذه الجولة **تُنجز شقّاً من الشرط (17)** في «(د)» من سجل `R3-ACT-01` (توسيع تغطية العقود)، **ولا تستوفيه** ولا تُغلق `R3` ولا تُقرَّر استيفاءه — **والتقدير للمالك بالأدلة** |
| **`R4`** | **غير مفعَّلة** — بلا تغيير |
| **جرد `P3`** | **`28 = 13 + 15 + 0`** · `UNCLASSIFIED = 0` — **بلا مساس**، ولم يُنشأ `P3-CI-*` جديد |

### (ب) تعارض مسار اعتماد العقود — **مسجَّل ولا يُحسم**

**التعارض مثبَتٌ بالقراءة عند خط الأساس، بين نصّين في المرتبة نفسها:**

| الوعاء | النصّ | الشرط المستفاد |
|---|---|---|
| `contracts/NAMING_AND_CONTRACTS_STANDARD.md` §2 | «‏**Approved حصراً بتوقيع SA أو بوابة FP معتمدة**» | ‏**SA signature `OR` approved FP gate** |
| `contracts/screens/SCREEN_CONTRACT_TEMPLATE.md` «دورة الاعتماد» | «ترقيتها Approved **عبر بوابة FP** الخاصة بشاشتها (‏retrofit أو بناء) **بتوقيع SA**» | ‏**FP gate `AND` SA signature** |

**قاعدة التنفيذ المطبَّقة في هذه الجولة — حرفياً:**
1. **لم يُحسم التعارض** — ولا يُحسم من داخل دفعةٍ لا تملك أيّاً من الوعاءين.
2. **لم يُرقَّ أيُّ عقد إلى `Approved`.**
3. **حالة العقدين محفوظة `Candidate`.**
4. **يُسجَّل التعارض بوصفه تبعيةً لاحقة لاعتماد العقود** — **لا مانعاً عن تحديث عقود `Candidate`**؛ إذ **لا نصّ أعلى رتبة يمنع تحديث عقد `Candidate`**، وقاعدةُ دورة الحياة تخصّ **الترقية** لا التحديث.
5. **الحسم يحتاج قرار مالك مستقلاً** يوحّد النصّين في وعائيهما — **وكلاهما خارج القائمة البيضاء لهذه الجولة**، فلم يُمسّا.

**التصنيف الحاكم:** ‏`CONTRACT APPROVAL PATH — AUTHORITY CONFLICT · RECORDED · NOT RESOLVED · NON-BLOCKING FOR CANDIDATE UPDATES`. **ولا يُقرأ هذا التسجيل حسماً ولا سابقةً ولا شرطَ رفعٍ.**

### (ج) التبعيات اللاحقة المسجَّلة — `DOWNSTREAM DEPENDENCY`

> **قاعدة قاطعة:** **تسجيلُ تبعيةٍ ليس إضافةَ شرط رفعٍ** لـ`OD-FP-0001-FREEZE`، وشروطُه **مغلقة على المنصوص عليه** في `:227` وتفصيلِه في `:386`؛ وأيُّ شرط جديد **يحتاج قرار مالك صريحاً يعدّل سجل التجميد نفسه**. **ولم يُعدَّل شيء منها هنا.**

| ‏ID | التبعية | الوجهة |
|---|---|---|
| **`DD-RL-1` · `DD-AC-1`** | **`contracts/fields/` غير موجود** ⇒ لا وعاء لمصدر الاسم المتاح **على مستوى العنصر** ولا لأسماء رؤوس الأعمدة والحقول المولَّدة من الميتاداتا | **أمر مالك مستقل** — يمسّ المرتبة 5. **ولم يُنشأ المجلد ولا مرجعٌ إلزامي معلَّق إليه** (يُقرأ مع `OD-R3-CODIFY-1` §(د) الفجوتين **1** و**9**) |
| **`DD-RL-2`** | **`filter` ليس من كتالوج الأفعال المغلق** (`ui/UI_FIELD_NAMING.md` §5) بينما صفّ الجرد B لـ`run.list` يكتبه | `OD-NM-1` — قرار الـRegistry |
| **`DD-AC-2`** | **`define` و`edit` ليسا من الكتالوج المغلق** بينما صفّ الجرد B لـ`admin.actions` يكتبهما — **تعارضٌ داخل المرتبة نفسها بلا وسم `Superseded`** ⇒ **لا حسم صامتاً** (`AUTHORITY.md` §2 القاعدة 2) | `OD-NM-1` — قرار الـRegistry · **ولم يُوسَّع `Action Registry` ولم يُضَف فعل** |
| **`DD-RL-3`** | **`column picker` بلا صفٍّ** في مصفوفة حسم الأسطح `ui/UI_INTERACTION_MODEL.md` §4، ونصُّ §2-4 يوجب «أضفها بقاعدة لا ترتجل» | `ui/**` — أمر مستقل |
| **`DD-RL-4`** | **`S19` و`S20` بلا وعاء سطحيّ معلَن** في بطاقة السلوك 2 رغم تصريح §4 من عقد `run.list` بانطباق `S16`–`S20` | `ui/UI_SCREEN_BEHAVIOR_CARDS.md` — أمر مستقل |
| **`DD-RL-5` · `DD-AC-3`** | **مسارات غير مسجَّلة** في `ui/UI_SCREEN_INVENTORY.md` (السجل المركزي الوحيد): صفحة حالة التصدير · سطح محرّر عقد الفعل | **قرار Specification Architect** (`UI_SCREEN_GOVERNANCE_STANDARD.md` §ج-2) — **ولم يُخترع مسار** |
| **`DD-RL-6` · `DD-AC-5`** | **`<control_id>`** إحدى قيم `focus_return_target` في القالب §3.2 **وليس من الأنواع الثمانية** في `ui/UI_FIELD_NAMING.md` §1 — بلا نمط ولا نطاق تفرّد | `ui/UI_FIELD_NAMING.md` §1 — أمر مستقل. **ولذلك لم تُستعمل القيمة في العقدين** |
| **`DD-AC-4`** | **عدد حقول عقد الفعل «13»** في بطاقة 12 و§6 **لا يطابق** عدد صفوف مخطط العقد في `ui/UI_ACTION_BUTTON_MODEL.md` §1 | `ui/**` — أمر مستقل. **ولم يُثبَّت عددٌ بديل** |
| **`DD-RL-8` · `DD-AC-7` · `DD-AC-8`** | **فجوات إنفاذ الإتاحة القائمة**: `SCREEN_CONTRACT_TEMPLATE.md` §8 بلا حدٍّ أدنى للإتاحة · `methodology/testing-strategy.md` بلا نوع اختبار إتاحة · `ui/UI_GATE_REVIEW_CHECKLIST.md` بلا بند إتاحة · `ui/UI_COMPONENT_STATES.md` بلا بُعد إتاحة لـ`S1`–`S20` | **قائمة قبل هذه الجولة** ومسجَّلة أصلاً في `OD-R3-CODIFY-1` §(د) الفجوات **2** و**3** و**4** و**5** — **تُحال ولا تُكرَّر ولا تُحسم** |
| **`DD-RL-7` · `DD-AC-6`** | **تعارض مسار اعتماد العقود** — التفصيل في §(ب) أعلاه | قرار مالك مستقل |

### (د) ما **لا** يفعله هذا السجل — قيود ملزمة

1. **لا يرقّي عقداً إلى `Approved`** ولا يدّعي ذلك · **ولا يملأ المرتبة 5** · و**`OD-GOV-2` يبقى مفتوحاً بلا مساس**.
2. **لا يحسم تعارض مسار اعتماد العقود** (§(ب)) ولا **تعارض ميثاق `R3`** (‏`AUTHORITY CONFLICT — SEPARATE OWNER ACTION`) ولا `GOV-OBS-03` ولا `OD-GOV-1` ولا `OD-P3-2` ولا `P3-CI-28`.
3. **لا يعدّل `ui/UI_DESIGN_SYSTEM.md` §9** ولا أيَّ ملف في `ui/**` · **ولا يعلن أي معيار من `R3-A11Y-01`..`07` بحالة `PASS`** — حالتها بلا تغيير.
4. **لا يُنشئ `contracts/fields/`** ولا مرجعاً إلزامياً معلَّقاً إليه · **ولا يخترع حقلاً ولا عقداً صامتاً**.
5. **لا يُنشئ نوع سطح خارج التعداد المغلق** ولا حالة `S` جديدة ولا نمط `S8` جديداً · **ولا محوراً ثالثاً** (`rendering_topology` · `authoring_origin` · `provenance_ref`).
6. **لا يُنشئ `UI-4` ولا `UI-5`** ولا يمسّ Rocket ولا Visual Baseline ولا مستودع `aql`.
7. **لا يوسّع `Action Registry`** ولا يضيف فعلاً ولا صلاحيةً ولا مساراً ولا قيمة تعداد.
8. **لا يعدّل `SCREEN_CONTRACT_TEMPLATE.md`** ولا `NAMING_AND_CONTRACTS_STANDARD.md` ولا أيَّ عقد شاشة آخر (`admin.record_types` · `admin.workflows` **لم يُمسّا**) ولا `AUTHORITY.md` ولا `constitution.md` ولا `methodology/**` ولا `decisions/adr/**` ولا `traceability/**`.
9. **لا يرفع التجميد ولا يفكّ حجب `FP-0001` ولا يفعّل `R4` ولا يغلق `R3`** · **ولا يضيف شرط رفعٍ ولا يعيد عدّها**.
10. **لا يُنشئ اختباراً ولا `CI` ولا حماية فروع** · **لا وسم ولا إصدار** · **ولا يعيد فتح حوكمة `Merge Agent`** (‏`OD-GOV-MERGE-1` بأقسامه **لم يُمسّ**).

### (هـ) السجل الحاكم السابق للدمج — PR #17 (‏ADR-0033 بند 12)

> **تنبيه ترقيمي — يُقرأ ولا يُخلط:** ‏`OD-GOV-MERGE-1` §(ل-1) قرّر أن **«`PR #17` غير مطلوب»** — وذلك يخصّ **PR توثيقيّ لاحق لأثر الدمج**، وقد سقط وجوبُه نهائياً. **وهذا الـPR #17 ليس ذاك**: هو **دفعة `R3` تعاقدية عادية** تخضع لـ**بند 12 كاملاً**، وتوافقُ الرقم **محضُ تسلسلٍ من GitHub**. **ولم تُنشأ دفعةُ توثيقٍ لاحقة ولن تُنشأ.**

```text
Authorization Principal       = Owner
Owner Order Reference         = OWNER EXECUTION ORDER — R3 Priority Screen Contracts Expansion
Owner Order Date              = 2026-08-04
Repository                    = sa4devops/local-rag-enterprise-specs
PR                            = #17
Target Branch                 = main
Base SHA                      = e81ea775d119971e2efed2646c5b006df1706388
Permitted Scope               = contracts/screens/run.list.md
                                contracts/screens/admin.actions.md
                                decisions/open-decisions.md
                                handoff/handoff.md
                                INDEX.md
Required Review Actor         = Independent Review Session (did not author or modify this PR)
Required Review Result        = APPROVE
Authorized Execution Actor    = Owner-Authorized Independent Merge Agent
                                (third session: not the author, not the reviewer)
Expected Technical Credential = GitHub token for account sa4devops used by the agent environment
Credential Statement          = The technical account sa4devops is NOT evidence of manual Owner execution
Merge Method                  = merge commit
Pending Merge SHA             = assigned automatically by Git
```

**نصّ رسالة Merge Commit المتوقَّع — مثبَّتٌ حرفياً:**

```text
Merge PR #17 — R3 — Expand Priority Screen Contracts (run.list · admin.actions)

Governance record: OD-R3-CONTRACTS-1 · H-0035
Executed by Owner-Authorized Independent Merge Agent.
Technical account sa4devops is not evidence of manual Owner execution.
```

**قواعد المطابقة:** تُقارن بعد **توحيد نهايات الأسطر إلى `LF`** · **حذف المسافات الطرفية من كل سطر** · **حذف الأسطر الفارغة الزائدة في البداية والنهاية**. **ولا يُسمح باختلاف الكلمات ولا ترتيب الأسطر.** ‏**واختلافٌ بعد التطبيع ⇒ `MERGE RECEIPT MISMATCH` ⇒ لا دمج.**

**ولا يُضاف `Authorized head` إلى الرسالة** — **الرأس النهائي المدموج هو الأب الثاني للـMerge Commit**، يُستخرج بـ`git show -s --format='%P' <MERGE_SHA>` (‏بند 12-(ج)). **ولا تُكرَّر في هذا الوعاء وقائعُ يملكها Git** (`Merge SHA` · الأبوان · التاريخ · الشجرة الناتجة).

**مراجعة هذه الدفعة — تسجيل حالة:** جلسةُ التنفيذ أجرت **`Structured self-review`** فقط، **وهي لا تستوفي §(د)-3** من `OD-GOV-MERGE-1` ولا **بند 10-(ج)** من ADR-0033. **فيلزم قبل الدمج مراجعةٌ مستقلة** بجلسةٍ لم تكتب ولم تعدّل هذه الدفعة، **ولا يُدمج رأسٌ لم يحصل هو نفسه على `APPROVE`** (بند 11 + استثناء دورة التصحيح).

**استثناء دورة التصحيح — منطبقٌ على هذه الدفعة:** تغيُّرُ الرأس الناتجُ **حصراً** عن معالجة ملاحظات المراجعة المستقلة، **داخل نطاق أمر المالك نفسه، وبلا إضافة ملفات ولا موضوعات جديدة** ⇒ **لا يحتاج أمر مالك جديداً** (‏ADR-0033 بند 11 · `OD-GOV-MERGE-1` §(ل-3))، بشرطَي **مراجعةٍ مستقلة كاملة للرأس الجديد** و**عدم دمج رأسٍ لم يُعتمد**. **وأيُّ تغيير خارج ذلك يُعيد بند 11 بكامل أثره.**


### (و) جولة التصحيح بعد المراجعة المستقلة — ‏Δ 2026-08-04

> **السند:** «‏**OWNER CORRECTION ORDER — PR #17 Required Review Corrections**» (2026-08-04).
> **الحالة:** ‏**`CORRECTIONS APPLIED — AWAITING FULL INDEPENDENT RE-REVIEW`**. **ولا يُعلَن `APPROVE` هنا**، **ولا تُسجَّل مراجعةٌ لم تقع بعد.**

**نتيجة المراجعة المستقلة على الرأس السابق:** جرت **مراجعة مستقلة** على الرأس **`01b9f2e5436747b50865b8a5ca28fa29686769ea`** وأصدرت **`REQUEST CHANGES`** بأربع ملاحظات واجبة التصحيح، بُلِّغت عبر أمر المالك أعلاه.

| ‏ID | الملاحظة | التصحيح المنفَّذ | الوعاء |
|---|---|---|---|
| **`C1`** | ‏`run.list`: تعارضٌ بين **`focus_return_target: AUTO_TRIGGER`** في `surfaces[]` وبين نثرٍ لاحق يسمّي **`record.search`** هدفاً بديلاً عند زوال الضابط ⇒ **هدفان متنافسان** ولا يقرأ القالبُ والبوابةُ هدفاً واحداً | **الإعلان البنيوي صار مصدر الحقيقة**: `drawer_row_preview` و`drawer_row_audit_chain` يعلنان **`focus_return_target: record.search`** — قيمةٌ من نوع **`<action_id>`** لفعلٍ **قائم معلَن في §2** وفعلُه من الكتالوج المغلق. و**أُزيل النثر المتعارض**؛ والسطحان الباقيان يبقيان `AUTO_TRIGGER` **بتعليل عدم زوال ضابطهما**. **ولم يُستعمل `<control_id>` ولم يُخترع معرّف** | `contracts/screens/run.list.md` §3-أ · §3-ب |
| **`C2`** | ‏`admin.actions`: ‏`drawer_definition_audit_chain` قد يزول ضابطُه (‏`filter` · `refresh` · `delete` · `scope change`) دون معالجةٍ لشرط القالب §3.2 | **طُبِّق الفرع الثاني المأذون: إثباتُ عدم الزوال بالدليل بدل إضافة هدف غير لازم.** ضوابط الأسطح الأربعة غير الرئيسة **كلُّها على `page_action_contract_editor`** لا على صفّ القائمة — بدليل أن `ui/UI_COMPONENT_STATES.md` §10 وعاؤها **«Admin Definition Card»** وحالتُها `S1` تصف سياق المحرّر، وأن **بطاقة السلوك 12 لا تُسند إلى القائمة إلا سطح المحاكاة**. و**لا فعل حذفٍ معلَناً** في §2. ⇒ **`AUTO_TRIGGER` كافٍ وصحيح**، **ولم يُضَف هدف بديل ولا `<control_id>` ولا معرّف جديد** | `contracts/screens/admin.actions.md` §3-ب القاعدة 6 |
| **`C3`** | ‏`run.list`: تناقضٌ حول **`S19`** — §3-ب ينسبه إلى `transient_action_feedback` بينما §4 و`DD-RL-4` يقرران **أن لا وعاء سطحياً معلَناً له** | **أُزيل `S19` من `transient_action_feedback`** ووُحِّد النص: السطح **إعلانُ نتيجةٍ بعد إقرار الخادم** (`S13`/`S14`)، و**`S19` حالةُ كتابةٍ قيد النقل قبل الإقرار** — **متمايزتان زمناً ووظيفةً**. و**`S19`/`S20` تبقيان `DOWNSTREAM DEPENDENCY`** (`DD-RL-4`). **ولم تُمسّ دلالة `S19` في `ui/UI_COMPONENT_STATES.md`، ولم يُنشأ سطح** | `contracts/screens/run.list.md` §3-ب · §4 · §7-أ |
| **`C4`** | ‏`run.list`: خطأُ عدٍّ — «**الأسطح الثلاثة** المرتبطة بصفّ» بينما المسمّى **سطحان** | **صُحِّح العدد إلى «السطحان»** مطابقةً للعناصر الفعلية. **ولم يُضَف سطح ثالث لإصلاح العدّ**، **ولم تتوسّع `surfaces[]`** | `contracts/screens/run.list.md` §3-ب القاعدة 2 |

**أثر سلطوي لازم — يُسجَّل ولا يُطوى:**
1. **الرأس السابق `01b9f2e5…` لم يعد مؤهلاً للدمج** — فقد صدر عليه `REQUEST CHANGES`، و**لا يُدمج رأسٌ لم يحصل هو نفسه على `APPROVE`** (‏ADR-0033 بند 11 · `OD-GOV-MERGE-1` §(ل-3)).
2. **الرأس الجديد يحتاج مراجعةً مستقلة كاملة** بجلسةٍ لم تكتب ولم تعدّل هذه الدفعة — **ولا تُسجَّل نتيجتُها قبل وقوعها**.
3. **استثناء دورة التصحيح منطبق**: تغيُّر الرأس ناتجٌ **حصراً** عن معالجة ملاحظات المراجعة، **داخل نطاق أمر المالك نفسه**، **بلا إضافة ملفات ولا موضوعات جديدة** ⇒ **لا يلزم أمر مالك جديد**، و**نطاق التفويض وقائمة الملفات ونصّ إيصال الدمج في §(هـ) تبقى نافذة بلا تعديل**.
4. **وكيلُ التنفيذ الذي كتب هذه الدفعة وصحَّحها لا يراجعها ولا يدمجها** (‏`agent-execution-model` §18.2-1).
5. **نقص تتبّع مسجَّل — لا يُخترع له معرّف:** لا يوجد **معرّف تقني دائم** لمخرَج المراجعة المستقلة على الرأس السابق؛ بُلِّغت نتيجتُها عبر أمر المالك. **يُسجَّل نقصاً في قابلية التتبّع** على سابقة §(ك-4) من `OD-GOV-MERGE-1`، **ولا يُعالَج بافتراض ولا باختراع معرّف**.

**ما لم يتغيّر بهذه الجولة:** **العقدان `Candidate` `v0.2`** — **لا ترقية ولا تغيير حالة** · **`surfaces[]` لم تتوسّع** (‏`run.list` ستة · `admin.actions` ستة) · **لا سطح جديد ولا `action_id` ولا حقل ولا حالة `S` ولا نوع سطح** · **لا `<control_id>`** · **تعارض `filter`/`define`/`edit` لم يُحسم** (‏`DD-RL-2` · `DD-AC-2`) · **`contracts/fields/` لم يُنشأ** · **`SCREEN_CONTRACT_TEMPLATE.md` و`ui/**` ومنها §9 و`methodology/**` و`decisions/adr/**` لم تُمسّ** · **لا `UI-4` ولا `UI-5`** · **`FP-0001` `BLOCKED`** · **التجميد `ACTIVE` وشروط الرفع بلا تعديل ولا إعادة عدّ** · **`R4` غير مفعَّلة** · **`R3` لم تُغلق** · **لا وسم ولا إصدار** · **لا ملف جديد ولا محذوف**.

**قيد عام على هذا القسم:** لا يُعيد ترقيم أي قرار قائم · لا يغيّر حالة أي `OD` آخر عدا تسجيل هذا القرار · لا يُنشئ ADR · لا يُعدّل `AUTHORITY.md` ولا `constitution.md` ولا `methodology/**` ولا `ui/**` ولا `decisions/adr/**` ولا `traceability/**` · لا يمسّ أي مخرج مجمَّد · لا يُنشئ طبقة سلطة جديدة · ولا يمنح أي وثيقة غير حاكمة سلطةً.

---

## سجل مواءمة عقود المرشَّح المتبقّية — `OD-R3-CONTRACTS-2` (‏Δ 2026-08-05 · R3 — `BATCH_A` — أمر مالك)

> **السند:** **أمر مالك صريح** — «‏**OWNER EXECUTION ORDER — R3 BATCH_A CONTRACT ALIGNMENT**» · ‏Workstream: **`R3 Remaining Contracts — BATCH_A`**.
> **الوعاء:** هذا القسم هو **الوعاء الحاكم** للقرار بمقتضى `methodology/PHASE_EXECUTION_STANDARD.md` §3.2 («لا قرار يعيش في تقرير أو محادثة فقط. التقرير إحالة، لا وعاء») و`AUTHORITY.md:13` (المرتبة **4**)، ويرافقه `handoff/handoff.md` → **H-0036** (المرتبة 9).
> **خط الأساس:** ‏`origin/main` = **`48fcdfd93eebf68abde85d664d107de7cbeda502`** — **مطابقٌ حرفياً** للـSHA المثبَّت في أمر المالك؛ ورأس **PR #17** المدموج = **`6c965377bb35611ba72499bb200b94cef027ea21`** — **مطابقٌ كذلك**.
> **الحالة:** **`RECORDED — EFFECTIVE UPON MERGE`** — ولا تُقرأ نافذةً قبل الدمج في `main`.
> **يُقرأ بعد `OD-R3-CONTRACTS-1` ولا ينسخه ولا يعدّله:** ذاك سجلٌّ **صحيحٌ في تاريخه** لعقدَي `run.list` و`admin.actions`، **وأقسامه (أ)..(و) لم تُمسّ**؛ وهذا سجلٌّ **لدفعةٍ تالية بعقدَين آخرين**.

### (أ) سجل الواقعة — `OD-R3-CONTRACTS-2`

| الحقل | القيمة |
|---|---|
| **المعرّف** | **`OD-R3-CONTRACTS-2`** — مواءمة عقود المرشَّح المتبقّية (‏`BATCH_A`) |
| **التاريخ** | **2026-08-05** |
| **السلطة** | **Owner (‏SA)** — أمر صريح · **Specification Architect** — الصياغة بتفويض |
| **المخرج المنفَّذ** | **مخرج `R3` المسجَّل في `methodology/RECONCILIATION_ROADMAP.md:11`** — «‏Contracts Coverage Expansion … المخرج: **عقود Candidate إضافية**». **وهذه الجولة تقع مباشرةً داخل ذلك المخرج** — لا توسيعَ ميثاقٍ ولا إلحاقَ نطاق |
| **النطاق المأذون — الملفات** | **خمسة حصراً:** `contracts/screens/admin.record_types.md` · `contracts/screens/admin.workflows.md` · `decisions/open-decisions.md` · `handoff/handoff.md` · `INDEX.md` |
| **ما نُفِّذ** | مواءمة العقدين مع **`contracts/screens/SCREEN_CONTRACT_TEMPLATE.md` v1.1** على **نمط PR #17 نفسه**: §3-أ **`surfaces[]` مهيكل** بالتعداد المغلق مع **جدول سند اشتقاق لكل سطح** · §3-ب **سلوك كل سطح** وقواعده الملزمة · §3-ج **قاعدة التعامد** بين `archetype` و`surfaces[].type` · §3-د **الترتيب المنطقي المعلَن** · §3-هـ **أنماط `S8`** · §4-أ **تصريح `S16`–`S20`** · §7-أ **جرد التبعيات اللاحقة** |
| **عدد الأسطح المعلَن** | `admin.record_types` = **سبعة** (‏`PAGE`×2 · `DRAWER`×3 · `DIALOG`×2) · `admin.workflows` = **سبعة** (‏`PAGE`×1 · `DRAWER`×3 · `DIALOG`×3). **كلها من التعداد المغلق الخماسي** — **ولم يُخترع نوع سطح**، و`WIZARD_STEP` و`COMMAND_SURFACE` و`AGENT_CONVERSATION_SURFACE` **بقيت مؤجَّلة ولم تُستعمل**، و`INLINE_PANEL` **غير مستعمل** |
| **خريطة التحويل** | **صفر ورودٍ** للأسماء النثرية القديمة (`page` · `modal` · `drawer` · `inline` · `toast`) بوصفها **وسوم أسطح** في العقدين — **لا في الإعلان ولا في السند ولا في بقية الأقسام** |
| **حالة العقدين** | **`Candidate` — بلا تغيير.** **ولم يُرقَّ أيُّ عقد إلى `Approved`**، ولم يُدَّعَ ذلك، و**المرتبة 5 تبقى خالية فعلياً** |
| **`version`** | `admin.record_types` **0.1 ⇒ 0.2** · `admin.workflows` **0.1 ⇒ 0.2** — بسطر `delta` على سابقة PR #17 نفسها، **بلا وسم أي نسخة `Deprecated`** لأن التغيير **مواءمةٌ بنيوية لا كسرٌ لعقد** |
| **`admin.workflow_detail`** | **لم يُنشأ عقدُها ولم يُنشأ ملفٌ باسمها** — تبقى **شاشةً شقيقة منفصلة**، **محجوبةً عن التأليف العقدي** حتى **أمرِ مالكٍ مستقل** ومصدرِ اشتقاقٍ خاصٍّ بها (بطاقة سلوك). **ولم تُنشأ بطاقة سلوك 25 ولم يُعدَّل `ui/UI_SCREEN_BEHAVIOR_CARDS.md` ولا `ui/UI_SCREEN_INVENTORY.md`** |
| **`OD-GOV-2`** | **مفتوح — بلا مساس.** **وهذه الدفعة لا تغلقه**: لم يُرقَّ عقدٌ إلى `Approved` فبقيت المرتبة 5 خالية |
| **`OD-P3-1`** | **مفتوح — بلا مساس.** **ولم تُثبَّت مصفوفة اعتماد افتراضية** |
| **`OD-P3-2`** | **مفتوح — بلا مساس.** **ولا canvas معتمَد في v1**؛ و**`CC-WF-1` يبقى غير محسوم** بصياغته وبوابته (‏عقد `admin.workflows` §6) — **ولم يُعتمد سلوك canvas من `aql`** |
| **`FP-0001`** | **`BLOCKED`** — بلا تغيير |
| **`OD-FP-0001-FREEZE`** | **`ACTIVE`** — بلا تغيير · **ولا شرط رفعٍ أُضيف ولا عُدِّل ولا أُعيد عدُّه** |
| **`R3`** | **`ACTIVE — SPECIFICATION ONLY`** — **لم تُغلق**. وهذه الجولة **تُنجز شقّاً آخر من الشرط (17)** في «(د)» من سجل `R3-ACT-01` (توسيع تغطية العقود)، **ولا تستوفيه** — **والتقدير للمالك بالأدلة** |
| **`R4`** | **`NOT_ACTIVATED`** — بلا تغيير · **و`R5` لم تُفعَّل** |
| **`UI-4` / `UI-5`** | **لم تبدآ** — بلا مساس |
| **جرد `P3`** | **`28 = 13 + 15 + 0`** · `UNCLASSIFIED = 0` — **بلا مساس**، ولم يُنشأ `P3-CI-*` جديد |

### (ب) ما **لم** يُحسم — تنازعٌ مسجَّل وقراراتٌ مفتوحة

**1) تنازع إسناد أفعال الـWorkflow — `DD-WF-ALLOC` (‏مسجَّل · غير محسوم):** ثلاثة نصوص **في المرتبة نفسها** تتنازع إسناد `wf.approve` و`wf.activate` و`wf.rollback`:

| الوعاء | ما يقوله | الإسناد المستفاد |
|---|---|---|
| `ui/UI_SCREEN_INVENTORY.md` جدول B | صفّ `admin.workflow_detail` يحمل `wf.approve/activate/rollback` | **`admin.workflow_detail`** |
| `ui/UI_SCREEN_BEHAVIOR_CARDS.md` بطاقة 13 | «‏modal: «اعتماد» ⇒ modal typed · «تفعيل» ⇒ modal typed للـ instances الجارية» **تحت `admin.workflows`** | **`admin.workflows`** |
| `ui/UI_ADMIN_CONSOLE_MODEL.md` §4-D | يجمع «`admin.workflows / admin.workflow_detail`» معاً بأفعالٍ واحدة | **غير مفصول** |

**قاعدة التنفيذ المطبَّقة — حرفياً:** (1) **لم يُحسم التنازع**، ولا يُحسم من داخل دفعةٍ لا تملك أيّاً من الأوعية الثلاثة. (2) **أُبقيت الأسطح المشتقّة من بطاقة 13 كما هي** في عقد `admin.workflows` لهذه المواءمة — **بأمر المالك الصريح**. (3) **لم يُنقل فعلٌ ولا حُذف ولا أُسند إلى شاشةٍ أخرى**، **ولم يُعدَّل الجرد ولا البطاقة ولا نموذج الـConsole**. (4) **الحسم قرارُ Specification Architect** (`UI_SCREEN_GOVERNANCE_STANDARD.md` §ج-2) بأمر مستقل.
**التصنيف الحاكم:** ‏`WORKFLOW ACTION ALLOCATION — SAME-RANK CONFLICT · RECORDED · NOT RESOLVED · NON-BLOCKING FOR CANDIDATE UPDATES`.

**2) ما بقي مفتوحاً بلا مساس:** ‏`OD-GOV-2` · `OD-P3-1` · `OD-P3-2` (ومعه `CC-WF-1`) · `OD-NM-1` · `GOV-OBS-03` · `OD-GOV-1` · `P3-CI-28` · **وتعارضُ مسار اعتماد العقود** المسجَّل أصلاً في `OD-R3-CONTRACTS-1` §(ب) — **يُحال ولا يُكرَّر ولا يُحسم**.
**3) `CC-WF-2` و`CC-WF-3` خارج نطاق هذه الدفعة** — وجهتُهما المسجَّلة **`R5`** (`methodology/RECONCILIATION_ROADMAP.md:13`)؛ و`CC-WF-2` مسجَّلٌ تبعيةً لاحقة `DD-WF-STATUS-LIFECYCLE`. **ولم يُعدَّل تعدادٌ ولا مسار.**
**4) `CC-RT-1`/`CC-RT-2`/`CC-RT-3`** تبقى بوجهتها المسجَّلة **نطاق `FP-0001` نفسه** — **ولم يُحسم أيٌّ منها**، و`FP-0001` **`BLOCKED`**.

### (ج) التبعيات اللاحقة المسجَّلة — `DOWNSTREAM DEPENDENCY`

> **قاعدة قاطعة:** **تسجيلُ تبعيةٍ ليس إضافةَ شرط رفعٍ** لـ`OD-FP-0001-FREEZE`، وشروطُه **مغلقة على المنصوص عليه** في `:227` وتفصيلِه في `:386`؛ وأيُّ شرط جديد **يحتاج قرار مالك صريحاً يعدّل سجل التجميد نفسه**. **ولم يُعدَّل شيء منها هنا.**

**التبعيات المسمّاة في أمر المالك — مسجَّلة كاملةً:**

| ‏ID | التبعية | الوجهة |
|---|---|---|
| **`DD-RT-ACTION-CATALOG`** | **`define` و`version` و`set` ليست من كتالوج الأفعال المغلق** (`ui/UI_FIELD_NAMING.md` §5) بينما صفّ الجرد B لـ`admin.record_types` يكتب `screen.define`/`screen.version`/`policy.set` — **تعارضٌ داخل المرتبة نفسها بلا وسم `Superseded`** ⇒ **لا حسم صامتاً** (`AUTHORITY.md` §2 القاعدة 2) | `OD-NM-1` — قرار الـRegistry · **ولم يُعَد تسمية أي معرّف ولم يُضَف فعل** |
| **`DD-RT-FIELDS`** | **`contracts/fields/` غير موجود** ⇒ لا وعاء لمصدر الاسم المتاح على مستوى العنصر ولا لحقول الـ Builder المولَّدة | **أمر مالك مستقل** — يمسّ المرتبة 5. **ولم يُنشأ المجلد ولا مرجعٌ إلزامي معلَّق إليه** (يُقرأ مع `OD-R3-CODIFY-1` §(د) الفجوتين **1** و**9**) |
| **`DD-RT-CONTROL-ID`** | **`<control_id>`** إحدى قيم `focus_return_target` في القالب §3.2 **وليس من الأنواع الثمانية** في `ui/UI_FIELD_NAMING.md` §1 — بلا نمط ولا نطاق تفرّد | `ui/UI_FIELD_NAMING.md` §1 — أمر مستقل. **ولذلك لم تُستعمل القيمة في العقدين** |
| **`DD-RT-SIBLING-SCREENS`** | **علاقة الشاشات الشقيقة غير محسومة:** ‏`admin.fields` صفٌّ مستقل بمسارٍ نصّه «(تبويب داخل الـ Builder)» · `admin.generation_docs` صفٌّ مستقل بمسارٍ فعلي وأولوية `Med` — بينما بطاقة 11 تجعل «الحقول» و«التوثيق» **تبويبين** داخل الـ Builder | **قرار Specification Architect** (`UI_SCREEN_GOVERNANCE_STANDARD.md` §ج-2) — **ولم يُحسم أنّ أيّاً منهما شاشةٌ أو تبويبٌ أو هدفُ عقد، ولم يُنشأ عقد، ولم يُخترع مسار** |
| **`DD-WF-ALLOC`** | **تنازع إسناد `wf.approve`/`wf.activate`/`wf.rollback`** — التفصيل في §(ب)-1 أعلاه | **قرار Specification Architect** — أمر مستقل |
| **`DD-WF-ACTION-CATALOG`** | **`define` و`validate` ليسا من الكتالوج المغلق** بينما صفّ الجرد B يكتب `wf.define`/`wf.validate`؛ **ويُضاف اختلافُ الاسم الموضوعي** `wf.*` (الجرد) مقابل `workflow.*` (`ui/UI_FIELD_NAMING.md` §3) | `OD-NM-1` — قرار الـRegistry · **ولم يُعَد تسمية أي معرّف** |
| **`DD-WF-STATUS-LIFECYCLE`** | **`workflow_status` الثنائية** (`draft` · `published`) **لا تكفي دورة SoD** الكاملة (تقديم ⇐ اعتماد بمعتمِدٍ مختلف ⇐ تفعيل ⇐ rollback) — وهو **`CC-WF-2`** نفسه | `contracts/enums/ENUMS_DICTIONARY.md` ⇒ **`R5`** — أمر مستقل. **ولم تُعدَّل التعدادات ولم تُضَف قيمة** |
| **`OD-P3-1`** | **مصفوفة الاعتماد الافتراضية** قرارٌ مفتوح ببوابة `Clarify` عند التفعيل — ومسجَّلٌ عقدياً `DD-WF-APPROVAL-MATRIX` | **`OD-P3-1`** بحاله — **لم تُثبَّت مصفوفة** |
| **`OD-P3-2`** | **مصير الـcanvas** — مفتوح، وبوابةُ `CC-WF-1` عقدُ `admin.workflows` §6؛ ومسجَّلٌ عقدياً `DD-WF-CANVAS` | **`OD-P3-2`** بحاله — **لا canvas في v1 ولا اعتماد لسلوك `aql`** |
| **`OD-GOV-2`** | **المرتبة 5 خالية فعلياً** — لا عقد `Approved` عند HEAD | **`OD-GOV-2`** بحاله — **وهذه الدفعة لا تغلقه** |

**تبعياتٌ عقدية إضافية مسجَّلة في §7-أ من العقدين — مشتقّة من الفجوات القائمة قبل هذه الجولة، تُحال ولا تُحسم:** ‏`DD-RT-DIFF-SURFACE` (تعارض وعاء الـdiff/المحاكاة: بطاقة 11 `drawer end` ⇄ `UI_COMPONENT_STATES` §10 `S3` «tab») · `DD-WF-FOCUS-RETURN` (تعذُّر إعلان هدف عودة تركيز بديل قبل حسم `DD-WF-ALLOC`) · `DD-WF-CONTROL-ID` · `DD-WF-DETAIL-SCREEN` (‏`admin.workflow_detail` بلا عقد وبلا بطاقة خاصة) · `DD-WF-FIELDS` · `DD-RT-APPROVAL-PATH` · `DD-WF-APPROVAL-PATH` (**إحالةٌ إلى `OD-R3-CONTRACTS-1` §(ب) — لا تكرار ولا حسم**) · `DD-RT-A11Y-ENFORCEMENT` · `DD-WF-A11Y-ENFORCEMENT` (**إحالةٌ إلى `OD-R3-CODIFY-1` §(د) الفجوات 2 و3 و4 و5**).

### (د) ما **لا** يفعله هذا السجل — قيود ملزمة

1. **لا يرقّي عقداً إلى `Approved`** ولا يدّعي ذلك · **ولا يملأ المرتبة 5** · و**`OD-GOV-2` يبقى مفتوحاً بلا مساس**.
2. **لا يُنشئ عقد شاشة جديداً** — و**`contracts/screens/admin.workflow_detail.md` لم يُنشأ**، و**لا يُقرأ أيُّ ورودٍ لاسمها في هذه الدفعة إيحاءً بوجود الملف** بل **إحالةً إلى شاشةٍ شقيقة وتبعيةٍ لاحقة**.
3. **لا يُنشئ بطاقة سلوك 25** ولا يعدّل `ui/UI_SCREEN_BEHAVIOR_CARDS.md` ولا `ui/UI_SCREEN_INVENTORY.md` ولا أيَّ ملف في `ui/**` (ومنها `UI_DESIGN_SYSTEM.md` §9) · **ولا يعلن أي معيار من `R3-A11Y-01`..`07` بحالة `PASS`**.
4. **لا يحسم تنازع إسناد أفعال الـWorkflow** (§(ب)-1) ولا **تعارض مسار اعتماد العقود** ولا `OD-P3-1` ولا `OD-P3-2` ولا `CC-WF-1` ولا `CC-WF-2` ولا `CC-WF-3` ولا `CC-RT-1..3` ولا `GOV-OBS-03` ولا `OD-GOV-1` ولا `P3-CI-28`.
5. **لا يعدّل التعدادات** (`contracts/enums/**`) ولا `contracts/identity/**` · **ولا يُنشئ `contracts/fields/`** ولا مرجعاً إلزامياً معلَّقاً إليه · **ولا يخترع حقلاً ولا معرّفاً ولا مساراً**.
6. **لا يعيد تسمية أي `action_id`** ولا يوسّع كتالوج الأفعال ولا الـRegistry.
7. **لا يُنشئ نوع سطح خارج التعداد المغلق** ولا حالة `S` جديدة ولا نمط `S8` جديداً · **ولا محوراً ثالثاً** (`rendering_topology` · `authoring_origin` · `provenance_ref`).
8. **لا يعدّل `SCREEN_CONTRACT_TEMPLATE.md`** ولا `NAMING_AND_CONTRACTS_STANDARD.md` ولا `contracts/screens/run.list.md` ولا `contracts/screens/admin.actions.md` ولا `AUTHORITY.md` ولا `constitution.md` ولا `methodology/**` ولا `decisions/adr/**` ولا `traceability/**` ولا `phases/**` ولا `catalogs/**` ولا `aql/**`.
9. **لا يرفع التجميد ولا يفكّ حجب `FP-0001` ولا يفعّل `R4` ولا `R5` ولا يبدأ `UI-4` ولا `UI-5` ولا يغلق `R3`** · **ولا يضيف شرط رفعٍ ولا يعيد عدّها**.
10. **لا يُنشئ اختباراً ولا `CI` ولا حماية فروع** · **لا وسم ولا إصدار ولا دمج ولا حذف فرع** · **ولا يعيد فتح حوكمة `Merge Agent`** (‏`OD-GOV-MERGE-1` بأقسامه **لم يُمسّ**) · **ولا يعدّل `OD-R3-CONTRACTS-1` ولا `OD-R3-CODIFY-1` ولا `R3-ACT-01`**.

### (هـ) السجل الحاكم السابق للدمج — PR #18 (‏ADR-0033 بند 12)

```text
Authorization Principal       = Owner
Owner Order Reference         = OWNER EXECUTION ORDER — R3 BATCH_A CONTRACT ALIGNMENT
Owner Order Date              = 2026-08-05
Repository                    = sa4devops/local-rag-enterprise-specs
PR                            = #18
Target Branch                 = main
Base SHA                      = 48fcdfd93eebf68abde85d664d107de7cbeda502
Predecessor Merged Head       = 6c965377bb35611ba72499bb200b94cef027ea21 (PR #17)
Permitted Scope               = contracts/screens/admin.record_types.md
                                contracts/screens/admin.workflows.md
                                decisions/open-decisions.md
                                handoff/handoff.md
                                INDEX.md
Required Review Actor         = Independent Review Session (did not author or modify this PR)
Required Review Result        = APPROVE
Authorized Execution Actor    = Owner-Authorized Independent Merge Agent
                                (third session: not the author, not the reviewer)
Expected Technical Credential = GitHub token for account sa4devops used by the agent environment
Credential Statement          = The technical account sa4devops is NOT evidence of manual Owner execution
Merge Method                  = merge commit
Pending Merge SHA             = assigned automatically by Git
```

**نصّ رسالة Merge Commit المتوقَّع — مثبَّتٌ حرفياً:**

```text
Merge PR #18 — R3 — Align Remaining Candidate Screen Contracts

Governance record: OD-R3-CONTRACTS-2 · H-0036
Executed by Owner-Authorized Independent Merge Agent.
Technical account sa4devops is not evidence of manual Owner execution.
```

**قواعد المطابقة:** تُقارن بعد **توحيد نهايات الأسطر إلى `LF`** · **حذف المسافات الطرفية من كل سطر** · **حذف الأسطر الفارغة الزائدة في البداية والنهاية**. **ولا يُسمح باختلاف الكلمات ولا ترتيب الأسطر.** ‏**واختلافٌ بعد التطبيع ⇒ `MERGE RECEIPT MISMATCH` ⇒ لا دمج.**

**ولا يُضاف `Authorized head` إلى الرسالة** — **الرأس النهائي المدموج هو الأب الثاني للـMerge Commit**، يُستخرج بـ`git show -s --format='%P' <MERGE_SHA>` (‏بند 12-(ج)). **ولا تُكرَّر في هذا الوعاء وقائعُ يملكها Git** (`Merge SHA` · الأبوان · التاريخ · الشجرة الناتجة).

**مراجعة هذه الدفعة — تسجيل حالة:** جلسةُ التنفيذ أجرت **`Structured self-review`** فقط، **وهي لا تستوفي §(د)-3** من `OD-GOV-MERGE-1` ولا **بند 10-(ج)** من ADR-0033. **فيلزم قبل الدمج مراجعةٌ مستقلة** بجلسةٍ لم تكتب ولم تعدّل هذه الدفعة، **ولا يُدمج رأسٌ لم يحصل هو نفسه على `APPROVE`** (بند 11 + استثناء دورة التصحيح). **ووكيلُ التنفيذ الذي كتب هذه الدفعة لا يراجعها ولا يدمجها** (‏`agent-execution-model` §18.2-1).

**قيد عام على هذا القسم:** لا يُعيد ترقيم أي قرار قائم · لا يغيّر حالة أي `OD` آخر عدا تسجيل هذا القرار · لا يُنشئ ADR · لا يُعدّل `AUTHORITY.md` ولا `constitution.md` ولا `methodology/**` ولا `ui/**` ولا `contracts/**` خارج العقدين المأذونين ولا `decisions/adr/**` ولا `traceability/**` · لا يمسّ أي مخرج مجمَّد · لا يُنشئ طبقة سلطة جديدة · ولا يمنح أي وثيقة غير حاكمة سلطةً.
