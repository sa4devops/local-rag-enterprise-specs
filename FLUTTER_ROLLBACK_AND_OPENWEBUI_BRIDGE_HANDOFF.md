# FLUTTER_ROLLBACK_AND_OPENWEBUI_BRIDGE_HANDOFF.md — تقرير دفعة تنحية Flutter وجسر Open WebUI المؤقت

> **Version:** 1.0 · **Date:** 2026-07-11 · **المنفِّذ:** وكيل مفوَّض من SA · **الفرعان:** `chore/remove-flutter-adopt-openwebui-bridge` في المستودعين (مدفوعان — main لم يُمس)
> **التصنيفات الست المعتمدة لكل بند:** Implemented and tested · Scaffolded · Documented only · Deferred · Not found · Unverified

## 1) الملخص التنفيذي
جُرد كل أثر لتجربة Flutter ‏(AQL) في المستودعين والجهاز المحلي: **المستودعان نظيفان أصلاً (صفر إشارة شجرةً وتاريخاً)**، والمجلد المحلي الوحيد للتجربة أُرشف ببصمة ثم حُذف. ثُبِّت React خطاً قائماً **دون إعادة فتحه** (لم يُمس D1/ADR-0017). صدر قراران: **ADR-0029** (العميل القابل للاستبدال — ترقية الدستور مشروطة باختبار الاستبدال) و**ADR-0030** ‏(Open WebUI واجهة محادثة مؤقتة ضمن شرط م6 ببوابات تفعيل وخطة خروج). بُني **scaffold معزول موثق بالكامل** في `platform/integrations/openwebui/` — **بلا تثبيت تشغيلي وبلا ادعاء تكامل**. أُضيفت حزمة حوكمة وثائق مشروطة، ودلتا محدودة في الخارطة، وتحقق أن مؤشرَي المواصفات متزامنان أصلاً على المرجع القطعي.

## 2) جدول الجرد قبل الحذف (Phase 1)
| العنصر | المسار/الرابط | آخر commit | دليل الصلة | التصنيف |
|---|---|---|---|---|
| تجربة AQL ‏Flutter (نسخة العمل المحلية) | `/Users/sapc/Desktop/flutter` | `4552bb1` ‏(2026-07-10 15:08 — "feat: build ws.main AI workspace screen per institutional design system") | ‏remote = ‏`sa4devops/aql.git` · ‏pubspec name: ‏aql · ‏aql.iml · ‏flutter_quality_kit.md · تغييران غير مرسلين | **DELETE** (نُفِّذ) |
| ‏Flutter/Dart SDK | `/opt/homebrew/bin/{flutter,dart}` + ‏cask | — | أداة عالمية بإدارة Homebrew؛ لا إثبات أنها ثُبتت حصراً للتجربة | **KEEP** (توصية مؤجلة) |
| إعدادات SDK العالمية | `~/.flutter` · `~/.config/flutter` · `~/.dart-tool` | — | ملفات مستوى SDK لا مشروع | **KEEP** |
| ‏GroupSMS | `~/Desktop/SMS app/GroupSMS` | — | مشروع Xcode ‏(GroupSMS.xcodeproj) — ليس Flutter | KEEP (غير ذي صلة) |
| إشارات Flutter/Dart/AQL في SPECS | — | — | ‏grep شامل (شجرة + رسائل التاريخ + ‏git grep على آخر 40 مراجعة) = صفر | **Not found** |
| إشارات في PLATFORM | — | — | نفس المنهج = صفر | **Not found** |
| مستودع AQL على GitHub | github.com/sa4devops/aql | — | **حذفه SA بنفسه قبل هذه الدفعة (حقيقة مقررة)** — لم يُبحث عنه لحذفه ولم تُستخدم gh repo delete/archive؛ الدليل المتبقي: عنوان الـ remote في النسخة المحلية المؤرشفة | مؤكَّد سابق للدفعة |

## 3) النسخ الاحتياطية (Phase 2)
| الأرشيف | المسار | sha256 |
|---|---|---|
| ‏aql-flutter-experiment-2026-07-11.tar.gz ‏(32,812,611 بايت · 827 مدخلاً · يشمل ‏.git والتغييرات غير المرسلة) | `~/local-rag-removal-backup/` + ‏MANIFEST-aql-flutter-2026-07-11.md | `71fbc0366241fb73540325838368890a9bd9a03efeadcca34ddd1c11f7199f5d` |

الأرشيف لم يُرفع لأي مستودع ولن يُحذف في هذه الدفعة (قاعدة Phase 2).

## 4) المحذوف محلياً (Phase 3)
- `/Users/sapc/Desktop/flutter` — حُذف بعد الأرشفة؛ تحقق بعدي: المسار زال، لا worktree/remote مكسور، المستودعان يعملان (HEADات سليمة). **لا حذف واسع، ولا مساس بأي SDK أو مشروع آخر.**

## 5) تعديلات SPECS ملفاً-ملفاً (Phases 4/6/7/9)
| الملف | التغيير | السبب | Commit |
|---|---|---|---|
| (تنظيف Flutter) | لا شيء | صفر إشارة — **Not found**؛ ‏commit ‏«retract flutter references» أُسقط بمبرر (لا شيء يُسحب)، والتنحية موثقة في H-0012 وهذا التقرير؛ **لا ADR جديداً لـ Flutter** لغياب أي قرار رسمي سابق به | — |
| decisions/adr/ADR-0029-replaceable-client-principle.md | جديد | مبدأ العميل القابل للاستبدال بقائمتي ممنوع/مسموح + شرط ترقية الدستور | `ca6c77c` |
| decisions/adr/ADR-0030-openwebui-temporary-chat-interface.md | جديد | الواجهة المؤقتة ضمن شرط م6: حدود + بوابات + خروج + معايير مستودع مستقل | `6372a7c` |
| decisions/license-review.md | +صف صورة الحاوية | ‏v0.6.5 مثبَّت (لا latest) · Needs Legal Review · digest إلزامي عند أول سحب — **في السجل الوحيد** (حدده Phase 0) | `6372a7c` |
| decisions/adr/README.md | +صفا 0029/0030 + قاعدة المستويين | فهرسة القرارين + توجيه القرارات بين المستودعين دون تكرار | `6372a7c` |
| decisions/open-decisions.md | +فهرس الدفعة (صفان) | ‏conventions الفهرسة — دون تكرار نص القرار | `6372a7c` |
| README.md + vision.md | +سطر «منصة تطبيقات مؤسسية، ليست تطبيق أعمال واحداً» | بند الحزمة — في الملفين المحددين فقط | `0da5bfe` |
| methodology/coding-standards.md | +جدول تفصيل D8 + سلّم المصطلحات الرباعي | إضافة موضحة لا إعادة كتابة؛ قاعدة «لا مفردات منتج داخل النواة» — تسمية بلا طبقات | `0da5bfe` |
| PROJECT_EVOLUTION.md | جديد | الشرط تحقق: التسلسل مشتق كاملاً من وسوم/handoff/ADRs فعلية بلا فجوة؛ فهرس سردي **ليس مصدر حقيقة ثانياً** | `0da5bfe` |
| handoff/handoff.md | +اتفاقية التشغيل (لقطة PHASE_N + ترتيب قراءة) + H-0012 | تحسين الآلية القائمة فقط — لا نظام موازياً | commit التقرير |
| phases/phase-roadmap.md | +بند «Open WebUI interim client» | دلتا محدودة بشروط خروج — لا يلغي مرحلة ولا يقدّم Backend | `aca624c` |
| github-docs-structure.md | +‏0029/0030 + ‏PROJECT_EVOLUTION + هذا التقرير | فهرسة؛ **نقص قائمة ui/ للإصدارات السابقة مغلق أصلاً بـ FIX-9 ‏(v0.8) — تحقق، لا فعل** | `aca624c` |
| PACKAGE_CHECKLIST.md | ‏v1.10 + ‏3 صفوف | فهرسة ملفات الدفعة | `aca624c` |

## 6) تعديلات PLATFORM ملفاً-ملفاً (Phases 5/8)
| الملف | التغيير | السبب | Commit |
|---|---|---|---|
| (تنظيف Flutter) | لا شيء | صفر إشارة — **Not found**؛ ‏commit ‏«remove flutter remnants» أُسقط بمبرر | — |
| docs/SPEC_SOURCE.md | **لا تعديل** | متزامن أصلاً على المرجع القطعي (Phase 0) — ‏commit ‏«spec source sync» أُسقط بمبرر (لا diff) | — |
| README.md | +سطر واحد | الإشارة إلى الـ scaffold المعزول (وثائقي، مبوَّب التفعيل) | `dab9eda` |
| integrations/openwebui/ — ‏README · compose.yaml · .env.example · policies/ · config/ · actions/ · tools/ · filters/ · adapters/ ‏(10 ملفات) | جديد | الـ scaffold: صورة مثبتة v0.6.5 خلف profile مطفأ · قيم dummy فقط · سياسات القفل السبع · كل مجلد موثق الغرض والمرحلة (**لا مجلدات فارغة**) | `dab9eda` |
| integrations/openwebui/docs/ ‏(6 وثائق) | جديد | ‏ARCHITECTURE (التدفق الـ17 خطوة موسوماً بمراحله) · SECURITY_BOUNDARY (اعتماد أحادي + SSO اتجاهاً بعقد وشرط خروج **دون ادعاء جاهزية**) · DATA_OWNERSHIP (المنصة مصدر الحقيقة + معرفات محايدة + تسجيل فوري + مزامنة هجرة) · CUSTOMIZATIONS (سجل config-only) · UPSTREAM_VERSION (البصمة التزام عند أول سحب — **لم تُختلق**) · DECOMMISSION_PLAN (الحد الأدنى لواجهة React + تسلسل الخروج الستّي) | `2c019b4` |
| integrations/openwebui/tests/ ‏(3 مواصفات) | جديد | خطط الاستبدال/العزل/الترقية كمواصفات مؤجلة مربوطة بمراحلها — **بلا Backend وهمي** | `9ca49dd` |

## 7) بنية التكامل وحدوده واتجاه الاعتماد
عزل تام في `integrations/openwebui/` · **اتجاه الاعتماد أحادي: العميل يعتمد على المنصة ولا عكس** · كل نداء عبر العقود المعلنة حصراً (adapters) · لا DB مباشرة · لا صلاحيات في العميل (المنصة تعيد التحقق — ADR-0029) · لا اعتماد على جداول Open WebUI الداخلية (معرفات محايدة + تسجيل فوري في المنصة) · نماذج عبر LLM Gateway فقط (م11) · لا white-label (قيد الترخيص).

## 8) المرجع القطعي المعتمد في المزامنة (Phase 0 → Phase 9)
- ‏**Tag:** ‏`v1.0-architecture-baseline` · ‏**Commit:** ‏`260780f9d52fb452c6c743e81103b24e4bf3c49c` · كائن الوسم: `28bafe986293c8beb121bfddcfa0faae66d60af8`.
- **مصدر الإثبات:** ‏`platform/docs/SPEC_SOURCE.md` (القيمة الحالية) + ‏specs README (قسم الحالة) + ‏handoff ‏H-0010 («المرجع النافذ بعد الدفع») وH-0011 («الوسم لا يتحرك») + ‏`git rev-list`. **ملاحظة إثباتية:** آخر GitHub Release هو `v0.8` — **وليس** المرجع؛ المؤشران كانا متزامنين أصلاً فلم يلزم تعديل.
- **سجل التراخيص الوحيد (Phase 0-a):** ‏`specs/decisions/license-review.md` — ‏(ملف المنصة LICENSE_NOTICE.md إشعار حقوق لا سجل؛ ‏AGENTS يحيل إلى سجل specs).

## 9) حالة كل بند (التصنيفات الست حصراً)
| البند | الحالة |
|---|---|
| إزالة إشارات Flutter من SPECS / من PLATFORM | **Not found** (لم توجد أصلاً — دليل الجرد §2) |
| حذف تجربة AQL المحلية + النسخة الاحتياطية | **Implemented and tested** (المسار زال · الأرشيف مقروء 827 مدخلاً · sha256 مسجل) |
| حذف مستودع AQL من GitHub | سابق للدفعة بيد SA (حقيقة مقررة — خارج التصنيف التنفيذي) |
| ‏React baseline | لم يُمس — قائم كما هو (D1/ADR-0017) |
| ‏ADR-0029 · ADR-0030 · فهارس القرارات · صف الترخيص | **Documented only** (قرارات نافذة وثائقياً؛ بوابة الترخيص القانونية معلقة) |
| حزمة الحوكمة (سطر المنصة · قاعدة المستويين · جدول D8 · سلّم المصطلحات · اتفاقية handoff · PROJECT_EVOLUTION) | **Documented only** |
| ‏scaffold ‏Open WebUI (18 ملفاً) | **Scaffolded** (لا تشغيل، لا تكامل مُدّعى) |
| ‏SSO/OIDC · ‏Actions bridge · مزامنة المحادثات | **Deferred** (اتجاهات بعقود وشروط — لم تُختبر ولا يُدّعى عملها) |
| اختبارات الاستبدال/العزل/الترقية | **Deferred** (مواصفات مكتوبة؛ التنفيذ مربوط بمراحله — لا Backend وهمي) |
| ‏digest صورة الحاوية | **Deferred** (التزام ملزم عند أول سحب متحقق — لم يُختلق) |
| ‏MODULE_BOUNDARIES.md | **Deferred** (المصفوفة القانونية قائمة في المنهجية §7/§8 — مصدر واحد) |
| حصرية تثبيت SDK للتجربة | **Unverified** (لا دليل حصرية — أُبقي) |

## 10) الاختبارات والفحوص المنفَّذة (أوامر ونتائج)
| الفحص | الأداة/الأمر | النتيجة |
|---|---|---|
| جرد المصطلحات (شجرة الفرعين) | ‏grep -rniE ‏(flutter·dart·aql·pubspec·…) | صفر إصابة نشطة؛ بعد الدفعة: الإصابات الوحيدة = توثيق السحب ذاته (المقبول المنصوص) |
| جرد التاريخ | ‏git log --all -i --grep + ‏git grep على آخر 40 مراجعة | صفر |
| سلامة الأرشيف | ‏tar -tzf + ‏shasum -a 256 | ‏827 مدخلاً · البصمة في §3 |
| حذف المسار وتحقق البيئة | ‏rm -rf مسار واحد + ‏git status/worktree في الفرعين | المسار زال · المستودعان سليمان |
| الروابط الداخلية للملفات الجديدة/المعدلة | فاحص Python (مسارات نسبية + روابط ADR) | صفر روابط مكسورة |
| الأسرار | ‏grep أنماط مفاتيح/كلمات سر على integrations/ و.env.example | لا أسرار — قيم CHANGE-ME فقط |
| تحقق GitHub | ‏git ls-remote + ‏gh api ‏(contents على الفرع) | الفرعان على GitHub بالمحتوى الكامل؛ ‏main والوسم بلا تغيير |

## 11) الفروع والـ hashes وحالة GitHub والوسوم
- ‏SPECS ‏`chore/remove-flutter-adopt-openwebui-bridge`: ‏`ca6c77c` → ‏`6372a7c` → ‏`0da5bfe` → ‏`aca624c` (+‏commit هذا التقرير وH-0012 يليها) — مدفوع ومتحقق (`ls-remote`).
- ‏PLATFORM ‏`chore/remove-flutter-adopt-openwebui-bridge`: ‏`dab9eda` → ‏`2c019b4` → ‏`9ca49dd` — مدفوع ومتحقق؛ محتوى الـ scaffold ظاهر عبر GitHub API.
- ‏**main في المستودعين لم يُمس** ‏(`a1a7041` / `f3c4a5a`) · **الوسوم بلا تغيير** — ‏`v1.0-architecture-baseline` بكائنه `28bafe98…` · **لا وسم إصدار جديد في هذه الدفعة** (توصية: يقرره SA بعد دمج الفرعين).

## 12) Deferred Recommendations (كل بند بسببه ومعيار تفعيله)
1. **MODULE_BOUNDARIES.md + الإنفاذ الآلي (import-linter):** أُجّل لأن المصفوفة القانونية تعيش في المنهجية §7/§8 (قاعدة مصدر الحقيقة الواحد — ADR-0026)؛ **معيار التفعيل: معيار قبول (acceptance criterion) لأول مرحلة كود (Phase 0 تنفيذاً)**.
2. **ترقية مبدأ العميل القابل للاستبدال إلى constitution.md:** مشروطة داخل ADR-0029؛ **المعيار: نجاح اختبار الاستبدال** (المواصفة في tests/contract).
3. **‏digest التشغيلي للصورة:** ‏**المعيار: أول سحب متحقق/اعتماد تشغيلي** — يسجَّل الحقيقي في UPSTREAM_VERSION.md.
4. **‏PROJECT_EVOLUTION:** أُنشئ (الشرط تحقق) — يبقى تحديثه دورياً عند كل وسم جديد توصيةً لا التزاماً.
5. **إزالة Flutter/Dart SDK ‏(Homebrew) وإعداداته العالمية:** أُجّلت لغياب إثبات الحصرية؛ **المعيار: قرار SA** ‏(brew uninstall) بعد تأكد عدم حاجة أي مشروع آخر.
6. **حذف أرشيف النسخة الاحتياطية:** مؤجل عمداً؛ **المعيار: قرار SA بعد اطمئنان كافٍ**.
7. **بوابة الترخيص القانونية لصورة Open WebUI:** الصف مسجل Needs Legal Review؛ **المعيار: اعتماد قانوني قبل أي تشغيل** (بوابة ADR-0030 رقم 1).
8. **قرار الدمج ووسم إصدار توثيقي جديد للمواصفات:** بيد SA بعد مراجعة الفرعين.

## 13) المخاطر المفتوحة
- تشغيل الواجهة المؤقتة قبل اعتماد صف الترخيص = خرق بوابة ADR-0030 (محكوم توثيقياً — لا إنفاذ آلياً بعد).
- ترقية عمياء للصورة إلى 0.6.6+ تصطدم بقيود branding (موثقة في UPSTREAM_VERSION وصف السجل).
- الفرعان غير مدموجين — أي عمل لاحق يبدأ منهما أو ينتظر الدمج (ازدواج محتمل إن تشعب العمل قبل الدمج).
- بقاء SDK عالمي قد يوحي خطأً باستمرار مسار Flutter — سطر التثبيت في H-0012 وهذا التقرير يعالجه توثيقياً.

## 14) الخطوات التالية بالأولوية
1. مراجعة SA للفرعين ودمجهما في main ‏(PR أو دمج مباشر بقراره).
2. قرار بوابة الترخيص القانونية لصورة الحاوية (يفتح/يقفل مسار أي تشغيل بيني).
3. **تصميم تفعيل Phase 1** ببطاقة مهمة (المسار الرئيسي — لم يتغير).
4. عند أول نية تشغيل بيني: سحب الصورة وتسجيل digest + تفعيل policies + ‏SSO كعقد.
5. عند أول مرحلة كود: تفعيل بند الحدود Deferred-1 كمعيار قبول.

---

## PROJECT CONVERSATION HANDOFF — ملخص مستقل قابل للنسخ

> مشروع Local RAG Enterprise (مستودعا sa4devops: ‏specs + ‏platform). **المرجع المعماري النافذ: الوسم `v1.0-architecture-baseline` على ‏commit ‏`260780f`** (وليس أحدث GitHub Release — ذاك v0.8)؛ يثبته `platform/docs/SPEC_SOURCE.md` وhandoff ‏H-0010/H-0011. المعمارية مقفلة بالمادة م21 (التغيير عبر ADR حصراً — السجل حتى **ADR-0030**). تجربة عميل Flutter ‏(AQL) **أُنهيت نهائياً**: مستودعها حذفه SA بنفسه قبل هذه الدفعة، ونسختها المحلية أُرشفت ببصمة ‏sha256 ثم حُذفت، والفحص أثبت **صفر إشارة Flutter** في المستودعين شجرةً وتاريخاً (لم يوجد أي ADR سابق لها — فلا ADR سحب). واجهة المشروع المعتمدة تبقى **React+Vite+TS+Tailwind+shadcn ‏(D1/ADR-0017)** بلا إعادة فتح، ومؤقتاً اعتُمد **Open WebUI واجهة محادثة بينية** بقرار **ADR-0030** ضمن شرط الدستور م6 (ليست الواجهة النهائية وليست مصدر حقيقة، بلا DB مباشرة، بلا صلاحيات ذاتية، بلا اعتماد على جداولها الداخلية)، محكومة بمبدأ **العميل القابل للاستبدال (ADR-0029)** الذي تُرقّى صياغته للدستور فقط بعد نجاح اختبار الاستبدال. بُني لها **scaffold معزول وثائقي** في `platform/integrations/openwebui/` ‏(صورة مثبتة v0.6.5 — لا latest؛ ‏digest يسجَّل عند أول سحب؛ ‏Needs Legal Review في سجل التراخيص الوحيد `specs/decisions/license-review.md`) **دون أي تثبيت تشغيلي**، مع سياسات قفل ووثائق حدود أمنية وملكية بيانات وخطة إخراج. العمل كله على فرعَي `chore/remove-flutter-adopt-openwebui-bridge` (مدفوعان؛ ‏main لم يُمس؛ لا وسم جديداً). **التالي:** دمج الفرعين بقرار SA → بوابة الترخيص → «تصميم تفعيل Phase 1 — Governed Core + Screen Builder» ببطاقة مهمة، مدخله تصميم v0.3 المستعاد + دلتا v0.8، والتقرير الحاكم لتسليم المعمارية: `ARCHITECTURE_BASELINE_FINAL_HANDOFF.md`.
